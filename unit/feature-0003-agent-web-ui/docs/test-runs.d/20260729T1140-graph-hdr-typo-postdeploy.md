---
run_at: 2026-07-29T11:40:00+09:00
session: ai/claude/feature-0016-hdrtypo-pd
scope: POST-DEPLOY 라이브 검증 — graph-hdr-label-typo (PR #1037 → main 50c5a854 → 배포 50c5a854)
verdict: PASS
---

### Run (2026-07-29 11:40) — graph-hdr-label-typo POST-DEPLOY (HT.9) — **Environment: Windows-browser**

#### 1. 대상

`20260729T0930-graph-hdr-label-typo` cycle 의 HT.9. 위계 헤더 라벨 재설계(레벨별 단일 크기 + 예약 행 완전
수용)가 **실제 배포 자산**에서 사용자가 지적한 시각 불편을 해소했는지 실 브라우저 육안으로 판정한다.
헤드리스는 방출 계약(폰트 arity·팔출 0·밴드 불변)만 고정하고, *렌더 품질*(글자 판독성·알약 수용의 시각적
수용성)은 판정하지 못한다.

#### 2. Environment

- Bridge: `relay` @ `http://172.26.144.1:9223` (`doctor` → `ok: true`)
- Browser: Windows Chrome/150.0.7871.115 (실제 Windows 창 — WSL headless 아님)
- URL: `https://localhost/admin` → 그래프 뷰 · edge `/healthz` `git_commit=50c5a854` · `mysql_ok`/`pg_ok` true
- 컨테이너: `repo-web-a-1` / `repo-web-b-1` = `mysql-ai-web:50c5a854` (둘 다 healthy)
- 자산 스탬프: `?v=6133a5e9907a`
- 데이터: `mysql-gz-qa-global`, `gunzgame` 스키마 펼침 (테이블 121 · 함수·프로시저 300 = 421) →
  위계 헤더 **84개**(GH + CATH) 방출 — hdr-label-fit POST-DEPLOY(HF.6)와 **동일 조건**이라 직접 대조 가능
- Runner: AI
- Evidence: `artifacts/feature-0016-metadata-graph/20260729-graph-hdr-typo-postdeploy/` 9매
  (`01_fit_expanded.png` 전체 조망 · `02_crop2x.png` 헤더 2× 확대 · `03_zo3.png`/`04_zo6.png` 억제 구간 ·
  `05_recover.png` 복귀)

#### 3. 신 코드 실행 증거 (판정 전제)

주입 QA 오염(서버 파일은 신버전인데 브라우저가 구 모듈 실행)을 배제하기 위해 **배포 이미지 자산 내용**을
먼저 확인했다 — web-a/web-b 양쪽 `static/graph/graph-state.js` 에 신설 `_hdrLevels` **7회** 출현,
폐기 API `_metaHdrFitFont` **0회**(재설계로 제거), 폐기 상수 `STEP_CAP` **0회**. 즉 서빙 자산이 신 구현이다.
런타임에서도 밴드 문자열이 신 계약인 `<col>/<label>/h<N>` 형태(`9/9/h2`, `15/9.5/h3`)로 관측됐다.

#### 4. 판정 (HT.9 6항목)

| # | 항목 | 실측(배포본) | 판정 |
|---|---|---|---|
| ① | **형제 헤더 크기 일치** | `계정캐릭터기본조회…` · `커스터마이징프로…` · `로그인선물보상…` · `게임플레이로그·…` · `클랜게임기록…` 이 **전원 동일 크기**. 구 구현의 24.6/27.1/30.1 박스별 편차(크기가 정보가 아니라 노이즈였던 원인 ①)가 소멸 | **PASS** |
| ② | **알약이 텍스트를 감쌈** | 전 헤더가 흰 칩 안에 온전히 들어가고 칩이 텍스트보다 크다. 좁은 박스/넓은 박스 구분 없이 **동일 스타일** — 구 구현의 `fillOpacity 0.45` 소프트닝·유무 불일치(②) 없음 | **PASS** |
| ③ | **밴드 헤더 > 컨텐츠 카테고리** | 제품 카테고리 밴드 헤더(`📁 건즈 글로벌 QA · 3 DB · 184 테이블`)가 컨텐츠 카테고리 헤더보다 육안으로 명확히 크다(부모 상한 24 > 자식 상한 20). 위계 역전(③) 없음 | **PASS** |
| ④ | **라벨이 박스 안(테두리 물림 0)** | 84개 헤더 칩이 모두 각 그룹 박스의 **예약 행 안쪽**에 수용. 박스 테두리를 무는 칩 0 — 구 구현의 칩 38 > 예약 행 26 팔출(⑤⑥) 소멸 | **PASS** |
| ⑤ | **잘림 감소** | 폰트 상한이 64 → 20/24 로 내려가 텍스트 폭 여유가 구조적으로 확대. 다만 **라이브 긴 한글 헤더에서는 여전히 ellipsis 가 관측**된다(예 `계정캐릭터기본조회 …`) | **부분** (아래 §6) |
| ⑥ | **pageerror** | 전 조작 구간(펼침 → 줌아웃 6단 → 줌인 복귀 6단) 누적 **0건** | **PASS** |

#### 5. 밴드 불변 실측 — codex P2 수정의 라이브 확증

줌아웃 단계별 관측(`window.__META_GRAPH_PERF.label`):

| zoom | band | dropped | headerDropped |
|---|---|---|---|
| 0.6672 | `9/9/**h2**` | 0 | 0 |
| 0.3416 | `15/9.5/**h3**` | 501 | 0 |
| 0.2733 | `18.5/12/**h3**` | 503 | 0 |
| 0.2186 | `23/15/**h3**` | 506 | 3 |
| 0.1749 | `24/18.5/**h3**` | 506 | 3 |
| 0.1399 | `24/23/**h3**` | 586 | 83 |
| 0.1119 | `24/24/**h3**` | 587 | 84 (전량) |

헤더 밴드 성분이 `h2` → `h3` 전이 후 **줌아웃 하한(0.1119)까지 `h3` 고정**이다. 구 구현(상한 4)이었다면
z≈0.4579 에서 `h3`→`h4` 전이가 한 번 더 걸려 **렌더가 완전히 동일한 구간에 full `setData`/draw** 가
발생했을 지점이다 — codex P2 수정(상한을 레벨 스펙에서 파생)이 라이브에서 확증됐다.

억제 시작 줌도 HT.8 헤드리스 예측과 정합: 0.1749 까지 81/84 유지 → 0.1399 에서 83 억제 →
0.1119 전량. 예측치 `group-hd` 0.1600 / `cat-hd` 0.1333 사이에 실측이 정확히 들어온다.

**반동 대칭 복귀**: 하한(0.1119, 전량 억제)에서 줌인하면 0.1526(`24/21/h3`, hdrDropped 83) →
0.1907(`24/17/h3`, hdrDropped 3) 로 복귀. 정보 손실 0(확대하면 그대로 돌아온다).
상태줄 오독-가드 마커(`줌아웃 — 이름표 표시 축약(확대 시 전체 표시)`)도 억제 구간에서 정상 표시됐다.

#### 6. 한계 (정직 표기)

- **⑤ 잘림 감소는 "부분"이다.** 폰트 상한 축소로 폭 여유가 커진 것은 구조적으로 성립하지만,
  *구 배포본과의 라이브 직접 대조*는 불가능하다(구 이미지가 이미 교체됨). 실제로 긴 한글 헤더
  (`계정캐릭터기본조회 …` 등)에서는 **여전히 ellipsis 가 남는다**. 즉 "잘림이 줄었다"는 기하 논증
  (폰트 64→20/24, 폭 3배 이상 여유)으로 뒷받침되지, 이번 라이브 관측이 증명한 것은 아니다.
  잘림 자체를 없애려면 별도 수단(헤더 2줄 수용 또는 박스 중앙 워터마크 area-label)이 필요하다.
- **본 Run 은 개요 판독의 *절대적* 개선을 주장하지 않는다.** 억제 시작 줌이 1차 구현(hdr-label-fit,
  0.05)보다 후퇴(0.16/0.13)한 것은 TASK 결정 기록대로 **의도된 대가**다. 원래 고정 폰트 대비로는
  1.9~2.0배 개선이지만, 직전 배포본 대비로는 헤더가 더 일찍 사라진다.
- 상태줄 마커는 `aftertransform` 에서 append 되므로 **노드 종류 토글 직후에는 한 transform 동안
  부재**한다(label-lod cycle 의 TL.12 로 이미 별도 항목으로 등재된 기존 동작). 본 Run 범위 밖.

#### 7. Pass/Fail

- **PASS** — HT.9 의 판정 항목 ①②③④⑥ 이 배포본에서 확인됐고, ⑤ 는 구조적 여유 확대는 성립하나
  라이브 잘림 0 은 아니라는 사실을 그대로 기록한다. pageerror 0. Runner: AI.
