---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0046-native-client
agent: icon_ux_review
timestamp: 2026-09-08T02:12:00Z
trigger: UI/아이콘 배선·패키징
verdict: PASS
---

### 1. Blocking issues

(no findings)

### 2. Cross-domain concerns

(no findings)

### 3. Challenge to current spec

소스 트레이가 Python 아이콘을 사용하던 차이를 ICO→EXE→시스템 아이콘 복구 순서로 해소했다. 실제 Windows ExtractIconW에서 최종 ICO 추출 성공을 확인했다. PyInstaller 데이터 목적지와 branding.ICON_PATH가 일치하고 WebView2/Tk가 그 자산을 사용한다. 설치본의 EXE 아이콘은 트레이·바로가기·URL 스킴·UninstallDisplayIcon으로 이어진다. 설치기 산출물을 현재 버전의 정확한 파일명으로 선택하고 부재 시 실패하므로 이전 릴리스를 완료 산출물로 오보고하지 않는다. 브라우저 폴백 창 아이콘은 브라우저 관리 영역이며 사용자 확정 범위에 따라 문서에 한계를 명시했다. 전체 빌드·동결본 검증은 주 작업의 실측을 따른다.

후속 exporter 리뷰: 재변환 ICO가 바이트 동일하고 DIB/PNG 길이·오프셋이 정상이다. 기본 EXE/Setup 아이콘 그룹을 직접 확인하지 않던 P2는 `ExtractIconW(path, 0)` 픽셀 대조·실패 핸들 검사·finally 해제로 보완했다. 최종 재검토 PASS, 잔여 발견사항 없음.

### 4. Verdict

PASS — P1 0 / P2 0. Human Approval Needed: no.
Codex 독립 subagent `icon_ux_review`의 수정 후 최종 리뷰. 파일 변경 없음.
