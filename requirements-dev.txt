# TASK-0126 (#11): CI / 로컬 `make test` 용 개발 의존성 (런타임 이미지에는 미포함).
pytest>=8.0
ruff>=0.6
# docker-compose.yml 구조를 단정하는 테스트(feature-0020/0041)가 사용. 지금까지는 런타임
# 의존의 전이 설치에 기대고 있었는데, 그것이 사라지면 배포 불변식 테스트가 collection 단계에서
# 통째로 사라진다(초록 CI 인데 아무것도 안 지키는 상태) — 명시 고정.
PyYAML>=6.0
