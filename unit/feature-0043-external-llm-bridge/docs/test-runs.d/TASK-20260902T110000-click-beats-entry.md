---
run_at: 2026-09-02T11:00:00+09:00
session: ai/claude/feature-0043-click-beats-entry
scope: 진입 자동 시도가 클릭을 삼키던 회귀 수정 (CHG-20260902T100000 후속)
verdict: PASS (회귀 재현 + 수정 + 뮤테이션) — 스킴 발사 축은 별개로 이월
---

# Run — TASK-20260902T110000 클릭이 자동 시도를 이긴다

## Environment: Windows-browser — **배포본 실측으로 회귀를 적발**

- `bin/win-browser.py` relay → 실 Windows Chrome, `https://localhost/` (배포 `5128b97e`)
- 서빙 JS 에 새 심볼 **14건** 적재 확인(`_connectEntry`·`_prefetchLaunch`·`_autoLaunchEligible`·
  `_maybeAutoEntry`) — 배포 실물이 새 코드임을 먼저 고정

| # | 관측 | 결과 |
|---|---|---|
| P1 | 진입 시 칩 | `업데이트 필요` (자동 실행 **자격 있음**) |
| P2 | 진입 직후 신규 토큰 행 | account 10 · account 1 각각 **발급됨** → **자격 판정 + 프리페치가 실제로 동작** |
| P3 | 칩 클릭 → 12초 | 모달 닫힘 · 칩 그대로 |
| P4 | 칩 클릭 → **40초 이상** | **모달 여전히 닫힘 · 상태 문구 없음** ← **회귀** |

P4 가 결함이다. 진입 자동 시도가 대기창(~30초) 동안 `_launchBusy` 를 들고 있어 클릭이 조용히
삼켜졌다 — 실행도 창도 없다. **이 개선 이전(무조건 창 열기)보다 나쁘다.**

## 수정

사용자의 클릭이 진행 중인 **자동** 시도를 대체한다(클릭끼리는 여전히 중복 방어). 대체 판정은
일련번호(`_launchSeq`) — 대체된 시도는 결과를 반영하지도, 잠금을 풀지도, 창을 열지도 않는다.

## 검증

| 대상 | 결과 |
|---|---|
| `tests/test_autolaunch_runner.py` **22건**(신규 3) | **PASS** |
| 뮤테이션: 클릭 우선 규칙 제거 | **KILL 2건** |
| 모달 JS ESM 문법 | PASS |

## 이월 (이 cycle 의 변경 대상이 아님)

스킴 실행이 **러너 재기동까지 이어지는지**는 확인하지 못했다. 이 검증 머신은 브라우저가
Windows·러너가 WSL 인 조합이라 **핸들러 등록**이 별개 축이고(그 축의 정본은
`TASK-20260831T…` 계열), 이번 변경은 그 축을 건드리지 않는다. 자격 판정·프리페치가 동작한
사실은 P2(토큰 발급)로 독립 확인했다.
