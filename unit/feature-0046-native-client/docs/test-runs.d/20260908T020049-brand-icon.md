---
run_at: 2026-09-08T11:28:00+09:00
session: codex-brand-icon
scope: TASK-20260908-brand-icon
verdict: PASS
---

# DQA 1.1.2 — 마상 브랜드 아이콘

## 산출물

- 생성 원본: [`dqa.png`](../../src/client/assets/dqa.png), 1254×1254 RGB. 공식 마상소프트·마상게임즈 이미지를 참조한 Q·데이터 2행, 사선과 시안→보라→마젠타 흐름.
- ICO: 16/20/24/32/40/48/64/128/256px 9개. 16~128px DIB, 256px PNG. `export_icon.py` 재실행이 바이트 동일함을 독립 리뷰에서 확인.
- Windows PyInstaller + Inno Setup 실제 빌드 rc=0. 동결 자가진단 + 동봉 Python 러너 모듈 임포트 PASS.
- 설치기 `DQAConnect-Setup-1.1.2.exe`: **26,023,355 bytes**.
- SHA-256: `43ba354b565eb0c5d45663bc4d07c83376f1dd9d64c73a3c013f4c0155f5c583`.
- Windows→프로젝트 `artifacts/dqa-brand-icon/` 복사 후 크기·SHA-256 동일.

## 자동 검사

- 변경 관련 스위트: packaging, embedded_window, tray_and_windowless, updater, build_client_console **211 PASS**.
- 병합 전 전체 feature 스위트: **512 PASS / 4 FAIL (37.25초)**. 4건은 변경 전 main `37fc2034`에서도 동일하게 재현했다. 아이콘 변경으로 발생한 실패는 0건이다.
- 기존 실패: `test_handoff_seam.py`의 사라진 터미널 안내·항상 노출 실행 버튼·실패 안내 3건, `test_web_shell.py::test_launch_button_is_pointless_inside_the_app` 1건. 소스의 첫 문자열 등장 위치를 전제로 한 검사와 현재 웹 연결 코드의 불일치가 포함된다. `MSG_RELAUNCH_NO_UPDATE`에는 이미 제거된 1단계 명령을 가리키는 기존 문구도 남아 있다. 이번 사용자 확정 범위(아이콘·설치 파일)의 변경과 구분한다.
- 최종 main `0f58a1de` 병합 후 전체 feature 스위트 **524 PASS / 0 FAIL / 0 SKIP (38.393초)**. 위 기존 4건도 #1606 수정으로 해소됐다. 최종 증적: `artifacts/dqa-brand-icon-tests-1.1.2.xml`.
- 신규 packaging 행위 회귀: 출력 디렉토리에 이전 설치기가 남아도 현재 버전 설치기와 그 해시를 보고한다.
- 변경 Python `ruff check` PASS; `git diff --check` PASS.

## Environment: Windows-native

Windows 빌드 Python 3.14.0 + Tk 8.6 + pywebview/pythonnet + Inno Setup 6에서 `tests/windows/verify_brand_icon.py` 실행. `--surface native`와 `--surface webview`는 별도 프로세스로 실행한다.

1. EXE와 Setup의 **각 9개 RT_ICON payload**가 ICO 프레임과 byte 동일.
2. EXE/Setup **기본 아이콘(index 0)**을 `ExtractIconW`로 직접 추출해 원본 32px과 pixel 동일. 리소스 존재만 세지 않는다(UX 후속 리뷰 P2 반영).
3. PyInstaller `_internal/client/assets/dqa.ico`가 원본 ICO와 byte 동일.
4. 실제 트레이, Tk 창, WebView2 창의 아이콘이 해당 32px 프레임과 **pixel 동일**.
5. 결과: `artifacts/dqa-brand-icon/{native,webview}/result.json` 모두 PASS. 실제 소비 아이콘 PNG 5종을 같은 폴더에 저장하고 시각 확인.

실측이 수정한 결함: PNG만 든 ICO를 Tk가 16px에서 확대해 흐리게 표시했다. 혼합 DIB/PNG 인코딩 후 원본 32px과 정확히 일치했다. 하네스도 x64 부호 확장 핸들, Tk 클래스 기본 아이콘 조회, 단일 프레임 기대값 선택을 교정했다. WebView2는 웹 내용과 무관한 창 아이콘 검사이므로 `shown`에서 읽고 닫는다. 엔진 초기화 중 닫혀 E_ABORT가 기록될 수 있으며 웹 페이지 로드 성공 주장에 사용하지 않는다. AI 감지·로그인·질의는 실행하지 않았다.

## 디자인·경계

독립 design/ux 리뷰 최종 PASS. 16/24/32/48px을 밝고 어두운 배경에서 시각 확인. Windows 고대비 모드 미실측. 설치기 실제 설치/제거 및 기존 머신의 자동 업데이트 왕복은 이번 검사에 포함하지 않는다. 바로가기·제거 표시·스킴은 기존 EXE 리소스 참조 계약이며 기본 EXE/Setup 아이콘은 위에서 실측했다.

Environment: Windows-browser 해당 없음 — 이번 변경은 Windows 네이티브·패키징 자산이고 웹 HTML/CSS/JS 변경은 없다. 브라우저 폴백 창의 아이콘은 브라우저 관리 영역이며 웹 테마는 사용자 선택으로 제외했다.

## 병합·재빌드

#1606의 러너 복구 개선이 먼저 1.1.1로 배포되어, 양쪽 변경을 보존하고 아이콘 적용판을 1.1.2로 올려 다시 빌드했다. 이전 로컬 1.1.1 아이콘 시제품은 게시하지 않았다. 문서 충돌은 양쪽 TASK·요구·리뷰 이력을 보존했고, 자동 병합된 GUI/빌드 스크립트도 양 부모 diff로 대조했다. 최종 1.1.2 EXE/Setup에 Windows 5개 표면 검증과 동결 자가진단을 다시 수행해 PASS.

## 라이브 릴리스

- PR #1608, main `2e50db70` 병합 완료. 이후 기존 `publish_release.py`로 wrapper의 `artifacts/client-release`에 반입했다.
- 게시 시각: **2026-09-08T02:44:54Z**. 먼저 게시된 러너 복구 1.1.1을 보존하고 1.1.2를 최신으로 전환했다.
- 실제 엣지 `https://112.185.196.20/api/ai/client/latest` **HTTP 200**, version=1.1.2.
- 광고된 `/client/DQAConnect-Setup-1.1.2.exe` 실제 다운로드 **HTTP 200**, **26,023,355 bytes**. 프로젝트 rootCA로 TLS를 검증했으며 인증서 검증을 끄지 않았다.
- 다운로드 SHA-256 `43ba354b565eb0c5d45663bc4d07c83376f1dd9d64c73a3c013f4c0155f5c583`. 원본 Windows 빌드→Linux 보관본→라이브 다운로드까지 크기·SHA-256·전체 byte 동일.
- 증적: `artifacts/dqa-brand-icon/latest-live.json`, `download-live-1.1.2.exe`. 서버 컨테이너 재시작 없이 기존 bind mount 릴리스 채널로 제공된다.
- 최종 main #1609/#1610 동기화에서는 클라이언트 배포 소스 변경이 없음을 대조했다. 수정된 handoff/web_shell 계약 테스트 86 PASS. 사용자 앱 설치/제거·업데이트 적용 왕복은 실행하지 않았다.
