---
doc_type: REVIEW
feature_id: feature-xxxx-template
status: active
edit_policy: append-only
source_of_truth: true
---

# Review Records

## REV-YYYYMMDD-0001
- Related Change:
- Reason:
- Alternatives Considered:
- Risks:
- Open Questions:
- Human Approval Needed:

## REV-20260903T140000-ai-claude-native-client-initial [SKIPPED:live-measured-new-component] — ACCEPTED
- Related TASK: TASK-20260903T140000 (ROADMAP ITEM-08)
- Timestamp: 2026-09-03T14:00:00+09:00
- **[SKIPPED] 사유**: 세션 상위지시로 AgentTool(subagent panel) 금지 → §18.8.2-1 제약 없는 채널로 검증했다. 이 컴포넌트의 고유 위험은 **「실제 Windows 에서 되는가」** 이고 그것은 정적 리뷰가 아니라 **실 머신 실행**으로만 답해진다 — 그래서 검증 예산을 그쪽에 썼다.
- **실 Windows 실측 (WSL interop `/mnt/c` + powershell.exe)**:
  - AI 감지 — `claude` 를 `C:\Users\…\.local\bin\claude.exe` 에서 찾았다. **PATH 밖 표준 위치**이고, 이것이 정확히 `shutil.which` 만으로는 못 찾던 실측 사례(REQ-20260901-win-ai-detect)다. 로컬 리눅스 테스트로는 이 경로가 검증되지 않는다.
  - 로그인 상태 — `claude auth status` JSON 파싱으로 `loggedIn=true` + 계정 표시. codex·gemini 미설치를 정확히 보고.
  - GUI — tkinter 위젯 구성 성공, 한글 타이틀 정상.
  - 빌드 — PyInstaller `--onefile --windowed` → 9,174,548 bytes 단일 exe.
- **⚠ 실측이 결함 1건을 적발했다 (로컬에서는 보이지 않는 종류)**: `--windowed` 빌드는 `sys.stdout` 이 `None` 이라 `print()` 가 예외를 내고, 창 없는 앱의 미처리 예외는 **사용자 입력을 기다리는 대화상자**가 되어 프로세스가 멈춘다. 실제로 5분 타임아웃까지 걸려 강제 종료했다. → `tell()`(messagebox)로 교체하고 **재빌드·재실행으로 해소 확인**. 회귀 잠금 2건 추가(`gui.main` 에 `print` 금지 · `tell` 은 콘솔·디스플레이 없이도 안 죽음).
- **스택을 SPIKE-02 권고에서 바꿨다** — SPIKE 는 Tauri 를 **추정**으로 권고했으나 실측이 전제를 뒤집었다: Rust 없음 / Windows 파이썬 3.14 실재 / tkinter 8.6 동작 / 러너 서드파티 0. 러너가 이미 파이썬이므로 tkinter 면 **sidecar 가 불요**하다. 추정을 실측이 이긴 사례이며, SPIKE 가 그것을 「추정」으로 표기해 둔 것이 이 전환을 가능하게 했다.
- **경계 검사를 「안 한다」로 걸었다** — `test_client_never_touches_vendor_credentials` 가 `.credentials.json`·`auth.json`·`keychain`·`*_token` 등을 금지어로 검사한다. 2026년에 Google 이 이 선을 넘은 도구의 **유료 구독자 계정을 정지**했으므로, 이 테스트가 지키는 것은 코드 품질이 아니라 **사용자 계정**이다.
- **커버리지 과장 금지** — gemini 는 SPIKE-02 에서 로그인 명령을 실측하지 못했다. 지어내지 않고 「이 프로그램에서 대신 실행할 수 없습니다」로 강등하며, 테스트가 그 문구를 잠근다(P0-I 계약).
- 단위 20/20 PASS. **미수행**: 실 서버 연결 왕복(유효 토큰 필요) · gemini 경로 · 서명 없는 설치의 사용자 체감.
- Human Approval Needed: 아니오 — 사용자가 2026-09-03 에 Windows 우선·미서명 진행을 명시 결정했다.

## REV-20260903T142000-ai-claude-anchor-scope-followup [SKIPPED:doc-only-gate-fix] — ACCEPTED
- Related TASK: TASK-20260903T140000 (후속)
- Timestamp: 2026-09-03T14:20:00+09:00
- 범위: **문서 2건**(ANCHOR §4 형식 · TASK §9 Requested Scope). 코드 변경 **0**.
- **[SKIPPED] 사유**: 직전 cycle 의 게이트 지적을 해소하는 문서 수정이고, 그 cycle 에서 이미 실 Windows 실측 + 20건 테스트로 검증했다. 새 신뢰경계·새 코드 경로가 없다.
- **배운 것 (기록 가치, 실증으로 확정)**: §4 검사기의 「비었음」 판정은 `^<!--` · `^-->` · 빈 줄 · `^(엔트리 없음` 로 **시작하는 줄만** 걸러낸다 — HTML 주석의 **본문 줄은 걸러내지 않는다.** 그래서 템플릿 §4 의 설명 주석을 남겨 두면 「내용은 있는데 엔트리는 0」이 되어 FAIL 한다. 통과하는 다른 feature 들(0041·0043)을 실측하니 전부 §4 가 **헤더 + `(엔트리 없음…)` 한 줄**뿐이었다.
  - ⚠ **추측으로 두 번 헛돌았다**: ① 산문을 성의 있게 채웠다가 FAIL ② 템플릿 §4 를 통째로 복원했다가 또 FAIL. 세 번째에 **통과하는 파일을 직접 대조**해서야 원인을 잡았다. 검사기의 계약은 문서가 아니라 **통과하는 실물**에 있다.
  - `created_at` 이 미래 시각(14:00Z)이던 것도 함께 정정했다(현재 02:20Z).
- Human Approval Needed: 아니오.

## REV-20260903T232000-ai-claude-client-actually-runs — ACCEPTED (자기 적대 리뷰)
- Timestamp: 2026-09-03T23:20:00+09:00

**왜 기존 20건이 셋 다 놓쳤나.** `test_client_core.py` 는 `core` 함수를 **직접 호출**했다.
진입점은 아무도 건드리지 않았고(`grep -c __main__` = 0), 지문 테스트는 **자기가 만든 값**을
넣어 클라이언트 계산과 같은 모양이라 늘 통과했다. 프로세스 경계 양쪽을 각각 시험하면 값의
«모양» 어긋남은 잡히지 않는다 — 봉투는 **상대 소스에서 재현**해야 한다. 그래서
`test_fingerprint_seam.py` 는 `oauth_as.py` 의 포맷터를 읽어 와 봉투를 만든다.

**vacuous 방지.** 각 축에 대조군을 넣었다 — `client/__main__.py` 를 최상위로 돌리면 **여전히
죽는다`(E1 전제)`, CP949 스트림은 수정 없이는 **정말 예외를 낸다**, 서버 봉투와 원본은
정규화 없이는 **실제로 다르다**. 정규화가 검증을 무력화하지 않았음도 함께 단정한다(다른
인증서·잘린 지문은 여전히 거부).

**배경 조작 실측(사용자 요구).** 포커스를 뺏지 않고 Tk 를 구동하려 4가지를 시도해 전부
실패했다 — toplevel 좌표 `PostMessage`, 버튼 `TkChild` HWND 직접, `WM_ACTIVATE`+`WM_SETFOCUS`
후 클릭, 격리 데스크톱(`CreateDesktop`+`lpDesktop`)에서 활성화 후 클릭. Tk 는 실제 입력이
아니면 위젯을 구동하지 않고, 격리 데스크톱은 입력 데스크톱이 아니라 `SendInput` 도 닿지
않는다. 결국 Tk 자신의 `invoke()` 로 눌렀다 — **버튼에 걸린 command 를 그대로 호출**하므로
배선은 통과하고 OS 히트테스트 계층만 건너뛴다.

**정직하게 남기는 한계 2건.**
1. `invoke()` 는 **소스 패키지**를 구동한다. 동결된 exe 축은 「실행되어 준비 상태 도달」까지만
   실측됐다(스크린샷). exe 안에서의 버튼 클릭은 검증되지 않았다.
2. 웹 체크리스트의 「답할 AI 있음」은 여전히 ❌ 다. 러너의 caps probe 가 `claude` 에서
   실패한다(「답을 받지 못했습니다」). **이것은 이 클라이언트 탓이 아니다** — 13:12·14:23 의
   `ai: auto` 런(다른 build 포함)도 같은 실패를 겪었다. 별도 cycle 대상으로 남긴다.

## REV-20260903T234500-ai-claude-fingerprint [CODEX:core.py 지문 비교] — ACCEPTED
- Timestamp: 2026-09-03T23:45:00+09:00
- 도구: `codex exec -s read-only` (codex-cli 0.152.1). ⚠ **첫 호출은 10분 무응답이었다** —
  여러 줄 코드 블록을 `$(cat file)` 로 실어 보낸 것이 원인이었고, 원인을 먼저 진단하라는
  사용자 지시에 따라 최소 프롬프트로 가르니 **4초**에 응답했다. 프롬프트를 3줄로 줄여 재요청.

**지적 3건과 처리**

1. `split()` 이 위치 무관하게 모든 공백을 지운다 — **의도**다. 사용자가 웹에서 값을 복사해
   붙이는 경로가 있고, 표기 변형을 흡수해도 **다른 값이 통과하지는 않는다**. 불채택.
2. `:`·`-` 를 개수·위치 제한 없이 지운다 — 같은 이유로 불채택.
3. **정규화 후 `^[0-9a-f]{64}$` 를 검증하지 않는다 — 채택.** 이것이 실질적이다.
   기대값이 `":"` 이면 truthy 라 `if plan.ca_sha256` 가드를 통과하고, 정규화 후 `""` 가 된다.
   계산값도 `""` 라면 **둘이 같다고 판정된다.** 지금 계산 경로(`hashlib`)는 항상 64자를
   내므로 도달 불가능하지만, **무결성 검사가 「우연히 도달 불가」에 기대는 것은 옳지 않다.**
   → `fingerprints_match()` 신설, 두 호출부 모두 경유. 테스트 10건 추가.

**뮤테이션 13종 중 11 KILL · 2 생존 — 생존 2건은 등가 뮤턴트다(구멍이 아니다).**

- `F8 형식 검증을 한쪽만(a 만)`: `_HEX256.match(a)` 를 통과하고 `a == b` 가 성립하면 `b` 도
  반드시 유효하다. `b` 검사는 **결과를 바꿀 수 없다**. 대칭성을 위해 코드에는 남긴다.
- `F6 return a.startswith(b)`: 형식 검증이 양쪽을 **정확히 64자**로 강제하므로, 같은 길이
  문자열에서 `startswith` 는 `==` 와 동치다. → 즉 **codex 지적을 반영한 형식 검증이 이 뮤턴트를
  등가로 만들었다.** 반영 전이라면 잘린 지문이 통과했을 것이고, 그때는 진짜 구멍이었다.

등가 뮤턴트를 죽이려고 테스트를 더 쓰지 않는다 — 죽일 수 없는 것을 죽이려 들면 구현을
그대로 베낀 테스트가 되고, 그것은 회귀를 잡지 못한다.

## REV-20260904T003000-ai-claude-packaging [SKIPPED:형식 전환·자기 적대 리뷰] — ACCEPTED
- Timestamp: 2026-09-04T00:30:00+09:00

**무엇을 버렸는지 먼저 적는다.** 나는 「exe 가 자기 자신을 파이썬으로 쓴다」를 구현하다가
멈췄다. 그 접근은 동작했을 수 있지만, PyInstaller 가 못 보는 러너 모듈을 **빌드 시점에 계속
추측**해야 하고(러너는 런타임에 서버에서 받는다) 표준 밖 동작이라 다음 사람이 읽지 못한다.
형식을 바꾸니 그 코드가 **통째로 필요 없어졌다** — 우회가 필요했다는 사실 자체가 형식이
틀렸다는 신호였다.

**남은 위험과 대응**
- 임베더블 배포판은 `._pth` 로 `sys.path` 를 좁히고 `site` 를 끈다. 러너가 쓰는 모듈이
  빠지면 설치는 성공하고 **연결만** 실패한다 → 빌드가 실제 임포트로 검증하고, 테스트가
  「검증 목록이 러너의 실제 임포트를 덮는가」를 러너 소스와 대조한다.
- `fetch_embedded_python` 은 인터프리터가 없으면 **예외**다. 빈 폴더를 돌려주면 런타임 없는
  설치본이 나가고, 그것이 이 cycle 이 고친 결함 모양 그대로다.
- 자동 시작은 **기본 꺼짐**. 요청하지 않은 상주는 놀라움이고, 이 프로그램은 사용자의 AI
  사용량을 쓴다.

**정직하게 남기는 한계**
- 동결 앱 **안에서의 버튼 클릭**은 여전히 미검증이다. 배경 조작으로 Tk 를 구동할 수 없었다
  (4가지 시도 전부 실패 — REV-20260903T232000). 대신 버튼이 부르는 경로를 인터프리터 축에서
  직접 확인했다: 설치본의 `runtime\python.exe` 로 러너 `--check` 가 exit=0 이다.
- 코드 서명은 여전히 없다(사용자 결정). 설치기 첫 실행에 SmartScreen 이 뜬다.

## REV-20260904T013000-ai-claude-wsl-and-scheme [SKIPPED:자기 적대 리뷰] — ACCEPTED
- Timestamp: 2026-09-04T01:30:00+09:00

**가장 중요한 교훈은 내 오판이다.** 나는 타임아웃 2회를 근거로 「Windows claude 는 답하지
못한다」고 문서에 적었다. 재측정에서 같은 런타임이 17.8초에 답했다. **관측이 제한시간에
걸린 것과 대상이 불가능한 것은 다른 사실**이고, 나는 그 둘을 뭉갰다. 사용자에게도 그렇게
보고했으므로 여기와 보고 양쪽을 정정한다.

**두 번째 오판.** 모든 런타임에 `-p` 를 써서 `codex` 를 「답하지 못한다」로 판정했다.
0.1초 실패는 「못 한다」가 아니라 **내가 잘못 불렀다**는 신호였다 — 빠른 실패와 느린 실패를
구분해 읽었어야 했다. 정본(`_RUNTIME_SPECS`)을 확인하니 codex 는 `exec` 하위명령이었다.

**vacuous 방지.** 스킴 파싱은 다른 스킴·빈 값·모르는 키를 각각 거부하는지 단정하고,
WSL 축은 «WSL 없음» 대조군을 둔다. 러너 인자는 두 진입점(`check_connection`·`spawn_runner`)이
같은 조립기를 쓰는지 AST 로 확인한다 — 한쪽만 고치면 확인은 WSL 로 가고 상주는 Windows 로 간다.

**정직하게 남기는 것**
- `--cmd` 경로에서는 러너가 능력을 신고하지 않아 웹의 「답할 AI 있음」이 ❌ 로 남는다.
  답변은 정상이다. 근본 해소는 러너가 WSL 자리를 아는 것이고 별도 cycle 이다.
- 연결 주체를 웹에서 클라이언트로 완전히 옮기는 일(사용자 요구 B)은 **이번에 하지 않았다**.
  이번 것은 그 전제(스킴 인계·쓸 수 있는 AI 도달)를 세운 것이다.

## REV-20260904T020000-ai-claude-ssot-catch [SKIPPED:CI 적발 대응] — ACCEPTED
- Timestamp: 2026-09-04T02:00:00+09:00
- CI 의 `test_name_ssot.py` 가 내 새 테스트 파일에서 **옛 스킴 리터럴**을 잡아 적색이 됐다.
  로컬 feature 테스트만 돌려서는 안 보였다 — 게이트가 다른 feature 에 있었기 때문이다.
- 허용 목록에 예외를 뚫지 않고 **정본(`shared/dqa_identity.LEGACY_SCHEME`)을 읽도록** 고쳤다.
  예외를 뚫으면 그 파일은 개명 게이트의 사각지대가 되고, 다음 개명 때 조용히 남는다.
- 교훈: 이 저장소의 게이트는 feature 경계를 넘는다. 커밋 전 회귀는 **관련 feature 까지**
  함께 돌려야 한다(이번엔 `feature-0043` 의 SSOT 테스트가 그것이었다).

## REV-20260904T020500-ai-claude-gate-order [SKIPPED:절차 수정] — ACCEPTED
- Timestamp: 2026-09-04T02:05:00+09:00
- 이 커밋은 앞 커밋의 **절차 오류를 바로잡는다**. 나는 `verify-completion` 결과를 확인하기
  전에 `git commit && git push` 를 한 줄로 이어 붙여 실행했고, 게이트는 그때 FAIL 이었다.
  셸에서 `A; B` 로 이으면 A 의 실패가 B 를 막지 못한다 — 게이트를 통과 **조건**이 아니라
  통과 **표시**로 쓴 셈이다. 앞으로 이 자리에서는 `A && B` 로 잇는다.
- 내용 변경 자체는 CHG-20260904T020000 참조. 리터럴 제거 후 `test_name_ssot.py` 를 포함해
  재실행했고(feature-0046 114건 + SSOT 22건) 전건 통과했다.

## REV-20260904T033000-ai-claude-client-first-handoff [SKIPPED:자기 적대 리뷰] — ACCEPTED
- Timestamp: 2026-09-04T03:30:00+09:00

**가장 큰 교훈은 내 검증 방식의 결함이다.** 직전 cycle 에서 나는 스킴 URL 을 직접 조립해
클라이언트에 넣고 「동작한다」고 보고했다. 그것은 하네스가 자기 자신을 시험한 것이고,
**프로세스 경계의 «봉투 모양»** 은 그 방식으로 절대 검증되지 않는다. 같은 결함 클래스를
이 저장소에서 이미 겪었는데(지문 대문자·콜론) 또 반복했다. 처방은 같다 — **봉투는 상대
소스에서 가져온다**. 이번 테스트는 `dqa_identity.scheme_url()` 을 호출해 만든다.

**SSOT 테스트를 고친 이유.** `test_name_ssot.py` 가 `_ident.scheme_url(token)` 리터럴을
요구해 인자 추가만으로 깨졌다. 의도는 「헬퍼를 거친다」이지 인자 개수가 아니므로 호출
형태 대신 **자체 조립 금지**를 단정하도록 바꿨다 — 원래 의도를 더 정확히 잠근다.

**신뢰 판단을 명시한다.** 딥링크에 `base` 를 싣는 것은 「남이 만든 링크를 클릭하면 그
서버에서 러너를 받는다」는 위험을 연다. CA 지문 대조는 방어가 안 된다(지문도 같은 URL 에서
온다). 그래서 TOFU 고정을 뒀다. 첫 연결은 물을 근거가 없어 통과시킨다 — 종전 복사·붙여넣기
경로와 **같은 신뢰 수준**이고, 화면이 서버 주소를 보여 준다. 이것은 완전한 해소가 아니라
**위험을 첫 1회로 좁힌** 것이다.

**정직하게 남기는 것**: 연결 주체의 완전한 이동(클라이언트가 스스로 토큰을 받는 흐름)은
아직이다. 이번 것은 웹이 인계를 제대로 넘기게 만든 것이다.

## REV-20260904T041000-ai-claude-gate-order-2 [SKIPPED:절차 수정] — ACCEPTED
- Timestamp: 2026-09-04T04:10:00+09:00
- 같은 절차 오류를 **두 번** 냈다: 게이트 결과를 확인하지 않고 커밋·푸시. 첫 번째 때 남긴
  교훈이 「`A && B` 로 잇는다」였는데, 그것은 **형태**에 대한 교훈이라 형태가 달라지자
  (verify 를 별도 명령으로 분리) 그대로 재발했다.
- 교훈을 다시 적는다: **게이트는 통과를 «조건» 으로 써야 한다.** 출력이 눈에 보이는 것과
  그 값이 다음 동작을 막는 것은 다르다 — 사람이 읽고 판단하는 자리는 자동으로 건너뛰어진다.
- 이 저장소가 다른 곳에서 반복해 배운 것과 같은 형태다(「보고된 green 이 곧 검증은 아니다」).

## REV-20260904T050000-ai-claude-connect-page-launch [SKIPPED:자기 적대 리뷰] — ACCEPTED
- Timestamp: 2026-09-04T05:00:00+09:00

**같은 실수의 세 번째 변형이다.** ① 지문 봉투(서버 모양을 안 봄) ② 딥링크(내가 만든 URL 로
검증) ③ 이번(문구가 가리키는 버튼이 그 화면에 있는지 안 봄). 셋 다 **내가 만든 것끼리
맞춰 보고 «검증했다»고 말한** 형태다. 공통 처방은 하나다 — **상대편 실물을 가져와 확인한다.**

이번에는 그것을 테스트 구조로 강제했다: 문구를 찾으면 **그 화면의 버튼 목록에서 실재를
확인**하고, 그 id 가 **해당 화면의 스크립트에 배선**돼 있는지까지 본다. 두 화면 모두에
같은 파라미터로 돌려 한쪽만 고쳐지는 것을 막는다.

**정직하게 남기는 것**: 이 cycle 은 화면 구조만 고쳤다. 실제 클릭으로 클라이언트가 뜨는지는
배포 후 PB-0008 로 확인하며, 그 결과를 `test-runs.d` fragment 에 덧붙인다.
