---
run_at: 2026-07-28T11:40:15+0900
session: ai/claude/feature-0016-graph-edge-flow
scope: unit/feature-0003-agent-web-ui/src/static/graph (graph-renderer-pixi·graph-roleviz·graph-core)
verdict: PARTIAL
---

### Run (2026-07-28) — graph-edge-flow(§83): 관계선 방향성 곡선 + 밀도 누적 + 부모 볼륨 다발 — **Environment: Windows-browser (곡선·반투명·다발 라이브 PASS / 읽기·쓰기 2선 육안·곡선 히트테스트·드래그 추종은 POST-DEPLOY 잔여)**

#### 1. 헤드리스 격리검증 (Environment: CLI — 계약 잠금)

- 신규 `tests/headless/test_graph_edge_flow.js` **48 PASS / 0 FAIL** (번들 인자 = graph 7모듈 sed 연결 레시피, adapter 는 기본 경로):
  - **A. 곡선 기하(PixiAdapterPure)** — A1 곡률 0=직선 · A2 **방향 뒤집으면 반대편 호**(왕복 관계선 분리의 근거) · A3 `Q(0.5)` 중점 편차 = `off`(컨트롤 포인트 수학) · A4 상한 44px 클램프/하한 5px 보장 · A5 샘플 끝점 정확 일치 · A6 adaptive 세그먼트(줌아웃 시 감소·lowFi 상한 축소·하한 보장) · A7 **대시 위상 연속**(폴리라인 구간 경계에서 리셋 안 됨 — 총 on 길이가 직선 `dashSegments` 와 동치) · A8 다발 오프셋 중앙 대칭·간격·6가닥 클램프 · A9 **곡선 인지 히트테스트**(호 위 점=히트, 직선 중점=미히트, 직선 엣지는 종전대로) · A10 역방향 조회 시 곡률 부호 반전.
  - **B. 스타일 어휘(graph-roleviz)** — 기본선 <1.2px·`strokeOpacity` 0<α<1 · 신뢰 강도↑=진함 · 전 분기 곡률 주입 · 단일 관계는 `strands` 키 미방출 · 관계 수→가닥(1~3=1 / 4=2 / 8=3 / 16=4 / 1000=4 클램프, 단조 비감소) · SCHEMA_REF 굵기·불투명도 단조 상승하되 굵기는 완만(<1.8px, 포화 방지).
  - **C. 빌드 계약(graph-core `_metaG6Build`)** — **C1 읽기+쓰기 = 관계선 2개**(종전 1개 병합) · C2 각각 반대 화살표(read=startArrow / write=endArrow)+둘 다 곡선+집계 id 분기 · C3 읽기만이면 1개·`relation_type` 미상은 읽기 폴백 · C4 부모 카드로 승격된 9건 = 1선 + 가닥 3.
- **적대 검증(테스트가 실제 회귀를 포착하는가)**: 같은 시나리오를 `main` 시점 모듈 번들로 실행 →
  `OLD: ROUTINE_USES 관계선 1개 · 방향키=[{start:false,end:false,curve:0}]` vs
  `NEW: 2개 · [{start:true,curve:0.15},{end:true,curve:0.15}]`.
  OLD 가 방향을 **둘 다 잃은 것**(집계 시 `delete s.startArrow`)까지 재현 — C 섹션이 회귀를 실제로 잡는다.
- **회귀 0**: 그래프 전 스위트 **536 PASS / 0 FAIL**
  (edge_visibility 73 · minimap_reuse 65 · detail_dbgroups 62 · edge_flow 48 · category 26 · colnav 22 · collod 20 · layoutmemo 19 · vpack 19 · reveal 17 · simgroups_p2 16 · cullrefkeep 15 · agglod 8 · detail_colsel 8 · pixi_adapter 112 · viewportcull 6).
  `test_g6build_edge_visibility.js` T1 은 **계약 갱신**(볼륨 인코딩이 굵기 단독 → 다발 가닥) — 임계 완화가 아니라 새 인코딩(가닥 3 + 굵기·불투명도 동반 상승 + 곡률)으로 재기술.
- `node --check` 3모듈 PASS · pytest 전량 PASS · ruff PASS.
  ⚠ 최초 pytest 7 FAIL 은 **worktree base stale**(main 이 5커밋 진행 → 마운트 소스와 컨테이너 baked 코드의 `web_context` 심볼 불일치)이 원인 — `origin/main` 리베이스 후 전량 PASS. 본 변경과 무관.

#### 2. 라이브 시각검증 (Environment: Windows-browser)

- 방법: PB-0008 — `bin/win-browser.py`(실 Windows Chrome **150.0.7871.115**, CDP relay `172.26.144.1:9223`) → `https://localhost/admin` → 지식베이스 > 그래프 뷰.
  정적 자산이 web 이미지에 baked 이므로 web-a/web-b 에 변경 3모듈을 `docker cp` 주입(+ 자산 스탬프 정규화로 모듈 캐시 우회).
- **PASS 확인**:
  - **곡선 렌더**: 공용(common) 스코프 제품(19)↔데이터소스(27) `USES` 관계선이 직선에서 **부드러운 호**로 전환됨을 육안 확인(스크린샷 `edgeflow-01/03`). 방향이 반대인 선끼리 서로 다른 쪽으로 휘어 갈라진다.
  - **가늘고 반투명 + 밀도 누적**: 개별 선은 옅고, 여러 선이 한 데이터소스로 수렴하는 구간에서 색이 **눈에 띄게 진해짐**(요구 ② 의 "겹칠수록 명시적" — `edgeflow-03` 중앙 수렴부).
  - **부모 볼륨 다발**: mysql-gz-dev 스키마 카드 `gunzgame ↔ gunzlog`(SCHEMA_REF count **47** → 가닥 4)가 단선이 아닌 **다발**로 렌더(`edgeflow-07` 확대).
  - **무에러**: 그래프 뷰 진입·데이터소스 스코프 전환(common → mysql-gz-dev)·fit·줌 전 과정 `window.__errs` 누적 **0**.
- **미검증(POST-DEPLOY 잔여)**: ① 같은 두 객체의 읽기/쓰기 2선이 반대편 호로 갈라지는 **육안** 확인(루틴 보유 스코프 필요) ② 곡선 엣지 우클릭 히트테스트(real-mouse) ③ 노드 드래그 중 lowFi 강등 → dragend 고품질 복원.
  사유: 검증 중 **병렬 세션의 재배포로 컨테이너가 2회 교체**(`0062acb3` → `6d7fe391`)되어 주입본이 소실. 라이브 경합 위험이 커 주입 QA 를 중단하고 **이미지 원본으로 즉시 원복**(admin.html·admin.js·graph/*.js 를 `mysql-ai-web:6d7fe391` 에서 추출해 web-a/web-b 복원, `admin.js?v=7fc11a708399` 정상 · `/livez` 200 · 잔재 grep 0).
  → 위 3항목은 머지·배포 후 **POST-DEPLOY PB-0008** 로 검증한다(`visual_verification_scope: always`).
- 증거: `artifacts/tmp/edgeflow-0{1,3,5,6,7}-*.png` (라이브 캡처 5매).

#### 3. 성능 관점

- 실선 곡선은 Pixi 네이티브 `quadraticCurveTo`(내부 tessellation) — **샘플링 0**. 대시 곡선만 화면 픽셀 기준 adaptive 샘플(5~22, 줌아웃 시 자동 감소).
- 드래그 중(`_lowFi`)엔 가닥 1개·세그먼트 상한 10 으로 강등하고, `dragend` 의 flush 가 **저품질로 그린 엣지 전량을 고품질로 복원**한다(`_lowFiTouched` 누적 — `_refreshIncidentEdges` 가 `_objSig` 를 갱신하므로 복원하지 않으면 저품질 도형이 다음 full draw 에서 '서명 동일=재사용' 으로 살아남는다).
- 단일 가닥 엣지(대다수)는 모듈 상수 `EDGE_NO_STRAND` 를 재사용해 페인트마다의 배열 할당을 없앴다.
- 집계선 굵기 가산(`+0.8`)을 제거하고 가닥 수로 대체 — 굵은 선 1개보다 얇은 선 N개가 fill rate 상 유리하고, 관계 수 신호가 상한에서 포화되지 않는다.
