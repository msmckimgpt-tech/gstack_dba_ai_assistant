---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0003-agent-web-ui
agent: backend
timestamp: 2026-09-08T13:15:00+09:00
trigger: API/response shape/스키마 keyword matched — 단계 표시 payload 조립 변경
verdict: BLOCK
---

리뷰 방식: 라이브 원장 `agent_runtime.steps` **9,759행 전건 재생**(실제 `_resolve_step_display`·
판정기 실행). staged diff 가 아니라 워크트리 실물 기준.

### 1. Blocking issues

- **B1 (critical) 규칙 (c)가 정상 제목 394행을 파괴한다.**
  - Evidence: 걸러낸 412행 중 실제 도구 구문은 18행뿐이고 394행(96%)이 정상 제목이었다
    (전부 `work_source='llm'` — 제보와 무관한 내부 에이전트 경로). 실측 before→after:
    `dk_game_release_240의 Character 테이블 구조 확인 (NOT EXISTS 비교 대상)` →
    `` `dbo`.`Character` 구조를 확인한다 ``. execute_sql 192행에서는 사용자 어휘(`log_v2`)가
    내부 카탈로그(`information_schema.tables`)로 치환돼 **내부 식별자 노출이 오히려 늘었다**.
  - Location: `unit/feature-0003-agent-web-ui/src/routers/_conv_store.py:6315`
  - Reason: 수용기준 「human-written narration passes through」가 394행에서 깨진다. 「대체값이
    같은 사실을 담아 손실이 없다」는 정당화도 실측에서 성립하지 않는다. 손익비 1:22.
  - Action: (c) 를 제거하거나 「선두 식별자이면서 그 이름이 서버 도구 census 에 있을 때」로
    좁혀라. 판정기 변경마다 라이브 재생으로 오탐률을 산출해 회귀 기준선으로 세워라.
- **B2 (high) 사유 축은 대체가 없는데 정상 사유 8건이 통째로 삭제된다.**
  - Evidence: 규칙 (b) 로 「… 단일 execute_sql 쿼리로 UNION 집계하여 …」가, 규칙 (a) 의 kwargs
    가지로 「…(CREATE_OPTIONS='partitioned')…」가 지워진다.
  - Location: `src/routers/_conv_store.py:4237`
  - Reason: work 축에는 파생 대체가 있으나 표시층 reason 축에는 없어 **순수 손실**이다.
  - Action: 표시 경로도 `_derive_step_reason` 으로 대체하고, 사유에서는 (b)·kwargs 가지를 꺼라.
- **B3 (high) index 와 워크트리가 달라 커밋 대상에 이미 고친 결함이 살아 있다.**
  - Evidence: `MM` 3파일 + `??` test-runs fragment. staged 판에는 식별자 폴백이 남아 있다.
  - Location: `docs/test-runs.d/TASK-20260908T125500-step-tool-syntax-leak.md`(untracked)
  - Reason: 이 상태로 커밋하면 수정의 핵심이 무효화되고, node 없는 CI 에서 L8 이 FAIL 한다.
  - Action: 전부 stage 후 컨테이너에서 `make test` 재실행, diff 를 워크트리 기준으로 재생성.

### 2. Cross-domain concerns

- **C1 판정 (b) 의 모수가 «이 행의 도구 하나»** — 다른 도구 이름은 통과한다. 전각·제로폭 우회도 통과.
  - Location: `src/routers/_conv_store.py:6348`
  - Reason: 배지 표는 census 전체를 모수로 삼는데 제목 판정은 행의 도구 하나만 본다(§16.7 G12).
  - Action: (b) 의 모수를 census 로 확장하고 NFKC + 제로폭 제거를 넣어라.
- **C2 `intent` 가 sanitize 없이 클라이언트·익명 공유 뷰로 나가고, 렌더러 한 곳이 아직 제목으로 쓴다.**
  - Evidence: 라이브 `intent LIKE '%{''%'` **21행**. `static/app.js:4102` 이 `step.intent` 폴백.
  - Location: `src/app.py:2131` · `src/static/app.js:4102`
  - Reason: 스코프의 「intent·tool 폴백 제거」가 렌더러 3개 중 1개에만 적용됐다.
  - Action: `_SHARE_STEP_ALLOWED_KEYS` 에서 `intent` 제거, 활동 행 렌더러를 공용 헬퍼로 통일.
- **C3 진행/완료 경로가 같은 행에 다른 `work_source`·`reason_source` 를 낸다.**
  - Location: `src/routers/ai_tools.py:4283` · `src/routers/_conv_store.py:4252`
  - Reason: 「출처 표기 정직화」가 목표인데 두 경로가 다른 값을 말하면 판정 불가가 된다.
  - Action: 단일 이음매로 합치고 동치 단언에 출처까지 포함하라.
- **C4 `_derive_step_work_tail` 이 `sql_text` 를 agent_core 의 `tool_result` 슬롯에 넘긴다.**
  - Location: `src/routers/_conv_store.py:6480`
  - Reason: 그 인자가 truthy 면 agent_core 가 `` `{tool}` 도구를 실행한다 `` 분기를 켠다 —
    마스킹된 결함을 계약으로 삼고 있다.
  - Action: 세 번째 인자를 제거하라.
- **C5 배지 표 모수가 «정의 census» 라 라이브 배출 2종을 영구히 놓친다.**
  - Evidence: 원장에 `materialize_attachment` **48행**, `query_sql` 1행. 둘 다 표 밖 → 배지 `"도구"`.
  - Location: `src/static/app.js:3415` · `tests/test_step_tool_syntax_leak.py:409`
  - Reason: 수용기준 (d)가 정의 census 로만 충족되고 실제 배출 census 와 어긋난다.
  - Action: 두 이름을 census·라벨 표에 넣고, census 의 정의(정의 vs 배출)를 문서화하라.
- **C6 `_resolve_step_display(None)` 이 AttributeError 로 죽는다.**
  - Location: `src/routers/_conv_store.py:4252`
  - Reason: None 을 흡수하도록 쓰인 함수가 새 분기에서만 죽는다. 행 단위 루프라 한 행이 죽으면
    그 대화의 단계 표시가 통째로 사라진다.
  - Action: 원문 판정에 쓰는 값을 None-safe 하게 붙잡아라.

### 3. Challenge to current spec

수용기준 (a)의 전제(「파생이 원문보다 낫다」)가 라이브에서 412행 중 18행에서만 성립한다.
스펙을 「도구 구문을 지운다」가 아니라 **「도구 호출 표기(인자 매핑 리터럴 + 도구 이름)만
지운다」**로 좁히는 편이 사용자 가치가 크다. 수용기준 (d)의 census 정의(정의 vs 배출)가
비어 있고, 「내부 식별자를 숨긴다」와 「무엇을 했는지 알린다」가 execute_sql 에서 충돌하는데
어느 축이 우선인지 스펙에 없다. 위협 모델도 명시되지 않았다 — 적대적이면 정규화가 필요하고,
협조적이면 (c) 같은 과잉 차단이 정당화되지 않는다. (ReDoS 는 문제없음: n=20000 에서 3.15ms.)

### 4. Verdict

BLOCK
