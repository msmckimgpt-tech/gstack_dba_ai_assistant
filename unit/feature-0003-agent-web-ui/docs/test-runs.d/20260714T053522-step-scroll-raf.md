---
run_at: 2026-07-14T14:35:22+09:00
session: ai/claude/feature-0003-step-scroll-raf
scope: TASK-20260714T053522-step-scroll-raf
verdict: PASS (code/unit de-risk) · real-browser DEFERRED(post-deploy 사용자/PB-0008)
---

### Run (2026-07-14) — step-scroll-raf: 펼친 "결과 보기" 가로 스크롤 layout-timing 0-clamp 후속 — **Environment: Windows-browser**

- 배경: step-scroll-preserve(REQ-...T015432) 배포 후 사용자 재보고 — **가로 스크롤이 지속적으로 초기화 여전**(스크린샷: character 테이블 구조 결과셋, 하단 가로 스크롤바 리셋). 라운드1 은 동기 복원이라 재렌더 직후 결과 표 layout 미확정 상태에서 `scrollLeft` 를 써 브라우저가 0-clamp.
- **코드/단위 de-risk (수행)**:
  - `node --check app.js` PASS.
  - `tests/verify_step_result_scroll_preserve.mjs` **29/29 PASS** (+5 신규): [3b] `_scheduleStepPanelScroll` 이 동기 `_applyStepPanelScroll` + `requestAnimationFrame` 재적용 배선 · [7] 동기 복원 즉시 반영 + rAF 콜백 1건 예약 + rAF flush 후 0→값 재복원(0-clamp 복구 경로) + rAF 큐 소진(재진입 경합 없음). 기존 [1]~[6](배선 양경로·capture/restore 계약·접힘 skip·새 단계 무영향·방어) 유지.
- **real-browser 실측 — DEFERRED(배포 후)**: 로컬에 chromium 이 없고(bun/puppeteer/playwright 미설치, chromium 다운로드 환경 차단) headless 실브라우저 repro 를 만들지 못했다. jsdom 은 `scrollLeft` 를 clamp 없이 verbatim 저장하므로 이 결함(실브라우저 0-clamp)을 격리 테스트로 재현 불가 — 라운드1 이 통과했음에도 실패한 원인이 이것이다. 따라서 근본원인(well-known layout-timing 클래스)에 표준 해법(rAF 재적용)을 additive 로 적용하고, 최종 시각확인은 **배포 후 사용자 실측 + PB-0008** 로 닫는다(visual_verification_scope: always).
- 확인 절차(배포 후): 다단계 실행(가로로 넓은 결과 표) 중 사이드 패널/인라인 progress 카드에서 "결과 보기" 펼쳐 표를 가로 스크롤 → 다음 단계 폴링 도착 시 가로 스크롤 위치 유지. 세로 스크롤·펼침/접기·하단추종 자동스크롤 회귀 없음.
