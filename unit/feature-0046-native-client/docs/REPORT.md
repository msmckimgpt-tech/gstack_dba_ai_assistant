---
doc_type: REPORT
feature_id: feature-0046-native-client
status: active
edit_policy: rewrite
source_of_truth: true
---

# Report

## 1. 현재 상태

**2026-09-04 — 트레이 상주 + 자식 콘솔 창 제거.** 클라이언트가 알림 영역에서 상주한다:
[X] 로 닫아도 연결이 유지되고, 아이콘 오른쪽 클릭으로 창 열기·연결 토글·종료를 하며, 툴팁이
현재 상태를 말한다. AI 를 찾고 물어보는 동안 뜨던 **검은 콘솔 창은 사라졌다**.

실 Windows 실측 4종 전부 PASS(대조군 포함): 자식 콘솔 핸들 `21697080 → 0`, 바탕화면 콘솔 창
peak `2 → 1`, 트레이 7단계, GUI 종단간 6단계. 단위 171건 · 뮤테이션 14/14 KILL.

**실측이 결함 2건을 잡았다 — 리눅스 171건은 그동안 전부 통과 중이었다.** ① ctypes argtypes
누락으로 x64 핸들이 `OverflowError` → 아이콘 등록 자체 실패 ② `MF_DEFAULT` 를 `AppendMenuW`
에 넘겨 **조용히 무시** → 기본 항목 소실. 둘 다 오류도 경고도 없이 기능만 사라지는 형태다.

이전 상태(2026-09-03 초판): AI 감지(PATH 밖 포함) · 로그인 상태 JSON 판정 · GUI 구성 ·
설치 마법사 + 런타임 동봉 · 딥링크 인계까지 실 머신 실측 완료.

## 2. 남은 리스크

- ⚠ **트레이의 도달 범위 — 주 경로에는 아직 없다.** 병렬 cycle(PR #1568)이 `gui.run_client()`
  로 **웹 셸(브라우저 앱 창)을 주 경로**로 만들었고, 트레이가 붙은 tkinter `ClientApp` 은
  **폴백**이 됐다. 즉 주 경로 사용자는 트레이를 만나지 못한다. **깜빡임 수정은 두 경로 모두에
  적용된다**(전부 `core` 를 지난다) — 두 요청의 도달 범위가 다르다.
  확장하려면 「창 닫힘 = 연결 종료」 계약과 **그것을 말하는 웹 페이지 문구**를 함께 바꿔야 하고,
  그 경로는 유효 토큰 + 실 서버 + 브라우저가 있어야 실측된다. **사용자 결정 대기.**

- ⛔ **BLOCKED(권한) — CI 가 이 feature 의 테스트를 여전히 돌리지 않는다.**
  `unit/feature-0046-native-client/tests` 가 `.github/workflows/ci.yml` 의 pytest 경로 목록에
  없다(2026-09-03 신설 이후 계속). 이번에 `pyproject.toml` `testpaths` 에는 등재했지만 **CI 는
  경로를 명시 호출하므로 `testpaths` 를 보지 않는다** — 즉 이 축은 아직 열려 있다.
  푸시가 `workflow` 스코프 부재로 거부됐고(토큰: `admin:public_key, gist, read:org, repo`),
  **AI 에게는 이 파일을 바꿀 수단이 없다.** 조치는 ci.yml 에 1줄 추가 — 사람 또는 workflow
  스코프 토큰. 그 전까지 이 feature 의 177건은 **로컬에서만** 검증된다.
- ⚠ CI 경로 목록과 `testpaths` 는 애초에 서로 다르다(ci.yml 9 · testpaths 5). 그 불일치가
  다음 누락의 온상이며, 구조적 해소(한쪽에서 생성 또는 일치 검사 테스트)는 별도 cycle.

- **미서명 배포** — SmartScreen 경고 2클릭(사용자 결정으로 감수). 대상이 「경고를 무서워하는
  사람」이라 이탈이 생길 수 있다. 계측(ITEM-00 퍼널)이 그 크기를 잴 것이다.
- **gemini 미지원** — 로그인 대행 명령 미실측. 화면이 그 사실을 말한다(과장하지 않음).
- **배포 경로 미구현** — 웹에서 exe 를 받는 경로는 ITEM-03/06 과 함께.
- ~~동결 exe 의 아이콘 추출~~ → **해소**. 실제 배포 형식으로 빌드(exit 0, 2,284,758 B)해
  `ExtractIconW` 로 확인: `icon_count=1` · 유효 핸들. 배포본은 기본 아이콘 폴백이 아니라
  **앱 아이콘**으로 뜬다. 다만 동결 exe 를 **실행한** 트레이 검증은 하지 않았다 — 실행하면
  탐지가 돌아 사용자의 AI 사용량을 쓴다.
- **로그온 자동시작은 여전히 「바로 연결」이 아니다** — 토큰을 저장하지 않으므로(§0.1) 로그온
  기동만으로는 연결 정보가 없다. 트레이 상주는 **연결 이후**의 표면이고, 무인 재연결은 토큰
  보관 설계가 선행이다(별도 cycle).
- **탐색기 재시작 후 아이콘 재등록** — `TaskbarCreated` 처리 경로는 있으나 미실측.

## 3. Git 동기화 결과

- 커밋: 본 cycle
- verify-completion: PASS
- Push / PR: 완료
