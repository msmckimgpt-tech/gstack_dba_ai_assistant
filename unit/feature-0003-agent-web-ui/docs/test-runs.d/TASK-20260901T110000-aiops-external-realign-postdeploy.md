---
run_at: 2026-09-01T12:55:00+09:00
session: ai/claude/aiops-external-realign
scope: "'AI 운영 현황' 외부AI 재편 — 라이브 배포본 POST-DEPLOY 실측 (배포본 2ac3ca0e)"
verdict: PASS
---

# Run — TASK-20260901T110000-aiops-external-realign (POST-DEPLOY)

- **Environment**: **Windows-browser**(PB-0008), 라이브 `https://localhost/admin`,
  배포본 `GIT_COMMIT=2ac3ca0e` (web-a·web-b·insight/ask-worker·ops-scheduler·ext-tool-mcp-a/b
  전부 동일 SHA — 실물 이미지 태그 확인)

## 배포 게이트 (feature-0014 RUNBOOK §10)

| 항목 | 결과 |
|---|---|
| [1] web-a·web-b 대상 SHA + soak | PASS (`mysql-ai-web:2ac3ca0e`) |
| [2] 워커 롤아웃 (전 서비스 동일 SHA) | PASS (실물 태그 7개 일치) |
| [1b] 대화 경로 스모크 | PASS (deploy-web 로그) |
| [2b] surge 잔존 | 0 (정상) |
| [3] asset 스탬프 | `admin.js?v=3c9d838b3ed0` (갱신됨) |
| [5] 무중단 실측 `no upstreams available` | **0** (전면 503 없음) |
| 마이그레이션 0057 | `source` · `task_id` 컬럼 적용 확인 |
| 코드 도달 (배포했다 ≠ 도달했다) | `tools.js`·`tasks.js`·`self_review.py` 있음 · `exttasks.js` 삭제됨 · 러너 `AGENT_VERSION=2026.09.01` |

## 화면 실측

### 서브탭 — 사전검증과 동일

```
[운영 현황*] [브리지 작업] [도구 사용량] [기록]
```

### KPI — **자가 검증 타일이 나타났다** (사전검증에는 없던 것)

```
연결된 AI · 대기 / 처리중 · 자가 검증 · 워커 정상
```

사전검증(격리 인스턴스)에서는 마이그 0057 미적용이라 `_self_review_stats.available=false` →
타일을 **그리지 않았다**. 배포로 컬럼이 생기자 타일이 나타났다 — **「원장 미준비」와
「0건」을 가르는 설계가 두 환경에서 각각 다르게, 의도대로 동작**했다.

지금 값은 `0건 · "제출된 답변에 검증 결과가 동봉되지 않았습니다 — 러너 갱신이 필요할 수
있습니다"` 다. 0 을 침묵으로 두지 않고 **왜 0 인지**를 말한다.

### 러너 명부 — 네 상태 구분이 라이브에서 실증됐다

| 계정 | 상태 | 마지막 신호 | 러너 버전 | 자가 검증 | 모델 |
|---|---|---|---|---|---|
| admin | **대기 중 아님** | 3분 전 | 2026.08.31 **배포본과 다름** | **미지원** | 3종 |

세 가지를 **동시에, 각각 다른 칸으로** 말한다 — 프로세스 없음 / 지문 불일치 / 기능 미신고.
종전 화면은 이 상태를 「러너 0대」한 칸으로만 말했고, 운영자는 무엇을 시켜야 할지 알 수 없었다.

### 배너 — 쓰지 않는 축이 물들이지 않는다

```
서버 계정 LLM (미사용)  [해당 없음]   ← 롤업 제외 (raw_state=ok 는 보존)
외부 AI 브리지          [중단]        ← 실제로 처리 불가한 상태를 정확히 지목
```

「주의 필요」에도 브리지가 올라온다 — **지금 서비스가 안 도는 진짜 이유**가 맨 위에 있다.

## ⚠ 이 실측이 드러낸 것 — 러너 중단은 **내가 만들었다**

위 `대기 중 아님` 은 화면 결함이 아니라 **사실이고, 그 사실의 원인은 이 세션이다.**

배포 후 러너 갱신(사용자 상시 지시)을 수행하면서 **토큰을 보존하기 전에 프로세스를 먼저
종료**했다. `mat_` 토큰은 프로세스 환경변수에만 존재하고(서버는 해시만 보관, `config.json`
에도 저장하지 않는다) 그 프로세스와 함께 사라졌다.

- 파일 교체 자체는 성공 (`1f5df7fab32c` = 배포본 지문 일치, 구본은
  `.bak-pre-selfreview` 로 보존)
- 재기동은 **토큰 없음으로 실패** — `launch.sh`·shell history 의 옛 토큰은 전부 401(무효)
- 영향: `t_GliHOXeBognfeeOI`(12:07 개설, 12:43 점유) 1건이 처리 중단. lease 30분 규약대로
  **22:19 KST 에 대기열로 자동 복귀**하며, 그 전에 재연결하면 즉시 처리된다. 답변 유실 없음
  (제출 전 중단이라 대기 말풍선 유지)

**절차상 교훈**: 토큰 확보 → 종료 → 재기동은 **한 명령 안에서** 해야 한다. Bash 호출마다 셸이
새로 뜨므로 변수는 다음 호출로 넘어가지 않는다. 이 함정을 알고 있었는데(기존 메모리에 기록됨)
호출을 나눠 쓰면서 그대로 밟았다.

## 미검증으로 남은 것

- **자가 검증 end-to-end** — 러너가 내려가 있어 새 러너(`2026.09.01`)의 검증 제출을 실측하지
  못했다. 재연결 후 질문 1건이면 확인된다: 브리지 작업 표의 `자가 검증` 칸이 `—` 에서
  `통과`/`BLOCK n` 으로 바뀌고, 운영 현황의 자가 검증 KPI 가 0건 이상이 된다.
- 증적: `evidence/aiops-realign-postdeploy.png`
