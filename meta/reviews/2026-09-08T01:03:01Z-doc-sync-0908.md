# doc_sync 09-08 — META 정합 검증 아티팩트

- 창: `a93fe005`(직전 doc_sync META-0072) .. `577b5ea1`(HEAD) — 46커밋 / 240파일, 착륙일 전량 2026-09-07
- 방식: 4축 병렬 sweep(릴리즈노트 / wiki / 정책문서 / 전역 폐기값) + 2조 적대 교차검증 (6에이전트 · 1,243,899 토큰 · 오류 0)

## 판정 집계
| 단계 | 수 |
|---|---|
| 축 제안 (wiki 27 · 정책 13 · 폐기값 21 + RN 12항목) | 73 |
| 검증조 verdict | CONFIRMED 66 · PLAUSIBLE 7 · REFUTED 1 |
| overreach 환원 (A3-13 선재 §참조 → report-only) | 1 |
| span 충돌 (같은 바이트 구간 배타 치환) | 13쌍 → 승자 54 |
| 최종 적용 (META) | 54 + ORCH-01 = 55 |

## 이 창의 두 축
1. **로컬 LLM 전면 폐기**(`fac4d1cc`) — `local-llm|ollama|gemma` 정규식이 docs/·wiki/ 에서 93히트/27파일.
   과거시제·아카이브·ADR 이력·릴리즈노트 과거 블록은 정당으로 분류하고 **현재형 명제만** 정정.
2. **터미널 연결 경로 제거**(`836547e5`) — 미러의 현재형 「터미널에 붙여넣기」·「연결 준비」 서술 정정.
   ⚠ `bridge_setup.sh` 는 repo 루트에 실재하므로 「파일이 사라졌다」 류 과잉 정정은 REFUTED 처리.

## 게이트 실측 (변경 후)
- `bin/wiki-lint.sh` → total **32** · orphan PASS 0 · broken WARN **1**(선재 `Log.md:149 [[project_task_conv_entry_defaults]]`)
  · missing-entity 19 · split-suggest 12. **직전 창 baseline 32 로 복귀**(실행 전 33).
- `bin/gen-status.sh --check` → **최신** (frontmatter 21 · passthrough 25 · 신규 0)
- `bin/ssot-lint.sh` → 4 WARN (wiki-sot, baseline 동일)
- `ls -d unit/feature-* | wc -l` = **46** = `wiki/Features/feature-*.md` 46

## report-only (doc_sync 범위 밖)
- `docs/DOC_REGISTRY.md` 의 선재 §참조 깨짐(A3-13) — 창이 만든 drift 가 아니라 정본 자체 문제.
- `wiki/Log.md:149 [[project_task_conv_entry_defaults]]` — 장기 선재 broken link.
- 커밋 `836547e5` 메시지는 제거 항목을 「16건」으로 적고 정본 문서 3곳은 「12건」으로 적는다(정본 내부 모순).
- `unit/feature-0046-native-client` 행: 정본 TASK.md frontmatter 가 `feature_status` 만 갖고 요약 필드가 없어
  `gen-status` 재생성 시 셀이 공란 표준형으로 정규화된다. feature 커밋이 생성 구간 행을 수기로 채웠던 것이
  `--check` 실패의 원인이었다. **정본 frontmatter 보강은 owning feature cycle 의 일**이다.
