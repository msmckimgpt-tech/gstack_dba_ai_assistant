---
run_at: 2026-08-24T19:40:00+09:00
session: ai/claude-corp/feature-0003-reorder-affordance
scope: 재배치 연출 재설계(오버슈트 제거 + 도착 표식) — CHG-20260824T190000-reorder-affordance
verdict: PASS (Environment: Windows-browser)
---

# Run — PB-0008 시각검증 (Environment: Windows-browser)

- 브리지: relay @ `http://172.26.144.1:9223`. 대상: 라이브 web 이미지 + 본 worktree `src/static`
  디렉터리 bind-mount 컨테이너(`web-verify-aff`, `https://localhost:18098`).
- 계측: `tests/pb0008_sidebar_reorder_measure.py`(궤적) + 도착 표식 전용 probe(rail/배지/재렌더).

## A. 모션 — 오버슈트가 사라졌는가 (PASS)

폴더 이름 변경(29px 이동): **8 프레임**(112~226ms), offset 최소값 **+1** — 즉 목표를 지나치는
구간이 없다(오버슈트 곡선이면 음수 구간이 나온다). 거리 적응형 하한(160ms) 대역과 일치.

## B. 도착 표식 — 모션이 끝난 뒤에도 남는가 (PASS, 이 cycle 의 핵심)

| 시점 | rail(`box-shadow`) | 배지 | 비고 |
|---|---|---|---|
| 이동 직후 | `rgb(37,99,235) 3px 0 0 inset` | "이동됨" (`aria-label` 有, `position: absolute`) | 펄스는 0.9초에 종료 |
| **무관한 재렌더 후**(그룹 접기/펼치기 2회) | **유지** | **유지** | ★ 지속성 실증 |
| 3.5초 경과 | `none` | 없음 | 자연 소멸(잔류 없음) |

프레임 추적으로도 확인: 펄스는 ~900ms 에 걷히고 rail·배지는 **3500ms 까지 유지**된 뒤 사라진다
(렌더 1회 상황에서도 동일 — 표식 수명이 렌더 횟수에 의존하지 않는다).

## C. 이 실측이 잡은 결함 2건 (하네스는 통과했다)

1. **후처리 복원의 취약성.** 처음 구현은 "렌더가 끝난 뒤 표식을 되붙이는" 방식이었다. 이름 변경
   직후 이어지는 두 번째 렌더에서 표식이 조용히 사라졌다(재렌더 +60ms 유지 → +360ms 소실).
   → **행을 만드는 순간 부여**하도록 구조를 바꾸고, 그 배선을 하네스 계약으로 잠갔다.
2. **세대 없는 만료 타이머.** 같은 행을 연달아 이동시키면(폴더를 잇달아 만들고 이름 변경) 앞선
   앵커의 3.5초 타이머가 **살아 있는 최신 표식**을 지워, 수명보다 일찍(≈1.5초) 사라졌다.
   → 토큰 동일성으로 **자기 세대만 거두게** 봉인. 연속 이동 계약을 하네스에 추가.

## D. 진단 과정에서 겪은 계측 함정 (다음 사람을 위해)

- **브라우저 캐시**: 정적 자산이 `?v=dev` placeholder 라, 검증 컨테이너를 새로 띄워도 옛 JS 를
  보고 "구현이 안 됐다" 고 오판하기 쉽다 → CDP `Network.setCacheDisabled` 후 재로드.
- **컨테이너가 잡은 옛 디렉터리 inode**: worktree 디렉터리가 재생성되면 bind-mount 는 삭제된
  디렉터리를 계속 서빙한다(파일은 최신인데 컨테이너 안은 옛 내용) → 컨테이너 재생성으로 해소.
  판정은 `docker exec … grep` 으로 **컨테이너 안 파일**을 직접 본다.

## E. 미수행

라이브 서빙본 확인은 배포 후 POST-DEPLOY 로 수행한다.
