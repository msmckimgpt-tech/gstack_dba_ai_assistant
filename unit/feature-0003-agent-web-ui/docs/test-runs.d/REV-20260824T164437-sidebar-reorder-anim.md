---
run_at: 2026-08-24T17:05:00+09:00
session: ai/claude-corp/feature-0003-sidebar-reorder-anim
scope: 좌측 대화목록 명칭 변경 시 재배치 전환(FLIP + 시야 유지) — REQ-20260824-sidebar-reorder-anim
verdict: PASS (Environment: Windows-browser)
---

# Run — PB-0008 시각검증 (Environment: Windows-browser)

- 브리지: `bin/win-browser.py doctor` → `relay @ http://172.26.144.1:9223`, Chrome/151.0.7922.170.
- 대상: **미머지 브랜치를 라이브 무접촉으로** 검증하기 위해 라이브 web 이미지(`mysql-ai-web:596a722a`)
  + 본 worktree 의 `src/static` **디렉터리** bind-mount 컨테이너(`web-verify-reorder`, `https://localhost:18099`).
  단일 파일 mount 는 편집 시 inode 가 고정돼 옛 내용을 계속 서빙하므로 디렉터리로 mount 했다.
- 서빙본 확인은 토큰 grep 이 아니라 **코드 블록 출력**으로: `/static/app/sidebar.js` 의
  `renderConversationList` 가 `_beginSidebarReorder()/_commitSidebarReorder(snap)` wrapper 이고,
  `requestSidebarReorderAnimation` 정의 · `/static/css/shell.css` 의 `@keyframes convReorderFlash` 존재.

## 계측 방법 — 스크린샷이 아니라 궤적

정지 이미지로는 "순간이동인지 트윈인지" 를 가릴 수 없다. 그래서 rAF 샘플러로 대상 행의 **화면 y** 와
**computed transform 잔여 offset** 을 프레임 단위로 기록했다(인라인 `style.transform` 은 invert
프레임에만 존재하므로 computed matrix 를 읽는다). 계측기는 자산화했다 —
`tests/pb0008_sidebar_reorder_measure.py --mode folder|conv`.

> ⚠ **창을 전면화(bring_to_front)하지 않으면 거짓 판정이 난다.** 비활성 탭에서 Chrome 이 rAF/타이머를
> throttle 해 애니메이션이 아예 진행되지 않는다(첫 시도에서 실제로 2프레임만 샘플링되어 "전환 없음"
> 으로 보였다). 계측기는 attach 직후 `bring_to_front()` 를 수행한다.

## A. 대화 제목 변경 — 사용자가 보고한 그 시나리오 (PASS)

오래된 날짜 그룹의 대화 제목을 바꿔 '오늘' 그룹으로 점프시켰다(1차 `month:2026-06`, 최종본
`day:2026-08-04` — 1차 대상은 검증으로 `updated_at` 이 갱신돼 6월 그룹 자체가 사라졌다).

| 항목 | 값 |
|---|---|
| 이동(1차, 리뷰 전) | 화면 y **442 → 233** (그룹: 6월 → 오늘), **19 프레임** 트윈(324~621ms), 강조 324~1405ms |
| 이동(최종본, 리뷰 반영 후) | 화면 y **319 → 142** (그룹: 8월 4일 → 오늘), **18 프레임** 트윈(306~602ms), 강조 306~985ms |
| 궤적(최종본) | 319 → 269 → 211 → 168 → 153 → 145 → 142 (감속 곡선) |
| 잔류 인라인 스타일 | 없음(`transform`/`transition` 모두 정리됨) |
| 제목 원복 | 완료 (`restored_title` 확인) |

곡선은 후반으로 갈수록 간격이 좁아지는 감속(ease-out `cubic-bezier(.22,.61,.36,1)`)이고, 변경 전
동작이었다면 이 구간이 **0 프레임**(즉시 점프)이다. 검증 후 제목은 원복했다.

## B. 폴더 이름 변경 (PASS)

임시 폴더 2개(앵커 + 대상)를 만들어 대상 이름을 정렬 앞쪽으로 바꿨다.

| 항목 | 값 |
|---|---|
| 이동 | 화면 y **175 → 146**(1차) · **146 → 117**(최종본 재현) |
| 트윈 프레임 | 1차 16 프레임(115~365ms) · 최종본 **16 프레임**(113~362ms), 강조 113~1212ms |
| 궤적(최종본) | 146 → 134 → 126 → 121 → 119 → 118 → 117 (감속 곡선) |
| 강조 | 대상 행에만 부여, 인접 행은 이동만 |
| 정리 | 임시 폴더 전부 `DELETE /api/folders/{id}` → 200 |

계측기의 **판정 자체도 검증**했다: 앵커 폴더 없이 폴더가 하나뿐일 때는 이름을 바꿔도 자리가 그대로라
`verdict=instant-or-none` 이 나왔다(거짓 통과 아님). 앵커를 추가해 실제 이동을 만들자 `verdict=tween`.

## C. 함께 확인한 것

- 이름이 실제로 바뀌지 않은 경우(같은 이름으로 커밋)에는 재배치 예약이 서지 않는다 — 계측에서
  transform 0 프레임 + flash 0(코드의 `name === folder.name` 조기 반환 경로와 일치).
- 스크린샷: `artifacts/reorder-conv-*.png` · `artifacts/reorder-folder-*.png` ·
  `artifacts/pb0008-reorder-*.png`. 1차 실행의 `reorder-folder-2-mid.png` 에 이름을 바꾼 행이
  강조(파란 배경)된 채 이동 중인 상태가 잡혔다. 최종 계측기는 캡처가 rAF 를 멈추지 않도록
  샘플링 창 **밖**에서만 찍으므로(아래 E-2) 전환 중 프레임은 궤적 데이터가 정본이다.

## D. 미수행 — 스크롤 추종의 라이브 실측 (사유 명시)

검증 계정의 목록이 짧아(`scrollHeight == clientHeight == 714`) 스크롤 자체가 발생하지 않았다.
따라서 "대상이 스크롤 밖일 때 시야로 끌어온다"(AC-…-2 ③)는 라이브 화면으로 재현하지 못했고,
전용 하네스의 좌표 계약(뷰 아래/뷰 위/이미 시야 안/스크롤 범위 clamp, 뮤테이션으로 KILL 확인)
으로만 잠갔다. 대화가 화면 한 폭을 넘는 계정에서 다음 관측 시 확인한다.

## E. 적대 리뷰 반영 후 재실측 + 실측이 잡은 회귀 (중요)

§18.8 codex 리뷰([P1] 1 · [P2] 4)를 흡수한 뒤 **양 경로를 다시 실측**했다(위 표의 "최종본" 행).
그 과정에서 **하네스가 green 인데 라이브에서 기능이 사라진** 회귀를 실측이 잡았다:

1. **[P1] 수정의 부작용 — 예약이 자기 갱신을 지나침.** 예약을 데이터 버전에 귀속시키자,
   `_commitFolderRename` 이 `loadFolders()`(버전 bump) **뒤에** 예약하던 기존 순서가 곧바로
   결함이 됐다 — 예약 시점의 버전이 이미 최신이라 어떤 렌더도 소비하지 않는다. 화면에서는
   **애니메이션이 조용히 사라진다**(강조만 뒤늦게 붙음). 하네스는 "예약이 PATCH 성공 경로에
   있다" 만 검사해 통과시켰다. → 예약을 데이터 재적재 **앞으로** 옮기고, 하네스에 순서 계약
   2건(폴더/대화)을 추가해 뮤테이션 KILL 을 확인했다.
2. **계측기 자신의 결함 — 캡처가 계측을 깨뜨림.** 샘플링 창(2.2초) 안에서 Playwright
   스크린샷을 찍으면 캡처가 렌더를 블로킹해 rAF 가 멈추고, **정상 트윈이 0 프레임으로**
   보고된다. 코드를 의심하기 전에 원시 프레임 추적으로 브라우저가 정상 트윈 중(vy 29 → 0,
   265ms)임을 먼저 확인한 뒤 계측기를 고쳤다(캡처를 샘플링 창 밖으로).

교훈 2개: **계약을 새로 만들면 그 계약의 시간 순서도 함께 잠근다**, 그리고 **측정값이 이상하면
대상보다 계측을 먼저 의심한다**.
