---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0003-agent-web-ui
agent: codex
timestamp: 2026-09-08T15:00:00+09:00
trigger: 라운드 3 확인 라운드 — codex review --uncommitted (§18.8.1 경량 채널)
verdict: CONCERN
---

`codex review --uncommitted` (codex-cli 0.153.4). Node 하네스 통과. pytest 는 codex 샌드박스의
소켓 제약으로 초기화 단계에서 막혀 실행되지 않았다(시험 정본은 컨테이너 실행 — 8,143건 PASS).

### 1. Blocking issues

(no findings) — **P1 0건**. 라운드 2 에서 지적된 항목(가드 모수·`intent` payload·census 폴백·
사유 파생·빈 census·주석 제거·라벨 중복·활동 툴팁·scratch 문구)이 재지적되지 않았다.

### 2. Cross-domain concerns

- **P2 클라이언트 정규식이 서버보다 넓어 서버가 살린 문구를 지운다.**
  - Evidence: `설정값 {'theme': 'dark'} 이 든 컬럼을 확인한다` 는 서버가 **일부러 보존**하는데
    (앵커 규칙), 프런트의 앵커 없는 `STEP_ARGS_LITERAL_RE` 가 그 안의 딕셔너리에 매칭돼
    `SQL 실행 단계` 로 강등한다. 활동 행 설명도 같은 필터가 지운다.
  - Location: `src/static/app.js:3476`
  - Reason: 심층 방어가 서버 판정을 **뒤집으면** 방어가 아니라 손실이다. 두 층의 술어가
    같아야 「서버가 정본」이라는 구조가 성립한다.
  - Action: 클라이언트 술어를 서버의 호출-접두 규칙과 정렬하고, 산문 속 JSON 회귀 케이스를
    프런트 하네스에 추가하라.

### 3. Challenge to current spec

(no findings)

### 4. Verdict

CONCERN
