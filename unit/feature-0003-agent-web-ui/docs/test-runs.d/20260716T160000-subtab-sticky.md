---
run_at: 2026-07-16T16:00:00+09:00
session: subtab-sticky (ai/claude/feature-0021-subtab-sticky)
scope: 서브탭 바(.admin-subtabs)를 스크롤 시 컨테이너 상단에 sticky 고정 (AI 운영 현황·설정 프롬프트, 사용자 요청)
verdict: PASS (CSS 전용 + POST-DEPLOY Windows-browser 라이브 — AI 운영 현황·설정 프롬프트 서브탭 바가 스크롤 후에도 컨테이너 상단 고정 실증)
---

### Run (2026-07-16) — subtab-sticky — **Environment: CLI (CSS 전용 · 마크업/JS 무변경)**

- **변경**: `.admin-subtabs { position: sticky; top: 0; z-index: 6; background: var(--surface); }`
  + ai-console pane 배경 오버라이드(--bg). 마크업·JS·백엔드 무변경이라 단위 테스트 대상 로직 없음.
- **make test**: 프론트 CSS 는 CI pytest 미검증(기존 스위트 회귀 대상 아님) — 기존 통과 상태 유지.
- **Windows-browser 미수행 사유**: sticky 스크롤 동작은 배포된 web 에서 실제 스크롤로만 확인 가능.
  배포 직후 PB-0008 로 (1) 'AI 운영 현황' usage 서브탭에서 하단 스크롤 시 서브탭 바 상단 고정,
  (2) 설정 '프롬프트' 서브탭 바 스크롤 시 상단 고정을 검증하고 POST-DEPLOY Run 으로 갱신한다.


### Run (2026-07-16) — POST-DEPLOY 라이브 검증 — **Environment: Windows-browser (win-browser.py relay, 배포 a80c2347)**

- **배포 전달 (PASS)**: 서빙 web-a `GIT_COMMIT=a80c2347`, soak 통과.
- **(1) AI 운영 현황 서브탭 sticky (PASS)**: ai-console pane 을 scrollTop=400 스크롤해도 `.admin-subtabs`
  bounding top = 72 (pane top = 72)로 **불변** — 상단 고정. computed position=sticky, z-index=6.
  스크린샷: 상단 요약 카드는 스크롤되어 사라지고 차트·역할별 막대가 보이지만 서브탭 바
  [LLM 사용량|운영 현황|추론]은 상단 유지.
- **(2) 설정 프롬프트 서브탭 sticky (PASS)**: 작동 지침 서브탭에서 detail-col scrollTop=265 스크롤 후
  바 top = 151 (detail-col top 132 + padding 18)로 **불변** — 상단 고정. position=sticky.
- **판정: PASS** — 두 스크롤 컨테이너(pane / detail-col) 모두 서브탭 바 sticky 정상. 증거 스크린샷
  `scratchpad/sticky-aiconsole-scrolled.png`. 회귀 0(아래 콘텐츠 접근성·클릭 불변).
