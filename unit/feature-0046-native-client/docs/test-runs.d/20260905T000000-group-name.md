---
run_at: 2026-09-05T00:00:00+09:00
session: group-name (ai/claude/feature-0046-group-name)
scope: 업그레이드 시 시작 메뉴 그룹 이름
verdict: PASS (덮어 설치 실측) / 폴더명 확인은 재빌드 후
---

### Run (2026-09-04) — 덮어 설치 — **Environment: Windows-browser (실제 설치)**

설치 전:
```
Start Menu\Programs\DQA Connect\
  DQA Connect 제거.lnk · DQA Connect.lnk · DQA 제거.lnk · DQA.lnk
```
설치 후(`[InstallDelete]` 적용본):
```
Start Menu\Programs\DQA Connect\
  DQA 제거.lnk · DQA.lnk          ← 옛 이름 **사라짐** ✅
```
**남은 절반**: 폴더 이름이 그대로다. Inno 는 업그레이드에서 레지스트리에 적어 둔 지난 그룹
이름을 재사용한다 — `DefaultGroupName` 만으로는 도달하지 않는다. `UsePreviousGroup=no` 추가.

⚠ 이 결함은 **덮어 설치해 봐야** 보인다. 앞 주기에서 「고쳤다」고 적은 것이 절반만 참이었고,
그 사실은 코드를 다시 읽어서가 아니라 **다시 설치해서** 드러났다.

### Run (2026-09-04) — 폴더 이름 — **Environment: Windows-browser (재빌드 후로 이연)**

- 계획: 재빌드 → 덮어 설치 → `Start Menu\Programs\DQA\` 에 `DQA.lnk`·`DQA 제거.lnk` 만 존재,
  `DQA Connect` 폴더 부재.
