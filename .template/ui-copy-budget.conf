# .template/ui-copy-budget.conf — 사용자 대면 텍스트 분량 예산 (AGENTS.md §16.8 D)
#
# 형식: <최대문자수><TAB><정규식>   — 캡처그룹 1이 검사 대상 텍스트.
# 검사 대상은 staged diff 의 **추가 라인** — 손대지 않은 기존 문단은 통과한다.
# 오검출(문서·테스트 fixture 의 HTML 예시)이 나면 정규식을 더 좁힌다.

# 관리 콘솔 섹션 안내 (admin.js 렌더 + admin.html 정적). §16.8 C 기준은 1문장 ~60자이며,
# 한글 조사/기능명이 붙는 실측을 반영해 80자로 시작한다.
80	<p class="reasoning-section-hint">(.*?)</p>

# 설정 패널 hint — 오설정 비용이 큰 항목만 2문장 허용(§16.8 C).
180	<p class="admin-settings-panel-hint">(.*?)</p>
