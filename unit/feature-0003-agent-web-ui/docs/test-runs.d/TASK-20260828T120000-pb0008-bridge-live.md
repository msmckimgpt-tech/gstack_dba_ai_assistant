---
run_at: 2026-08-28T00:00:00+09:00
session: ai/root/feature-0043-runner-timeout
scope: feature-0043 대화 제목 자동 설정 + 실행 단계 사유 — **실 브라우저 검증**(사용자 요청)
verdict: PASS — 두 기능 모두 라이브 화면에서 확인 / deferred 는 계정 불일치로 미재현
---

# Run — PB-0008 실 브라우저 검증 (사용자 요청 2026-08-28)

Environment: **Windows-browser** (relay @ `http://172.26.144.1:9223`, Chrome/151.0.7922.170)

## 브리지가 이번엔 성립했다

직전 두 cycle 은 `win_host` 를 DNS 주소(`8.8.8.8`)로 오판해 CDP 도달에 실패했고, 그래서
"미수행" 으로 기록했다. 사용자가 브리지를 구성해 두어 이번에는 `doctor` 가 `ok: true` 를
돌려주었다:

```
{"ok": true, "win_host": "172.26.144.1", "bridge_mode": "relay",
 "endpoint": "http://172.26.144.1:9223", "cdp_version": "Chrome/151.0.7922.170"}
```

대상 대화: `20260827100816-f8a7a5e4` (owner=admin, 배포 `a0762051` 이후 처리분).
브라우저 세션은 격리 프로필의 `bootstrap_admin`(관리자 — 타 계정 대화 **읽기 전용** 조회).

## ① 대화 제목 — PASS

DOM 실측:

```
#conversationTitle → "gunzlog 파티셔닝 자동화 구조 분석"
```

- "새 대화" 가 아니다.
- DB(`agent_runtime.core_conversations.topic`)와 **같은 값**이고, `updated_at` 은 첫 답변
  제출 시각(22:43:28)이다 → 개인 AI 가 `submit_answer` 에 실어 보낸 **맥락 제목**이 반영된 것.
- 배포 **이전** 대화 2건(`...-79b25387`, `...-5ab532d1`)은 `topic` 이 여전히 비어 있다
  (화면 "새 대화") — 이 변경 전후가 데이터로 갈린다.

## ② 실행 단계 사유 — PASS

실행 단계 패널(27단계)에서:

| 실측 | 값 |
|---|---|
| 「근거」 배지 렌더 수 | **68** |
| 구체 작업 문구(`… 데이터를 조회한다` 등) | **71** |
| 뭉뚱그린 `SQL을 실행한다` | **4** (SQL 에서 테이블명 추출이 안 되는 형태 — 그 4건도 근거는 있다) |

화면 예시(스크린샷 참조):

```
25. [SQL 실행]  22:41:19 · 2.0초 · 누적 4분 8초
    `information_schema.TABLES` 데이터를 조회한다
    [근거] 파티션이 적용된 테이블 전체 목록과 용량을 확인해 자동화 등록 대상과 대조
    SELECT TABLE_NAME, CREATE_OPTIONS, TABLE_ROWS, … 
```

DB 대조(`agent_runtime.steps`) — **사유의 출처가 실제 개인 AI** 임을 확인:

```
reason_source = external-ai   (서버 파생값이 아니다)
work_source   = derived
reason_text   예) "미등록 4개 테이블을 등록하면 첫 실행에서 만료 삭제가 즉시 …"
              예) "개선본에 파티션 키 사전 검증(날짜형·NOT NULL·유니크키 …"
```

즉 「어떤 이유로 → 어떤 작업」 구조가 **화면에 그려지고**, 그 사유는 조사한 AI 가 직접 쓴
문장이다.

**증적**: `evidence/pb0008-20260828-steps-reason.png`

## ③ 검증 중 발견 — 러너 타임아웃 (이 cycle 이 고친 것)

같은 대화에 다음 답변이 있었다:

```
AI 호출이 900초를 넘겨 중단했습니다.
(이 답변은 연결된 AI 에서 생성하지 못해 자동 안내로 대체된 것입니다.)
```

27단계짜리 조사가 러너의 `_AI_TIMEOUT_SEC = 900` 벽에 걸린 것이다. 서버 점유 lease 는 30분
이므로 **서버는 아직 기다리는데 러너만 먼저 포기하는 구간이 15분** 있었다. 사용자 결정에 따라
`1700`(lease − 제출 여유 100초)으로 올리고, 두 값의 관계를 계약 테스트로 고정했다.

## 미재현 — 미연결 보관·이어받기(deferred)

브라우저 격리 프로필 세션이 `bootstrap_admin` 인데 상주 러너(PID 862921)는 **`admin`** 계정
토큰으로 붙어 있다(`tool_call_usage`: account_id=10, `wait_for_request` 92회). 계정 경계상
`bootstrap_admin` 의 질문은 그 러너가 가져갈 수 없고, 검증 세션의 MCP 토큰도 만료(401)라
대신 처리할 수도 없다.

→ **admin 세션으로 브라우저에 로그인**하면 전체 시나리오(연결 해제 → 질문 → 보관 확인 →
재연결 → 자동 처리)를 이어서 검증할 수 있다. 사용자 결정에 따라 그 경로로 진행 예정.
