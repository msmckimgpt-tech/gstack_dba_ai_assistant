---
run_at: 2026-09-07T04:00:00+09:00
session: cookie-postdeploy (ai/claude/feature-0003-cookie-postdeploy)
scope: 세션 쿠키 수명 — 배포 후 라이브 실측
verdict: PASS
---

### Run (2026-09-07) — 배포 후 — **Environment: Windows-browser + WebView2 (라이브)**

배포 `ee45a260` · 엣지 무중단 `no upstreams available` **0건**.

고리를 **둘로 갈라** 각각 실측했다. 하나만 보면 「고쳤다」와 「도달했다」가 갈리는 자리다.

| 고리 | 방법 | 결과 |
|---|---|---|
| 서버가 영구 쿠키를 **보낸다** | 라이브에 실제 로그인 → 브라우저 쿠키 DB 조회 | `mysql_ai_session` `persistent=1` `expires=2026-12-06` (**오늘+90일**) |
| WebView2 가 재실행 뒤에도 **들고 있다** | 같은 프로필로 창을 띄워 영구 쿠키를 심고 **프로세스 종료** → 다시 열어 조회 | `SURVIVED` |

⚠ 함께 확인한 것: `weblb`(로드밸런서 고정)는 **여전히 비영속**이다. 그것은 세션 범위가
맞으므로 건드리지 않았다 — 「쿠키에 수명을 준다」가 모든 쿠키로 번지지 않았음을 확인한다.

### 수정 전 대조군 (같은 방법, 2026-09-07 앞서 측정)

사용자 앱 프로필의 영구 쿠키 저장소 **0건** · `logged_in: false` · 상태 칩 `hidden`.

### 남는 것

이미 발급된(수명 없는) 쿠키를 가진 사용자는 **한 번은 다시 로그인**해야 그 뒤부터 유지된다.
