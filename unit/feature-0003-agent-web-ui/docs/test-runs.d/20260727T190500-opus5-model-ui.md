---
run_at: 2026-07-27T19:05:00+09:00
session: ai/claude/feature-0007-opus5-model
scope: [composer-model-picker, admin-runtime-settings, model-catalog-propagation, admin-copy]
verdict: PASS (POST-DEPLOY PB-0008 Windows-browser 라이브 — 모델 선택기·실 대화 e2e·관리 콘솔 pane)
---

### Run (2026-07-27) — opus5-model 프론트 표면 (Claude Opus 5 카탈로그 추가의 UI 파급) — **Environment: Windows-browser (PB-0008)**

cycle: `ai/claude/feature-0007-opus5-model` · 정본 feature = feature-0007-bedrock-llm-provider
(CHG/REV-20260727T184425-opus5-model · CHG/REV-20260727T190500-opus5-model-postdeploy).

- **feature-0003 변경 범위**: ① `src/routers/admin_usage.py` — `_LLM_PRICE_USD_PER_1M` 에 `claude-opus-5`
  ($5/$25) 등록(비-UI) ② `src/static/admin.html` — '모델별 추론 예산' pane 헤더 힌트 카피 1줄
  (`Sonnet 128K / Haiku 64K` → `Opus·Sonnet 128K / Haiku 64K`). **모델 선택기·컴포저 라벨·runtime-settings
  pane 은 코드 변경 0** — 전부 `shared/model_catalog` 카탈로그 자동 파급이라 본 Run 은 그 파급의 실화면 검증이다.

- **Runner**: AI · **Bridge**: relay @ `http://172.26.144.1:9223` · **Browser**: 실 Windows Chrome/150.0.7871.115
- **대상**: `https://localhost/` · `https://localhost/admin` (로그인 `bootstrap_admin`), **서빙 배포본 413703b9**

- **AC-OPUS-UI-1 PASS — 컴포저 모델 선택기**: '+' → '모델' 메뉴에 3항목이 카탈로그 순서대로 렌더 —
  `claude-opus`(Claude / "Anthropic Claude Opus (frontier 최상위, 장기 추론·에이전트 작업)") **최상단** ·
  `claude-sonnet` · `claude-haiku`(✓ 체크 = 기본값 유지). 라벨은 버전 넘버링 없이 노출(2026-07-24 사용자 지시 정합).
  Evidence: `docs/evidence/pb0008-opus5-model-menu-20260727.png`.
- **AC-OPUS-UI-2 PASS — 실 선택 → 대화 e2e**: 메뉴에서 `claude-opus` **실제 클릭** → 메뉴 닫힘 +
  컴포저 라벨 `모델: claude-opus` 전환 + 추론 강도 항목 활성(`높음`) → 프롬프트 전송 →
  **7초(준비 1.7초 · 추론 5.3초)에 정답 `OPUS-PB0008-OK` 렌더**, 대화 상태 `done`, 페이지 error 0.
  Evidence: `unit/feature-0007-bedrock-llm-provider/docs/evidence/pb0008-opus5-live-answer-20260727.png`.
- **AC-OPUS-UI-3 PASS — 관리 콘솔 '시스템 > 설정 > 모델별 추론 예산'**: `CLAUDE-OPUS` 카드 신규 렌더 —
  `라운드당 40,000 tokens` · 입력 40000 · `즉시 반영` 배지 · `기본값 40000tokens`. 그 아래
  **'추론 강도 (ADAPTIVE THINKING)' guide-note** 렌더("이 모델은 adaptive thinking 계열입니다 … 모델별
  thinking budget(토큰) 설정은 적용되지 않아 감췄습니다") → **죽은 budget 슬라이더 0건**
  (CHG-20260724T085937-sonnet-reasoning-budget-guide 규약이 신규 모델에 자동 적용됨을 실화면으로 확인).
  Evidence: `docs/evidence/pb0008-opus5-admin-budget-pane-20260727.png`.
- **발견 → 같은 cycle 수정**: 위 pane 헤더 힌트가 `max_tokens, Sonnet 128K / Haiku 64K 까지` 로 남아
  **Opus 를 누락**(본 변경으로 stale 해진 카피). `admin.html` 1줄 정정. ⚠️ 첨부한 pane 스크린샷은
  **정정 전** 화면이라 구 문구가 보인다 — 정정본은 다음 배포 후 육안 재확인 대상(카피 1줄, 동작 영향 0).
- **부가 검증(비-UI, 같은 세션)**: 라이브 web 컨테이너에서 `PUBLIC_API_MODEL_OPTIONS` 3종 ·
  `API_DEFAULT_MODEL=claude-haiku-4`(기본 불변) · `adaptive_models=['claude-opus-5','claude-sonnet-4']` ·
  `reasoning_budgets` 모델=haiku 뿐 확인. PG `llm_usage` 에 `model=claude-opus-5` /
  `resolved_model=claude-opus-5-chat` 기록 + 비용 USD 0.1981 계상($0 오표시 아님).
- **Notes**: §18.8 subagent 패널은 **본 세션의 사용자 환경 정책(Agent tool 미허용)으로 미수행** —
  feature-0007 REVIEW 에 `[SKIPPED:live-empirical-verification+precedent-checklist]` 로 사유와 대체 검증
  (라이브 실증 + sonnet 4차 결함 역이용 체크리스트 + 자기 적대 검토 + 전체 pytest 2452 PASS)을 명시했다.
