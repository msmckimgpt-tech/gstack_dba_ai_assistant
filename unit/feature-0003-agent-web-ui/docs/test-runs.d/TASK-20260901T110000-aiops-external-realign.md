---
run_at: 2026-09-01T11:45:00+09:00
session: ai/claude/aiops-external-realign
scope: "'AI 운영 현황' 을 외부AI 운영축으로 전면 재편 + 외부 AI 자가 검증(5축) 구현"
verdict: PASS
---

# Run — TASK-20260901T110000-aiops-external-realign

- **Environment**: container (`make test` 전량) + **Windows-browser**(PB-0008, bind-mount 격리
  인스턴스 — 라이브 무접촉)

## 1. 자동 검증

| 대상 | 결과 |
|---|---|
| `make test` 전량 (feature-0002·0003·0006·0008·0014·0020·0023·0041·0043) | **PASS** (exit 0) |
| ruff | All checks passed |
| `bin/migrate-lint.sh` | 0057 **expand-safe** |
| `bin/gen-routemap.py` | 261 → **264 routes** (신규 3) |
| 신규 회귀 | **54건** (자가 검증 축 27 · 재편 축 27) + 기존 계약 3건 갱신 |

### 기존 계약 3건을 갱신한 이유 (약화가 아니라 축 정정)

| 테스트 | 종전 단언 | 왜 바꿨나 |
|---|---|---|
| `test_route_table_matches_golden_snapshot` | 골든 스냅샷 | 의도적 route 3건 추가 — 골든 재생성(diff 가 그 3건뿐임을 확인) |
| `test_runner_declares_features_and_version` | `AGENT_VERSION == RUNNER_MIN_AGENT_VERSION` | **등호가 틀린 계약이었다.** 러너를 고칠 때마다 서버 하한을 함께 올리도록 강제되고, 하한이 오르는 순간 **아직 갱신하지 않은 전 사용자의 콘솔 작업이 끊긴다.** 하한 이상(`version_at_least`)으로 정정 |
| `test_batch_consent_is_opt_in_on_the_runner` | `api.features = tuple(AGENT_FEATURES) + ("batch_jobs",)` 소스 한 줄 | 신고 조립이 두 축(`--batch`·`--no-self-review`)으로 늘며 그 줄이 사라졌다. 문자열 대신 **결과**를 잠갔다 — 특히 「다른 축을 꺼도 배치 동의가 살아남는가」(따로 대입하면 나중 대입이 앞을 지운다) |

## 2. PB-0008 실 Windows 브라우저 (라이브 무접촉)

라이브 이미지(`mysql-ai-web:65641296`) + 이 브랜치의 `static/`·`routers/`·`oauth_store.py`·
`shared/{self_review,bridge_tasks}.py` 만 bind-mount 한 별도 컨테이너
(`web-verify-aiops`, `https://localhost:18096`). 세션은 `session-login`(bootstrap_admin).

⚠ 이 인스턴스는 **라이브 PG** 를 보므로 마이그레이션 0057 이 미적용이다 — 자가 검증 원장이
없는 상태다. 그것이 오히려 검증 대상이었다: **미준비를 0 으로 접지 않는가**(아래 ②·⑤).

### ① 서브탭 재편 — 첫 화면이 곧 우선순위 (AC-2)

```
[운영 현황*] [브리지 작업] [도구 사용량] [기록]     (* = is-active, activePane="ops")
```

구 서브탭 버튼(`usage`·`reasoning`·`exttasks`)은 DOM 에서 사라졌다.

### ② 첫 화면 KPI 에 `llm_usage` 출처 타일 **0개** (AC-1 — 이 cycle 의 판정 핵심)

```
kpiLabels = ["연결된 AI", "대기 / 처리중", "워커 정상"]
```

종전 첫 화면을 채우던 `24시간 활동`·`단계 간 간격 p50/p95` 가 사라졌다 — 둘 다
`agent_runtime.llm_usage` 출처라 차단 배포에서는 영원히 0 이고, 0 인 타일이 맨 앞에 있으면
화면 첫인상이 "아무 일도 일어나지 않는 서비스" 가 된다.

`자가 검증` KPI 도 **뜨지 않는다** — 원장 미준비(`available:false`)를 0 으로 그리지 않는
설계대로다(0 으로 그렸으면 "아무도 검증을 안 한다" 로 읽혔을 것이다).

### ③ `LLM 제공자` 축이 배너를 물들이지 않는다

```
서버 계정 LLM (미사용)  [해당 없음]  state=ok · 서버 계정 LLM 미사용 —
                                     이 축은 지금 서비스에 영향을 주지 않습니다
외부 AI 브리지          [정상]       러너 1 (콘솔 작업 가능 1) · 대기 0 · 처리중 1
```

원본 상태(`raw_state`)는 계속 실린다 — 게이트를 되돌리는 날 되살아날 제한이라, 전환 전에
확인할 수 있어야 한다. **롤업에서 빼는 것과 감추는 것은 다르다.**

### ④ 연결된 AI 명부 — 「러너 N대」가 답하지 못하던 질문

| 계정 | 상태 | 마지막 신호 | 러너 버전 | 자가 검증 | 모델 |
|---|---|---|---|---|---|
| admin | 수신 중 | 23초 전 | 2026.08.31 | **미지원** | 5종 |

라이브 러너가 구버전(`2026.08.31`)이라 `self_review` 를 신고하지 않는다 — 화면이 그것을
**「미지원」으로 정확히 말한다.** 이 칸이 없으면 아래 ⑤ 의 `—` 가 「검증했는데 통과」로
읽힌다(그 오독이 이 축을 만든 이유다).

### ⑤ 브리지 작업 — 종전에 어디에도 없던 것이 보인다

```
전체 59건 중 최근 40건
개설                   작업          상태   요청계정 수행계정 원 질문              자가 검증 답변
2026-09-01 11:28:28  대화 질문 웹   대기   admin   admin   확인했습니다. 첨부…   —        —
```

**대화 질문이 대기 중인 것**은 종전 두 화면(위임 작업 현황=job 만 / 외부 AI 작업=상태·계정
없음) 어디에도 나오지 않았다. 정작 사용자가 기다리는 것이 그것인데.

`자가 검증 = —` 는 「검증 없음」이고 「통과」가 아니다(툴팁: 구 러너이거나 검증이 꺼져 있음).

### ⑥ 도구 사용량 — 콘솔에 처음 생긴 축 (AC-3)

```
도구 호출 2,956 · 반환 행 10,184 · 전송 바이트 1.4 MB · 사용 계정/작업 3/61 · 비정상 판정 2

도구별            호출    반환 행  바이트    지연 p95  비정상
wait_for_request  2,186     60    24.4 KB    55.27s     0
execute_sql         338   3,732   473.2 KB    2.32s     …
```

이 트래픽은 토큰 축의 모든 화면에서 **0 이었다**. 부하는 우리 DB 가 내고 상한도 이 축에
얹혀 있는데(`AGENT_EXT_TOOL_*`) 집계가 콘솔 어디에도 없었다.

### ⑦ 기록 탭 — 지우지 않되 기본 진입에서 뺀다 (AC-2)

세 섹션 전부 **접힘**으로 시작하고, 펼칠 때 조회한다(진입 비용을 실제 열람에만 붙인다):

| 섹션 | 펼친 뒤 |
|---|---|
| 서버 AI 활동 이력 | `에이전트 추론 99회 · 6,865,474 토큰 · $4.74` — 과거 기록 온전 |
| 서버 LLM 토큰 사용량 | `요청 209 · 호출 13,595 · 총 토큰 110,726,647 · 추정 비용 $318.00` |
| 서버 측 자가 리뷰 | `서버가 답변을 만들지 않으므로 … 아래 수치는 전환 이전 기록입니다` + 리뷰 9건 |

### ⑧ 옛 deep-link 가 빈 pane 에 착지하지 않는다 (AC-2)

**제품 경로 그대로** 검증했다 — 대시보드 위젯의 「열기 →」(서버가 보낸 `tab` 값을 그대로
`switchTab` 에 넣는 경로):

| 위젯 | 서버가 보내는 `tab` | 착지 | 빈 pane? |
|---|---|---|---|
| `ai_ops` | `"ai-ops"` | `ai-console` / **ops** | 아니오 |
| `usage` | `"usage"` | `ai-console` / **archive** | 아니오 |

`admin_console.py:695` 가 아직 `tab: "usage"` 를 보낸다 — 별칭이 없었으면 **존재하지 않는
서브탭**으로 가 빈 화면에 착지했을 자리다(feature-0021 적대검증 MAJOR#1 과 같은 형태).

### ⑨ 커버리지 — 하지 않는 일을 '계측됨' 으로 나열하지 않는다

```
계측됨: 외부 AI 도구 호출(호출·행수·바이트), 브리지 작업(개설·점유·제출·반영),
        외부 AI 자가 검증 판정(5축), 러너 연결·수신·기능 신고
미계측: 개인 AI 의 토큰·비용 (추론이 각자의 머신·계정에서 일어나 우리 원장에 남지 않는다) …
        추론 비용·토큰은 … 0 이 아니라 우리가 세는 축이 아닙니다.
```

**자체 발견 1건(실측에서만 드러남)**: 이 문구에 마크다운 강조(`**…**`)를 썼는데 이 문자열은
`esc()` 를 거쳐 **평문으로** 렌더되어 별표가 화면에 그대로 떴다. 제거 후 재확인
(`hasAsterisk: false`). 소스 검사로는 잡히지 않고 화면에서만 드러나는 부류다.

- 증적: `evidence/aiops-realign-{ops,tasks,tools}.png`

## 3. 미검증으로 남긴 것 (정직 표기)

- **자가 검증 end-to-end** — 마이그레이션 0057 이 라이브 PG 에 미적용이고, 살아 있는 러너도
  구버전이라 `self_review` 를 신고하지 않는다. 저장·표시 경로는 단위 테스트(27건)와 화면의
  **부재 표시**(④ `미지원` · ⑤ `—`)로만 확인했다 — 실제 판정이 원장에 앉는 것은
  **배포 + 러너 갱신 후 POST-DEPLOY fragment** 에서 확인한다.
- `--no-self-review` 로 끈 러너의 신고 제외는 argparse 를 실제 파싱해 단위로 확인했고
  라이브 실측은 하지 않았다.
