---
run_at: 2026-09-01T18:58:00+09:00
session: ai/claude/feature-0043-newest-runner-wins
scope: 판정축 정정 (빌드 지문 → 연결 순서) + 모달 진동 해소
verdict: PASS
---

# Run — TASK-20260901T183000 판정축 정정

## Environment

- 컨테이너 `repo-unittest-agent:latest`, worktree 마운트(`/work`), Makefile `test` 와 같은 배선
- **Environment: Windows-browser — 미수행(사유)**: `static/**` 변경은 러너 파이썬 사본 1개뿐
  (브라우저가 실행하지 않는 다운로드 파일). HTML·CSS·JS **변경 0**. 서버 변경도 SQL 정렬·
  응답 필드이며 화면 마크업이 아니다. 다만 이번 결함의 **증상**은 화면(연결 모달)에 나타나므로
  §5 에 배포 후 실측 항목으로 남긴다.

## 1. 라이브 근거 — 왜 직전 축이 무력했나

`WebOAuthTokens` (2026-09-01 18:52, account 10):

| 토큰 | 지문 | 마지막 하트비트(UTC) |
|---|---|---|
| 107 | `05d75225aded` = 배포본 | 09:52:42 |
| 108 | `05d75225aded` = 배포본 | 09:52:38 |
| 106 | `d0ac1263d454` | 09:49:52 |

두 러너의 **지문이 같다** → 「배포본과 다른 지문」 축은 아무 판정도 세우지 못한다. 그래서
오래된 연결이 끝나지 않았다. 모달 미닫힘은 `account_runner_build` 가 「가장 최근 하트비트」로
정본을 골라 지문이 다른 러너들 사이에서 `runner_stale` 이 30초 주기로 진동한 결과다.

## 2. 단위 검증

| 대상 | 결과 |
|---|---|
| `tests/test_stale_runner_yield.py` **23건**(신규 3 + 축 정정 반영) | **PASS** |
| `feature-0043` + `0003` + `0041` + `0023` 전량 | **rc=0** |
| ruff | All checks passed |

## 3. 뮤테이션 역검증 (4종 전건 KILL)

| # | 주입 | KILL |
|---|---|---|
| M1 | 후보 질의에 `RunnerBuild <> %s` 추가(= 축을 빌드로 되돌리기) | ✔ |
| M2 | 화면 정본 정렬을 `LastHeartbeatAt DESC` 로 되돌리기(진동 복원) | ✔ |
| M3 | 하트비트 이력 없는 토큰도 당사자로 포함(MCP 경로 사망) | ✔ |
| M4 | 후보 질의의 `Id >` 비교 무력화 | ✔ |

⚠ **최초 시도에서 M1 이 생존했다.** 테스트 더블이 후보 질의의 술어를 평가하지 않고 「peer 행이
있으면 무조건 반환」했기 때문이다 — 판별력 없는 더블은 그 자체가 거짓 통과 장치다. 더블이
`Id > %s` 와 `RunnerBuild` 비교를 직접 계산하도록 고친 뒤 KILL 됐다. 이 사실을 남기는 이유는,
**뮤테이션이 없었다면 「축을 고쳤다」는 주장이 검증되지 않은 채 통과했을 것**이기 때문이다.

## 4. 발효 범위

- 서버 축(양보·집행·억제·화면 정본): **즉시**.
- 러너 자가 종료: `runner_update.superseded` 를 아는 빌드에서 발효. 라이브의 두 러너는 이미
  `05d75225aded`(PR #1491)라 **재다운로드 없이** 오래된 쪽이 물러난다.

## 5. 배포 후 라이브 실측 필요분

1. 두 러너가 붙은 상태에서 **오래된 쪽 프로세스가 실제로 종료**되는가(러너 로그
   `hb.superseded` → `run.stop`).
2. 연속 연결 시 **연결 모달이 닫히는가**(`runner_stale` 진동 소멸).
