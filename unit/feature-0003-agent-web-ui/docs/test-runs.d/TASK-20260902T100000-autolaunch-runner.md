---
run_at: 2026-09-02T10:20:00+09:00
session: ai/claude/feature-0043-autolaunch-runner
scope: 자동 [내 AI 실행] + 「업데이트 필요」 클릭 갱신 (REQ-20260902-autolaunch)
verdict: PASS (단위·뮤테이션·수정 전 실측) — 배포 후 라이브 왕복 이월
---

# Run — TASK-20260902T100000 자동 실행

## Environment: Windows-browser (수정 **전** 기준선 실측)

- `bin/win-browser.py` relay → 실제 Windows Chrome, `https://localhost/` (배포본 = 변경 전 코드)
- 브리지 프로브 `eval "1+1"` → `{"ok": true, "result": 2}` (게이트 통과)

| # | 관측 | 결과 |
|---|---|---|
| B1 | 진입 시 칩 상태 | `대기 중` → 잠시 후 **`업데이트 필요`** (라이브 러너가 배포본과 다름) |
| B2 | **칩 클릭** (`업데이트 필요` 상태) | **연결 창이 열림** · 러너는 그대로 낡은 상태 |

B2 가 이번 요청이 지목한 바로 그 상태다 — 「업데이트 필요」를 눌러도 **창만 열리고 러너는
재구성되지 않는다**. 수정 후 이 자리는 «클릭 → 실행 → 런처가 최신본 교체» 가 된다.

> ⚠ **수정 후 관측은 배포 뒤에만 가능하다.** 변경 파일이 ES 모듈(`static/app/connect-modal.js`)
> 이라 `docker cp` 로 얹어도 자산 스탬프가 주입되지 않고 브라우저 모듈 캐시가 옛 사본을
> 실행한다(이 저장소에서 실증된 함정). 그래서 이 fragment 는 **기준선**을 담고, 수정 후 실측은
> §5 의 POST-DEPLOY 항목으로 남긴다.

## 1. 단위 검증

| 대상 | 결과 |
|---|---|
| 신규 `tests/test_autolaunch_runner.py` **19건** | **PASS** |
| `feature-0043` + `0003` + `0041` + `0023` 전량 (컨테이너) | **rc=0** |
| ruff | All checks passed |
| `bridge_setup.ps1` UTF-8 BOM 보존 (`efbbbf`) | **PASS** |
| `bash -n bridge_setup.sh` · 모달 JS ESM 문법 | **PASS** |
| 배포 사본 3종(agent·setup.sh·setup.ps1) = 정본 | **SAME** |

## 2. 뮤테이션 역검증 (4종 전건 KILL)

| # | 주입 | KILL |
|---|---|---|
| M1 | 프리페치 제거 → 클릭이 `await` 뒤에 이동(사용자 활성화 상실) | ✔ |
| M2 | 로그인 진입 실패 시 모달을 열도록 변경 | ✔ |
| M3 | 런처의 러너 갱신 블록 제거(= 종전 결함 재주입) | ✔ |
| M4 | 갱신을 토큰 검증 **앞**으로 이동(임의 사이트가 러너 파일 교체) | ✔ |

M1 이 이 파일의 핵심이다 — 그 형태는 **테스트 없이는 보이지 않는다**(코드는 멀쩡히 돌고,
크롬만 조용히 거른다).

## 3. 기존 계약 2건 갱신 (되돌리라고 요구하는 게이트가 되지 않도록)

`test_gate_button_opens_the_connect_modal`·`test_indicator_opens_the_modal` 은 칩·게이트가
**리터럴 `openConnectModal()`** 를 부르는지 보고 있었다. 지금 그 자리는 단일 진입점
(`_connectEntry`)이고 창은 **폴백**이다. 계약의 뜻(«표시가 곧 조치 경로»)은 그대로 유지하되
잠그는 대상을 함수 이름에서 **관계**로 옮겼다 — 진입점으로 이어지는가 + 그 진입점에 창으로
떨어질 경로가 있는가.

## 4. 이력 없는 사용자 무회귀

`_autoLaunchEligible` 이 `_everConnected` 를 요구하므로, 한 번도 연결한 적 없는 사용자는 네
경로 모두 **종전과 동일**(연결 창)이다. 단위 테스트로 고정.

## 5. POST-DEPLOY 라이브 실측 필요분

1. `업데이트 필요` 칩 클릭 → 스킴 실행이 실제로 발사되는가(수정 전 B2 와 대조).
2. **로그인 진입 자동 실행** — 크롬이 사용자 활성화 없는 외부 스킴 이동을 거르는지 여부는
   **실측 대상**이다. 걸러지면 이 경로는 조용히 아무 일도 안 하고 화면은 종전과 같다
   (지금보다 나빠지지 않는다) — 그 결과를 그대로 기록한다.
3. 이력 있는 미연결 계정에서 칩 클릭 → 창 없이 실행되는가.
