---
run_at: 2026-09-07T05:00:00+09:00
session: client-live-verify (ai/claude/feature-0003-client-live-verify)
scope: 실제 설치본·사용자 계정으로 세 결함 종단 실측
verdict: PASS
---

### Run (2026-09-07) — **실제 클라이언트** 종단 실측 — **Environment: Windows-browser (설치본 + CDP)**

사용자 요청(*"실제 클라이언트 실측까지 진행해주세요"*)에 따라 **설치된 `DQAConnect.exe`**를
그대로 띄우고, 그 창의 WebView2 에 CDP 를 열어(`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=
--remote-debugging-port`) 측정했다. 배포 `ee45a260`.

⚠ 이 방법이 필요했던 이유: 앱 창은 devtools 가 없고, 프로필이 잠겨 있어 밖에서 들여다볼
수단이 없었다. 디버깅 포트는 **측정 동안만** 열고 끝나면 정상 실행으로 되돌렸다.

#### 1회차 — 로그인된 앱 창

| 항목 | 값 |
|---|---|
| 계정 | `admin` (사용자 본인 세션) |
| 상태 칩 | `ai-conn` · **`대기 안 함`** · `idle` — **표시됨**(`hidden` 아님) |
| 위치 | `sameLine: true` · `x=260` — **계정 이름 옆** · 잘림 없음 |
| 클라이언트 브리지 좌표 | 실재(`sessionStorage['dqa.bridge']`) = 진짜 앱 창 |
| `/api/ai/connect/token` 요청 | **0건** → 자동 실행이 돌지 않음 = **승인창 없음** |

#### 종료 직후 — 쿠키가 디스크에 남는가

```
112.185.196.20   mysql_ai_session   persistent=1   expires=2026-12-06
```
**수정 전 같은 자리는 0건**이었다(2026-09-07 앞선 측정).

#### 2회차 — 재실행

| 항목 | 이전(결함) | 지금 |
|---|---|---|
| 계정 | `—` (로그아웃) | **`admin`** — 로그인 유지 |
| 상태 칩 | `ai-conn hidden` | **`ai-conn` · `대기 안 함`** |
| 위치 | 아래 줄(`x=8, y=860`) | **계정 옆**(`x=260`) |
| 토큰 요청 | — | **0건** |

### 부수 관측 — 폴백이 실제로 동작했다

1회차 종료 직후 재실행에서 **디버깅 포트가 아직 풀리지 않아** WebView2 창 생성이 실패했고,
클라이언트는 설계대로 **브라우저 앱 모드로 내려갔다**(`--app=` 프로세스 관측). 포트를 바꿔
다시 띄우니 내장 창이 정상 기동했다. 3단 폴백이 실측된 셈이다.

### 정리

디버깅 포트·릴레이·임시 프로필·스크립트를 모두 제거하고 클라이언트를 **정상 실행**으로
되돌렸다(창 제목 `DQA` 확인).
