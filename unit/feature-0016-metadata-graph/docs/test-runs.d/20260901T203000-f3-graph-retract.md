---
run_at: 2026-09-01T20:30:00+09:00
session: main worktree (데이터 운영 — 코드 변경 0)
scope: F3 그래프 유령 정점 회수 — 해소 불가 datasource · 축 오배치 중복 · 유령 용어 (A+B+D)
verdict: PASS
---

### Run 1 — 진단 (dry-run)
AGE 는 **MERGE-only** 라 정점 회수 경로가 없다. 원본이 사라져도 그래프에는 남는다.
총 **78,362 정점**을 4종으로 분류했다.

| # | 분류 | 규모 | 중복/근거 |
|---|---|---|---|
| A | 해소 불가 datasource 3종 | 2,545 | `_resolve_datasource_by_scope` → None (원본 datasource 자체가 없음) |
| B | product 축에 올라간 **물리 스키마** 객체 | 4,590 | **Table 507/507 · Column 1,525/1,525 = 100% datasource 축 중복, 전용 0** |
| C | `common` 축 물리 스키마 객체 | 1,730 | 대부분 중복이나 **전용 Table 20 · Column 11** 존재 |
| D | DB 에 없는 유령 용어 정점 | 107 | `kb_glossary` 634행 vs 그래프 741 |

**가장 결정적인 증거**: 같은 날 소급 정리로 지운 범용어가 그래프에 그대로 살아 있었다 —
`product.gz_qa_g|트랜잭션` · `|복합 인덱스` · `product.gz_dev|정규화` · `|멱등성`.
몇 시간 전에 생긴 유령이라 **지금도 계속 쌓이고 있다**는 증거이기도 하다.

부수 발견: `pverewardinfo` 가 해소 불가 scope `mysql-a4f572f222a2` 에도 있었다 —
전역 ENUM 귀속 정정 때 관측한 것과 같은 잔재다(두 작업이 같은 뿌리를 본 셈).

### Run 2 — 적용 (사용자 승인 범위: **A+B+D**)
C 는 전용분 판정이 남아 제외했다(사용자 결정).

- **되돌리기 매니페스트 먼저** 기록(fail-closed):
  `/shared/glossary-sweep/20260901T203000-f3-graph-retract.json` (3.6MB, 7,242 정점의
  `id`·`label`·전체 속성). ⚠ AGE 는 정점 복원 API 가 없다 — 복원은 속성으로 재-MERGE 하거나
  원본(`agent_kb` 테이블)에서 sync 를 다시 돌리는 방식이다. 매니페스트는 **무엇이 있었는지**를
  남기는 것이지 원클릭 복원이 아니다(정직하게 그렇게 기록했다).
- 삭제: A **2,545** · B **4,590** · D **107** = **7,242 정점**
  (`DETACH DELETE` — 엣지도 함께 회수).
- 총 정점 **78,362 → 71,120**.

### Run 3 — 검증
| 검사 | 결과 |
|---|---|
| 오늘 지운 범용어가 그래프에서 사라졌는가 | **0건** (완전 제거) |
| 그래프 GlossaryTerm(product+common) vs `kb_glossary` | **634 = 634 완전 일치 (유령 0)** |
| B 삭제가 물리 스키마를 잃지 않았는가 | `Achievement`·`masangsoft_editor_autosave`·`t_accountblock` 모두 **datasource 축에 온전** |
| 축 잔여 | product 축에 물리 스키마 **0** (GlossaryTerm 627만 남음 — 올바른 축) |

### 남는 것 (정직)
- **C(1,730)는 미처리** — `common` 축 물리 스키마. 전용 Table 20 · Column 11 이 있어 통째
  삭제는 그 31개를 잃는다. 판정 로직을 따로 검증해야 한다.
- **D'(datasource 축 GlossaryTerm 415)는 미처리** — 용어는 product 축 개념이라 축 오배치지만,
  지우기보다 **옮기는** 게 맞고 그러려면 제품 귀속 판정이 필요하다.
- ⚠ **재발한다.** sync 가 MERGE-only 인 한 다음 스캔에서 다시 쌓인다. 이번 작업은 **누적분
  청소**이지 원인 제거가 아니다. 근본 해소는 sync 에 회수 단계를 넣는 것이고 **코드 변경이라
  별도 cycle** 이다. 재발 여부는 `kb_glossary` 행 수와 그래프 GlossaryTerm 수의 차이로 관측한다
  (오늘 0으로 맞춰 뒀으니 다음에 벌어지면 그게 신호다).
