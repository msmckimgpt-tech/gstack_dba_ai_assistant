---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0003-agent-web-ui
agent: codex
timestamp: 2026-09-08T13:15:00+09:00
trigger: 코드 변경 — codex review --uncommitted (경량 채널, §18.8.1)
verdict: BLOCK
---

`codex review --uncommitted` (codex-cli 0.153.4). 전체 pytest 는 codex 샌드박스의 소켓 제약으로
수집 전에 막혀 실행되지 않았다 — 시험 결과의 정본은 컨테이너 실행이다.

### 1. Blocking issues

- **P1 저장 문구를 버리기 전에 폴백을 완성하라.**
  - Evidence: `_derive_step_work` 가 census 23종 중 12종의 분기를 갖지 않아, 읽을 수 있던
    `read_attachment` 제목이 `` `read_attachment` 도구를 실행한다 `` 로 바뀐다. 신설된
    `test_l2_derived_narration_is_a_fixed_point` 가 그 때문에 실패한다.
  - Location: `src/routers/_conv_store.py:4234`
  - Reason: AGENTS.md 의 내부명 노출 금지와 정면 충돌한다.
  - Action: 버리기 전에 완전한 공유 문구 매핑을 두라.
- **P2 식별자로 시작하는 정상 사유를 보존하라.**
  - Evidence: `order_items 테이블에 주문 금액 컬럼이 있는지 확인하기 위해` 같은 사유가
    선두 snake_case 휴리스틱에 걸려 설명이 통째로 지워진다. 사유는 `work` 와 달리 어느 표시
    경로에서도 대체를 받지 못해 「손실 없는 교환」 논거가 성립하지 않는다.
  - Location: `src/routers/_conv_store.py:4235`
  - Reason: 정상 워크로드 실측을 요구하는 규정(§16.7 G9-c)에 어긋난다.
  - Action: 사유 판정을 실제 도구 호출 표기로 좁혀라.

### 2. Cross-domain concerns

(no findings)

### 3. Challenge to current spec

집중 probe 로 폴백 불완전과 정상 설명 소실을 확인했다. JS 하네스는 통과했다.

### 4. Verdict

BLOCK
