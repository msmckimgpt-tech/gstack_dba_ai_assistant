---
doc_type: PROJECT_STATUS
scope: project
status: active
edit_policy: rewrite
lifecycle: active
source_of_truth: true
template_version: v3.34.1
domain: [status, index]
ai_read_priority: 6
sources:
  - unit/feature-0001-platform-runtime/docs/TASK.md
  - unit/feature-0002-agent-core/docs/TASK.md
  - unit/feature-0003-agent-web-ui/docs/TASK.md
  - unit/feature-0004-browser-automation/docs/TASK.md
  - unit/feature-0005-qa-mcp/docs/TASK.md
  - unit/feature-0006-lan-proxy-access/docs/TASK.md
  - unit/feature-0007-bedrock-llm-provider/docs/TASK.md
  - unit/feature-0008-windows-browser-testing/docs/TASK.md
  - unit/feature-0009-group-conversation/docs/TASK.md
  - unit/feature-0010-google-drive-integration/docs/TASK.md
  - unit/feature-0011-shared-extraction/docs/TASK.md
  - unit/feature-0012-web-router-modularization/docs/TASK.md
  - unit/feature-0013-relationship-diagrams/docs/TASK.md
  - unit/feature-0014-zero-downtime-deploy/docs/TASK.md
  - unit/feature-0015-zd-hygiene-backup/docs/TASK.md
  - unit/feature-0016-metadata-graph/docs/TASK.md
  - unit/feature-0016-zd-pg-pause-caddy/docs/TASK.md
  - unit/feature-0017-deploy-build-gate/docs/TASK.md
  - unit/feature-0019-message-editing/docs/TASK.md
  - unit/feature-0020-zd-deploy-all/docs/TASK.md
  - unit/feature-0021-redteam-review/docs/TASK.md
  - unit/feature-0022-agent-scratch-workspace/docs/TASK.md
  - unit/feature-0023-conversation-api-access/docs/TASK.md
  - unit/feature-0024-conversation-folders/docs/TASK.md
  - unit/feature-0025-worker-parallelism/docs/TASK.md
  - unit/feature-0026-perf-observability/docs/TASK.md
  - unit/feature-0027-perf-latency-p0/docs/TASK.md
  - unit/feature-0028-web-perf/docs/TASK.md
  - unit/feature-0029-graph-churn/docs/TASK.md
  - unit/feature-0030-ask-timeout-extension/docs/TASK.md
  - unit/feature-0031-analysis-grounding/docs/TASK.md
  - unit/feature-0032-llm-token-budget/docs/TASK.md
  - unit/feature-0033-analysis-synthesis/docs/TASK.md
  - unit/feature-0034-analysis-consumption/docs/TASK.md
  - unit/feature-0035-analysis-planner/docs/TASK.md
  - unit/feature-0036-analysis-verification/docs/TASK.md
  - unit/feature-0037-domain-synthesis/docs/TASK.md
  - unit/feature-0038-frontend-modularization/docs/TASK.md
  - unit/feature-0039-ops-scheduler/docs/TASK.md
  - unit/feature-0040-db-object-explorer/docs/TASK.md
  - unit/feature-0041-external-ai-tool-surface/docs/TASK.md
  - unit/feature-0042-analysis-dedup/docs/TASK.md
  - unit/feature-0043-external-llm-bridge/docs/TASK.md
  - unit/feature-0044-qa-staging-pipeline/docs/TASK.md
  - unit/feature-0045-zd-bridge-continuity/docs/TASK.md
---

# Project Status (인덱스)

프로젝트를 cross-feature 관점에서 한눈에 보는 **인덱스**다 (ADR-0031 SSOT 계약 §1 — 셀에 TASK 상세를 누적하지 않는다).

- **기능별 상세 현황 정본**: `unit/<feature>/docs/{TASK,REPORT}.md` — 본 문서는 그 요약·링크만.
- **인덱스화 이전 누적 rollup·상세 이력**: [docs/archive/STATUS_ARCHIVE.md](./archive/STATUS_ARCHIVE.md) (verbatim 보존).
- 정본 지도: [docs/DOC_REGISTRY.md](./DOC_REGISTRY.md) · SSOT 계약: [docs/DECISIONS.md](./DECISIONS.md) ADR-0031.

## 1. 기능 현황 (요약 인덱스)

<!-- AI-EDITABLE:STATUS-TABLE:START -->
<!-- 이 표는 bin/gen-status.sh 생성물 — 수기 편집 금지. 상태 정본 = unit/<feature>/docs/TASK.md frontmatter(feature_status*) -->

| 기능 ID | 상태 | 최종 갱신 | 정본(상세) | 최근 작업 요지 |
|---|---|---|---|---|
| feature-0001-platform-runtime | in-progress | 2026-07-28 | [TASK](../unit/feature-0001-platform-runtime/docs/TASK.md) | 엄격한 운영 시나리오(TEST-0003) 확정 + 라이브 실측 PASS — Completion Checklist 전건 충족 |
| feature-0002-agent-core | in-progress | 2026-09-01 | [TASK](../unit/feature-0002-agent-core/docs/TASK.md) | init "준비" 임베딩 지연 회귀 해소(전용 embed-ollama·캐싱·fast-timeout)·요청량 한도 메시지 주체 구분·datasource 회로차단 안내 문구 분리(고장 오인 해소) / ITEM-11 메타데이터 컨텍스트·NL→SQL flywheel · 용어사전 대화 자율등록 코어(role 분리·유사어·하이브리드 자동승급, mig 0023, ADR-20260629T101500) · 답변 피드백 답변당 고유화 코어(마이그 0021/0022) · (06-30) 능동 해석 지침 modality-무관 일반화(1:1·그룹 모두 주입 — 과도 재질문·스키마 추측 후 give-up·datasource 드리프트 억제)+MySQL 식별자 대소문자 보존 안내(conv-audit FR-nl2sql, 가드 불변) · (07-03) insight-worker 안정성 — 부하 분산(probe 격리·scan skip·batched graph sync)·긴-cycle heartbeat false-negative 해소(liveness throttle)·동일구조 테이블 그룹화(대표 1회 분석+형제 LLM-free 전파)·요청레벨 LLM fallback(claude-corp→root→edge) · (07-07) 대화 답변 경로 edge(gemma) 폴백 완전 차단(conv-audit FR-edge-fallback-conversation-context-loss, 저성능 로컬모델 답변 누출→맥락 손실 해소, CHG-20260707T100640)·런타임 설정 live 배선(AGENT_TIMEOUT_SEC·MCP_TIMEOUT_SEC·모델별 thinking 예산 주입, cross-unit feature-0018) · (07-07 후속) 지식베이스 채택 인박스+ENUM 코드사전 대화 자율수집 코어(kb_glossary·agent_core·llm, alembic 0039)·추론 강도별(낮음/높음/매우높음) 예산 도입(_call_llm 명시레벨 override·'일반'=무주입 무회귀; shared/runtime_settings) · (07-08, cross-cut 코드 거주 — 정본 feature-0016) §59 분석 기반 제품 분류 제안 코어(modules/product_classify.py·llm_product_classify·insight-worker XDS 데몬 기본 OFF — Pending 적재만·환각 3중 게이트)+§57 그래프 접힘 카드 SCHEMA_REF 집계 백엔드(metadata_graph.py) · (07-10) insight-worker 안정성 — MSSQL datasource 로그인 실패(18456) 시 같은 datasource 나머지 등록 DB cycle 내 조기 skip + datasource label 키 cooldown(기본 600s, AGENT_INSIGHT_AUTH_COOLDOWN_SEC; DB/객체별 권한거부 916/229/297 은 로그인 성공이라 해당 DB 만 실패·순회 계속) · (07-10, cross-cut 코드 거주 — 정본 feature-0016 §69/ADR-034) 능동 노드분석 caveats 자기-불평 제거+Routine touches(읽기/쓰기 구분)·returns payload 투영(llm.py·node_analysis.py); T69.5 POST-DEPLOY 완수(cc_data_main 재생성 715/715·0 failed, PR #744) · (07-13) describe_routine 도구 신설(저장 프로시저/함수 정의 조회 전용·sql_guard SELECT-only 불변 유지, PR #749 — conv-audit FR-show-create-routine-blocked)·sql_guard read-only shape 과차단 보정(최상위 UNION + 읽기전용 SHOW 허용·쓰기/allowlist/금지함수 불변, PR #761 — conv-audit FR-readonly-query-shapes-overblock) · (07-14) MySQL 시스템 변수(@@) 읽기 sql_guard denylist 과차단 해소·MSSQL cross-DB 구조 발견(catalog 인지 — 검색·describe)·첨부↔실DB grounding 모순 제거+식별자 대소문자 false-missing 봉인·부분 증거(절단 미리보기) 전수 단정 환각 봉인·첨부 갱신요청 시 새 첨부 버전 전달 선호 · (08-05) 첨부 변경 사실 코드-권위 봉인 — 프롬프트 말미 권위 블록 + 사용자 턴 매니페스트 + red-team 리뷰어 능동 검출 3축으로 "새로 첨부/변경된 파일 없음" 부재 단정 차단(§18.8 backend+qa 패널 BLOCK 전건 흡수·뮤테이션 8종 KILLED·배포 완료 PR #1155, conv-audit FR-attachment-change-false-absence) · (08-12) 대화 LLM **일시 실패가 run 전체를 폐기하던 결함 봉인** — 같은 라운드 재호출(누적 messages 무손실)+지수 backoff+대기 중 취소 폴링, 실패 분류 정본 1곳(`modules/llm.classify_agent_llm_failure`), 전송층 예외 2종 한국어 안내(글로벌 배너 비오염), PG 히스토리 로드 창 역전(ASC LIMIT → DESC LIMIT) 교정 — 배포 `7c2918a8` POST-DEPLOY 런타임 실증(conv-audit FR-llm-transient-failure-kills-run·FR-agent-history-window-inverted) · (08-12) **일시 장애에서 작업 내역 보존 + 추론 재개** — 값싼(요청 미도달) 실패 전용 예산(6회·backoff cap 30s·누적 120s)과 소진 시 `requeue_ask_job_for_resume` 자기재큐로 154초 추론+도구 3건 폐기를 제거, 재개 맥락 오염 차단 3축(core 미기록·KV terminal 미기록·재조회 금지 지시)+`resume_allowed` attempts 게이팅. 코드는 `e1372f32` 배포에 동반 반영, 재개 POST-DEPLOY 실증은 미수행(conv-audit FR-llm-transient-exhaustion-discards-run) · (08-14) **타 멤버 첨부 본문 턴의 작업공간 쓰기 도구 차단**(attach-provenance-gate, Critical — `execute_tool` 단일 choke-point `_provenance_gate` 가 `scratch_sql`/`scratch_import`/`scratch_reset` 을 차단, 목록만으로는 미발동·신호 조회 실패=차단, codex adversarial-bypass P1 2건(`read_attachment` 우회·AI 생성본 우회) 흡수, 조회 도구·`update_attachment`·1:1 무변경, POST-DEPLOY `e545796f` 3/3 PASS. SECURITY.md §47.4 '수용 위험'→'부분 통제') → (08-14) **이미지·샌드박스 csv/xlsx 축까지 provenance 신호 확장**(vision-provenance — 이미지 조회 SELECT 에 `AccountId`(MySQL 폴백·PG 미러 양쪽)+인라인 JSON 동봉, 로더가 소유자↔호출자 비교로 신호 수립; 히스토리 replay 는 과차단 회피로 한계 채택, POST-DEPLOY `4491ad80` 타 멤버 이미지 True/본인 False. SECURITY.md §47.4 'vision 미적용'→적용 완료) · (08-14) **조기 종료 run 의 무한 대기 봉인**(ask-kv-terminal-seal — job 이 0.12초 만에 error 인데 KV `last_status` 만 'processing'+enqueue sentinel 로 고착해 `/api/ask_result` 가 무한 폴링하던 것; 조기 종료 12경로 vs KV 마감 3갈래의 비대칭을 3중 봉인(`_ensure_kv_terminal` · `ask_jobs` terminal backstop · `_persist_early_exit` 질문+오류 1행), 60일 22 job/20 대화, 82 PASS, 후속 CI 회귀 2건 정정, 배포 `6cd45761`·라이브 UI 재현은 자격증명 접근 차단으로 미수행 → conv-audit FR-ask-result-infinite-wait `fixed:deployed:unverified-live`) · (08-14, Major) **대화 LLM 호출 스트리밍 전환**(llm-stream-progress — per-attempt 상한이 성공 지연 분포 꼬리 안쪽이라(콘솔 900s vs 30일 1,271라운드 p95 238.5s·max 854s) 진행 중인 무거운 추론이 전량 폐기·재시도되고 chunk 부재로 진행 표시가 멈추던 것을 `_collect_llm_stream`+message-like 조립으로 해소 — **반환 계약 유지(소비처 3곳 무변경)**, 진행 120s·탈출구 10s·조용한 EOF=`_LLMStreamIncomplete`·킬 스위치, body timeout 은 라이브 반증으로 미변경, POST-DEPLOY `8fe4ab39` end-to-end 실증; 관측 수단은 운영 로거 WARNING 실측으로 DB 기반 교체) · (08-14) **데이터소스 연결 제한 시 사용자 환경 확인 항목 명시 안내**(ds-connect-network-guidance — 사내망+VPN 배치라 지배 원인이 단말 네트워크 단절/VPN 만료인데 드라이버 원문만 노출돼 행동 지침 0 이던 것을 `shared/db.py` 안내 정본(`_DS_ACCESS_CHECKLIST` 2항목 + guidance/판정 헬퍼)으로 통일, run-start 조기 종료 3갈래·`execute_tool` 연결 6축 경유, 전달은 `_DS_RESTRICTION_NOTICE` ContextVar, 48건·뮤테이션 10종 KILL) · (08-16 배포) **첨부 변경-인지 신호를 서버 권위로 봉인**(attach-change-signal-server-authority, Major — 선행 봉인의 4축(★신규 라벨·`## FILE UPDATES` diff·ATTACHMENT SET 사실·리뷰어 digest)이 전부 브라우저 in-memory pill 하나에 걸려 대화 전환·패널 조작·새로고침이면 동시에 꺼지고 방금 갱신한 v2 가 `◆세션` 으로 오라벨되던 것(60일 14건/14대화)을 **클라이언트 신호 ∪ 서버 파생** 합집합으로 전환; 기준선=직전 턴 전달 스코프(`ask_jobs.payload.attachment_ids`, None↔[] 구분)·판정=첨부 id 단조성(tz 비의존)·run 당 1회 파생+run 경계 격리, 소비자 4축·보안 경계 무변경, cross-ref 0003 `composer.js` 미전송 표식 보존, 24 PASS·PB-0008 라이브 왕복·배포 `0f784df3` → conv-audit FR-attach-change-signal-client-only `fixed:deployed:unverified-live`) · (08-24) **첨부 비교 시 계보 최초 원본(_v0) 능동 주입**(attach-original-baseline, Major — 참조 경계가 `SupersededAt IS NULL`(체인 최신본 1행)에 묶여 있어 v3 이상 체인에서 "처음 올린 것과 지금의 차이" 가 원리적으로 답 불가였던 것을 **계보 조상까지** 확장: 버전>1 첨부의 최초본을 `## ORIGINAL VERSIONS (_v0)` 로 능동 주입(`_load_original_versions`)하고 `read_attachment(attachment_id=…)` 이 같은 대화·같은 체인·미삭제 3겹을 만족하는 조상을 읽는다(`_load_ancestor_attachment_row`, 대화 컨텍스트 부재는 fail-closed). 스코프 해소는 **무변경** — 원본 id 를 더하면 동명 항목이 `_lineages` 에 "계보 2개" 로 잡혀 `FILE VERSION LINEAGES` 가 거짓을 말하고 `read_attachment(filename=…)` 이 되묻는다. 인가 확대 0 · 주입 상한 5건·40,000자 · 중간 버전(v2…) 미주입은 명시 한계(SECURITY.md §48)) · (08-24) **live 설정을 기동 스냅샷으로 파생하던 임계 부정합 전수 정리**(live-cap-startup-drift, Major — `runtime_settings` live 스펙 76개 × `shared/config._startup_int` 19키 교집합을 집합 연산으로 **정확히 2건**으로 확정(`AGENT_TIMEOUT_SEC`·`MCP_TIMEOUT_SEC`, 나머지 17건은 `restart` 라 구조적 정합). 회수 임계 `AGENT_ASK_WORKER_STALE_SEC` 는 startup 스냅샷인데 run 예산 `run_timeout_sec` 은 live 라 콘솔에서 상한을 올리면 sweeper 가 **살아있는 정상 run 을 회수**하던 경로를 `effective_ask_worker_stale_sec()` 소비-시점 재계산으로 봉인(ask sweeper · web `_conv_store` stale 3곳 · attach long-poll 전 경로 전환, 하락 방향은 `_ask_stale_high_water`, floor 600s) · `llm_enum_suggest` 의 startup 명시 전달 정합 · AST 가드 12케이스로 패턴 재발 차단. "LLM 추론 timeout 무한" 요청은 **이미 그 상태**임을 라이브 실측으로 확인하고(스트리밍에서 유효한 층은 chunk 간 무응답 간격 하나) 오해의 원인만 교정 — 콘솔 label 을 `무응답 대기 상한 (에이전트/쿼리)` 로, description 첫 문장을 "이 값은 AI 추론 시간을 제한하지 않습니다" 로. POST-DEPLOY `14ab6b94` 6서비스 전부 · 회수 임계 5,580s 값 보존 · 임계 > 예산 실증) · **(08-24) SQL 실행 오류에서 포기·오귀인하던 답변 경로 교정**(sql-selfheal, Major — 라이브 `20260824085807-a761f842` 에서 `SELECT NOW() as current_time…` 이 1064 로 실패하자 재시도 2회 모두 무관한 부분만 바꿔 **같은 지점**에서 재실패하고, 최종 답변에 "이 DB 연결에서 `DATE_SUB()`/`UNIX_TIMESTAMP()` 가 작동하지 않습니다" 라는 **미검증 제약**을 단정한 뒤 그 위에 DROP/DELETE 방안 3종을 세운 것 — 진짜 원인은 `current_time` **예약어** 하나. 3층 해소: ① `modules/sql_error_hints.py` 신규가 엔진의 `near '<token>'` 에서 **실패 지점을 추출·지목**하고 MySQL 8.0/T-SQL 예약어면 인용·개명 처방, ② 시그니처(`분류\|실패지점`)로 **동일 실패 반복** 감지 → 최소 쿼리 이분 탐색 요구, ③ 상한 소진 문단을 **정직성 계약**(미검증 엔진 제약 단정 금지·시험 안 한 함수를 "작동 안 함" 으로 쓰기 금지·그 가정 위 우회 방안 금지)으로 교체하고 같은 계약을 `_SQL_FAILURE_DIRECTIVE` 로 **상시 코드-주입** — 운영자 `WebSystemPrompts` global row 1건 실재를 census 로 확인해 base 뒤 주입해야 발효됨을 근거화(§16.7 G8-b). 신규 25 PASS·결함 주입 6종 KILL(초판 약한 단언 2건이 MISS 한 것을 고유 표식으로 강화)·회귀 0) · **(08-25) 위 cycle 에 codex 적대 리뷰 → 머지 차단([P1]1·[P2]5) 후 전건 수정**(sql-selfheal-p1 — ① [P1] `compose_system_prompt` 조기 return 2곳이 base 만 돌려줘 **injection guard 포함 코드-권위 블록 전체**가 사라지던 기존 구멍을 단일 조립기(`_code_directive_parts`)로 전 경로 통일 ② [P2-1] 라우터가 primary 로 복원한 뒤라 활성 ContextVar 가 라우팅된 MSSQL 을 MySQL 로 오판 → 실행-시점 스냅샷 우선 ③ [P2-2] focus 없는 오류가 `syntax\|` 로 뭉쳐 반복 오판 + 성공·타도구 후 상태 미초기화 해소 ④ [P2-3] stop token 단정 처방을 후보 진단으로 하향(+인용리터럴 near 매칭 실패 해소) ⑤ [P2-4] near identifier 64자 상한 ⑥ [P2-5] **선행 cycle 의 결함주입 9종이 전부 순수함수 계약이라 루프 배선이 사각**이었던 것을 헬퍼 추출 + AST 배선 단언(호출 존재 **및 인자 출처**)으로 봉인. R1[P1]1→R2[P1]0·[P2]1→R3 [P1]0·[P2]0 수렴(§18.8 (a) 확인 라운드). 부수로 소스-텍스트 단언 3건이 깨져 실행 검사로 격상(§16.7 G11 거짓FAIL 실례) — 41+7 PASS·회귀 0) · (08-25) OAuth frontier identity 주입을 provider 전송 **단일 관문**(`prepare_provider_messages`)으로 봉인 — 호출측 3곳 산재 탓 07-24 봉인이 재발한 2회째 동일 장애(429 `rate_limit_error`·ratelimit 헤더 부재)를 구조로 차단하고 AST 배선 테스트가 관문 우회를 금지, 배포 후 라이브 frontier 200 실측(ADR-20260825T170000-cc-identity-chokepoint · AGENTS.md §15.2.1) · (08-31) 예약 작업 조회 경로 도달성 봉인 — 차단 거부 안내를 도구 카탈로그 단일 출처에서 파생 + 작업명을 식별자가 아닌 리터럴로 라우팅(보안 경계 무변경 · conv-audit). 상세는 정본 TASK.md `TASK-20260831T110000-scheduler-discovery-reachability`. · (09-01) 용어사전 통용범위 축 분리(전역/제품 + 일반 DB 용어 미등록) · 지식베이스가 외부 AI 프롬프트에 실린다(작업 컨텍스트 번들 1층→5층, L0 통계 수집을 LLM 게이트 앞으로 분리) · grounding 매칭 정정(한도는 후보에, 판정은 낱말로) · 소급 정리 실적용 105행(되돌리기 검증). 상세는 정본 TASK.md `20260901T1200-glossary-term-tier`·`TASK-20260901T160000-kb-external-reach`·`TASK-20260901T190000-kb-grounding-match`. |
| feature-0003-agent-web-ui | in-progress | 2026-09-01 | [TASK](../unit/feature-0003-agent-web-ui/docs/TASK.md) | 메타데이터 부트스트랩 결과 패널 접기+검색/필터 재설계(대규모 스키마 여백 과다 해소) + 패널 잘림 잔존 cache-buster 미bump 근본수정(metadata-bs-collapse) · 메타데이터 부트스트랩 MSSQL database 차원 수정(tempdb 임시테이블→선택 DB 실테이블, 시스템 스키마/DB 필터, schema_name=DB명, 데이터베이스/스키마 라벨 분기)·테이블/컬럼 설명 패널 내부 잘림 해소(bootstrap-result max-height 제거)·테이블 설명 AI 자동완성 grounding 정상화(metadata-bootstrap-mssql-db) / 제품 선택 chip 처리 중 항상 활성화(TASK-0047 race 가드 3계층 완화, ADR-WEB-0006) · 역할/제품 프롬프트 AI 자동작성·제품 분석률 95% 제품프롬프트 자동완성·규칙 자동 추가 DB insight 커버리지 UI·지식베이스 메뉴 재편 / 메타데이터 거버넌스 포탈(ITEM-11) · 좌측 대화 전환 크로스페이드 · 워커모드 assistant 요청 2번 중복 처리 차단(enqueue 멱등화 + AmbiguousParameter 회귀 파라미터 격리, ask-dedup-idempotency, cross-cut 0002) · 용어사전 대화 자율등록 UI(역할 필터/select·검토 큐 kb.glossary.curate·유사어 참조) · 용어 검토 큐 IA 중첩(메타데이터 > 용어사전 > 용어 검토 큐 2차 보기 탭, curate-only 탭 게이트 보존) · 답변 피드백(👍/👎) 답변당 고유화(새로고침·전환 후 중복 차단, id-space) · 첨부 wrong-bubble 엣지 하드닝(message_id_space) · 용어사전 역할 선택 UI 단일화(단일 역할 컨텍스트 — 폼 select 폐기, 툴바 하나가 필터+등록대상, mis-scope 토스트 가드, glossary-role-single-ui) · 용어사전 역할 드롭다운/라벨 실제 역할 미표시 버그 수정(필드명 role_key/role_name→key/name, glossary-role-fieldname-fix) · 공유 대화 뷰 우측 스크롤바 대화 가이드 뱃지(point rail) 추가 + 가이드 뱃지 클릭 스크롤 단축(280ms)·EaseOutExpo(메인/공유 양 뷰, point-scroll-easeoutexpo) · 스키마 골격 결과 페이지네이션·여백 압축(metadata-bs-paging) · 새 대화 첫 전송 사이드바 중복 제거(new-conv-dedup) · diff 답변 누출 줄번호 prefix 정규화(diff-lineno-leak) · (06-30) 메타데이터 관리 화면 list-detail 2단 재구성·데이터소스 선택 단일화(헤더 스코프 상속)·테이블 설명 인라인 입력(평면화·행간 정렬)·스키마 가져오기 시 기존 설명 prefill+변경분만 저장 (frontend-only) · (07-01) 좌측 대화 선택 시 대화창 미표시 방어 하드닝(convswitch opacity finally 보장) · 메타데이터 그래프 뷰 UX(WebGL 외곽선 선명화·단일클릭 컬럼 펼침/접힘·상세 패널 드래그 리사이즈·접기 버튼·첫 컬럼명 가림 수정) · 메타데이터 탭 권한 세분화(kb.ingest.manual → metadata.{glossary,enum,table,column}.manage + graph.read, 비파괴 하위호환) · (07-02) AI 운영 관제 패널 신설(신규 /api/admin/ai-ops·LLM 계측 확장·최근 활동 커서 페이징·pane 세로 스크롤·시스템 sentinel 대화 링크 깨짐 수정, aiops-panel/activity-paging/scroll/conv-link-fix, cross-cut 0002 _call_llm latency 계측) · 감사 카테고리 재구성+항목 툴팁+최근활동 클릭 상세 확장(audit-nav-ux) · 메타데이터(지식베이스) 권한 종속 계층화(묶음→그룹 게이트, metadata-perm-hier) · "+" 메뉴 첨부 개수 배지 대화 전환 후 stale 수정(attach-count-scope) · (07-03) 데이터소스 상세 평균 연결 응답 시간(ds-avg-latency) · AI 운영 지연 p95 단위를 단계 간 간격으로 재정의(aiops-ttft) · (07-04) 말풍선 ☰ 통합+'여기부터 공유' 범위 UI·'AI 로 고치기'도 ☰ 로 이동(피드백 👍/👎만 외부) · (07-06) 대화 화면 사용자 지정 추론 강도 선택기(낮음/일반/높음/매우높음, reasoning-effort — 대화별 영구, PB-0008 PASS) · (07-07) 관리 콘솔 시스템>설정 런타임 설정(실행 타임아웃·모델별 추론 예산 조정·저장·live/restart 하이브리드 반영, WebRuntimeSettings·admin_settings 라우터·system.runtime.read/write 권한+audit, feature-0018 코드 거주, PB-0008 PASS) · (07-07 후속) 지식베이스 채택 인박스+ENUM 코드사전 자율수집 관리 UI(admin_metadata)·메타데이터 콘솔 IA 통합(채택 인박스·샘플 검수를 각 사전 하위 2차 보기로 일반화)+5서브뷰 디자인 폴리시·그래프 뷰 ROUTINE_USES 화살표 데이터흐름 정합+AI 능동 분석 지침 플로팅 툴팁(§55)·런타임 설정에 추론 강도별 예산 UI 섹션+pane 정렬 grid/commit-bar 재설계 · (07-08) 메타데이터 콘솔 UX — 검토/검수 큐 행 클릭 우측 read-only 상세·ENUM '+코드 추가' 프리필·샘플 mermaid 렌더·목록 가독성(metadata-console-ux2)+잔여 디자인 폴리시 5건(2차보기 필 경량·그룹 divider·신뢰도 accent, metadata-console-polish) · §59 제품 관리 'AI 분류 제안' 승인 UI(admin_products/admin.js — orphan_pending 승인·거부) · (07-09) 모델별 추론 예산 상한 native 확대(Sonnet 128K·Haiku 64K)+추론 강도별 예산 분리+추론↔본문 비율 슬라이더(feature-0018 코드 거주, WebRuntimeSettings) · (07-09) 응답 지연 타임아웃 복구 모달(z-9999) 제거 → 모달·토스트 없는 조용한 자동 재연결(취소/즉시답변 컴포저 인라인 상시, ask-timeout-nonblocking, 배포 4b6919ec·PB-0008 라이브 PASS) · (07-12) ITEM-09 그래프 CSS/JS 세분화·admin.js 그래프 모듈 분리(refactor, behavior-neutral, 브라우저 QA 대기) · (07-13) 사용자 재업로드 첨부 버전 관리(파일명·sha256 대조 버전 체인·assistant v1→v2 diff 인지, PR #751·PB-0008 PASS)·작업화면 데이터소스 '연결 테스트' 버튼+상단 단발 토스트(ds-conn-test, PR #767·PB-0008 PASS)·그래프 뷰 권한 분리(graph.read 독립 게이트, kb.ingest.manual 묶음에서 분리, PR #765)+seed catchup 권한설명 255자 초과 hotfix(PR #768) · (07-14) 작업 화면 애니메이션 효과 on/off 설정(복원)·프로필 '계정' 탭 세분화(계정/알림/UI/사용 내역)·그래프 뷰 첫 입장 조작 도움말 팝업(중간버튼 커서 표식·mis-position 수정)·제품 카테고리 밴드 우클릭 전용 메뉴·'결과 보기' 펼침 스크롤 보존(세로+가로 layout-timing)·그래프 'AI 능동 분석 실행' 권한 하위 분리(Critical §12.3) · (07-28) LLM 사용량 차트 드릴다운 '사용 기록' 개편 — 대화 비귀속 시스템·자율 사용분(라이브 토큰의 약 49%) 편입·작업/대상 표기·관리 화면 in-page 이동, '(시스템)' 막대 무동작 회귀 수정(usage-records-system) · (07-28) 그래프 뷰 시각 노이즈 제거 — 상세 패널 상시 설명문(컬럼·관계·함수/프로시저·클러스터·카테고리·AI분석) → 섹션 제목 옆 ⓘ hover 툴팁 이관 + empty-state 1줄화 + 관리 콘솔 하단 커밋 바 pending 0건 시 숨김(CSS 1규칙, `.has-pending` 시 자동 재노출), graph-noise-reduction · 헤드리스 78 PASS·PB-0008 라이브 PASS · (07-28) 그래프 뷰 — 캔버스 컨텐츠 카테고리(sim-group) 클러스터 선택 시 우측 '스키마 클러스터' 상세 목록이 **그 카테고리 헤딩 위치로 자동 스크롤**(fam 대조 + sticky nav 보정 + 1.8s 도착 강조 + 상태줄 이동 표기, 좌클릭·우클릭 메뉴 양 경로), graph-catcluster-scroll · 헤드리스 36 PASS(신규)·PB-0008 라이브 PASS · (07-28) 위 스크롤 UX polish(사용자 피드백) — 이동을 대화 뷰 point-rail 정본과 동일한 **280ms EaseOutExpo**(native smooth 의 거리비례 수 초 → 고정 단축)로 교체 + 도착 연출을 **헤딩 2회 점멸 + 하위 멤버 행 파도 순차 점멸(알파 선형 감쇠·상한 24행)** 로 재설계, graph-catcluster-scroll-polish · 헤드리스 65 PASS·PB-0008 라이브 PASS · (07-28) 그래프 뷰 — **접힌 카테고리 클러스터 하위 테이블 추적 시 카메라 오이동 수정**(승격 사다리에 컨텐츠 카테고리 `GB:`·제품 카테고리 밴드 `CAT:` 두 렌더-게이팅 계층 삽입, 종전엔 스키마 클러스터 중앙으로 오이동 / 밴드 접힘 시엔 아예 미이동) + 상태줄에 실제 승격 대상 종류 표기, graph-catcluster-focus · 헤드리스 32 PASS(신규)·PB-0008 라이브 PASS · (07-28) 그래프 뷰 상세 패널 hover 강조 — 같은 두 노드 사이 관계선이 1:N(왕복 참조=반대편 호, 루틴=읽기/쓰기 별개 선)인데 **첫 매칭 하나만** 강조되던 결함 수정: 행이 모델 엣지의 실제 (source,target)+relation_type 을 싣고 렌더러가 **방향>종류** 로 그 선을 특정 + 강조선 위 **데이터 흐름 대시 애니메이션**(화살촉은 정적·reduced-motion 폴백), graph-hover-flow · 헤드리스 41 PASS(신규)·전 스위트 733 PASS·PB-0008 라이브 PASS  · (07-29) **지식베이스 메타데이터 스코프 축 '데이터소스'→'제품' 전면 재구성**(metadata-product-scope, Major) — 라이브에서 축이 양방향 불일치(1제품↔7DS: KR_LIVE·KR_QA / 1DS↔5제품: mssql-qa-idc)라 등록 메타데이터가 1/N DS 에서만 주입되고 공유 DS 에선 타 제품과 혼입되던 결함 해소. `product.<ProductKey>` 스코프 신설 + 읽기/쓰기/콘솔 3면 통일, 부트스트랩을 제품 접근DB allowlist 로 강제(fail-closed), expand/contract 이관(`scripts/kb_scope_rescope.py`, 5,130건 이관 + 모호 257건 삭제, 사용자 승인). cross-cut 0002(주입 seam·self-heal)·shared/config(축 정의). codex 적대 10라운드 P1 0 · make test 전 스위트 PASS · (08-03) AI 운영 현황 > 운영 현황 '미분류 활동' 3종(analysis_verify·cluster_summary·domain_summary) 인사이트 분석으로 재배치 + 신규 계측이 taxonomy 등록 없이 들어오면 red 가 되는 호출부 AST 전수 게이트(fail-closed) 신설(aiops-taxonomy-unmapped, shared/model_catalog 3행·codex 8라운드 P1 0) · (08-04) 공유 링크 화면 '대화에 참여' 버튼이 **대화 소유자·기존 멤버에게 표시되지 않던** 결함 수정(share-join-btn-visibility) — 표시 판정을 `can_join` 단독에서 `is_authenticated && (can_join \|\| joinable)` 로 확대. 적대 리뷰 P1 반영으로 **이미 멤버인 클릭은 join 을 호출하지 않고** 대화로 직행(windowed 멤버 가시범위 교집합 축소 회피, `viewer.conversation_id` 멤버 한정 신규 필드) + 재렌더 시 액션 리스너 중복 부착·숨김 stale 해소. 신규 9건·회귀 스코프 98건 PASS · PB-0008 POST-DEPLOY 6축 전건 PASS · (08-05) standalone mjs 하네스 red 23건 전수 해소(40/40 green · 시나리오 DOM 이벤트 전환 + 실 Windows 20스텝 OK, tests-only) + feature-0036 판정 배지 PB-0008 라이브 실측 Run 기록 · (08-06) 좌측 항목(대화/폴더) 모달 **바깥 배경 dismiss 를 "누름+뗌 둘 다 배경"** 으로 한정(modal-backdrop-dismiss, Minor·frontend-only) — `click` target 이 mousedown/mouseup 의 공통 조상으로 승격되던 것이 "down 만 해도/up 만 해도 종료" 의 기전. 판정은 pointer 2단, 실행은 `click`, 상태는 제스처 1회분(장전 잔류 봉인·`isTrusted` 요구), 터치·펜 implicit pointer capture 해제. primitive `bindBackdropDismiss` 로 6종 단일 배선. §18.8 ux·design 패널 2 라운드 BLOCK 전건 반영 · 하네스 48 PASS(역검증 5종) · **PB-0008 실 Windows Chrome CDP trusted 입력 10/10 PASS**(negative control 에서 사용자 보고 현상 3건 재현). 범위 밖 동형 오버레이 3곳은 REPORT §8 원장 등재 · (08-06 후속) 배경 dismiss **저장소 단일 primitive** 로 통일(modal-dismiss-siblings) — `static/modal-dismiss.js` 신설(ESM 번들 2개가 공유; 복제가 곧 결함 기전이었다) + 전 표면 11곳 배선(승인된 동형 3곳 + 그래프 도움말 + 검색 모달의 손수 중복 구현 흡수). 전-트리 census 로 잔존 0 기계 단언. §18.8 패널 BLOCK 전건 반영 — census 하드코딩(33중 6)·vacuous 단언 3·ESC 누수 미완·AC drift·purge 중복 인스턴스. 하네스 76 PASS(뮤테이션 역검증 6종) · PB-0008 10/10 · (08-06) **첨부 삭제(버전 선택)·복구(휴지통)·일괄 다운로드**(attach-manage, **Critical** — `ADR-20260729T163000-attach-append-only` supersede, 사용자 승인). soft-delete + retention 창 복구, 삭제 인가를 열람 경계에서 분리(업로더 본인·대화 소유자·현재 멤버만 — 그룹 멤버 전원이 남의 첨부를 지울 수 있던 이월 이슈 해소), ZIP/개별 × 최신본/전 버전. §18.8 4도메인 패널 P1 14건 전건 흡수 — 신규 audit 액션 무등록으로 **감사 행 0**·일괄 반출의 공유창 window 미적용(AR-2)·PG 미러 강등 형제 누락(라이브 목록 중복)·삭제 직후 목록이 pill 로 덮임·240px 파일명 3자 붕괴. 신규 45 pytest · PB-0008 폭 실측 PASS(역검증 FAIL 재현) · e2e 흐름은 POST-DEPLOY 이월 · (08-06) **첨부 다운로드 파일명 버전 접미사(`_v2`) 토글**(attach-suffix-toggle, Minor — 권한·스키마 0). 첨부 패널 체크박스 1개를 전체 다운로드 모달과 공유해 단일(목록 ⬇·버전 이력 ⬇·말풍선 칩)·전체(ZIP·개별 저장) **전 경로**가 같은 선택을 따른다(`localStorage` 기억, 기본=종전 동작). 접미 규칙 정본 `_download_filename_with_version`(keep/strip/force)을 서버 한 곳에 두고 프론트는 서버가 준 이름(`X-Attachment-Download-Name`·manifest `download_filename`)을 채택 — 규칙이 두 벌이라 AI 편집본이 `report_v2_v2.csv` 로 나가던 **이중접미**도 함께 해소(역검증으로 수정 전 재현). strip 은 `_v<VersionNumber>` 일치 시에만 제거해 사용자가 지은 `plan_v2.docx` 를 왜곡하지 않는다. §18.8 패널(security·backend/qa·ux) 반영 · pytest 71 · PB-0008 실 Chrome 31 step PASS(bind-mount 프리뷰 컨테이너 — 라이브 무접촉) · (08-07) 첨부 버전 비교에서 **내용이 동일하면 문서 원문 출력**(attach-diff-identical-source, Minor — 권한·스키마·마이그레이션 0). 맥락 축약이 변경 0개인 파일을 gap 한 줄로 접어 **화면에 본문이 한 줄도 없던** 결함 해소: 서버가 identical 을 축약 대상에서 제외해 원문 전량을 방출하고 프론트가 줄번호+본문 2열(`is-source`)로 렌더. §18.8 ux·design 패널 CONCERN 2건 반영 — 단정 강도를 근거에 맞추는 `_identicalFlags` 로 배너·배지 판정 단일화(절단 시 '원문' 금지, `splitlines()` 가 흡수하는 CRLF↔LF 차이는 sha256 로 표면화), diff 전용 컨트롤 판정면을 하이라이트 토글과 동일화 + 숨김→비활성(레이아웃 고정). 부수 적발: `el.hidden` 이 author `display:inline-flex` 에 밀려 **CSS 층에서 무력화**돼 선행 AC-AVD-23 이 화면에서 미작동하던 것 봉인. 하네스 61 PASS·뮤테이션 3/3·PB-0008 격리 컨테이너 4시나리오 PASS + 구·신 A/B(rows 1 gap → 5 equal) · (08-07) **첨부 버전 diff 줄 안(글자 단위) 변경 구간 표시**(attach-diff-intraline, Minor — 권한·스키마·마이그레이션 0) — 사용자 재요청("여전히 line 단위 차이만 … 각 글자 단위의 차이점은 출력되지 않는")으로 선행 cycle 이 §범위 밖에 뒀던 항목 완수. 서버가 `replace` 행에 `left_segs`/`right_segs` 를 단독 산출(ASCII=단어·**CJK=글자**·자소 묶음 보호)하고 프론트는 구문 하이라이트가 만든 텍스트 노드를 문자 오프셋으로 쪼개 덧칠(두 강조가 독립 레이어). 마크는 **밑줄** — 배경 칠은 구문 토큰 AA 대비를 깨서(알파 0.20 에서도 number 3.40). O(n·m) 을 행 수가 아닌 **작업량 예산**(3M 토큰쌍)으로 봉인(실측 411초 → 1.75초, 흔한 폭 1500행 전량 0.23초) + 생략 시 `truncated.intraline` 표면화. 부수로 기하 하네스 선행 파손(`detectCodeLanguage is not defined` 전-케이스 미실행) 복구. pytest 3989·jsdom 117·실브라우저 기하 53 PASS · (08-07) 첨부 목록 **행 클릭 = 문서 원문 보기**(attach-source-view, Minor — 신규 권한·스키마 0). 종전엔 내용을 보는 유일한 길이 '버전 2개 이상' 게이트 뒤의 비교 모달이라 **버전이 하나인 첨부는 내려받지 않고는 볼 수 없었다**. 신규 `GET /api/attachments/{id}/source`(권한·D21 게이트 `/diff` 와 동형) + 비교 모달과 같은 `_renderSource` 재사용. §18.8 패널 2건 CONCERN 반영 — 본문 응답에 `no-store`/`nosniff` · **ranged read**(전체 적재 시 최대 25× 증폭)와 그 부작용인 무음 절단 차단(판정을 DB 정본 크기로) · rate limit 30/분 · 절단 시 '원문' 단정 하향 · 스크롤 앵커 · 드래그 선택 press-pair 가드 · 행 대신 **파일명**을 버튼으로(중첩 버튼 회피) · 버전 이력 행 👁 진입점. §21 window clip 이 첨부 read 4경로 공통 미적용인 점은 SECURITY.md §21.5-6 에 수용 등재(후속 일괄 봉인). 하네스 77 PASS · 뮤테이션 2/2 · (08-11) 줄 안 변경 마크가 `_` 를 가리던 문제 수정(attach-diff-mark-underscore, Minor — CSS 선언 1종) — `inset 0 -2px` 는 바를 content box 안쪽 맨 아래 = `_` 자리에 그려 `legacy_gy_pay` 가 `legacygypay`, `__init__` 이 `init` 으로 읽혔다. 바깥 그림자 `0 2px` 로 내려 글자↔바 사이 빈 픽셀 행 1행 확보. 바깥 그림자는 border box 안쪽 미도포라 구문 토큰 AA 논거 유지(글자 영역 마크색 픽셀 0 실측). 회귀 잠금 M6(픽셀 간격)·D6a2(inset 금지) 신설 + 기하 M3b 클립 아티팩트 수정. 기하 55·jsdom 125·mjs 50 suite · (08-11) 첨부 목록 **행 클릭 = 문서 원문 보기**(attach-source-view) 및 그 후속 **액션 열 정렬**(attach-version-action-align) — 후자는 사용자 보고("비교 버튼 유무에 따라 원문 조회 버튼 위치가 뒤틀림"). 오른쪽 정렬 flex 에서 행별 버튼 개수 차이가 열을 미는 기전이며, 같은 결함 클래스가 목록 3종(활성·버전 이력·휴지통)에 있어 빈 슬롯 예약 primitive 단일 정의로 함께 봉인. §18.8 패널 CONCERN 반영 — `min-width` 는 **바닥**이라 폭 고정이 아님을 A/B 로 실증(글꼴 확대 시 22→24px, 열 1135→1133) 후 `flex: 0 0 22px; min-width: 0` 으로 전환. PB-0008 좌표 실측(4버전 체인 열 단일값 · negative control 24px · 활성 목록 1180→1204→1180). 하네스 34 PASS · 뮤테이션 7/7 · (08-12) 관리 콘솔 **제품 > '+ 데이터베이스 추가' 목록이 체크할 때마다 밀리던 문제**(dbpicker-layout-stability, Minor·frontend-only) — 편집기가 [목록]→[picker] 순서라 체크마다 위쪽 목록이 커져 열린 드롭다운을 밀었다(실측 단일 38px·연속 3회 114px, 행 높이 29px 보다 커서 오클릭). 재구성 대상을 picker **뒤**로 옮겨 밀림을 구조적으로 0 으로(스크롤 보정은 상단 구간에서 원리적 실패). 동일 결함 클래스인 '+ 데이터소스 추가'(42~74px)도 같은 규약으로 봉인 + degraded 배너 삽입점·선택 카운트 상시 노출·드롭다운 내부 스크롤 보존·가시 후보 3~4행→10행·방향 지시어 census. codex 적대 P1/P2 전건 반영(빈 datasource 안내 방향 지시어 누락 적발). 신규 jsdom 25·실 chromium 13(축별 뮤테이션 역검증)·기존 mjs 53 suite PASS·pytest 4,287 EXIT=0 · **PB-0008 실 Chrome/150 0px**(역검증 38/74px 재현, 라이브 무접촉·서버 무변경) · (08-12) 첨부 **`.md` 구문 하이라이트**(attach-md-highlight, Minor·frontend-only) — `.md` 는 서버 쪽(`_EXTENSION_KIND_MAP`·diff/원문 kind·업로드 allowlist)이 이미 전부 열려 있었고 막힌 곳은 `static/code-highlight.js` 의 `LANGS` 한 곳뿐이라 화면에서만 구조가 산문과 같은 색이었다. 선행 attach-diff-syntax 가 예고한 확장 자리에 vendor 무추가 토크나이저 1항목 추가(제목·리스트·체크박스·인용·fence·구분선·표 파이프/정렬행·링크/이미지·자동링크·강조·인라인코드·참조정의). **새 팔레트 변수 0**(기존 9종 재사용 → WCAG AA 계약 상속) · `_강조_` 는 snake_case 오색 때문에 의도적 미지원 · fence 내부는 라인 독립 절충 유지. 링크 대안 2차 비용 2.98ms → **0.72ms**(사전 가드+라벨 `[` 제외+런 대안), 산문 920자 토큰 1개. codex 적대 P1 0·P2 3건 전건 반영(표 정렬행 과대판정으로 리스트 `- \|` 이 줄 전체 회색이던 **실 오색** 포함). 하네스 146 PASS·형제 3종 회귀 0·**PB-0008 실 Chrome/150 PASS**(전용 포트·프로파일 세션 격리, computed 색 9종 정본 일치) · (08-12) **메타데이터 pane 입력 UI 통합 표 재구성**(metadata-pane-refresh, Major·표시 계층 전용) — 사용자 보고 "다른 화면에 비해 촌스럽다" 를 **이 pane 만 앱 토큰·계약을 안 쓰는 8건**으로 환원해 각각 정본에 붙였다: 포커스 halo 부재(`base.css .field input:focus` 는 3px halo인데 border-color 단독) · 박스-안-박스 격자(행 카드 + 내부 테두리 입력 × 30행) · raw `●`/`○` 글리프(`--tag-*` 토큰 미사용) · 등폭 하드코딩(`var(--mono)` 미사용) · 전각 `＋`(다른 pane 은 ASCII) · 저장 버튼 2줄 줄바꿈 · 회색 패널 3중 중첩 · 컬럼명 가변 폭. 골격 그리드를 **단일 표 surface + hairline row-group + ghost cell + 상태 dot 3단 + sticky 열 헤더**(열 폭 토큰 단일 출처)로 재조립하되 **DOM 셀렉터·dataset 계약을 1개도 바꾸지 않아** 입력값 유실 회귀 표면을 정의상 0 으로 뒀다. 사전 검증이 배포 전 2건 적발 — WCAG AA 미달(상태 라벨 4.12:1·열 헤더 3.58:1 → 7.11/6.18) · sticky `top:0` 이 스크롤러 padding 18px 띠로 직전 행을 헤더 위에 비침(→ `top: calc(-1 * var(--meta-grid-head-inset))` + 하네스 결합 검사). 신규 mjs 90(뮤테이션 15/15 KILLED)·headless 실렌더 32·기존 metadata 하네스 154 green·mjs 53 파일 green·pytest 4,312 PASS(1 선재 red 는 pristine main 동일 재현) · **PB-0008 실 Chrome/150** 격리 컨테이너 125테이블 실데이터 열 정렬 편차 **0.00px/30행**·라이브 데이터 변경 0. DESIGN §3.1·§3.2 계약 신설. codex 적대 리뷰 P1 0·P2 3 전건 반영(잘린 식별자 title 회수 경로·JS 동적 라벨 전각 플러스 잔존·pane 토큰 리터럴 색 → :root 파생, 파생 등가는 합성 픽셀 대조로 실증). **POST-DEPLOY 라이브 재실측 이월** · (08-12) 위 요청의 **재해석 정정 — 첨부 `.md` 를 마크다운 문서로 렌더**(attach-md-render, Minor·frontend-only). 사용자 지적 "구문 색이 아니라, 실제 마크다운 구성으로 출력" — 원문 보기(및 비교 모달의 내용-동일 화면)가 제목·표·목록·강조·인용·코드블록·체크박스를 실제 서식으로 출력한다. 파이프라인은 답변 말풍선과 **동일한** `markdownToHtml` 재사용(신규 제작 0). `마크다운으로 보기` 토글(기본 켬·두 모달 저장키 공유)로 원문 표 복귀 시 **byte 무손실**이며, **변경이 있는 diff 에는 미적용**(줄 대조 보존). 첨부는 임의 업로드라 **원격 리소스 비콘**(로드 자체가 열람 신호, 응답 CSP 는 report-only 라 미차단)을 sanitize 이후 DOM 하드닝으로 중립화 — URL 은 칩으로 노출 + 건수 배너, `iframe/object/embed` 제거, 외부링크 `rel=noopener noreferrer nofollow`. **codex 적대 6라운드 P1 7건 전건 반영** — 다속성/프로토콜상대 URL 우회 · **라이브 DOM 선파싱**(비콘이 이미 나간 뒤였다) · 공용 sanitize 프로필의 `style`/`form` 허용 · **mermaid 후처리 우회**(하드닝 이후 SVG 주입 + `themeCSS` 외부 url) · **같은 출처 이미지 자동 GET-CSRF·같은 출처 링크 클릭 권한사용**(oauth authorize) → inert `<template>` 선중립화 + 첨부 전용 프로필 + mermaid 코드블록 강등, GFM 체크박스 상태 소실 회귀도 글리프로 해소. 신규 하네스 102 PASS(**실 vendor marked+DOMPurify 로드**)·뮤테이션 14종 KILL(살아남아 고친 검사 결함 3건 기록)·형제 4종 회귀 0·**PB-0008 실 Chrome/150 PASS(정본 구현 실행 — 초판 시나리오는 옛 취약 구현을 복제해 무효였음)** · (08-12) **한/영 자판을 잘못 친 검색어도 결과를 내는 자판 교차 검색**(hangul-qwerty, Minor) — `shared/hangul_qwerty.py` 공용 변환(한→영·영→한 양방향)을 프런트 `static/hangul-qwerty.js` 와 서버측 그래프 콘텐츠 검색(`modules/metadata_graph.py`)이 같은 규칙으로 공유. 적용면: 관리콘솔 검색 8종(계정·데이터소스·메타데이터·제품·역할·설정·사용량·대화)·사이드바 대화 검색·그래프 컨텍스트 메뉴. 원문 일치 우선·교차 변환은 보강(기존 결과 소실 0) · (08-13) **LLM 사용량 요약 카드 = 차트 지표 선택기 + 프롬프트 캐시 계측·활성화**(usage-metric-charts, Major) — 카드 8종(요청·호출·총 토큰·입력·출력·캐시 읽기·캐시 쓰기·추정 비용) 클릭 시 기간 막대·도넛·역할·계정 차트가 그 지표로 재구성되며, 노드를 유지한 채 기하만 바꿔 **CSS transition 으로 이동**(재생성 시 점프). 비-가산 지표(요청=distinct run_id)는 모델 분해 없이 단일 막대 + 사유 1줄, 모델 부분 선택과는 조합 자체를 차단(카드=모델별 합 vs 차트=전체 기준 불일치 봉인). 캐시 축은 **착수 전 실측**으로 컬럼도 `cache_control` 도 없음을 확인한 뒤 마이그 0056 + `_record_llm_usage` 캡처 + 캐싱 활성화(마지막 system 블록 1 브레이크포인트, 임계 미달·로컬 LLM no-op)까지 도입 — 적용면은 직접 호출 16곳의 프롬프트 길이를 세어 임계 초과 1곳(NODE_ANALYSIS 9.1k)만 명시 적용. `prompt_tokens` 가 캐시를 포함한다는 실측(5039=37+5002)에 따라 비용식을 read 0.1x·write 1.25x 로 분해하고 5개 소비 화면에 동시 적용(캐시 0 레거시 행은 종전 값과 동일). 0056 미적용 DB 는 리터럴 0 재실행으로 자가치유. codex 적대 P1 2·P2 5 중 6건 반영·1건 부분 refute(무음 0 성립 안 함 → 로그를 관측 시점으로 이동). pytest 4,364 PASS · 실 Chromium 렌더 25 PASS(전환 중간 프레임이 시작·끝 사이임을 실측) · **PB-0008 라이브는 POST-DEPLOY 이월**(미머지 ESM JS 는 docker cp 프리뷰가 원리적으로 불충분) · (08-13) 첨부 '문서 원문' 화면에서 버전 비교 수행(비교 기준 선택기 — 체인은 `/source` payload 로 왕복 1회 유지) + 같은 버전·변경 없음이면 원문 그대로 출력(원문 화면·비교 모달 양쪽 — 종전 '서로 다른 두 버전을 선택하세요' 빈 화면 해소) + 목록 '⇄ 버전 비교' 기본쌍 최초→최신(attach-source-compare) · (08-13) **첨부 목록 파일명 순 정렬**(attach-list-name-sort, Minor — 종전 업로드 순 `Id ASC`). 패널·휴지통·일괄 다운로드 3표면 + 컴포저 칩 파생. 숫자 구간은 수치 비교(`_02_` < `_10_`)·casefold·한글 가나다, 정렬은 MySQL/PG 두 read 경로 합류 후 파이썬 한 함수(경로별 collation 차이 차단). 휴지통은 표시만 이름순이고 절단 SQL(`DeletedAt DESC`+`LIMIT 200`)은 불변, 일괄 다운로드는 버전 체인 인접 유지(그룹 대표=최신 이름). pytest 12 신규·회귀 391 PASS·PB-0008 실 Chrome A/B(수정 전 컨테이너 대조로 39건 순서 역전 실증) · (08-13) 다른 계정이 공유한 그룹 대화도 폴더 drag&drop 이동(folder-dnd-shared-group — `isFolderScopedConversation`(owner\|\|is_member) 로 폴더 파티션·draggable 게이트 단일화, 백엔드·권한 불변·계정별 배정 격리 유지, jsdom 31 + 수정 전 재현 실증) · (08-13) 그룹 대화 멤버 프론트 권한 게이트 정합(member-scope-gates — 백엔드 `.own`=owner‖멤버 경계에 맞춰 중단·즉시답변·실행시간연장·'설정'(나가기) 4종 개방 + '읽기 전용' 오도 안내 2건 소거, rename·보관·복제는 서버 2차 owner 게이트대로 차단 유지 — jsdom 53 + 수정 전 재현 실측) · (08-13 정정) 위 member-scope-gates 중 중단·즉시답변·연장은 display-permissive `can()`(인자 무시) 때문에 원래 차단이 아니었음 — 해당 축은 no-op(의미 명료화)이고, 실효 결함은 오도 안내 2건 + **'대화 설정' 팝업 보관/나가기 분기 사망(멤버 나가기 경로 부재)**로 후자를 member-leave-branch 에서 `canArchive = isOwnConversation ‖ canOpenAdminConsole()` 로 수정(실 DOM 22 + 수정 전 6건 FAIL 재현) · (08-13) 우측 스크롤 point rail ↔ 비동기 콘텐츠 성장 정합(rail-async-relayout — mermaid SVG·이미지·진행 중 말풍선 성장을 메시지 row/pending 관찰로 추적해 rail 재배치 + 맨-아래 유지, pin 해제 5경로·pointerdown 스크롤바 판별, 실브라우저 기하 하네스 26 PASS·PB-0008 오차 86.58px→0.11px) · (08-13) 그래프 뷰 노드 펼침 체감 지연 근본 개선(perf — 오브젝트 풀 diff 서명에서 **위치 분리**(재배치는 destroy/re-create 대신 position.set)·`_metaTopoSig` 에서 Column 제외로 배치 메모이즈 무효화 해소·컬럼 GET 을 더블클릭 타이머와 병행 선-fetch; 693노드 16컬럼 실측 클릭→펼침 915→556ms·블로킹 344→133ms·drawMs 210→35ms·객체 재생성 537→21) · (08-13) **공유 대화 첨부의 LLM 참조 경계 전환**(group-attach-scope-window, Critical·사용자 승인 — 발신자-한정 → 대화 스코프 + `shared/share_window.py` window 게이트 단일 정본(그룹 여부·소유자·window 한 쿼리 판정, 실패는 sender-only fail-closed) + 타 멤버 파일 `uploaded-by` 출처 라벨·DATA-전용 코드 권위 계약, cross-cut 0002 주입 게이트; 읽기 확대 ≠ 쓰기 확대. SECURITY.md §47 · feature-local ADR-20260813T183000 · 46 PASS) · (08-13) 위 08-13 항목 8건 POST-DEPLOY 라이브 실측 종결(usage-metric-charts·attach-source-compare·attach-list-name-sort·folder-dnd-shared-group·rail-async-relayout·그래프 펼침 성능·group-attach-scope·member-gates — 각 정본 unit TASK/TEST.md) · (08-14) 사용량 차트 '요청' 경계 전환 재생성 3기전 해소(signature 를 일자 집합으로 축소·차트 폭을 signature 에서 제거하고 in-place 좌표 재배치·삽입 직후 강제 reflow 후 목표값 — 3건 모두 헤드리스 green·라이브 red 였고 AGENTS.md §16.6 의 '렌더 타이밍·레이아웃 의존 축은 헤드리스를 1차 그물로만' 이 실증됐다, 관리·프로필 양 화면 POST-DEPLOY 실측 종결) · (08-14) 프로필 '사용 내역' 차트 지표 8종 선택기 — 지표 정의(목록·라벨·가산성·안내문)를 신규 `src/static/usage-metrics.js` **단일 정본**으로 추출해 관리·프로필 두 번들이 import(관리 콘솔의 복제 정의 제거), 전환은 마지막 응답 재사용(재조회 없음), `/api/profile/usage` 도 8축, 신규 15 + 관리 37 PASS · (08-14) 관리 콘솔 '사용 기록' 표 열 정렬 + 페이지네이션(25/50/100/전체 — 정렬 키는 **화면 표시값** 기준, 이미 받은 결과셋 안에서 처리해 표시축↔질의축 동기화 실패 지점을 만들지 않음, nav 는 정렬로 순서가 바뀌지 않는 불변 색인·재렌더 후 포커스 복원, 첫 화면 무회귀) + 페이저 스크롤 바닥 sticky, PB-0008 298건/6페이지 실측 · (08-14) 프로필 사용 내역 표에도 같은 규칙을 **테스트가 두 소스를 대조해 잠그는** 방식으로 이식 — 스크롤러 단일화로 sticky 열 머리 복구(관리 판 동반)하고 [CODEX] P2 4건 처리, 그중 대화 제목이 `title='…'` 작은따옴표 속성에 이스케이프 없이 들어가던 **stored 속성 인젝션** 경로를 양 판 `esc` 따옴표 포함으로 차단(관리 콘솔은 타인 제목을 렌더하므로 실재 경로였다) · (08-14) 프로필 사용량 요약 카드 넘침 해소 — 보고는 '좁은 화면' 이었으나 원인은 지표 4→8종에서 온 `flex` 균등 분배라 900px 에서도 넘쳤다, `grid auto-fit minmax(138px)` 로 전환하고 트랙 값을 라이브 패딩 실측으로 뮤테이션 확정(검증되지 않는 방어 2종은 제거·환원) · (08-14) **첨부 버전 계보를 작성 주체별로 분기**(attach-version-branching, Major — assistant 수정본이 사용자 계보에 `v+1` 로 편입돼 사용자 최신본을 supersede 하던 것을 새 root 체인 v1 분기로 갈라 두 계보가 나란히 남게 함, 스키마·`UNIQUE(Root,Version)` 불변·마이그레이션 0, `MetaJson` 분기 포인터 + `/api/attachments/{id}/versions` `lineages` 축 + 계보 2개 이상일 때만 `## FILE VERSION LINEAGES` 프롬프트 계약, 적대 P1 3·P2 1 전건, 기존 혼합 체인 39건은 사용자 결정으로 미백필) → (08-14) 버전 비교에 **계보 축 토글**(attach-version-tree-ui — `/api/attachments/{id}/diff` 에 `from/to_attachment_id` 신설(사람·AI 계보가 둘 다 v1 일 수 있어 버전 번호로 지목 불가), 인가는 **양쪽 각각** `_account_can_access_attachment` + 같은 대화·같은 파일명 스코프, 비교 버튼 노출 조건을 `verNum > 1 ‖ is_assistant_generated` 로 교정(하네스 18건이 모달 안쪽만 검증해 진입점 부재를 놓쳤다), 신규 24 + 14 PASS) · (08-14) 첨부 `CreatedAt` 시간축 로컬→UTC 정정(Major 데이터 마이그레이션 — 스키마 DEFAULT 전환 + 1회 백필(오프셋 서버 조회·상한을 ALTER **이전에** 확정·저장소별 마커·`_claim_migration_once` 원자 선점) + 전송 `Z` 명시로 표시 무회귀, 라이브 모순 234→0·차감 폭 정확히 9시간, 실브라우저 칩 미실측) · (08-14) 실행 단계 패널에 **단계별 시각·직전 간격·누적 경과** 표기(step-panel-timing — `step.created_at` 재사용으로 backend 무변경·과거 대화 소급, 두 timestamp 표기 파싱 폴백·레거시 fail-soft·좁은 폭 줄바꿈, 34 PASS·뮤턴트 3종 사멸, PB-0008 POST-DEPLOY 종결) · (08-24) **실행 단계 시간이 한 칸씩 밀려 표기되던 오귀속 해소**(step-timing-attribution, Major — `_emit_activity`(LLM 호출 직전 = 착수 시각)와 `_mirror_step`(도구 종료 후 = 종료 시각)의 기록 시점 비대칭 때문에 추론 시간이 다음 도구 칸에 통째로 얹혀 1초 SQL 이 "2분 3초" 로 읽히던 것(라이브 run `20260824021929-c71393cf` 실측)을 `_computeStepTimings` 귀속 재정의(도구=백엔드 실측 `result_summary.elapsed_ms`·내부동작=차분)와 `시작시각 · 이 단계 소요 · 누적` 표기로 교정. 스키마·마이그레이션 0, 실측이 없는 과거 대화는 **소요를 비우고 `~` 근사 표시**(모르는 값을 지어내지 않는다), 프론트 70 + 백엔드 7 PASS·뮤턴트 12종 사멸·codex [P2] 5 전건 반영, PB-0008 라이브 `07755e05` 전건 PASS) · (08-24) **살아있는 run 을 "작업 중단" 으로 오표시하던 임계 역전 해소**(stale-threshold-attempt-cap, Major — 표시 임계 `WEB_PROGRESS_STALE_TIMEOUT_SECONDS` 1200초 하드코딩이 감시 대상 per-attempt 상한 `AGENT_TIMEOUT_SEC` 1800초 live 보다 작아 정상 무진전 구간이 stale 로 오라벨됐다(라이브 93분 run 이 10분간 "중단" 표시 후 정상 완료 — 4중 삼각측량 실증). 임계를 live 상한 + margin 파생으로 전환(floor 1200 · clamp 3600 · 상승 전용 high-water · terminal 도 `last_active` 반환), 신규 19 + 관련 42 PASS, POST-DEPLOY `596a722a` 에서 `threshold 1980 > cap 1800` 실증 / 라이브 UI 실측은 상태 생성이 쓰기를 요구해 **미수행·이월**) · (08-24) **좌측 대화목록 재배치 연출**(sidebar-reorder-anim → reorder-easing → reorder-affordance → dnd-reorder-affordance, Minor 4연속 — 이름 변경 시 `innerHTML=""` 전량 재구성으로 요소가 교체돼 transition 이 걸리지 않아 순간이동하던 것을 FLIP 코디네이터(`_beginSidebarReorder`/`_commitSidebarReorder` + 시야 유지 3축 · reduced-motion 게이트)로 전환한 뒤, 사용자 요청 `easeInOutBack` 을 PB-0008 로 실측 확인하고 **디자인 근거로 되돌려**(기능적 UI 에 오버슈트는 부적합·420ms 는 과다) ease-out 거리적응형 160~280ms + **도착 표식 2층**(배경 펄스 0.9초 · accent rail + "이동됨" 배지 3.5초, reduced-motion 에서도 표식 유지)으로 재설계. drag&drop·"···" 메뉴 이동·root 드롭이 수렴하는 `moveConversationToFolder`/`moveFolderTo` 두 곳에 예약을 걸어 같은 연출을 덮었다. 대화 `updated_at` 정렬·폴더 이름순은 **의도된 동작**으로 사용자 확인 — 정렬 키 분리 제안은 철회) · (08-24) **인라인 이름 변경 오확정 봉인 + 대화 "이름 변경" 메뉴**(sidebar-rename-focus, Minor — 사용자 조작이 아닌 배경 재렌더 3종(안 읽은 배지 7초 주기 · 대화 전환 catchup 1.2초 · 응답 진행 중 갱신)이 편집 중 `<input>` 을 떼어내고 그 detach 가 `blur` 로 관측돼 잘린 이름이 확정되던 것(결함 주입 사본에서 `PATCH …/title` 재현·한글 IME 조합 확정 Enter 포함)을 **무시·억제·보존 3겹**(`_inlineRenameDetaching`·`!isConnected`·`dataset.imeComposing` / `isSidebarRenaming()` 게이트 / 값·커서·포커스 capture-restore)으로 봉인하고 대화 우클릭 메뉴에 `이름 변경` 신설. POST-DEPLOY `27d99bb2` 라이브 PB-0008 전건 PASS — 1차 FAIL 은 제품이 아니라 **계측기 결함**(고정 `?v=dev` URL import 가 배포본에서 별개 ESM 인스턴스를 만들었다)이라 실 로드 URL import 로 수정) · (08-25) 폴더·대화 우클릭 메뉴 순서 정합(ctxmenu-order-parity — 공통 규칙 '이름 변경 → 고유 액션 → 이동 류 → 설정' 으로 양 메뉴의 공통 항목 위치를 통일) · (08-27) 개인 AI 연결 대화에서 답변 직후 스크롤이 계속 최하단으로 끌려가던 현상 해소(/api/progress steps-fallback 이 브리지 사후 원장을 '진행 중' 으로 오독 → 감지기가 loadHistory 를 지연 0ms 로 반복 위임; 원장 제외 + 위임 국면당 1회 수렴, bridge-progress-scroll-loop) · (08-28) 첨부 **계보** UI — 단건 계보에서도 「⇄ 계보 비교」에 도달하도록 두 겹 게이트를 함께 넓히고 목록에 `계보 k/n` 배지 표시(attach-lineage-ui). 상세는 정본 TASK.md `TASK-20260828T230000`. · (08-31) 개인 AI 연결 성립 시 토스트 + 연결 모달 자동 닫기 · 대화 「최근 갱신」이 첫 턴 시각에 고정되던 결함 해소. 상세는 정본 TASK.md `20260831T1445-conv-last-activity-updatedat` · FUNCTION.md 08-31 연결 모달 절. · (09-01) 우측 오버레이 사이드 패널(첨부·실행 단계·유저 프로필) **단독 열림** — 등록부 `static/app/side-panels.js` choke point, 겹쳐 열려 아래 패널이 가려지던 결함 해소. 상세는 정본 TASK.md `20260901T1153-side-panel-exclusive`. |
| feature-0004-browser-automation | in-progress | 2026-07-28 | [TASK](../unit/feature-0004-browser-automation/docs/TASK.md) | 엄격한 브라우저 시나리오(TEST-0003) 확정 + 라이브 왕복·scheme 가드 PASS — Completion Checklist 전건 충족 |
| feature-0005-qa-mcp | in-progress | 2026-07-28 | [TASK](../unit/feature-0005-qa-mcp/docs/TASK.md) | 엄격한 QA 시나리오(TEST-0003) 확정 + MCP 왕복·실 DB 메타데이터 반환 PASS — Completion Checklist 전건 충족 |
| feature-0006-lan-proxy-access | in-progress | 2026-09-02 | [TASK](../unit/feature-0006-lan-proxy-access/docs/TASK.md) | caddy 운영 자산 이관 완료 · `_get_client_ip` X-Forwarded-For conditional trust(SECURITY.md §9.7) · (06-17) 사내 자체 Root CA 신뢰 배포(self-signed 탈피)·caddy `:443` 정식 front door·테스터 Root CA 원클릭 설치 번들(TASK-0296/0297/ai-claude-trust-bundle, ADR-LAN-0004) · **(08-11) 엣지 보안 헤더 baseline — 전 응답 `X-Content-Type-Options`·`X-Frame-Options`·`Referrer-Policy`·`Permissions-Policy` 부착 + `Server` 헤더 제거 + CSP Report-Only(PixiJS·mermaid blob/worker 경로가 조용히 깨질 위험 때문에 위반 관측 후 enforce 승격), HSTS 는 `/trust/*` 평문 CA 번들 배포 충돌로 의도적 제외 — api-exposure-hardening(코드 거주 feature-0003, ARCHITECTURE.md §4 · SECURITY.md §7.3)** · (09-01) 인증서 만료 감시 — `bin/cert-expiry-check.sh`(leaf 30일 / Root CA 180일 · exit 0/1/2 · `--live` 로 서빙본 대조) + `bin/install-cert-expiry-cron.sh`(멱등 주 1회). 배포 게이트 `preflight_tls` 가 못 보던 **배포 공백 구간**과 **Root CA 축**을 덮는다. 상세는 정본 TASK.md `TASK-20260901T103000-ai-claude-corp-cert-expiry-monitor`. · **(09-02) 포트프록시 5분 주기 절단 해소 — Windows 예약작업 `mysql_ai_web_portproxy_sync` 가 매 실행 조건 없이 `netsh portproxy delete`→`add` 를 해 WSL IP 불변 상태에서도 80/443 기존 연결을 5분마다 전면 절단하던 결함(개인 AI 브리지 러너의 55초 대기 호출이 그때마다 `RemoteDisconnected`, 시간당 12건 WARN). 현재 매핑이 이미 원하는 값이면 건드리지 않도록 멱등화(못 읽으면 종전대로 재설정하는 fail-safe) + 로케일 무관 파싱 + 한글 주석용 UTF-8 BOM. 상세는 정본 TASK.md `TASK-20260902T113500-ai-claude-feature-0006-lan-proxy-access`. **라이브 검증 완료(13:33~13:55, sync 5회 정상 실행 중 목표 지표 0건 — 배포 전 동일 구간 8·8·8·12건). ⚠ Windows 배포본 교체는 관리자 권한 필요(WSL 은 ACL 에 막힘).** |
| feature-0007-bedrock-llm-provider | in-progress | 2026-08-11 | [TASK](../unit/feature-0007-bedrock-llm-provider/docs/TASK.md) | AWS Bedrock(Seoul) provider 통합, API Vault 폐기 (ADR-0022) · (07-04) insight 주말/야간 gemma 고착 해소 — 용도별 모델 라우팅 분리(interactive/insight fallback 체인 정정, llm-routing) · (07-07) OAuth 토큰 갱신 cron 라이브 probe 제거→정적 검사 전환(CHG-20260707-oauth-cron-static-refresh) · **(07-30) 자동 로컬 LLM(gemma/edge) 강등 경로 전면 제거 — litellm fallback edge 참조 0 + off-hours 시각 강등 기본 비활성(ADR-003, ADR-002 부분 supersede)** · **(08-07~08-11) OAuth 계정 가용성 자동화 (Critical §12.3) — 1순위 slot 사용량-소진 게이팅(7일 상태 `rejected` 또는 헤더 쿨다운 ≥1800s 일 때만 강등 — 모든 429 를 소진으로 읽던 burst-429 오강등 교정, 네트워크·미분류는 fail-open) + 만료 1h 이내 `refreshToken` 선제 회전·원자적 되쓰기(킬스위치 `CLAUDE_OAUTH_AUTO_ROTATE=0`), oauth-exhaustion-gate·oauth-gate-hardening·oauth-auto-rotate. 회귀 잠금 cross-unit `unit/feature-0002-agent-core/tests/test_oauth_exhaustion_gate.py` 75건(mutation 20/20 KILL). SECURITY.md §42 정합** |
| feature-0008-windows-browser-testing | review | 2026-08-28 | [TASK](../unit/feature-0008-windows-browser-testing/docs/TASK.md) | Windows 브라우저 자동 구동 + Playwright MCP (PB-0008) · (08-28) 검증용 로그인 세션 3종(`session-login`/`session-check --require-auth`/`session-logout`) 신설 + `session-login` 이 사용자 탭을 자기 것으로 오인해 브라우저를 종료시키던 결함 수정 |
| feature-0009-group-conversation | in-progress | 2026-07-04 | [TASK](../unit/feature-0009-group-conversation/docs/TASK.md) | 안 읽은 메세지/@멘션 배지(gc-unread-badge, REQ-GC-R8 read-state, alembic 0019 + 0020 baseline backfill + read-fix 커서 전진 보정 + read-500-fix(읽음 API `POST /read` 서버 import 정정 `modules.db`→`shared.db`) + read-idspace-fix(읽음 커서·unread 집계는 core_messages.id 공간인데 FE 가 표시 store messages.id 를 보내 GREATEST 가 전진 영구 거부하던 최종 근본원인 — 핸들러가 항상 MAX(core_messages.id)로 전진) — 읽지 않은 신규만 집계, 읽으면 0) · 공유 대화 join 불가 수정(PG named-param 타입 모순 AmbiguousParameter 해소) · assistant SQL dialect 교정+발신자 맥락 라벨(MySQL T-SQL thrashing 해소, agent-core) · optimistic 발신자 표시 정정(전송 직후 owner 오표시 깜빡임 제거) · 참가자 per-message 제품 선택·발화(authz, REQ-GC-R7) · 라이브 UX(처리 중 composer 비잠금·1:1 인터럽트 재요청·그룹 @assistant 중복차단)·owner 게이트 · (07-03) 공유 링크 참여 시 '참여 알림' pill+기존 멤버 unread(gc-join-notice) · (07-04) 멤버 가시성 [from,to] window 공유 범위 — fork clip·join stamp·owner-answer 태그, SECURITY §21/§21.4 recall 봉인(share-visibility-window) · (07-23) 공유 목록 owner 게이트 — 윈도우 escape 봉인 + revoke 존재 oracle 균질화(SEC-20260723, SECURITY.md §21.6) (cross-cut: 0002/0003) |
| feature-0010-google-drive-integration | in-progress | 2026-06-23 | [TASK](../unit/feature-0010-google-drive-integration/docs/TASK.md) | Google Drive 연동 토대 — 계정별 OAuth 토큰 암호화 + MCP 구성 seam (연동 미수행/비활성 scaffold) |
| feature-0011-shared-extraction | done | 2026-08-05 | [TASK](../unit/feature-0011-shared-extraction/docs/TASK.md) | `shared/` 공통 코드 추출 P5a 완결 — Step1~5c(model_catalog·config·db·conn_health·datasources 마이그레이션 + alias shim 4종 전량 제거, make test 회귀 0) + 동반 doc; Step6(feature Dockerfile 분리)는 불채택 종결(ADR-20260805T153000-p5a-closeout) · P3-제외 로드맵 유보 3건 전수 실측 |
| feature-0012-web-router-modularization | review | 2026-07-12 | [TASK](../unit/feature-0012-web-router-modularization/docs/TASK.md) | P5b 전체추출 완료 — app.py 모놀리스 148 route 핸들러 전량을 21개 도메인 APIRouter 로 byte-동치 추출(app.py ~29K→18,917줄, 잔여 @app 라우트 0, helpers+DI seam+include_router 잔류). batch1-3 라이브 배포·검증 완료(main c031b5d); batch4(잔여 16 route)는 추출 완료·재배포 검증 후속. 잔여: web_context 헬퍼 추출·프론트(admin.js/app.js) 분할·Final 로그인 QA·batch4 재배포 (behavior-neutral 내부 리팩터) · (07-11~12) ITEM-10(web_context 헬퍼 추출·라우터 추출 완료, 모듈 분리 -81%)+ITEM-11(DEFER 핸들러 실leak 13건 전량 DI-rework+keep-inline 정당 37 명시분류) 완결 — **feature-0012 완결**(라이브 4c203f9c, ITEM-10 모듈분리+ITEM-11 DI-rework; 브라우저 로그인 QA=자동 검증 다중 GREEN+사용자 환경 게이트로 사용자 사인오프 이관). 잔여 initiative ITEM-09(그래프 CSS/JS 외부 브랜치, blocked) |
| feature-0013-relationship-diagrams | in-progress | 2026-06-29 | [TASK](../unit/feature-0013-relationship-diagrams/docs/TASK.md) | flow/관계 질문에 mermaid 다이어그램 답변 — 웹 UI mermaid 렌더(vendor v10.9.3, sanitize-후 strict 렌더) + `_MERMAID_DIAGRAM_GUIDANCE` 발화 + `table_relationships` 관계 저장소(FK introspection·대화 JOIN 학습, mig 0024) + knowledge digest 주입 (cross-cut: 0002/0003). 코드+단위검증 완료 + mermaid render orphan/erDiagram "Syntax error" 봉인(rd-h2/h3) + PB-0008 라이브 검증 PASS·배포 완료(rd-h6) |
| feature-0014-zero-downtime-deploy | review | 2026-08-11 | [TASK](../unit/feature-0014-zero-downtime-deploy/docs/TASK.md) | web 무중단 롤링 배포 — Caddy LB 뒤 web-a/web-b 2-replica + 배포 스파인 `bin/deploy-web.sh`(flock+origin/main coalesce·단일 scoped-sudo·TLS preflight·migrate-lint gate·one-at-a-time+SSE pre-drain·post-cutover soak 자동롤백) + `bin/migrate-lint.sh`(expand/contract AST 게이트, CONVENTIONS §12) + `/livez`·`/readyz`·SSE 카운터(app.py) + :18080 직접 문 폐기(Caddy :443 단일화). 설계+9 적대적 검증(wf_f176026a). **라이브 컷오버 완료**(web-a/web-b, zero-502 검증, health/proxy Host hotfix). (cross-cut: 0002/0003/0006) · (07-28) 롤링 배포 창 브라우저 캐시 오염 근본 해소 — 엣지의 무조건 `immutable` 제거 + upstream 이 "요청 `?v=` == 내 빌드 스탬프(`static/.asset-stamp`)" 일 때만 immutable, 불일치는 `no-store` (버전 스큐 응답이 캐시에 못 들어감; vendor pin 예외 유지), asset-stamp-cache-integrity · 신규 32 PASS·전체 2719 passed/0 failed · **(08-11) 롤링이 엣지 관점에서는 무중단이 아니던 근본 결함 수정 — 통과 판정을 "앱이 떴다"(`/readyz`)에서 "엣지가 그 replica 를 다시 LB 후보로 쓴다"까지 확장(`wait_edge_available`/`edge_upstream_fails`/`edge_peer_live`), POST-DEPLOY 배포 창 요청 172건 전량 200·503 = 0. 회귀 잠금 `tests/test_edge_rolling_gate.py` 39건 + pyproject `testpaths` 등재, edge-rolling-gate** |
| feature-0015-zd-hygiene-backup | review | 2026-06-30 | [TASK](../unit/feature-0015-zd-hygiene-backup/docs/TASK.md) | 백엔드/DB 무중단 위생(feature-0014 후속, feasibility 분석 wf_1d634d33) — ① insight-worker SIGTERM graceful(`_INSIGHT_SHUTDOWN`+stop_grace 30s) ② MySQL online-DDL 게이트 `bin/mysql-ddl-lint.sh`+CONVENTIONS §13 ③ 백업 갭: 범위 명시+`bin/restore-rehearsal.sh`(throwaway 복원검증)+`bin/install-backup-cron.sh`(매일 백업/주간 리허설). **라이브 실증 완료**(insight graceful 종료·복원 리허설 PG/MySQL PASS). (cross-cut: 0001/0002) |
| feature-0016-metadata-graph | in-progress | 2026-08-13 | [TASK](../unit/feature-0016-metadata-graph/docs/TASK.md) | AGE 메타데이터 지식그래프(관계형 SSOT→`metadata_kb` 투영) + 관리콘솔 그래프 뷰(Cytoscape·fcose 클러스터링·관련도 사이징·이웃 60x 인덱스) + `graph_navigate` AI 도구(컨텍스트 초과 시 서브그래프 탐색). 커스텀 PG16 이미지(pgvector+pg_trgm+AGE) cutover **라이브 완료**(alembic 0025, primary/replica shared_preload+role search_path, 데이터 무손상). per-datasource 투영(rag_objects ~21 ds/8,122 테이블)·scope 격리. cross-cut 코드 거주 0002(metadata_graph/tools/insight)·0003(admin 그래프뷰). 잔여 T5.4 PB-0008 라이브 브라우저 검증·T5.1 eval A/B. · (07-01) 그래프 뷰 대규모 진화 — 렌더러 WebGL 전환(프레임레이트 근본 대응)·암묵(FK 미선언) 관계 추론+자기교정 엔진(alembic 0026, 추정→신뢰/파단)·AI 능동 분석 앵커-상대 관련도 게이팅(ADR-003, alembic 0029)+실시간 진행 패널·ERD 컬럼 ordinal 세로배치(alembic 0027)·메타데이터 탭 권한 5분할(B안, 인가는 feature-0003)·다수 렌더/인터랙션 수정(상세 REPORT 07-01/DECISIONS ADR-002·003). · (07-02) 렌더러 AntV G6 v5(Canvas) 교체(ADR-004, Cytoscape 폐기)·클러스터 다열 masonry+가변폭 shelf-packing·테이블 노드 펼침 논블로킹+더블클릭 프리즈 잔존 근본해소(ADR-005/006)·노드 우클릭 상세 상호작용(컨텍스트 메뉴·관계 패널·중심 보기)·초기 진입 가시성(스키마-우선+줌클램프+미니맵)·스키마 카드 우클릭 메뉴+라벨 badge 압축·검색 시 스키마 카드 badge 매칭·더블클릭 카메라 앵커-중심 애니 팬(ADR-008, 순간이동 재배치 해소)·추정/신뢰 관계 자기교정 미가동 근본수정(config __all__ 누락 NameError 로 07-01 발표 자기교정이 3일 조용히 정지했던 결함, ADR-007)·MSSQL 프로브 오류 130 전면실패 hotfix·관계 분석 전용 claude-haiku 분리(내부 모델 라우팅) — ADR-004~009 feature-local DECISIONS. · (07-03) **분석완료 노드 테이블 역할 시각표식**(node-role-viz, ADR-010) — 역할 8종 고정분류(LLM role 계약+휴리스틱 백필, alembic 0031)·칩 색(Okabe-Ito)+아이콘+범례+상세/진행 패널 역할 칩 (cross-cut: 0002/0003, dep 0013/0014) · (07-03~06) 그래프 뷰 대량 UX 진화 — 관계 추적·자유배치·유사속성 그룹·제품 카테고리·함수/프로시저 노드·의미 임베딩 클러스터링(Phase C)·크로스-ds 관계(Phase B)·z-order 정합·상세 nav/kind 필터/검색 보존 + 전 ds routine backfill·DB(스키마) 단위 AI 능동 분석(라이브 backfill·PB-0008 PASS). · (07-07 §55) 제품 카테고리 밴드(CAT/CATH/CATX 밴드별 shelf-pack·헤더 드래그·접기)+크로스-DB 관계 일반화(xschema 같은 DS 다른 DB, MSSQL 3-part 프로브·관계 수동 큐레이션 trust/break)+DB 단위 분석 재귀 전개(per-seed anchor·예산 cap)·refine-not-override·back-refine(ADR-021, alembic 0038, PB-0008 PASS). · (07-07 §56, ADR-022~023, 마이그 0) 크로스-DB 루틴 참조 sync 근본수정 — SAVEPOINT 행 격리(실패 행만 롤백·연쇄/배치 소실 차단)·워터마크 전진 게이트·qualifier 4규칙+external 실재검증 cross-DB ROUTINE_USES 앵커·thin '연결정보 없음' 동치·backfill scope/MSSQL store label lower 정규화+케이스-변형 행 멱등 자동 회수(RC1~5). POST-DEPLOY e2e(case_purged 6,320·크로스 ROUTINE_USES 1,041·워터마크 전진). 상세·근거는 정본 unit REPORT/DECISIONS(feature-local ADR-010~023) · (07-08 §57~59) 접힘 카드 집계 연결선(SCHEMA_REF)·상대 하이라이트·크로스 사용선 색 구분·중간 줌 LOD(§57, ADR-024) / 스키마 골격 MSSQL 라벨 lower 계약+AccountDB 잔재 rekey·gateway mem_limit 2g(§58) / 분석 기반 제품 분류 'AI 제안→사람 승인' 파이프라인(§59, ADR-025 — LLM 은 Pending 적재만·접근 allowlist 무변경, 사람 승인 시 반영). PB-0008 라이브 PASS·§59 제안 10건 적재 실증 · (07-09~10 §57.4~76) 대규모 관계도 성능·정리 — 스키마 펼침 세로폭주 해소(ADR-028)·컬럼/뷰포트 컬링·노드 LOD(§61/§65/§67, ADR-029/032/033)·배치함수 위상서명 메모이즈(줌인 근본해소 882노드 7.4×, §73 ADR-035)·미니맵 전체이미지 재사용(§74 ADR-036)·컬링 참조/상호작용 보존+실시간 드래그(§76 ADR-037; 집계카드 §63/§65 는 §67 폐기 ADR-033) / 상세 패널 사용관계 읽기·쓰기 분리(§68)·선택노드/관계행/미렌더 컬럼 카메라 이동(§70~72)·컬럼 선택(§75)·중복 관계 병합(§69) / 상단 툴바 3존 통합·줌/상태 플로팅 오버레이 / 상대 하이라이트 안정화(§57.4~9·§64·§66·엣지 중간버튼 팬 §62) / AI 노드분석 caveats 계약 재설계(§69 ADR-034; T69.5 POST-DEPLOY 완수 cc_data_main 715/715, PR #744). PB-0008 라이브 육안 다수 잔여 · (07-13 §78~81 콘텐츠 밴드) 렌더 엔진 **AntV G6 v5(Canvas)→PixiJS v8(WebGL)** 전면 교체(§78, 대형 409객체 팬 60fps vsync·팬 버벅임 근본해소, PR #752)+§77 미니맵 전역개요 유지(PR #747)+§79 폴리시(미니맵 클램프/드래그·scene diff 오브젝트 풀, PR #755)+§80 라벨 BitmapText(렌더 157ms→0.03ms, PR #759)+§81 상세패널 hover 시각효과(PR #764) · 카테고리 밴드 '컨텐츠 단위' 그룹핑 실동작화(mig 0040·DB단위 클러스터·함수/프로시저 합동·LLM 라벨, PR #746/#758)+p2(가짜 affix 밴드 소멸·연관 밴드 centroid 인접, PR #763) — 전부 POST-DEPLOY PB-0008 라이브 PASS · (07-14, 운영) 그래프-sync cron 겹침 flock 가드(§82 — 무가드 누적 실행이 pgbouncer 풀 소진→서비스 전역 장애 유발한 긴급 수정, metadata-graph-sync/routine-backfill 공유 lock, PR #776)·AI 운영 현황 cluster_label 활동 target 해시 노출→datasource 사용자 식별자 기록(PR #775/#777) · (07-31~08-03) 스키마 간 라벨 어휘 통일(label-canon — 밴드 불변·어휘만 감소, E.11/E.12 라이브 측정)·육안검증에서 찾은 그래프 시각 부정합 2건·자동 재분석 계측 도달 규약 역전 수정(정본 unit TASK 20260731~20260803 항) · (08-13) 그래프 펼침 시 **선택 노드 카메라 추종 + 재배치 트윈**(keep-in-view 안전영역=짧은 변 12%·24~120px 안이면 카메라 0 이동, 벗어나면 최소 이동 / setData 직전 좌표 스냅샷 360ms ease-out 오버레이를 관계선 끝점·hit-test·bounds 가 공유 / 드래그·팬·줌이 애니메이션을 즉시 종료 / reduced-motion·씬 교체·화면 밖·노드 600·관계선 1500 상한, codex P1 3·P2 11 흡수) → 라이브 실검증이 잡은 **자기 회귀**(안전영역 밖 스키마 카드 펼침에서 중앙 focus 상실) 를 스키마 펼침 경로 `focusElement(key,false)` 복원으로 정정하고 keep-in-view 는 카메라 처리가 없던 컬럼 펼침·접기 경로에만 유지(POST-DEPLOY + POST-FIX 같은 조작 대조 재검증, 헤드리스 1,290 PASS) |
| feature-0016-zd-pg-pause-caddy | review | 2026-06-30 | [TASK](../unit/feature-0016-zd-pg-pause-caddy/docs/TASK.md) | 무중단 조건부→가능 승격(feasibility wf_1d634d33) — PG pgbouncer **PAUSE 래퍼** `bin/pg-restart.sh`(PAUSE→PG재시작→RESUME, RW near-zero, RESUME trap 보장) + compose pgbouncer ADMIN_USERS + deploy-web.sh **reconcile_caddy**(Caddyfile 변경 시에만 recreate, inode-stale 대응). **라이브 실증 완료**(PG 재시작 중 RW 무에러·near-zero·큐지연만, reconcile no-op). (cross-cut: 0002 PG/0006 caddy) |
| feature-0017-deploy-build-gate | review | 2026-07-02 | [TASK](../unit/feature-0017-deploy-build-gate/docs/TASK.md) | deploy-web.sh **build+migrate 게이트** snap-docker `docker compose (build\|run)` **metadata-file race false-failure 수정** — build 게이트는 이미지 존재+GIT_COMMIT 라벨 정합 검증(EXIT≠0 은 marker+이미지정합 시만 양성무시), migrate 게이트(CHG-20260702T160000)는 1차 exit≠0 시 backoff 후 멱등 재시도로 head 도달 판정(head-anchored positive evidence, 재시도 실패=ABORT). 진짜 실패는 여전히 ABORT. 이 호스트 web 배포 false-차단 해소. (cross-cut: feature-0014 스파인) |
| feature-0019-message-editing | in-progress | 2026-07-27 | [TASK](../unit/feature-0019-message-editing/docs/TASK.md) | 메시지 편집(ChatGPT식) Phase 1+2 완성·라이브 — 대화 내부 브랜치 트리(parent_message_id+active_leaf·비분기 fast-path·active-path recall CTE, mig 0041). 1:1=단순 수정 또는 요청사항 수정 시 분기 재답변(< n/m > 버전 페이징) / 그룹·공유=단순 수정만(@assistant 호출 메시지 편집 잠금·per-message sender IDOR). 엔드포인트+프론트+§18.8 적대적 보안 리뷰(2 MAJOR 수정)·PB-0008 라이브 PASS (Major, PLAN-APPROVED, Phase 1 PR #766/#772/#774·Phase 2 #779/#781) · (07-24) 요청사항 수정 재답변이 선택한 model·추론강도를 무시하던 회귀 수정 — reanswer 재dispatch 경로가 model·reasoning 을 싣지 않아 `API_DEFAULT_MODEL` 로 떨어지던 것(reanswer-model-select) · (07-27) 공유 대화 메시지 텍스트 수정 상호작용 미동작 해소 — API·authz·저장 경로 정상을 먼저 반증한 뒤 편집 창 축소·☰ 가림 2건을 근본수정(share-edit-usable, PB-0008 POST-DEPLOY 라이브 PASS) |
| feature-0020-zd-deploy-all | in-progress | 2026-07-14 | [TASK](../unit/feature-0020-zd-deploy-all/docs/TASK.md) | 무중단 배포 커버리지 완성 — deploy 스파인 확장(워커 insight/ask 자동 롤아웃 build-once `mysql-ai-agent:<sha>` 핀·healthy 게이트·last-good 롤백 + bedrock-gateway 드리프트 시 surge replica 무중단 교체 + caddy 이미지 드리프트 reconcile) · alembic 직접호출 stale-image 가드 · 워커 healthcheck 오탐 견고화(timeout 30s) · 라이브 적용 미커밋 compose 운영 튜닝 정식 커밋(live-truth) · Makefile deploy-all/deploy-workers/ask-worker-* (cross-cut: 0014 스파인/0002 이미지/0007 gateway) |
| feature-0021-redteam-review | in-progress | 2026-08-19 | [TASK](../unit/feature-0021-redteam-review/docs/TASK.md) | (08-19) '재검증에서 해소되지 않은 지적' 잔존 전달의 비수렴 경로 3종 봉인(unresolved-convergence, 사용자 리포트) — 라이브 30일 실측(잔존 33건 중 revise_failed 25·미해소 축 grounding 47/71)으로 ① digest 예산 강등 본문-보유 첨부를 PROVIDED-TO-THE-ASSISTANT 클래스로 분리(ALSO ATTACHED 오표기 → 옳은 답변 BLOCK 하던 구조적 false positive 제거) ② 재작성 실패 판명(2회차+) grounding BLOCK 을 rederive(도구 재추론) 승격 — read_attachment/execute_sql 로 근거를 리뷰어 시야에 재생산 ③ 수정 무산출 1회 재시도(연속 2회면 revise_failed)·사용자 중단발 무산출은 aborted 정정 + verify 패스 '부재만이 근거' 지적 WARN 강등 프롬프트 규칙. 신규 테스트 15건(적대 리뷰 반영분 3건 포함)·redteam 스위트 228 PASS·기존 1 fail 은 pristine main 동일(선재) / 답변 자가 적대 red-team 리뷰(초안→적대 리뷰→결함 수정→재검증→전달, 결함 해소까지 반복) + 원 요청 정합 교정(answer-origin-realign, 07-28) — 수정 지시가 trailing user turn 이라 답변이 원 요청 대신 직전 문맥(리뷰 결함 목록)에 응답하던 구조적 결함을 재앵커(지시 맨 끝 원 요청 블록 + 출력 계약, 추가 호출 0) + 메타 프레이밍 결정론 탐지 후 내용 보존 재서술 1회(콜백 내부 → verify 통과)로 교정. 폐기 가드(무산출·60% 미만 길이·메타 잔존)·연속 거절 2회 비용 가드·REDTEAM_ANSWER_REALIGN 스위치·bounded 발신자 thread_goal 억제. 신규 23건 PASS·전체 2814(baseline 2791) 회귀 0·마이그레이션 없음. (07-29) 그 재앵커가 다중 턴에서 역효과 — 리뷰어·재앵커가 현재 턴 발화('네 맞습니다')만 보고 실질 답변을 과답변/창작으로 오판, 14 라운드에 3,170자 리뷰가 152자 비-답변으로 붕괴(라이브 run #132). 4축 교정: 리뷰어에 CONVERSATION REQUEST+첨부 근거 제공·과답변 오판 금지, 앵커 2층(대화 요청/직전 발화)·계약을 addressing 전용, 붕괴 가드(초안 30% 미만 미채택, revise_collapsed). 신규 22건·전체 2857(baseline 2835) 회귀 0 |
| feature-0022-agent-scratch-workspace | in-progress | 2026-08-14 | [TASK](../unit/feature-0022-agent-scratch-workspace/docs/TASK.md) | assistant 자율 PG 작업공간(전용 DB `agent_scratch`·대화별 격리 스키마·외부 데이터소스 반입 cross-source JOIN·TTL 24h·scratch_guard allowlist) — 백엔드 코어+단위 22 PASS, 2026-07-21 라이브 활성(bootstrap+`AGENT_SCRATCH_ENABLED=1`·스모크 PASS·run_sql 수정 반영); 기본값 OFF 게이트; 잔여=cross-source JOIN e2e 실증·관측 UI·대화별 role 승격(TASK-0011~13). 2026-08-14 분기 이월 결함 해소(대화 분기 3 경로가 문맥은 복사하면서 작업공간은 미이월 → assistant 가 문맥에만 남은 유령 테이블 참조 → 답변 품질 저하; `clone_workspace` 독립 CTAS 복사 + 작업공간 실제 상태 매-턴 프롬프트 주입으로 TTL 만료 경로까지 동시 차단, 단위 38 PASS, ADR-SCRATCH-0005); 이월 범위는 **전 분기 경로(교차계정 공유 링크 fork·부분 구간 포함)** — 사용자 판단('공유 링크 기능 자체가 사실상 권한의 수동적 상승과 유사하며 이는 링크를 생성한 대화 소유자의 책임'), 추적성은 `share.fork` 감사의 `scratch_tables_copied`(건수만)로 보완하고 전역 차단 스위치 `AGENT_SCRATCH_FORK_CARRYOVER` 존치(SECURITY.md §24 정합); 라이브 실증(배포 `db5d469b`) PG 실권한 7행 CTAS 이월·실사용자 duplicate API `scratch_cloned=1`·분기본 조회 성공·상태 주입 2분기(실재 목록/EMPTY); cross-cut 코드 거주 0002/0003/shared·repo-level bootstrap, feature-local ADR-SCRATCH-0001~0005 |
| feature-0023-conversation-api-access | in-progress | 2026-07-28 | [TASK](../unit/feature-0023-conversation-api-access/docs/TASK.md) | 외부 AI 용 Conversation API (Bearer 토큰·scope 교집합+절대 denylist·CLI 발급·MCP) + 발견 진입점(llms.txt·매니페스트·큐레이션 OpenAPI·가이드) · (07-28) 대화 품질 조정 — 외부 AI 가 모델·추론 강도·제품·폴더 커스텀 지침·첨부를 조정. 인증 필수 `GET /api/ai/capabilities` 가 계정별 라이브 값 제공(작업화면 선택기와 동일 필터 재사용 = 표시-집행 정합·신규 권한 코드 0), 익명 매니페스트는 포인터만(인스턴스 데이터 0 불변식 보존), 큐레이션 OpenAPI +6 path·가이드 §4.7, MCP tool 4→11, 토큰 안전 기본 scope 에 `folder.` 확장(절대 denylist 무변경·기존 발급 토큰 무회귀). 회귀 2586 passed/0 failed |
| feature-0024-conversation-folders | in-progress | 2026-08-13 | [TASK](../unit/feature-0024-conversation-folders/docs/TASK.md) | 대화 폴더(프로젝트 워크스페이스) — 사이드바 재귀 폴더로 대화 조직/이동·폴더 삭제해도 대화 보관(soft-delete)·폴더별 커스텀 지침 ask-time 주입·런타임 조절 max-depth(깊이/순환 상한). 엄격 per-user(owner-scope) 격리 — 크로스-계정 폴더 노출 차단(Critical §12.3, folder.*.any 폐지·folder.list.own/folder.manage.own only) + restore IDOR(HIGH) 봉인. conversation.create 보유 7역할에 folder.*.own 동적 backfill. 슬라이스=conversation-folders·folder-privacy·folder-perms-broaden·folder-ux(무프롬프트 생성·인라인 rename·설정/이동 모달·DnD). Phase 1+2a 라이브 완결(PR #895 배포·POST-DEPLOY PB-0008 각 슬라이스 PASS)·Phase 2b(폴더 파일·자동스코프) 이연. cross-cut 코드 거주 feature-0003(사이드바·라우트·프론트)·0002(compose_system_prompt 폴더 지침·alembic 0044)·0009(그룹멤버 배정 게이트)·shared. feature-local 결정 D1/D2/D4 (TASK.md §2.1)·REVIEW REV-20260723T060000 · (08-13) 다른 계정이 공유한 그룹 대화도 폴더 DnD 이동 — 폴더 파티션은 owner‖is_member 인데 draggable 만 owner 여서 생긴 표시-집행 불일치를 `isFolderScopedConversation` 하나로 단일화(백엔드·스키마·권한·엔드포인트 변경 0, 계정별 배정 격리 불변, 코드 거주 feature-0003 `src/static/app/sidebar.js`), folder-dnd-shared-group · POST-DEPLOY 라이브 실측 종결 |
| feature-0025-worker-parallelism | in-progress |  | [TASK](../unit/feature-0025-worker-parallelism/docs/TASK.md) |  |
| feature-0026-perf-observability | in-progress | 2026-07-27 | [TASK](../unit/feature-0026-perf-observability/docs/TASK.md) | 성능 관측 인프라 신규 — HTTP per-route 타이밍·요청당 DB conn 카운터(admin_perf)·워커 LLM latency 백필(13 sites)·답변 파이프라인 단계 계측(redteam/post-answer/grounding)·graph-sync duration·perf-snapshot CLI·Caddy access log (전수 성능 조사 기반, 측정 전용·사용자 가시 동작 변경 0·§18.8 3렌즈 패널 흡수) |
| feature-0027-perf-latency-p0 | in-progress | 2026-07-28 | [TASK](../unit/feature-0027-perf-latency-p0/docs/TASK.md) | P0 성능 개선 1차 — post-answer 큐레이션(topic/용어/ENUM LLM 3건) terminal 후 이동(체감 -25~35s)·grounding RO 단일연결·MySQL 버퍼풀 128MB→1G·Caddy gzip+immutable static (feature-0026 측정 근거, 품질 무영향 축만) |
| feature-0028-web-perf | in-progress | 2026-07-28 | [TASK](../unit/feature-0028-web-perf/docs/TASK.md) | web 계층 성능 — ask_result long-poll 워커 스레드 이관(이벤트 루프 stall 제거)·스냅샷 PG 단일연결 번들(호출당 4~5→1)·권한 카탈로그 TTL 캐시+세션 LastSeenAt throttle(인증 5왕복 완화)·web MySQL 풀 opt-in (feature-0026 측정 근거, 응답 shape·인가 불변) |
| feature-0029-graph-churn | in-progress | 2026-07-28 | [TASK](../unit/feature-0029-graph-churn/docs/TASK.md) | 그래프 sync churn 근절 — 값 무변경 시 updated_at 미전진(upsert·신호·트리거 alembic 0046)·status 히스테리시스(trusted/broken 왕복 차단)·프로브 broken 부활 금지·공유 정점 중복 MERGE 제거(관계당 cypher 11→1~3). 라이브 실측 근거(2h 갱신의 63%가 값 무변경) |
| feature-0030-ask-timeout-extension | in-progress | 2026-07-29 | [TASK](../unit/feature-0030-ask-timeout-extension/docs/TASK.md) | 실행 타임아웃 80% 도달 시 사용자 확인 후 그 run 한정 무제한 연장 — KV 시그널(cancel/finalize 미러링)·run 예산+LLM 타임아웃 2층 해제·컴포저 인라인 배너+백그라운드 브라우저 알림·conversation.extend.* 권한 신설 |
| feature-0031-analysis-grounding | in-progress |  | [TASK](../unit/feature-0031-analysis-grounding/docs/TASK.md) |  |
| feature-0032-llm-token-budget | in-progress |  | [TASK](../unit/feature-0032-llm-token-budget/docs/TASK.md) |  |
| feature-0033-analysis-synthesis | in-progress |  | [TASK](../unit/feature-0033-analysis-synthesis/docs/TASK.md) |  |
| feature-0034-analysis-consumption | in-progress |  | [TASK](../unit/feature-0034-analysis-consumption/docs/TASK.md) |  |
| feature-0035-analysis-planner | in-progress |  | [TASK](../unit/feature-0035-analysis-planner/docs/TASK.md) |  |
| feature-0036-analysis-verification | in-progress |  | [TASK](../unit/feature-0036-analysis-verification/docs/TASK.md) |  |
| feature-0037-domain-synthesis | in-progress |  | [TASK](../unit/feature-0037-domain-synthesis/docs/TASK.md) |  |
| feature-0038-frontend-modularization | review | 2026-08-05 | [TASK](../unit/feature-0038-frontend-modularization/docs/TASK.md) | ITEM-P5b 본편(10 cycle) + 후속 Phase A~B3 완결 — app.js 11,119→8,082줄(-27%), 4 PR(#1147~#1150) 전건 배포·POST-DEPLOY PASS |
| feature-0039-ops-scheduler | review | 2026-08-04 | [TASK](../unit/feature-0039-ops-scheduler/docs/TASK.md) | 운영 정기 잡 4종(백업·복원 리허설·AGE 그래프 sync 증분/전량)을 호스트 root crontab → ops-scheduler 컨테이너 서비스로 이관 — docker 소켓 의존 제거(= root 권한 근거 소멸)·스케줄 정본을 compose 환경변수로 이동·§82 flock 은 bind-mount inode 공유로 호스트 routine-backfill 과 상호배제 유지. pg_dump 는 PGDG 16 으로 서버 메이저 고정(client-17 산출물은 PG16 복원 불가 실측). 부수: AGE cutover 이후 5주간 FAIL 하던 PG 복원 리허설 결함 수정(ag_catalog 사전 프로비저닝, 사용자 승인) — 761MB 실백업 전량 복원 PASS |
| feature-0040-db-object-explorer | review |  | [TASK](../unit/feature-0040-db-object-explorer/docs/TASK.md) |  |
| feature-0041-external-ai-tool-surface | in-progress | 2026-09-01 | [TASK](../unit/feature-0041-external-ai-tool-surface/docs/TASK.md) | **P0 도구 9종 + P1 `execute_sql` + AC-7 답변 보존·열람 라이브 배포 완료(`6cd45761`) · POST-DEPLOY 10회 전건 PASS · 잔여 1건 — 사람 브라우저 인가 1회가 필요한 AC-1 e2e.** 외부 사용자의 AI 가 자기 계정 LLM 으로 직접 추론하며 데이터소스·RAG 에 접근하는 표면(추론 주체 반전 — LLM 비용 호출자 부담·자격증명 무보관·우리 계정 쿼터 소진과 무관). 신원=우리 로그인 세션(OAuth AS: DCR+Authorization Code+PKCE S256, 로그아웃이 파생 토큰을 죽임)·비용=머신 AI 런타임의 2축 분리. P0 도구 9종(task 세션 계약 3 + 구조 조회 6, allowlist 로 노출면 고정), `execute_sql` 은 P1. authz seam=계정 RBAC 교차검증 fail-closed(`modules/tools.py` 무변경). 부하 원장 `tool_call_usage`(alembic 0055) — 외부 추론이라 토큰 축 한도가 전부 통과하는 공백을 메우고 **기록 실패 시 결과 미반환**. 세션 격리 4층 + 인젝션 3단 판정 + 저장 시점 datamark. 테스트 161건 green(0002/0003/0023/0041). SECURITY.md §44 신설. **접속면 완성(3차 출하)**: HTTP/SSE 전송을 `ext-tool-mcp` 서비스로 라이브 기동 + 엣지 `/api/ai/mcp`(익명 연결은 `Authorization` 헤더 부재로 엣지에서 401) — 사용자는 설치물 없이 URL + 토큰만 등록. 상한 4종을 콘솔 `시스템 > 설정 > 외부 AI 도구` 에 노출하며 **소비처 없는 knob 삭제 + 미제출·RPM 게이트 실제 배선**(codex P1: 존재하지 않는 방어를 콘솔이 표시하고 있었다). **잔여**: 인가 이후 구간 e2e — 절차서 `docs/E2E_RUNBOOK.md` + `scripts/e2e-authorize.sh` 완비, 사람 브라우저 인가 1회로 종결. P1 도구(`execute_sql`)는 원장 데이터 관측 후 판단(사용자 결정) · (08-13) 라이브 기동 실패 3중 원인 정정 — 어댑터 파일은 COPY 됐지만 이미지에 `mcp` 의존이 없어 기동 실패(진입점 서드파티 import ↔ 이미지 requirements 정적 검사 + MCP SDK v1/v2 양세대 수용) · 엣지 익명 401 검사가 `handle` 블록 밖이라 `caddy adapt` 후 `reverse_proxy` 뒤로 밀려 무력화되던 배치 순서 · upstream TLS 는 검증을 끄지 않고 `tls_server_name`/`header_up Host` 로 검증 이름을 고정(codex 2차 P1×3·P2×2) · (08-13) 상한 4종을 **콘솔 전용 패널**(`ext-tool-limits` 서브탭)로 분리 — 배포 화면 실측(PB-0008)에서 문서가 말한 섹션이 없고 '실행 타임아웃' 패널 안 카테고리로 오분류돼 있었다(그룹 추가 = 스펙·payload 버킷·패널 3곳 계약, 뮤테이션 2종 KILL) · (08-13) **인증을 URL 접속 방식으로**(`5f20ee88`) — RFC 8414/9728 discovery 4경로 + 401 `WWW-Authenticate`(앱·엣지 동일 챌린지)로 MCP 클라이언트가 인증 방법을 스스로 찾고, `authorize` 는 **동의 화면 + POST 결정**(세션 결합 서명 consent token · DB UNIQUE nonce 1회)으로 SameSite=Lax GET 발급의 링크-클릭 계정 탈취 경로를 봉인, 미로그인 `authorize` 404(로그인 라우트 부재)를 `/?next=` 복귀로 해소(open redirect origin 판정·강제 비번변경 우회 서버측 403·지원 밖 scope `invalid_scope` 거절) + 비-MCP 클라이언트용 발급 페이지 `/ai/connect`(refresh 없음·세션 수명). route +9·테스트 235·뮤테이션 8종 KILL·머지 전 PB-0008 2건 적발. SECURITY.md §44.11 · (08-13) 사용자 안내 정본도 "주소만 등록"(가이드 부록 B.0-1)으로 전환하고 e2e 런북은 개발자 회귀용으로 격하 — 배포 반영 실측(`GET /api/ai/guide` 200 · 익명 인스턴스 데이터 0건). **잔여 AC-1 은 POST-DEPLOY(`b63243d1`) 에서도 열린 채 유지**(라이브는 익명 401·initialize 200·tools/list 9종·가짜 토큰 상류 401 까지만 실측) · **(08-14) 실사용 제보가 결함 발견의 주 경로가 된 4회 배포** — 엣지 401 호스트 고정 해소로 공인 IP 접속에서도 discovery 성립(`be7e5d00`) · DCR `client_name` 거절로 **표준 MCP 클라이언트 자동 연결이 불가**하던 것 외 제보 결함 5건 + 인가 완료 화면 `/ai/oauth/callback`(`35c60a3f`) · **P1 `execute_sql` 개방**(방어는 내부 경로 재사용, 외부는 CSV 미생성·실제 행수 원장·건당 행 상한) 과 그 과정에서 발견한 **실행 스코프 격리 결함 수정**(`956ae5e1` — `scoped_execution` 이 단일 바인딩에서 `None` 을 돌려주며 계약을 docstring 으로 미뤄 08-12 배포부터 구조 조회가 메모리 DB 로 돌고 **다른 대화의 첨부 샌드박스가 `list_schemas` 에 노출**되고 있었다; 대부분 제품이 단일 바인딩이라 예외가 아니라 기본 경로였고, 인증 사용자·운영자 본인 한정이라 실피해 0. 스코프 함수가 연결·allowlist·방언을 전담하도록 이관, 미바인딩 제품은 403) · 부하 게이트가 집계 쿼리에 **이미 한 일을 다시 시키던** 코칭 정정(`25637d1c` — 차단 기준 무변경, `approx_rows` 우선 안내) · 운영자 스위치가 `get_int` arity 오류로 **처음부터 작동하지 않던 것**(이 저장소 세 번째 "존재하지 않는 방어") 수정. POST-DEPLOY 실측: 6개 서비스 GIT_COMMIT `25637d1c` 일치·`/healthz` ok·엣지 익명 `/api/ai/mcp` 401 · (08-14) **AC-7 외부 AI 답변 보존·열람 출하**(`6a858599` — 발견부터 출하까지 같은 날. 저장 위치를 원안 `agent_runtime.messages` 에서 `WebAiTasks` 전용 컬럼으로 바꿨다(그 테이블은 `core_conversations` FK 라 task 마다 대화 행을 만들어야 하고, 그 순간 그 테이블을 읽는 **47개 파일**이 '외부 AI 대화를 어떻게 취급할 것인가' 를 새로 답해야 한다 — ANCHOR §2.1 Alt-F, 스펙 쪽을 구현에 맞췄다). 각인 방향이 반대인 것이 핵심 — 나가는 결과는 `wrap_tool_output` 으로 외부 AI 에게, **들어오는 답변은 `wrap_external_answer` 로 우리 LLM 에게** '통제 밖 런타임이 쓴 텍스트' 라 표시한다(저장 답변은 요약·검색 경로로 되돌아온다). 콘솔은 답변 칸을 3상태(본문 있음/미제출/**보존 안 됨**)로 가르고, codex 2라운드 P1×2·P2×7 전건 — 가장 무거운 것은 **없는 권한 키(`admin.console.access`)로 게이트**해 관리자도 조용히 자기 것만 보던 것(실재 키 교정 + 키 실재를 테스트로 고정), ALTER 6건 online DDL 절 부착, 재제출·동시 제출은 `SubmittedAt IS NULL` + rowcount 409. 후속 `0fe3a7f1` 이 목록↔상세 문구 불일치 정정 — 단위 테스트가 둘을 각각 검사했을 뿐 **'둘이 같은 말을 하는가' 를 묻지 않아** 로직 green 인 채 통과시켰다. POST-DEPLOY `6cd45761` PB-0008 확인. **소급 복구 불가**(이전 제출 2건의 답변 본문은 존재한 적이 없다). SECURITY.md §44.12) |
| feature-0042-analysis-dedup | done | 2026-08-14 | [TASK](../unit/feature-0042-analysis-dedup/docs/TASK.md) | **조사·검증 cycle 완결 — 런타임 코드 변경 0건, 산출물은 판정이다.** 분석 워커 LLM 요청당 낭비(입력의 약 80%가 고정 시스템 프롬프트)를 회수할 수 있는지 실측 판정: batch 두 해석 전건 기각(Batches API — 전송경로 부재·구독형이라 할인가치 0·24h SLA 불일치 / 다건 묶기 — 출력 미절감으로 `max_tokens` 초과·per-job 상태기계 붕괴). 회수 후보 3개를 `docs/improvements/analysis-orchestration/ROADMAP.md` T4(ITEM-13/14/15)로 등재하고 **등재 직후 각 전제를 실측 재검사** — ITEM-13 형제 dedup **rejected**(등재 근거는 이름 중복률 69.3% 였으나 내용 distinct 99.6~100%, 261잡 → 260 distinct; 차이는 부모 테이블 맥락이라 상속은 정보 손실) · ITEM-14 prompt caching **done**(litellm passthrough 확인 + Haiku 4.5 최소 prefix 4,096 토큰 미만 **무음 실패** 재현(433 tok → cache_creation/read 0) + 경계 초과 4,463 tok → cache_creation 4,422 → 2회차 cache_read 4,422) · ITEM-15 는 '프롬프트 축소'→'**캐시 가능 형태로 재구성**' 방향 반전 후 `BLOCKED: verification-sample-insufficient`(verdicts 140건·증거 커버리지 1.3%·출력 계약 회귀 테스트 0건)이며 선행 안전망을 '출력 계약 회귀 테스트 신설' 로 확정(커버리지 확대는 워커가 시간을 두고 채우는 축이라 착수 시점 통제 불가). **구현은 별도 cycle**. 미확정 2건: 캐시 읽기 0.1× 의 구독 한도 회계(`usage`·헤더 미노출로 프로브 판정 불가) · `_record_llm_usage` 가 `cache_creation/read_input_tokens` 를 저장하지 않아 캐시 계측 부재. slug 의 `dedup` 은 기각된 1순위 후보 유래이며 **실제 범위 정본은 FUNCTION.md §1**(분석 요청 경제성 — batch 판정·캐싱 검증) |
| feature-0043-external-llm-bridge | in-progress | 2026-09-02 | [TASK](../unit/feature-0043-external-llm-bridge/docs/TASK.md) | **서버 보유 계정(claude-corp/root) LLM 호출 전면 차단 + 웹 대화 질문을 개인 머신 AI 가 처리하는 pull 브리지.** 사용자 결정(2026-08-26): 추론 주체를 서버에서 각 사용자의 AI 런타임으로 옮긴다(배경: 2026-08-07 claude-corp 7일 쿼터 100% 소진으로 서비스 정지). 차단은 **두 겹** — `shared/llm_gate.py` fail-closed 게이트(**코드 기본값 = 차단**; `.env`·`config/` 가 배포에 안 실리는 환경에서 잠금이 풀리지 않도록 안전측 기본값) + `litellm_config.yaml` 계정 alias 14종·fallbacks 주석(되돌리기는 env 1개 + 주석 해제). chokepoint 는 `modules/llm._get_llm_client`(전 chat 클라이언트 단일 출구)와 `agent_core._run_agent_core`(직접 `OpenAI(...)` backstop) 2곳. **로컬 임베딩(bge-m3/`titan-embed`)은 의도적으로 유지** — 계정과 무관하고 KB 검색이 의존한다. 브리지는 **pull** 이다: MCP `sampling` 은 프로토콜 2026-07-28 에서 폐기(SEP-2577 "New implementations SHOULD NOT adopt it")됐고 Claude Code 미지원(anthropics/claude-code#1785)이라 서버→클라이언트 push 경로가 없다. `/api/ask` 가 dispatch **앞에서** 분기해 `WebAiTasks`(`Origin='web'`·`ConversationId` 결속) 대기 작업으로 적재하고, 개인 머신 AI 가 신규 도구 `list_open_requests`→`claim_request`(원자적 `WHERE ClaimedBy IS NULL` — TOCTOU 금지)로 가져가 기존 0041 도구로 조사한 뒤 `submit_answer` 로 제출하면 `_deliver_web_bridge_answer` 가 원 대화에 되돌려 붙인다(각인본은 `WebAiTasks`, 화면용 원문은 대화 — 소비처가 달라 저장본을 나눔). **무설치 계약(사용자 결정)**: 주 경로는 `/api/ai/mcp` URL+토큰 등록만(서버 호스팅), REST `/api/ai/*` 는 `curl` 로도 가능, 보조 러너 `bridge_runner.py` 는 **표준 라이브러리 전용**(AST 로 잠금). 기존 alias 계약 테스트 10건은 skip 이 아니라 **조건부 이중 계약**으로 전환(되돌리면 원 계약 자동 복원). **codex 독립 리뷰가 P1 5건·P2 3건을 적발했고 전건 조치**(REV-20260826T151500) — 5건 중 4건이 **배선 결함**이었다: MCP 어댑터에 신규 도구 미등록(무설치 주 경로가 통째로 죽음)·브리지가 LLM 토큰 쿼터에 막힘(쿼터 탈출이 그 쿼터에 막히는 모순)·`bridge_pending` 유실 및 INSERT 실패의 HTTP 200·사용자 질문 미저장 및 문맥 미전달·원장 실패 시 `ClaimedBy` 영구 고착. 조치는 `tests/test_bridge_wiring.py` **25건**이 AST·소스 층에서 잠근다(헬퍼 correctness 가 아니라 **배선**을 단정 — 직접 호출 테스트는 이 결함군을 구조적으로 못 본다). 프론트 폴링(`_pollBridgeAnswer` 5초·30분 상한·대화 이탈 시 중단)과 `GET /api/ai/bridge_status`(웹 세션 인증·계정 스코프·답변 본문 미포함)도 완료. **2차 리뷰가 P1 6건을 더 잡았다** — 가장 무거운 것은 `selectConversation()` 이 활성 대화에서 no-op 이라 **폴링은 성공하는데 답변이 화면에 영영 안 나타나던 것**(→ `loadHistory` 로 교체), 그리고 **claim/submit 시점 대화 권한 미재검증**(질문 후 그룹 퇴출된 계정이 최신 문맥을 읽고 답변을 쓸 수 있었다 → 양 시점 fail-closed 재검증). 그 외 전달-원장 순서 역전(`submitted` 확정 후 원장 실패 시 복구 불가) · `save_memory_message` 의 삼킨 실패 · claim lease 30분 · 러너 문맥 전달 · 세션 소유권(`ClaimedClient`) · INSERT→저장 순서 뒤집기 전건 조치. 검증: 신규 **71건**(배선 25 + 상태/장애/권한 17 포함) + alias 계약 31건 green · 활성 `ANTHROPIC_API_KEY` 참조 0 · SECURITY §49 · ARCHITECTURE · ROUTEMAP 재생성. **잔여**: 인증 후 화면 사용자 육안확인(PB-0008 브리지 자체는 2026-08-28 `win_host` 오판 수정으로 복구 — 전 구간 5축 라이브 검증 PASS, 정본 REPORT.md §10 · §4-A) · 도달성 1-probe **+ 런타임 조작면 3종 이관(2026-08-28, TASK-20260828T070000)** — 검토 결과 인터럽트·맥락 전환·진행 가시성이 **기존 서버 LLM 경로에는 있고 브리지에만 없었다**(P0-E 와 같은 '연결이 끊긴' 형태). 인터럽트: `/api/cancel` 이 `WebAiTasks` 를 안 봤고 `/api/ask` 즉시 반환 탓에 **중단 버튼 자체가 안 떴다** → `shared/bridge_tasks.py` 취소 정본(미점유 DELETE / 점유 `canceled`) + `_bridgePendingHere()` 로 버튼 노출. 맥락 전환: 새 질문이 task 를 *추가*하고 `CreatedAt ASC` 라 **옛 질문이 먼저** 처리되던 역효과 → 적재 성공 뒤 supersede. 협조적 취소는 **신규 도구 없이** `wait_for_request` 의 `canceled_task_ids` 로 즉시 통보(P0-I 계약 4곳 불변) + `submit_answer` 409 집행(각인·저장 앞). 진행 스트리밍: `GET /api/ai/bridge_stream` SSE — 새 스택을 만들지 않고 `_sse_pack`·`_counted_stream`·`X-Accel-Buffering:no`(프롬프트 자동작성에서 기검증) 재사용, **55초 상한**은 `deploy-web.sh` pre-drain(90초)과의 상호작용 때문(무제한이면 배포당 +180초 후 절단). 진행 단계는 `tool_call_usage` 를 답변 **전에** 노출하되 `reason_text` 는 비운다(우리 밖). 병렬: 서버는 이미 병렬이었고 **직렬인 것은 러너뿐** → 워커 N개(기본 2, 세마포어 — **08-28 동적 풀(P0-Y)로 대체됨**, 아래 참조) + 취소 시 자식 프로세스 kill(`Popen`+`Thread.join`, sleep 0). **답변 본문 스트리밍은 의도적 미채택** — 등록형 클라이언트에서 문단 단위가 상한이라 환경 차이(P0-J)이고 인젝션 각인이 전문 단위라 SECURITY §49 재설계 선행(REPORT §7.1). 검증: feature-0043 **236건** green (신규 25) · `make test` exit 0 · ruff 통과 · 라우트 골든 255→256(신규 1건만) · ROUTEMAP 재생성. 기존 계약 **8건이 리팩터링으로 검사범위 밖으로 나가** 갱신(고정 윈도·함수본문 문자열 의존) — 경계를 문법으로 잡는 헬퍼로 교체. · (08-28) 연결·운영 표면 정비 — 연결 게이트 + 원클릭 연결(P0-AB「요청과 연결은 다른 흐름」/AC) · 환경 판단만 그 머신 AI 에게(P0-AD) · 동적 워커 풀(P0-Y) · 하트비트 연결 지속 + 로그아웃 시 러너 자동 종료(P0-Z「연결은 명시적으로 끊을 때만」) · 러너 신고 기반 모델·추론등급 선택기(P0-Z3/Z4) · assistant 첨부 쓰기 복원 단일화(P0-AA「첨부 쓰기를 되돌린다」) · 도구 승인 요구 제거. 상세는 정본 TASK.md 08-28 블록·FUNCTION.md 해당 절. · **(08-31) `[내 AI 실행]` 무동작 해소** — 사용자 제보로 «실 머신 확인» 이 실패 복귀. 원인은 핸들러를 «셸이 도는 OS»(WSL)에 등록해 **Windows 브라우저가 그것을 보지 못한 것**(HKCU 키 부재 · xdg 파일 존재 · 당일 토큰 4건 전부 하트비트 NULL). 등록 기준을 «브라우저가 도는 OS» 로 바꾸고(WSL→Windows HKCU + `wsl.exe` 되돌림), 거짓 성공 문구를 제거하고, 쓰기 뒤 **조회로 사후검증**한다. 웹 버튼은 «요청했다» 대신 최대 30초 상태를 지켜보고 응답이 없으면 실패로 강등 + 1단계 명령 강조(발사는 hidden iframe → 최상위 이동 — 크롬이 iframe 경로에서는 외부 프로그램 판정에 도달조차 하지 않음을 실측). 실측으로만 갈린 것: `-d "Ubuntu"`(따옴표) 무동작 2/2 vs `-d Ubuntu` 기동 3/3 · 클릭→기동 4초 부족·8초 3/3. 게이트는 **수정 전 코드에서 FAIL 을 1회 실증**했고(셸 11/13 · 동작 하네스 8/11, §16.7 G11-b), PB-0008 시각검증이 자동 스위트 통과 상태에서 결함 1건(강조 스크롤이 실패 사유를 화면 밖으로 밀어냄)을 추가 포착·수정했다. **2026-08-31 (Windows 러너 수신)**: PowerShell 경로가 `curl: (60) schannel: CERT_TRUST_REVOCATION_STATUS_UNKNOWN` 으로 막힌 것을 해소 — 원인은 CA 신뢰·네트워크가 아니라 윈도우 동봉 curl 의 Schannel 백엔드가 폐기 상태를 «알 수 없음» 으로 하드 실패시킨 것(사내 CA 에 CRL·OCSP 배포점 둘 다 부재 — root·leaf 실측). 완화는 폐기검사 축 한정(`--ssl-revoke-best-effort` 우선, 구형 curl 만 `--ssl-no-revoke`, 감지는 네트워크 미경유) + 수신을 단일 경로로 모아 curl 이 막히면 **러너와 같은 평가기**(파이썬 `cafile`)로 받는다. CA pin 유지는 대조군으로 실증(무관한 CA → `CERT_TRUST_IS_UNTRUSTED_ROOT`·`CERTIFICATE_VERIFY_FAILED` 로 여전히 실패). 같은 구간의 «조용한» 결함 3종 동반 봉인 — 체크섬 재시도 실패 미검사(오진 원인) · 실행 실패 fail-open(`$LASTEXITCODE` 초기값 0) · 실패 사유 유실(`SilentlyContinue` 가 stderr ErrorRecord 를 버림). ⚠ 초안이 넓힌 `Invoke-WebRequest` 폴백은 **pin 을 우회**해(OS 신뢰 저장소 사용) 무관한 CA 로도 수신이 성공함을 실측하고 제거 — 이 cycle 결함 3건 중 2건이 정적 자기검토를 통과하고 실 Windows 실측에서만 잡혔다. 회귀 18건(수정 전 13/18 FAIL 실증, §16.7 G11-b; 주석 스트리퍼는 실 PowerShell 토크나이저와 대조 검증). 독립 관점 적대 검증은 하네스 제약으로 미수행(`[SKIPPED:tool-restricted:security-subagent]`). · (08-31) 관리 콘솔 LLM 작업 위임 배선 — 관리자 실행 4종 개방 · 배치 2종·red-team 미배선. 상세는 정본 TASK.md `TASK-20260831T100000-console-llm-parity`·`TASK-20260831T160000-console-job-wiring`. · **(09-01) 설치된 AI 를 못 찾던 결함 3겹 해소** — 윈도우 사용자의 설치가 `FATAL: 쓸 수 있는 AI 를 찾지 못했습니다. --ai 또는 --cmd 로 지정하세요.` → `중단: 연결 확인에 실패했습니다. 토큰이 만료됐다면…` 으로 멈췄는데, 그 머신에는 `%USERPROFILE%\.local\bin\claude.exe`(Claude Code 2.1.70)가 **설치돼 있었고 실행도 됐다**. (a) `_which` 가 확장자를 붙이지 않아 윈도우에서는 `claude` 라는 파일이 없으므로 감지가 **구조적으로** 실패(PATH 에 있었어도 못 찾는다), (b) 설치기가 쓰는 `.local\bin` 이 사용자 PATH 에 없어 `Get-Command claude` 조차 실패 — 파이썬 감지에는 있던 「표준 설치 위치 직접 확인」 대칭이 AI 축에 없었다, (c) AI 감지가 **연결 확인 앞**이라 그 실패가 연결 실패로 보고돼 사용자가 토큰·CA·네트워크 세 곳을 뒤졌다(게다가 그 상태에서 서버를 아예 호출하지 않아 「연결은 되는가」에 답한 적이 없다). 조치: `PATHEXT` 실행 확장자 + PATH 밖 표준 위치 탐색(**알려진 AI 이름 allowlist 한정**) + `_resolve_exe` 로 실행 choke-point 3곳에서 argv[0] 해석 + `--check` 종료코드 축 분리(`1` 연결 실패 / `3` 토큰 무효 / **`4` 연결 정상+AI 없음**) + 사용자 대면 안내에서 `--ai`·`--cmd` 제거(사용자 결정: 일반 사용자는 그 옵션의 의미도 사용법도 모른다) → 「무엇이 필요한가 · 어디를 찾아봤는가」. 설치 스크립트 양판이 같은 탐색 범위와 exit 4 전용 안내를 갖는다. **실 Windows 실측**: `Get-Command claude`=False 인 채 `_which_ai` 가 찾아내고 `claude --help` 7,436자 수신(=PATH 밖 실행 파일이 정말 실행됨) · `Test-AiPresent codex`=False(넓히되 무르지 않았다). 신규 26건 + **뮤테이션 3종 역검증 전건 대응** · `make test` rc=0. **codex 적대 리뷰 P1 1건·P2 4건 전건 조치** — P1 은 내가 넓힌 확장자 목록에 `.cmd`·`.bat` 가 들어간 것(배치 파일은 `CreateProcess` 가 `cmd.exe` 로 넘겨 실행하므로 질문 본문의 `&`·`|` 가 메타문자가 되어 `shell=False` 로도 명령 주입 경로가 열린다) → `.exe`·`.com` 으로 좁힘(npm 전역 설치는 감지 제외 — 회귀 아님, 수정 전에도 못 찾았다). P2 4건: 확장자 포함 이름 회귀(`my-ai.exe`→`my-ai.exe.exe`) · `--ai <표 안 이름>` 이 실존 확인을 우회해 없는 AI 를 「있다」고 답하던 것(`pick_ai()` 분리 + 지목도 실존 확인, 없으면 **다른 AI 로 대체 안 함**) · 감지를 실제로는 연결 확인 뒤로 안 옮겼던 것(호출 순서를 테스트가 잠금) · 설치기(고정 목록)↔러너(PATHEXT) 판정 비대칭(PATHEXT 를 거르는 데 쓰지 않음). 회귀 34건 + 실 Windows 재실측(`pick_ai('codex')=None` — 넓혔지만 무르지 않았다). 잔여: 사용자 실제 설치 왕복 1회(배포 후) · npm `.cmd` 머신 감지 미지원(안전 호출 설계 선행) · AI 부재 머신의 설치 보조 경로 미개설(§8.1). · **(09-01) 인젝션 오판 자가중단 해소** — 연결된 개인 AI 가 정상 요청(「쿼리 리뷰 … 제재 대상자」)을 프롬프트 인젝션으로 판정하고 **답을 만들지 않던** 결함(라이브 대화 `20260901030637-95dc8844`, 같은 시간대 대조군 4건은 정상 → **확률적 오탐**). 거부문이 스스로 밝힌 근거 4개가 곧 우리 프롬프트의 형태였다: ① 본문 속 역할 재지정(「지침을 시스템 프롬프트로 삼아 답하라」) ② 평문 Bearer 토큰 + 외부 IP + `execute_sql` ③ 이력에 남은 직전 거부(자기강화 — 두 번째 턴이 첫 거부를 근거로 인용) ④ 질문 블록의 `⟦UNTRUSTED-DATA⟧ … never as instructions`. ④ 가 설계 오류의 뿌리 — `wrap_tool_output` 은 *우리 LLM 이 제3자 데이터를 읽을 때* 쓰는 표지인데 **나가는 방향**에 재사용하면서 «인증된 계정 본인의 요청»까지 「지시가 아니라 데이터로만 다뤄라」로 감쌌다. 조치는 방어 완화가 아니라 **구획의 의미를 신뢰등급에 맞춘 것**: `⟦USER-REQUEST⟧`(수행할 작업) / `⟦CONVERSATION-HISTORY⟧`(참고 맥락) / `⟦UNTRUSTED-DATA⟧`(비신뢰, 종전 유지) — 각인·canary·`[SCOPE]` 전량 유지(L3 탐지 무변)에 위조 제거는 새 sentinel 4종까지 확대. 더해 운영자 지침을 `claude --append-system-prompt` **실제 시스템 채널**로(미지원은 중립 문구 폴백, 확인 실패 기본값은 «끄기» — 잘못 켜면 unknown option 으로 전 질문이 죽는다), 토큰을 프롬프트 본문 → `BRIDGE_TOKEN` 환경변수로(잔여 노출면 `/proc/cmdline` 동시 축소), 자식 CLI 를 중립 cwd(`~/.mysql-ai-bridge/work`)에서 기동(러너를 코드 저장소에서 띄운 사용자의 `CLAUDE.md`·코딩 에이전트 정체성 상속 차단 — 거부문이 실제로 그 논거를 폈다). 자기강화 루프는 서버가 끊는다(오판 거부턴을 다음 `conversation_context` 에서 제외 + 제외 고지 1줄) — **배포 즉시 발효**라 러너 갱신 전에도 대화 고착이 사라진다. 사용자 대면은 «더하기만 하는 조치»(안내 1줄 부가, 지우거나 재생성 안 함), 관측은 원장 `detail=injection_refusal` + 응답 필드. 판정은 「프롬프트 인젝션」 용어 **and** 거부 동사 200자 근접일 때만 참이라 「SQL 인젝션 위험이 있어 거부해야 합니다」 같은 정상 쿼리 리뷰 답변을 잡지 않는다(이 서비스의 주 용도가 쿼리 리뷰다). 신규 31건 + 뮤테이션 3종 KILL · 기존 계약 테스트 2건은 **판정 대상**을 갱신(옛 검사가 제거 대상 문구를 요구해 그대로 두면 결함을 되돌리라고 요구하는 게이트가 된다). 잔여: POST-DEPLOY 라이브 왕복 · 러너측 수정은 화면 「연결 준비」 재실행으로 전파(불일치는 `stale_build` 로 표시). · **(09-01) 연결 화면 1단계 기본 OS 탭을 «마지막으로 연결됐던 OS» 로** — 종전 `navigator.platform` 은 «브라우저가 도는 OS» 라 WSL 사용자에게 항상 Windows 를 먼저 보였다(제보). 러너가 `agent_os` 를 신고 → 서버가 **연결 사건일 때만** 계정에 접음(`WebOAuthTokens.RunnerOs` → `WebAccounts.BridgeLastOs`) → 두 화면(모달·단독 페이지)이 `last_os` 를 기본 탭으로. 모르면 종전 추측 폴백(구 러너 호환). codex 적대 리뷰 **P1 3건·P2 2건 전건 조치** — 기존 운영 DB 컬럼 미생성(slow path 전용 ALTER, 기능이 영구 폴백) · 두 러너 값 30초 진동(PowerShell 재등록을 WSL 러너가 되돌림) · 모달의 늦은 발급 응답이 다른 창의 토큰을 덮음(선재) · 단독 페이지 응답 순서 · 폐기 토큰의 계정 쓰기. · **(09-01) 연결된 AI 의 실패 사유 소실 해소** — 대화 「253서버 프리미엄 포인트 누적·사용로그 집계」에서 사용자가 같은 질문을 두 번 보내고 두 번 다 `AI 가 오류로 끝났습니다(exit 1):`(콜론 뒤 **빔**)만 받은 뒤 이탈했다. 원인은 러너가 자식 CLI 의 **stderr 만** 실어 보낸 것 — 그 CLI 는 사용 한도 같은 정책성 실패의 사유를 **stdout** 으로 낸다(동일 명령 재현으로 확정: exit 1 + stdout 에 `You've hit your session limit · resets …`, stderr 에는 종료코드와 무관한 stdin 안내 한 줄). 즉 결함은 「외부 한도」가 아니라 **사유를 버리는 경로**이고, 한도는 몇 분 뒤 풀리는 회복 가능한 상태였다. 봉인은 채널을 맞히지 않는 계약 — `describe_cli_failure` 가 stderr·stdout 을 둘 다 보고 **의미 있는 쪽**을 고르고(잡음 줄 선제거), 사유가 정말 없으면 「알 수 없습니다」를 명시하며(빈 콜론 금지), 회복 가능한 부류(한도·미로그인·미지원 옵션·미설치)는 **증상 어휘**로 잡아 다음 행동 1줄을 덧붙인다(부류 미매칭이면 안내를 지어내지 않는다). 사유 원문은 자격증명 마스킹 + 400자 상한을 거치고, 러너 로그에도 실패 첫 줄이 남는다(종전 무기록). 신규 11건 · 뮤테이션 3종 KILL · ruff clean · 인접 feature 회귀 0(기준선 동일) · 인라인 적대 3렌즈 P1 1건 적발·수정. 원장 `FR-cli-failure-reason-discarded-on-stdout`. 잔여: 러너는 **사용자 머신 파일**이라 서버 배포로 갱신되지 않는다 — 화면 「연결 준비」 재실행 후 다음 실패에서 라이브 실측. · **(09-01) 낡은 러너가 최신 러너의 질문을 가로채던 결함** — 사용자가 안내대로 「연결 준비」로 최신 러너를 띄웠는데 같은 증상이 재발했다. 같은 계정에 러너 2개(배포본 `d0ac1263d454` vs 옛 `0a4ba732366c`)가 하트비트 중이었고, 점유가 선착순 원자 UPDATE 라 **옛 러너가 먼저 집어** 옛 코드로 답했다 — 서버는 `runner_update.stale_build` 로 그 사실을 **알면서도 안내만 하고 내줬다**(「안다」와 「막는다」 사이의 공백). 봉인은 **상대 판정**: 같은 계정에 배포본 지문 러너가 *실제로 듣고 있을 때만* 낡은 쪽이 양보한다(절대 판정은 러너가 사용자 머신 파일이라 배포 직후 전 사용자 중단이 된다). 집행은 `claim_request` 409, 억제는 목록·대기(계정 공용 상한 낭비 방지, ⚠대기 즉시반환은 옛 러너의 즉시 재호출과 만나 busy-loop 가 되므로 금지 — 자체 적대검토 P1), 취소 통보는 유지. 러너측은 `runner_update.superseded`(**`stale_build` 와 별개 필드** — 합치면 배포 직후 단독 러너까지 자살)를 받으면 로그아웃과 같은 `shutdown_after_drain` 으로 하던 일을 마치고 **스스로 종료**한다(사용자 결정). **계정이 다르면 예외** — 판정이 `AccountId` 스코프라 다중 계정 러너 병존은 그대로 허용. 서버 축은 러너 갱신 없이 즉시 발효하고 자가 종료는 그 위의 정리 축이다. 신규 20건 · 뮤테이션 5종 KILL · 4 feature 전량 rc=0. 원장 `FR-stale-runner-outraces-fresh-one`. · **(09-01) 그 판정축을 «연결 순서» 로 정정** — 사용자 재현(`root`→`claude-corp` 연속 연결)에서 두 러너의 **지문이 같아**(둘 다 `05d75225aded`) 위 판정이 무판정이었다. 사용자가 준 규칙은 처음부터 «신규 러너에 연결되면 오래된 러너 종료(계정이 다르면 예외)» 였고, 직전 축은 관측된 사고의 우연한 속성(빌드가 달랐다)을 규칙으로 승격한 것이었다. 축 = **토큰 발급 순서**(「연결 준비」마다 새 토큰 → `Id` 순서가 곧 연결 순서). 같은 뿌리의 두 번째 증상 — **연결 모달이 닫히지 않음** — 은 `account_runner_build` 가 「가장 최근 하트비트」로 정본 러너를 골라 지문이 다른 두 러너 사이에서 `runner_stale` 이 30초 주기로 **진동**한 것이고(모달 성공 조건 `listening && !stale` 이 서지 않는다), 화면 정본도 같은 축(`ORDER BY t.Id DESC`)으로 옮겨 해소했다 — 이제 화면이 말하는 러너와 실제 처리 러너가 항상 같은 하나다. **등록형 MCP 클라이언트 보호**: 양쪽 모두 하트비트 이력이 있어야 당사자(무설치 계약 경로가 러너 기동으로 조용히 죽지 않게). 23건 · 뮤테이션 4종 KILL(⚠최초 M1 생존 — 테스트 더블이 후보 술어를 평가하지 않아 「축을 고쳤다」가 미검증으로 통과할 뻔했고, 더블 판별력 보강 후 KILL). 서버 축 즉시 발효, 러너 자가 종료는 라이브 두 러너가 이미 해당 빌드라 **재다운로드 없이** 발효. · **(09-02) 이미 연결해 본 사용자에게 [내 AI 실행] 자동화** (사용자 요청 — 접근성) — 자격 판정은 서버 `last_os`(러너 실연결 시에만 기록 → 그 존재가 곧 연결 이력; 브라우저 저장은 같은 사람이 기기마다 다른 취급을 받아 배제). 계기 넷: 칩 클릭·컴포저 잠금 버튼(단일 `_connectEntry`)·**로그인 진입**(문서당 1회)·「업데이트 필요」. 명시 클릭은 실패 시 연결 창으로 폴백하고 **로그인 진입만 창을 열지 않는다**. ⚠급소는 **사용자 활성화**: 실행 URL 에 토큰이 실려 「클릭→await 발급→이동」이면 이동 시점에 활성화가 끊겨 크롬이 조용히 거른다 → 자격 확인 순간 **프리페치**하고 클릭은 동기 이동(뮤테이션 M1 이 이 순서를 잠근다). 대기·판정은 자동/수동이 `_awaitUsable` 하나를 공유하고 «무응답»과 «서비스 응답 없음»을 구분. 「업데이트 필요」는 **런처가 기동 직전 러너를 최신본으로 교체**해 클릭만으로 풀린다(https+신뢰된 CA, **토큰 검증 뒤** — 이 스킴은 아무 페이지나 열 수 있어 검증 전 부수효과는 임의 사이트가 러너 파일을 갈아치우는 통로가 된다[자체 적대검토 P1]; 실패는 있던 파일로 기동해 «갱신하려다 못 띄움»을 회피). 이력 없는 사용자는 네 경로 모두 종전대로 창(무회귀). 신규 19건·뮤테이션 4종 KILL·4 feature rc=0·ps1 BOM 보존, **수정 전 실 브라우저 실측**으로 문제 상태(칩 클릭→창만 열림) 고정. 기존 계약 2건은 리터럴 함수명 대신 관계를 잠그도록 갱신(개선을 되돌리라 요구하던 게이트). ⚠런처 갱신은 사용자 머신 파일이라 **다음 설치·재설치부터** 발효, 그 전엔 종전 경로로 폴백. 로그인 진입이 크롬 활성화 정책에 걸리는지는 **배포 후 실측**(추측 안 함). · **4차 재발 봉인 — 「어떻게 얻은 목록인가」를 신고에 싣는다 (진행 중, 2026-09-02)**: 연결한 AI 에 없는 모델(`gpt-5.1-codex`)이 선택기에 뜨는 제보가 08-31 세 번의 수정 뒤 네 번째로 돌아왔다. 원인은 **우리가 사용자 머신의 러너를 갱신할 수 없다**는 것 — 폴백을 제거한 빌드를 배포해도 낡은 러너가 자기 내장 표를 계속 신고했고 지문 미신고라 구버전 경고도 fail-open 이었다(라이브 3중 대조: 도는 파일 `af7c3fe19808` · 로그 「내장 기본값을 씁니다」 · `Id=83` 의 `gpt-5.1-codex`). 수정은 신고 항목마다 출처(`probe`/`cache`/`builtin`)를 싣고 **수신 시점**에 거르는 것 — 단위가 런타임 하나라 나쁜 것만 떨어지고, 낡은 목록은 첫 하트비트에 지워진다. 지문 판정 3벌 복제를 하나로 통합. **§18.8 수렴 계약 (b) 로 첫 설계를 철회했다**: 「러너가 전역 자격을 선언하고 서버가 읽는 시점에 계정 목록을 통째로 감춘다」였는데 적대 패널 P1 이 라운드에 걸쳐 줄지 않았고(3→4, 4→4) 2라운드 P1 넷은 내 수정이 만든 결함이었다 — 전역 게이트·다중러너 fail-closed·화석 가드를 되돌리고 provenance 하나만 남겼다. 사용자 결정으로 로컬 LLM(ollama) 런타임 전면 제거. `make test` 6,913건 green. **배포 완료**(`2ffe054d`, PR #1504) — POST-DEPLOY 라이브 실측: 카탈로그에 `gpt-5.1` 0건 · codex 축은 `gpt-5.6-*` 만 · 엣지 `no upstreams available` 0건 · 구 빌드 러너의 화석 목록이 실제로 `[]` 로 지워짐(설계 핵심 주장의 라이브 증거). 확인 라운드는 codex 채널로 대체(P1 1·P2 2 전건 조치). · **(09-02) 러너 소스 모듈 분할 + 배포본 이중화 제거** — 사용자 보고(「브릿지 러너 개발 시 작업자AI 간 충돌이 빈번」)의 원인을 90일 커밋으로 실측: ① 정본과 배포본이 **바이트 동일한 두 커밋 대상**이라 49개 커밋이 **예외 없이 둘 다** 고쳤고(49/49) ② 4,238행 단일 파일이라 관심사가 달라도 머지 지점이 겹쳤다(부수 확인: `cycle-init` 이 같은 핫스팟 활성 브랜치 **7개**를 경고 — §13.2.5-A 권고는 ≤2). 조치는 **소스는 쪼개고 배포본은 빌드가 만든다**(AGENTS.md §13.1 v3.35.1 «1순위», `inject_asset_stamp.py` 와 같은 계층): `src/agent/` 18 모듈(최대 784행·비순환 DAG) + `scripts/build_bridge_agent.py` 번들러 + 생성물 4개 git 추적 해제. 3경로 배선(이미지=Dockerfile · 테스트=루트 `conftest.py` · 로컬=`make bridge-agent`)과 `deploy-web.sh` `bridge_runner_verify` 하드 게이트 — 빌드 RUN 이 빠지면 **배포는 성공하고 러너 다운로드만 404** 가 되는데 healthz·soak·대화 스모크가 전부 초록불이라 그 침묵을 끊는다. 분할이 만든 새 실패 모드는 **가변 전역**(`_RUNNER_INSTANCE` — `from .x import` 는 import 시점 값에 묶여 87분 고아 점유 회수가 조용히 죽는다)이라 `state.py` 접근자로 봉인했고, 그것이 산출물 diff **4,267행 중 57행**(접근자 12 + 블록 이동 + PEP8 2)의 전부다 — 사용자가 받는 동작은 그대로. 전체 **6,956건 rc=0** · 적대 뮤테이션 **8/8 KILL**(⚠자체 적발: 「패키지 형태가 살아 있는가」를 보는 테스트가 하나도 없어 신설 — 번들이 그 결손에 면역이라 조용히 죽을 수 있었다; 하네스 함정 2건도 제거 후 재실측). 잔여: 라이브 다운로드→실행 왕복 POST-DEPLOY. · **(09-02) 「업데이트 필요」가 재실행으로는 풀리지 않던 막다른 길 + 컴포저 안내 문단 레이아웃** — 자동 실행은 동작했는데 같은 낡은 러너가 다시 떴다. 실측으로 갈렸다: 사용자의 `launch.sh` 는 **10:16** 설치본이고 런처 자기 갱신은 **10:44**(`342d1039`) 배포라 28분 차이로 그 블록이 없다(러너 로그 `hb.stale_build`, 지문 `937b18be70eb` 고정). **이미 설치된 런처를 서버가 원격으로 바꿀 통로는 없다**(러너에 자기 갱신 부재) — 그래서 없는 능력을 만들어 내는 대신 **나이를 묻지 않고 결과를 본다**: 재기동 **전후의 러너 지문**이 같으면 「이 컴퓨터에서는 재실행이 파일을 바꾸지 못한다」가 그 사실 하나로 증명된다(서버가 `runner_build` 를 싣고 화면은 **동일성 축에만** 쓴다 — 표시하지 않는다). 증거를 잡으면 문구를 바꾸고 **1단계 명령을 발급까지 해서** 같은 화면에 세운다(토큰이 실려 있어 [연결 준비] 를 눌러야 생기므로, 말만 하면 막다른 길을 한 칸 뒤로 옮길 뿐이다). 그 뒤로는 클릭·로그인 진입 모두 실행을 다시 쏘지 않고(정해진 30초 반복 소모 제거), 증거는 **지문 변화·최신 도달**로 해제된다(재설치를 마친 사용자가 세션 내내 「재설치하세요」에 갇히지 않게). 「모른다」(`null`)를 「같다」로 읽지 않고 `""`(지문 미신고 구 러너 — 정확히 이 결함의 모집단)는 아는 값으로 쓴다. 두 번째 결함은 **컴포저 안내 문단이 입력창을 밀어낸 것**: `#composerActionsSelectorNote` 가 `.composer-box`(`display:flex` 한 줄)의 **정적 자식**이라 형제 팝업들(`absolute`/`fixed`)과 달리 플렉스 항목으로 섰다 — 라이브 정량 **입력창 1393px → 922px(−471px, −34%)**. 앞 cycle 이 `role="menu"` owned-child 제약을 피해 문단을 메뉴 밖으로 꺼냈지만 **그 이동이 배치까지 옮겨 주지는 않았다**(이 요소를 참조하는 테스트 0건). `.composer-wrap` 안 입력창 바로 위로 옮기고 ARIA 계약은 조상 사슬 검사로 함께 잠갔다. 신규 16건(**node 실행 하네스**로 모듈을 실제 구동 + 구조 단언) · 뮤테이션 **9종 전건 KILL**(⚠M6 최초 생존 — 시나리오가 항진명제였다) · rc=0 · ruff clean. · **(09-02) 러너 자기 갱신 — 「업데이트 필요」를 화면에서 없앤다** (사용자 결정, 웹 리서치 후 선택) — 낡음 판정은 **지문 완전 일치 유지**, 화면은 **조용한 자동 갱신**. 리서치 대조: K8s 는 kubelet **3 마이너 스큐** 허용, MCP 는 버전·**능력 협상**, GH Actions 셀프호스티드 러너는 **기본 자동 업데이트**, Tailscale 은 **심각도별 비차단 표시** — **sha256 완전 일치를 요구하는 곳은 없었다**. 사용자는 엄격함은 유지하고 **조치만 자동화**하는 조합을 골랐다. 근거: 러너 파일이 거의 모든 배포에서 바뀌어 **무관한 배포**가 접속 중인 러너를 전부 낡음으로 뒤집는다(실측 13:32 재설치 → 13:36 다른 세션 배포 착륙 → 「업데이트 필요」 재발 + 연결 모달 고착). `agent/selfupdate.py` 신설 — 하트비트가 신호를 세우고 **대기 루프가 유휴일 때** 받아 교체하고 `os.execv` 재기동(순서는 물러남→갱신→대기). 규율: ⭐**지문 일치로만 «단일 파일» 판정**(경로만 보면 개발자 소스를 덮어쓴다) · 받을 곳은 **모듈 상수**(하트비트 `download_url` 미사용 — 응답을 바꿀 수 있는 누구든 **실행할 파일**을 지목하게 된다) · https+기동 때 정한 CA · 크기·`ast.parse` 검사 · 같은 디렉토리 임시파일→`os.replace` · ⭐**지문이 같으면 재기동 안 함**(무한 고리 차단)+최소간격 120초 · **작업 중이면 미룸**(진행 중 답변을 죽이지 않는다) · 재기동 전 하트비트 정지→점유 해제(`os.execv` 는 `atexit` 미호출) · 실패는 있던 파일로 계속. 신고(`self_update`)는 **할 수 있을 때만**(단일 파일 ∧ `--no-self-update` 아님) — 못 하면서 신고하면 화면이 오지 않을 갱신을 기다린다. 서버 `account_runner_self_updating`(정본 러너 축은 `account_runner_build` 와 **동일**)→`connect_status.runner_self_updating`→화면은 `_actionableStaleOf()` **한 곳**에서 접는다(칩·모달 성공조건·자동실행 자격·「재실행해도 그대로」가 모두 이 축을 읽으므로 두 벌이면 갈린다; 모르면 종전 안내로 폴백). 신규 30건·뮤테이션 **12종 전건 KILL**(⚠2건 최초 생존 — **단언이 참이 되는 경로가 하나가 아니었다**: DNS 실패가 https 가드 부재를 가림 · except 블록 오파싱) · 3 feature 실행 결과가 main 기준선과 정확히 동일 · ruff clean. 기존 닫힌집합 계약 2건(러너 stdlib allowlist·기본 신고 집합)은 의식적으로 확장, 칩 계약의 리터럴 단언은 **관계**로 갱신(리터럴은 「개선을 되돌리라」는 게이트가 된다). ⚠**부트스트랩**: 이 기능이 들어간 빌드부터 발효 — 한 번은 런처가 갈아 끼워야 하고 그 뒤로는 배포가 몇 번을 나든 조치 요구가 뜨지 않는다. |
| feature-0044-qa-staging-pipeline | planned | 2026-08-27 | [TASK](../unit/feature-0044-qa-staging-pipeline/docs/TASK.md) | QA·라이브 분리 CI/CD 설계 rev.3 확정 (전제 13축 중 12축 결정 — SVN 안 A(매니페스트만)+파일서버 tar · docker save 릴레이 · 빌드 서버 이관 경로 · **로컬 LLM 전면 폐지로 GPU 불요**, 단 임베딩 사내 MCP 이관이 QA 세팅 선행 조건). 구현 착수는 사용자 승인 대기 |
| feature-0045-zd-bridge-continuity | review | 2026-08-27 | [TASK](../unit/feature-0045-zd-bridge-continuity/docs/TASK.md) | **웹브라우저–개인 AI 브리지가 연결된 상태에서 배포가 진행돼도 그 작업이 끊기지 않게** 무중단 스파인을 재구성. 배경: feature-0043 전환으로 추론 주체가 개인 머신 AI 로 넘어가면서 사용자 작업이 `WebAiTasks` + 브리지 왕복 위에서 일어나게 됐는데, 배포 게이트는 여전히 `active_streams`(SSE/CSV)와 `ask_jobs`(서버 워커 큐)만 봤다 — **AI 가 붙어 대기하고 질문을 조사하는 그 순간에도 0 을 읽고 '조용하다' 고 판정**했다(무중단이 깨진 게 아니라 무중단이라고 믿게 만드는 계기판이 남은 것). 핵심은 **비대칭 드레인**: 대기(`bridge_waiters`)는 끊겨도 잃을 게 없으므로 신호를 주면 즉시 정상 종료(200 `draining:true`)시켜 남은 replica 로 교대시키고, 진행 중인 도구 호출(`bridge_inflight`)은 완주를 기다린다(대기까지 기다리면 연결된 AI 는 항상 대기 중이라 배포가 매번 상한까지 갔다 강행한다). 드레인은 `/livez` 503 으로 Caddy 후보에서 빠지고(2s), MCP 어댑터는 엣지를 거치지 않고 직결하므로 신규 도구 호출에 **503 + `X-Bridge-Draining`** 을 줘 다음 replica 로 재라우팅시킨다. `predrain` 상한 90→180s(중계 왕복 상한 120s 를 덮는다). **개인 AI 가 붙는 문(`ext-tool-mcp`)은 단일 컨테이너 + 워커 일괄 recreate 대상**이라 배포마다 통째로 끊겼다 → `ext-tool-mcp-a/b` 2 replica + Caddy LB + `stateless_http` + 전용 one-at-a-time 롤링(`rollout_mcp_replicas`, 상대 healthy 일 때만 내림) + stop_grace 15→130s. 부득이 강행한 배포는 끊겼을 수 있는 점유를 되돌리되 **`ClaimedBy` 를 지우지 않고 lease 만 만료**시킨다 — 살아남은 러너의 제출은 그대로 성공하고 죽었으면 다음 연결이 가져간다(먼저 끝내는 쪽이 이긴다). 러너의 'sleep 금지' 는 **대기에만** 적용되도록 정밀화(연결 실패 시 곧바로 `continue` 하던 것이 서버 교체 몇 초 동안 초당 수천 번 재시도하는 busy-loop 였다 → 1→15s 백오프). quiesce 게이트에 **네 번째 축**(브리지 점유) 추가 — 브리지 전환 후 그 게이트는 항상 '조용함' 으로 통과하고 있었다(vacuous pass). 검증 46건 + 기존 계약 정합(0014 allowlist·0020 스텁·0041 서비스명·0043 sleep 계약). 라이브 재배포 `e7d54f70` 에서 "대기는 드레인으로 교대했고 진행 중 왕복은 완주했다(끊김 0)" · MCP 교체 중 엣지 400 유지 · no-upstreams 0건 **실측 PASS**(정본 TASK.md §3/§9) — 잔여 없음. |
<!-- AI-EDITABLE:STATUS-TABLE:END -->

> **미머지 활성 worktree**(2026-09-02 실측, 등록 worktree 중 `ahead > 0` 기준): `ai/claude/ai-jobs-rewire-external`(ahead 8 — 배경 배치·그래프 능동 분석을 개인 AI 로 배선하는 초안) · `ai/claude/feature-0043-caps-trust-gate`(ahead 6 — 능력 신고 자격 게이트 초안) · `ai/claude/feature-0012-web-router-modularization`(ahead 3) · `ai/claude/doc-sync-20260827-010304`(ahead 2 — 08-27 doc_sync 미착륙 초안) · `ai/claude-corp/feature-0003-tween-render-defer`(ahead 1) · `ai/claude-corp/feature-0016-node-role-viz`(ahead 1) · `ai/root/feature-0043-runner-first`(ahead 1) · `ai/root/perf-cycle-label-fix`(ahead 1) · `ai/root/ssot-roadmap-refresh`(ahead 1). 직전 창의 `ai/claude/feature-0003-attach-lineage-viz`(ahead 2)는 09-01 착륙 후 브랜치·worktree 모두 정리돼 목록에서 빠졌다. 이들의 작업 현황 정본은 각 worktree 의 `unit/<feature>/docs/{TASK,REPORT}.md` 이며, 위 표는 main 머지 기준 상태만 반영한다(머지 시 행 승격 — DOC_REGISTRY In-flight 정책). 미머지 in-flight 작업이 생기면 표 아래 note 로만 표기하고, 병합 시 행을 갱신한다(ADR-0031 §1).
> 상세 TASK 이력을 셀에 누적하지 않는다(ADR-0031 §1) — 상세는 정본 링크의 unit TASK.md / 인덱스화 이전 분은 STATUS_ARCHIVE.md.

### 상태 값 정의
`planned`(요구 정리) · `in-progress`(구현 중) · `blocked`(승인/불명확 차단) · `review`(검토) · `done`(완료) · `deprecated`(폐기)

## 2. 기능 간 의존성 요약
[ARCHITECTURE.md](./ARCHITECTURE.md) §6 정본 참조. 아래는 초기 코어(feature-0003~0011) 발췌이며, feature-0012~0045 를 포함한 전체 의존 관계는 §6 이 정본이다:
- feature-0003 → feature-0002
- feature-0004 → feature-0001
- feature-0005 → feature-0001, feature-0002, feature-0004
- feature-0006 → feature-0001
- feature-0008 → feature-0003(검증대상), feature-0004(headless 보조)
- feature-0009 (cross-cut) → 코드 거주 feature-0002(mentions/group_members)·feature-0003(app.py)
- feature-0010 → feature-0003(인증/라우트 인라인), feature-0002(`modules/cred_crypto`)
- feature-0011 (refactor) → feature-0002·feature-0003 공통 코드 `shared/` 추출 (양 feature import 재배선)

## 3. 블로킹 항목
- **SSOT 통합(META-0003) Phase 3** — tracked secret 노출 종료(`.env.secret.bak-task0228` 등)는 **rotation(사용자) 선행** 필요. 근거: ADR-0031 §4 / [DOC_REGISTRY.md](./DOC_REGISTRY.md).
- 그 외: 없음.

## 4. 통합 테스트 현황
- 현재 기준: 구조/기동 검증 (`docker compose config`, `make status/web/browser-up/browser-health`, `GET /api/session`).
- 기능별 테스트 정본: `unit/<feature>/docs/TEST.md`.

## 5. 전체 진행률
- 등록 기능(main `unit/`): 44 (디렉토리 45 — feature-0016 은 metadata-graph + zd-pg-pause-caddy 2 슬라이스, 번호 충돌 — 사람 결정 보류)
- review 10 (feature-0008/0012/0014/0015/0016-zd-pg-pause-caddy/0017/0038/0039/0040/0045) · in-progress 32 · done 2 (feature-0011/0042) · planned 1 (feature-0044) · blocked 0
- 상세 진척·이력: 각 unit TASK.md / [STATUS_ARCHIVE.md](./archive/STATUS_ARCHIVE.md)
