## Run — TASK-20260904T033000 클라이언트 우선 연결 화면

- Environment: Windows-browser (PB-0008)
- Status: **미수행 (배포 대기)** — 사유: 이번 변경은 정적 자산(`index.html`·`ai-connect.html`·
  `connect-modal.js`)이라 **배포된 서버가 서빙해야** 실제 화면이 된다. 미머지 상태로
  bind-mount 검증을 하면 라이브 컨테이너를 건드리게 되는데, 같은 조작으로 web-a/web-b 를
  깨뜨린 적이 있다(2026-09-03 자초 사고). 그래서 **머지 → 배포 → 실측** 순서로 미룬다.
- ⚠ 이 fragment 는 게이트를 통과시키려고 적은 것이 아니다. 배포 직후 아래 항목을 실제로
  확인하고 이 파일에 결과를 덧붙인다. 확인하지 않은 채로 이 cycle 을 「완료」라 부르지 않는다.

### 배포 후 확인할 것
1. `/ai/connect` 와 대화 모달 **둘 다** 에서 터미널 단계가 **접혀** 있는가.
2. 안내 문구가 [내 AI 실행] 을 주 경로로 말하는가.
3. [연결 준비] → [내 AI 실행] 으로 클라이언트가 **연결 정보와 함께** 뜨는가
   (「연결 정보가 없습니다」가 더 이상 나오지 않는가) — 이 cycle 의 실제 목적.
4. 실행 실패 안내가 접힌 터미널 블록을 **자동으로 펼치는가**.

---

## 결과 — 2026-09-04 (배포 이미지 `c4e6684c`, PB-0008 실제 Windows 브라우저)

| # | 확인 | 결과 |
|---|---|---|
| 1 | 터미널 단계가 접혀 있는가 | ✅ `#terminalPath.open == false`, summary 「터미널로 연결하기 — 연결 프로그램을 아직 설치하지 않았다면」 |
| 2 | 안내가 [내 AI 실행] 을 주 경로로 말하는가 | ✅ 「연결 준비를 누른 뒤 내 AI 실행을 누르면 연결 프로그램이 열리고 나머지는 그 창에서 끝납니다」 |
| 3 | 딥링크가 연결 정보를 싣는가 | ✅ 서버 응답 `protocol` 의 쿼리 키 = `token`·`base`·`ca_sha256`·`agent_sha256` |
| 4 | 스킴으로 클라이언트가 **연결 정보와 함께** 뜨는가 | ✅ 「연결 정보가 없습니다」 사라짐 → 3개 런타임 탐지 후 「연결할 준비가 되었습니다」 |
| 5 | 실패 안내가 접힌 블록을 펼치는가 | ✅ `_revealCommand` 가 `connectModalTerminal.open = true` |

**클라이언트가 찾은 것** (종전 1개 → 3개, 각각 실제 응답 확인):
`claude` (Windows) · `claude (WSL)` · `codex (WSL)` — 전부 「답합니다」.

### ⚠ 이 검증에서 드러난 것 두 가지

1. **Chrome 은 외부 프로토콜 실행 전 확인을 요구한다.** 자동화 브라우저에서 `#launchClient`
   클릭은 등록되지만 클라이언트가 뜨지 않았다. 실사용자에게는 「이 사이트가 DQA Connect 을(를)
   열려고 합니다 → [열기]」가 뜨는 정상 경로다. 결정적 확인은 서버가 낸 URL 을 OS 핸들러에
   직접 태워서 했다.
2. **설치본이 낡으면 조용히 실패한다.** 재검증 초기에 계속 실패했는데, 원인은 설치된 exe 가
   스킴 파싱 이전 빌드였다는 것이다(해시 대조로 확인, `exit=2`). `--windowed` 라 argparse
   오류가 어디에도 보이지 않는다. **배포본 갱신 여부를 해시로 확인**해야 한다.
