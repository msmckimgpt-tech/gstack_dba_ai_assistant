---
run_at: 2026-07-29T18:40:00+09:00
session: ai/claude/feature-0016-graph-cols-partial
scope: 상한 전수 감사 — 데이터 절단 19건 제거(백엔드 sync·조회 / 프론트 표시), 컬럼 보강 3경로 전량화
verdict: PASS (유닛·회귀 전량 + 라이브 실측 확증 — Windows-browser Run 은 사유 명시 후 POST-DEPLOY 이월)
---

### Run (2026-07-29) — graph-cap-audit: "개수를 줄여 출력"하는 절단 제거 — **Environment: CLI (라이브 AGE/MSSQL 실측 + 컨테이너 런타임 실측) + headless(node vm 유닛 계약)**

#### 1. 사용자 리포트 + 방향 지시

> 여전히 컬럼이 누락된 항목이 있으며, 노드 또한 누락된 대상이 확인되었습니다.

> 다른 모든 상한값이 설정된 항목들을 검토해주세요.
> 이와 같은 현상은 오류이며, 개수를 줄여 출력하는 최적화는 다른 방향으로 이루어져야 합니다.

첨부: `cc_pyron.DT_Character_New` — 상세 패널 `컬럼 (2)`(LastPolymorphID·RespawnZoneID)뿐, 캔버스도 동일.

#### 2. 진단 — 라이브 실측

**① 컬럼 부분 투영 (직전 cycle 이 놓친 축)**

| 대상 | 그래프 `HAS_COLUMN` | 실 데이터소스 컬럼 |
|---|---|---|
| `cc_pyron.DT_Character_New` | **2** | **55** |

직전 보강 게이트는 "컬럼 0 **또는** `truncated`" 였다. 그래프 `Column` 정점은 `column_descriptions`
(설명이 달린 컬럼만) 원천이라 **부분 투영**이 흔하고, 그 경우 백엔드는 있는 걸 다 준 것이므로
`truncated` 도 false — **"부분"이라는 사실이 응답 어디에도 없다.** 그 2개 때문에 보강이 skip 됐다.

**② 노드 누락 — sync 투영 단계의 상한**

| 대상 | 그래프 `Routine` 정점 | 실제 루틴 |
|---|---|---|
| `cc_pyron` | **300** (정확히 cap) | **597** |

`_ROUTINE_CAP_DEFAULT = 300` 은 조회가 아니라 **투영** 상한이라, 잘린 루틴은 그래프에 정점 자체가 없다
(어떤 조회로도 복구 불가). 전체 규모: 루틴 보유 스키마 **232개 중 47개(20%)가 cap 에 포화** — 전부 조용히.

```
루틴 cap 포화 스키마: 47 / 232 · 그래프 루틴 23,507 중 14,105 가 포화 스키마 소속
schema_tables limit=300 초과 스키마: 2 (최대 719 테이블)
scope_schemas limit=500 초과 ds: 0 (최대 140)
```

#### 3. 처리 — 상한 전수 감사

**데이터 누락(오류) → 해소 19건**. 상한을 폐기하지 않고 **비현실 극단 전용 안전 가드**(실사용 최대치의
수십 배)로 성격을 바꾸고, 걸리면 **WARN 표면화**(조용한 절단 제거). `IN` 절 크기처럼 **배치 성격**인 것은
상한이 아니라 배치 크기이므로 **반복 조회로 전량** 처리(배치 단위 실패 격리).

컨테이너 런타임 실측(변경 반영 확인):

```
루틴 cap      : 20000      (종전 300)
참조 테이블   : 2000       (종전 40)
참조컬럼/테이블: 2000 / 총 20000   (종전 24 / 80)
컬럼배치      : 400 (반복)  (종전 400 절단)
파라미터      : 20000      (종전 500)
이웃 노드     : 20000 · 엣지 80000  (종전 300 / 1200)
검색          : 5000 · 후보풀 20000 (종전 80 / 240)
search_nodes 5000 · scope_roots 20000 · scope_schemas 20000 · schema_tables 20000
```

**유지(= 사용자가 말한 "다른 방향")**: 뷰포트 컬링(§65)·노드 LOD(§67)·컬럼 LOD(§61)·레이아웃 위상서명
메모이즈(§73)·라벨 BitmapText(§80)·scene diff 풀(§79)·`_META_DBGRP_ROW_CAP`(4000, 이미 "실사용 무제한"
가드)·`_META_DBGRP_BIG`(초기 접힘 — 펼치면 전량)·연출 상수. **데이터는 전량 보유하고 표시만 조절.**

#### 4. §18.8 적대 검증 — codex review **P1(GATE) 1건 흡수**

REV-20260729T090000-graph-cap-audit 참조.

- **P1 부분 펼침에서 조기 return 이 보강을 가로막음**: `_metaGraphToggleColumns` 초입의
  `if (_metaTableHasCols(key)) return;` 이 **내가 추가한 보강 로직보다 앞**이라, 그래프에 컬럼 2개만
  있는 테이블은 그 2개가 렌더되는 순간 `colsByTable > 0` → 함수 초입 반환 → 나머지 53개가 영영 오지
  않는다. **사용자 스크린샷이 정확히 이 상태**였으므로, 이 지적이 없었다면 "고쳤다" 고 보고하고도 증상이
  그대로 남았을 것이다. → "펼쳐졌다" 를 *완전히* 펼쳐졌다(컬럼 있음 **AND** introspect 완료)로 좁히고,
  실패 테이블은 `introspectMiss` 로 재시도 억제(스코프 전환 시 초기화).

#### 5. 검증 결과

| 항목 | 결과 |
|---|---|
| `test_detail_columns.js` | **57 PASS / 0 FAIL** (⑯ 부분투영 union·ordinal 보완 · ⑰ 3경로 계약 · ⑱ P1 계약 6건) |
| 헤드리스 스위트 전량 | **585 PASS** (직전 cycle 567 → +18) · 실패 집합 baseline 동일 |
| pytest (`make test`) | **전량 통과 · FAILED 0** (cap 계약 전환 + 배치 반복 계약 신설 반영) |
| ruff | All checks passed |
| `node --check` | PASS (graph-ctxmenu.js · graph-state.js · graph-core.js) |
| 상한 런타임 실측 | 위 §3 — 컨테이너에서 변경값 반영 확인 |

#### 6. Windows-browser Run 미수행 사유 (§15.4.1 · PB-0008)

본 Run 은 **커밋 전** 검증이다. 라이브 서버는 아직 수정 전 코드를 서빙하므로 `Environment:
Windows-browser` Run 은 **배포 후 POST-DEPLOY(GCA.7)** 로 이월한다 — 직전 cycle 들과 동일한 2단 패턴.

배포 후 확인할 것: ① `DT_Character_New` 상세·캔버스에 컬럼 **55개** ② `cc_pyron` 루틴 노드가 300 →
실제 수(sync 재실행 후) ③ 검색 결과 "50건 · 상한" 문구 소멸 ④ 대형 스키마 펼침 시 렌더 성능(컬링·LOD
가 상향된 데이터량을 감당하는지 — **이 축이 본 변경의 실질 위험**이다).

#### 7. 정직 표기 — 남긴 것

- **AI 분석 진행 패널의 완료 12 / 실패 4 표시 상한**: 그래프 데이터가 아니라 실시간 진행 로그 요약이고,
  전량 표시하려면 패널 레이아웃(스크롤 컨테이너) 변경이 동반된다 — 별도 항목으로 남긴다.
- **루틴 노드는 sync 재실행 후에 채워진다**: cap 상향은 *앞으로의* introspect 에 적용되므로, 이미 잘린
  47개 스키마의 누락 루틴은 다음 sync cadence(또는 `bin/routine-backfill.sh`) 이후에 그래프에 나타난다.
  배포 직후 즉시 반영되는 것이 아니라는 점을 POST-DEPLOY 검증에서 구분해 판정해야 한다.
- **부하는 관측 대상**: 상한 상향은 LLM 호출을 늘리지 않으나(루틴 introspect·파싱·AGE MERGE 는 LLM-free)
  sync 1회 처리량과 조회 응답 크기는 커진다. 그래서 상한을 제거하지 않고 안전 가드로 남겼다.
