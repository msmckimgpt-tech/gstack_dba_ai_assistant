---
run_at: 2026-09-07T06:00:00+09:00
session: auto-connect (ai/claude/feature-0046-auto-connect)
scope: 자동 연결 · 확인창 → 트레이 알림
verdict: PASS (단위·실행·뮤테이션) / 라이브는 배포 후
---

### Run (2026-09-07) — **Environment: host pytest + node 실행 하네스**

- `unit/feature-0046-native-client/tests` **413 passed**.
- 자동 연결은 **가짜 DOM 위에서 모듈을 실제 구동**해 잰다(소스 검사 아님) — 런타임 목록을
  주입하고 `connect` 가 나갔는지·어느 `id` 로 나갔는지 본다.

| 주입한 목록 | 기대 | 결과 |
|---|---|---|
| `codex(WSL)` 하나 | 자동 연결 | ✅ `codex (WSL)` |
| `codex(WSL)` + `gemini` (서로 다른 플랫폼) | 자동 연결 | ✅ `codex (WSL)` |
| `claude(Win)` + `claude(WSL)` + `codex(WSL)` | **묻는다** | ✅ 연결 없음 |
| 순서를 뒤집어도 | 같은 선택 | ✅ 고정 순서 |
| 답 못한 것 + 쓸 수 있는 하나 | 자동 연결 | ✅ (중복으로 안 셈) |

- 뮤테이션 **12/12 KILL**. 처음 10건 중 2건 생존 → 원인은 **내 쪽**(등가 가드 · 느슨한 단정)
  이었고 각각 처리한 뒤 재확인했다.

### ⚠ 하네스가 제품 실패로 둔갑할 뻔했다

자동 연결 테스트가 「이 컴퓨터의 연결 기능에 닿지 못했습니다」로 실패했다. 원인은 가짜 DOM 에
`createTextNode` 가 없어 `paint()` 가 던진 것이었고, **제품의 `.catch` 가 그 예외를 잡아
사용자 문구로 바꿔 놓았다.** 하네스의 결함이 제품의 증상을 입는 형태다.

### Run (2026-09-07) — 라이브 — **Environment: Windows-browser (배포 후로 이연)**

- 계획: 배포 → 설치본 재빌드 → 앱 창에서 ① 패널이 뜨자마자 자동 연결되는지 ② 확인 창이
  뜨지 않는지 ③ 끝난 뒤 알림 영역에 알림이 오는지.

### Run (2026-09-07, 배포 후) — 라이브 — **Environment: 실 Windows 설치본(재빌드·재설치) + WebView2 CDP**

설치본을 다시 빌드해(`build_client.py --service-base https://112.185.196.20`, 동결 자가진단
`{frozen, webview_import, runtime_present, available, tkinter}` 전부 True) 무인 설치한 뒤 실측.

| 항목 | 관측 |
|---|---|
| ② 확인 창 | **없음.** 연결 중 최상위 창 전수 열거에 확인 대화상자 없음 — 연결이 사용자 개입 없이 끝났다 |
| ③ 트레이 알림 | **떴다.** `DQAConnect.exe / DQA / 「codex (WSL) 로 연결했습니다. 이제 질문에 답할 수 있습니다.」` (화면 캡처로 확인) |
| ① 자동 연결 | 이 머신은 `claude` 가 두 자리(Win·WSL)라 **묻는 것이 맞다** — 실제로 묻는다. 고유 플랫폼 분기는 격리 프로필에서 자동 연결 확인 |

⚠ **처음 실측은 실패했고, 원인은 이 주기의 코드가 아니었다.** 모듈이 스탬프 밖에 있어
브라우저에 도달하지 못했다 — TASK-20260907T160000 에서 고치고 재실측해 위 표를 얻었다.
「단위·뮤테이션·회귀·배포 soak 전부 초록」과 「사용자 화면에서 동작한다」는 다른 명제다.
