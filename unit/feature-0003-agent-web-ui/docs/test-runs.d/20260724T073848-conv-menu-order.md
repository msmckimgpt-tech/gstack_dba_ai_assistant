### Run (2026-07-24) — conv-menu-order: 대화 목록 '···' 확장 메뉴 항목 순서 변경(공유 → 이동 → 설정) — **Environment: Windows-browser**

- 방법: PB-0008 — bin/win-browser.py(실 Windows Chrome, CDP relay), https://localhost 대화 목록 각 항목의 '···'/우클릭(universal-ctxmenu) 확장 메뉴.
- 사전(pre-deploy) 정적 검증: 서빙 대상 `static/app.js` `openConversationItemMenu` 항목 append 순서 = 공유 → 이동(`folder.manage.own` 조건부) → 설정 소스 확인 · `node --check` PASS · 항목 3종(공유/이동/설정) 전부 유지 · 순서를 assert 하는 테스트 부재.
- 라이브 육안 검증: **POST-DEPLOY 로 이월** — 순서 변경은 배포로 서빙돼야 실 화면에 반영되므로, `make deploy-web` 후 실 Windows Chrome 에서 대화 목록 '···' 메뉴(그리고 우클릭 universal-ctxmenu)의 항목이 위→아래 `공유 / 이동 / 설정` 순인지, 각 항목 클릭 시 기존 핸들러(공유 팝업 / 폴더 이동 모달 / 설정 팝업)가 정상 동작하는지 육안 확인 예정. deploy_scope 시퀀스(verify PASS → PR → deploy → POST-DEPLOY 라이브)와 정합.
- 결과: pre-deploy PASS. POST-DEPLOY 라이브 결과는 배포 후 본 fragment 에 append.
