---
doc_type: WIKI_HOT_CACHE
last_updated: 2026-09-08
---

# Hot Cache

## Last Updated
2026-09-08

## Key Recent Facts
- **연결하는 길은 «DQA 앱» 하나다** (09-07, 사용자 결정 「그냥 전면 제거」) — 「연결 준비」 버튼·터미널 1단계(OS 탭·명령·복사)·AI 지시문 두 갈래·토큰 경고가 **두 화면(대화 모달 · `/ai/connect`)에서 함께** 사라졌다(한쪽만 지우면 링크를 새 탭으로 연 사용자가 옛 화면을 본다). ⚠ **접은 것이 아니라 지웠다** — 「터미널은 접힌 보조 경로」로 적힌 서술은 전부 낡았다. 토큰은 **여전히 발급되고 그리지 않을 뿐**이며(스킴 URL 로 곧장 전달) 그래서 「닫으면 지운다」가 「애초에 넣지 않는다」로 **강해졌다**. 실행 URL 은 **창을 열 때** 미리 받는다(`_offerLaunch` — 클릭 시점에 발급하면 그 앞에 `await` 가 생겨 사용자 활성화가 끊기고 크롬이 스킴 이동을 조용히 거른다 · 자격은 서버가 아는 연결 이력 `last_os`). ⚠ 서버는 아직 `handoff`·`launch` 를 만들어 보낸다(되돌릴 자리). ⚠ **대가**: 앱을 못 받았고 서버가 배포본도 못 내놓으면 **연결할 길이 없다** — 사용자가 알고 고른 결정이다.
- **로컬 LLM 은 전면 폐기됐다 — 활성 `model_name` 이 0개다** (09-07). `local-llm-edge`/`local-llm-gateway` 컨테이너 · 모델 16GB · `embed-ollama` 서비스 · `titan-embed` alias · `edge-fallback` 정의 · `check-llm-network` 게이트가 전부 사라졌다. 폐기 근거는 실측이다 — 7일 로그 77,789줄 중 추론 `POST` **0건**(전량 healthcheck) · RSS 31.5MiB(모델 미로드). ⚠ **라이브에서만 열려 있던 fail-open 3경로**가 같이 닫혔다: `.env` 의 `LOCAL_LLM_API_BASE` 를 읽던 `_LOCAL_LLM_ENABLED` 가 **라이브에서만** 로컬 alias 를 카탈로그에 넣었고 **테스트 환경은 env 미설정이라 정반대를 단정하며 통과**했다. KB 검색은 죽지 않았다 — `kb_retrieval` 2-tier 로 `pg_trgm` 강등(기존 임베딩 154,365행·bge-m3 볼륨 **보존**). `bedrock-gateway` 철거는 **범위 밖**(feature-0020 무중단 배포 machinery 에 배선 — ADR-20260907T175000).
- **KB 검색은 이제 「문자 후보 + 외부 AI 판단」이다** (09-07, ADR-20260907T185500). 서버가 `pg_trgm` 으로 후보를 찾아 `claim_request.kb_context`·`get_task_context(focus=…)` 로 전달하고 **의미 적합성 판단·질문 재구성은 외부 AI 가 한다** — 새 임베딩 API 는 도입하지 않는다. 샘플은 제품+common 의 **승인·active** 행만, **벡터 NULL 도 검색된다.** MySQL 인사이트는 엔진 포함 endpoint hash + 제품 허용 DB 로 제한하고 **MSSQL 레거시는 catalog 출처 불명이라 원문 검색에서 제외**(구조 도구 안내). ⚠ **의미검색과 동등하지 않고 무매칭은 부재의 증거가 아니다** — 그 고지가 계약의 일부다.
- **모델 목록의 1차 출처는 CLI 자체 카탈로그다** (09-07) — `codex debug models`(실측 0.23초 · 토큰 0 · 로그인 무관). 종전엔 능력 질의가 22.7초에 `models: []` 를 주고 열거·확인이 전부 타임아웃인데 **협상이 기동 1회뿐**이라 목록이 영영 오지 않았고 화면은 「확인하는 중입니다」였다. `_probe` 순서는 **카탈로그 → 캐시 → 확인 → 열거 → 원장**이고, 확인된 목록을 얻을 때까지 배경 재시도한다(60→900초 · 표시 중 3600초). ⚠ 카탈로그 상한 **8MB** — 실측 출력이 413,777바이트라 종전 probe 상한 256KB 로는 **한 번도 성공한 적이 없었다**. 전부 실패하면 원장을 표시하되(`baseline_as_caps`) **건강 축은 건드리지 않는다.**
- **터미널 없이 연결하는 Windows 네이티브 클라이언트가 있다** — feature-0046(신규, 2026-09-03). `bridge_setup.ps1` 이 하던 일을 **같은 계약·같은 중단 지점**으로 수행하고 껍데기만 GUI — **3단**(기본 = 내장 WebView2 창 → 브라우저 앱 모드 → tkinter 폴백, 2026-09-04). ⚠ **미서명**(SmartScreen 2클릭 감수, 사용자 결정) · **Windows 우선**(macOS 는 경고가 아니라 차단이라 범위 밖) · 배포 형식은 설치 마법사 + **인터프리터** 동봉(러너는 동봉하지 않고 서버에서 받아 sha256 대조 — 동봉하면 배포가 갈린다).
- **「창 닫기 = 트레이로 · 종료 = 아이콘 우클릭 [종료]」는 세 껍데기(내장 WebView2 창 · 브라우저 앱 모드 · tkinter) 공통 계약이다** (2026-09-04). ⚠ 판정은 **닫는 순간** 묻는다 — `Shell.can_hide` 는 bool 이 아니라 `Callable` 이고 정본은 `gui._tray_alive` **하나**다(아이콘은 뜬 뒤에도 죽는다 → 기동 시점 bool 을 들고 있으면 창도 아이콘도 없는 프로세스가 남는다). `Bridge.resident` 는 **읽기 전용 property**(대입 자리 제거 = 구조 가드). 첫 닫기에 `gui.HIDDEN_NOTICE` **1회** — 단 **닫힘을 이벤트로 받는 두 껍데기(내장·tkinter)에만**. 브라우저 껍데기는 `WM_CLOSE` 가 오지 않고, 무신호를 「닫힘」으로 추정하면 **창이 열려 있는데** 안내가 뜬다(패널 ping 은 `initClientPanel` 안에서만 시작 · 배경 탭 스로틀 — codex 2라운드 적발). **없는 근거로 말하지 않는다.** 실측 `tests/windows/verify_embedded_close.py`(hide/quit).
- **클라이언트는 알림 영역(트레이)에 상주한다 — 세 껍데기 공통** (2026-09-04 재구성 · 그 시점엔 웹 셸·tkinter 둘이었고 직후 내장 창이 들어와 내장 WebView2 → 브라우저 앱 모드 → tkinter 3단이 됐다). [X] 로 닫아도 연결 유지, 우클릭 메뉴로 열기·연결 끊기·종료. ⚠ **상주 중에는 유휴가 종료 사유가 아니다**(웹 셸의 수명 신호 = 패널 생존인데, 패널을 닫아 두고 쓰는 것이 상주다). 웹 셸 트레이에 「다시 연결」은 **의도적으로 없다** — 연결 입구는 패널 하나다. ⚠ **트레이 생성 실패 시 그 동작을 켜지 않는다**(닫기=종료 폴백) — 「닫아도 유지」는 아이콘이 **지금 살아 있을 때만** 하는 말이고, 판정은 `tray is not None` 이 아니라 `Tray.alive`(backend 생존)다. `pystray` 불채택(Pillow 동반) — `ctypes`+`Shell_NotifyIconW` 직접.
- **주 표면의 연결 패널은 `app/client-bridge.js` 가 그린다** — `ai-connect.{html,js}` 는 **별도 페이지**다(둘 다 살아 있다). 앱 창은 서비스 루트를 열므로 화면 작업은 「앱 창이 여는 URL 과 그 화면을 그리는 코드」를 먼저 확정한다. ⚠ 그 모듈은 **아무것도 import 하지 않는다**가 성질이다(`connect-modal.js` 가 이 파일을 import 하므로 역방향은 순환) — 필요한 헬퍼는 **지역 정의**하고, 두 `_status` 가 같은 요소를 쓴다는 것을 테스트가 잠근다.
- **GUI 앱이 콘솔 앱 자식을 띄우면 Windows 가 «빈 콘솔 창»을 새로 할당한다** — 사용자가 본 깜빡임의 정체. 정본은 `core.hidden_child_kwargs()` **한 곳**이고 자식을 띄우는 **모든** 함수가 그것을 쓴다. ⚠ 종전에도 가드는 있었다(`spawn_runner` 한 곳) — 없었던 게 아니라 **모수가 좁았다**.
- **로그인은 「위임」이 아니라 「대행 실행」이다** — 벤더 공식 명령을 subprocess 로 띄우고 종료코드만 본다. 토큰 미접촉. 2026년에 3사가 구독 OAuth 제3자 사용을 차단했다.
- **개인 머신 클라이언트의 명칭 축은 넷이다 — 패키징 `APP_NAME`(`DQA Connect`) · 화면에 뜨는 `DISPLAY_NAME`(`DQA`) · 스킴 `dqa-connect` · 역-DNS `com.masangsoft.dqa-connect`** (2026-09-03 개명 + 2026-09-04 `DISPLAY_NAME` 분리, 정본 `shared/dqa_identity.py`). ⚠ 창·트레이·대화상자·바로 가기에 «Connect» 가 남으면 서비스와 별개의 무언가로 읽혀 **보이는 이름만** 바꿨다 — 설치 폴더·`AppId` 까지 바꾸면 업그레이드가 아니라 **두 벌 설치**가 된다. ⚠ 스킴을 `dqa` 로 줄이지 않는다 — URL 에 세션 토큰이 실리고 스킴은 **마지막 등록자 승** 머신 전역 네임스페이스다. MCP 키는 로컬이라 짧게(`dqa`). **하드 컷오버** — 옛 스킴 미등록, setup 재실행이 옛 홈·옛 핸들러를 지운다.
- **연결 상태는 3상태다** — 「모른다」를 「정상」으로 말하지 않는다. ⚠ 다만 **게이트 축의 fail-open 은 의도된 것**(`ai_blocked()` — 조회 실패 시 연결됨으로 간주)이고 표시 축과 별개다. 두 축을 섞어 「fail-open 은 폐기됐다」로 읽으면 오정정한다.
- **브리지 러너의 정본은 `src/agent/` 모듈군(20)이고, 배포되는 단일 파일은 «빌드 산출물»이다.** 소스만 고치면 **배포는 성공하고 러너 다운로드만 404** — 배포 스크립트가 하드 게이트를 건다.
- **KB 근거는 도구 호출이 아니라 «자동 주입»으로 도달한다** — `claim_request` 응답의 `kb_context`/`kb_notes`. ⚠ 배치가 러너 `compose_prompt` 에 있어 **구버전 러너에는 안 실린다**.
- **클라이언트는 자기 버전을 알고 새 버전을 받아 설치한다** (09-07). 정본은 한 방향이다 — 버전 `src/client/version.py`(**수치 비교**: 문자열이면 `1.10.0 < 1.9.0` 이 되어 열 번째 릴리스가 낡은 것으로 읽힌다) → 빌드가 `.iss` 에 `/DAppVersion=` 주입 → `publish_release.py` 가 호스트 `artifacts/client-release` 에 반입 → 서버 `routers/client_release.py` 가 **실물과 대조해 통과한 것만** 광고 → `client/updater.py` 가 받는다. ⚠ 규율은 러너 `agent/selfupdate.py` **이식**이다(같은 위험을 두 번 다르게 풀면 한쪽만 고쳐진다): **서버가 준 URL 을 쓰지 않고** 코드의 고정 경로 + TOFU 고정 서버 + 사내 CA 만 앵커로 쓴다 · `remembered_base`(수락한 딥링크 주소)는 **쓰지 않는다**(창을 여는 근거와 실행 파일을 받는 근거는 세기가 다르다) · 크기·sha256·PE·상한 **4축** · 더 새것일 때만 · 실패는 조용히. ⚠ **적용은 확인 후**다(사용자 결정) — 미서명 배포라 무음 자동 설치는 「알 수 없는 게시자」 실행을 사용자 모르게 하는 일이고, 자동은 홈 `update.json` 토글로만 켜진다. 입구는 **트레이 [업데이트 확인]** 하나다(웹 패널 표시는 `static/**` 이라 PB-0008 cycle 로 미룸). 설치기는 우리를 닫고 `/RELAUNCH` 로 다시 띄운다 — Windows 가 실행 중 exe 를 잠그기 때문이다.
- **기능 동작 순서도는 `wiki/Flows/` 에 있다.** `Architecture/Data-Flow` 는 컴포넌트 축, Flows 5문서가 사용자 행위 축. 정본은 `docs/`·`unit/<id>/docs/`, Flows 는 mirror.
- 우측 오버레이 사이드 패널(첨부·실행 단계·프로필)은 **한 번에 하나만** — 등록부 `static/app/side-panels.js` 가 choke point.
- **나가는 프롬프트는 신뢰등급 3구획**(수행할 작업 / 참고 맥락 / 비신뢰 데이터).

## Recent Changes
- (09-07) **「연결 준비」·터미널·AI 지시문 전면 제거** — 연결하는 길은 DQA 앱 하나. 전제 잠금 테스트 **12건을 지우지 않고 뒤집었다**(「지시문을 서버에서 받아라」→「지시문 표시가 되살아나지 않는가」·「닫을 때 토큰을 지워라」→「토큰을 넣는 자리가 있는가」). ⚠ 부수 소득: 행위 하네스 둘이 **2026-09-04 이후 한 번도 돌지 않고 있었다**(상대 import 미해소 · 하나는 인자 없이 부르면 usage 만 찍고 **exit 0**) — 문서에는 「25/0 PASS」가 남아 있었다. 되살려 **40/0 · 18/18 PASS**. 제품 결함 1건: 창을 여는 순간 상태 조회가 끝나기 전이면 [내 AI 실행] 이 영영 안 나타났다(알게 된 자리에서 한 번 더 준다)
- (09-07) **임베딩 없는 KB 검색을 외부 AI 에 연결** — `pg_trgm` 문자 후보 + 외부 AI 의미 판단(ADR-20260907T185500) · 승인·active 샘플만 · MSSQL 레거시 제외 · 읽기전용 5초 timeout · **203 PASS**
- (09-07) **로컬 LLM 전면 폐기** — `local-llm-edge`/gateway + `embed-ollama` 제거. 활성 `model_name` **0개**(`model_list: []`)로도 게이트웨이 기동 실측(liveliness/readiness 200) · 라이브 전용 fail-open 3경로 폐쇄 · 의도적 미적용 3건은 ADR-20260907T175000 로 영구화 · `titan-embed` 보존 계약(feature-0043 AC-7)은 supersede
- (09-07) **연결됐는데 모델 목록이 비던 상태 해소** — CLI 자체 카탈로그(`codex debug models`) 를 `_probe` 맨 앞으로 + 확인될 때까지 배경 재시도. 적대 리뷰 P1 3건(건강 축 오염·원장 화석·무음 강등) 조치 — **내가 쓴 테스트가 그 P1 들을 통과시키고 있었다**
- (09-07) **로그인이 앱 재실행을 견딘다**(세션 쿠키에 수명) · 앱 창 상태 칩 자리 고정 · 자기 자신 실행 승인창 차단 · **스탬프 없는 모듈 참조로 배포가 브라우저에 도달하지 못하던 결함**(`?v=` 전수 census) · 라우트 골든 스냅샷 재생성(main 선재 적색 복구)
- (09-07) **Codex 호환을 Claude 와 나란히** — 한쪽을 다른 쪽으로 교체하지 않는다(`docs/CODEX_MIGRATION.md` · `.codex/commands/`). 템플릿 v3.53.0 → **v3.54.0**(autonomy·verification 정책)
- (09-07) **클라이언트 업데이트 채널** — 버전 정본·반입(`publish_release.py`)·서빙(`GET /api/ai/client/latest`)·수신(`client/updater.py`)이 한 바퀴 돈다. ROADMAP §10.2 해소. 실 TLS 서버 **종단 8단계 PASS**(받은 바이트가 올린 바이트와 byte-동일 · 파일이 바뀌면 버림 · 서버는 갈린 상태를 광고 안 함) · 결함 주입 **15/15 FAIL**(G11-b) · 506/506 · `main` 697ffa84 배포 완료. 부수 적발: `test_connect_guidance` 의 함수 본문 추출이 `{0,1200}` **문자 예산**이라 함수가 길어지자 「함수를 찾지 못했다」로 죽었다 → 경계를 **구조**로 옮김
- (09-04) **설치본 종단간 확인** — `main` `a8f58f46` 재빌드 → 덮어 설치(`/VERYSILENT` exit 0) → **실행**: 창 [X] 뒤 `tasklist` 1건 유지 · 트레이 [종료] 뒤 0건. 종료는 트레이 창에 `WM_COMMAND` 를 보내 재현(합성 마우스 팝업은 이 저장소가 이미 실패를 기록한 방법). 이어 최종 설치본 라이브 완주 — 창 제목 `DQA`(연결 프로그램 자신의 창) · 브리지 동일 프로세스 · **딥링크 없이 아이콘만으로** 서비스가 열린다
- (09-04) **업그레이드가 옛 이름의 잔재를 남기지 않는다** — 보이는 이름을 «DQA Connect»→«DQA» 로 바꾼 뒤 덮어 설치하니 시작 메뉴에 바로 가기가 **두 벌**. Inno 는 자기가 더 이상 만들지 않는 바로 가기를 치우지 않는다(제거 때만 지운다) → **덮어 설치한 머신에서만** 나는 결함이라 깨끗한 환경 검증으로는 안 보인다. `[InstallDelete]` + `UsePreviousGroup=no`(폴더 이름까지 도달하는 나머지 절반) 로 해소 — 실측 후 그룹 폴더는 `DQA` 하나·바로 가기 둘뿐
- (09-04) **아이콘만 눌러도 앱이 뜬다**(인자 없는 실행) — 이 프로그램은 **웹의 부속물**이라 딥링크로 켜질 때만 성립했고 시작 메뉴·바탕화면·[지금 실행]·자동 시작은 전부 「연결 정보가 없습니다」였다. `server.json` `base` → `last` → 설치 폴더 `service.json` 3단 해석 + 목적지 검증 + 단일 인스턴스 OS 잠금이고, 두 번째 실행은 대화상자가 아니라 **창을 다시 연다**
- (09-04) **러너가 WSL 안의 AI 를 직접 찾는다** — `--cmd` 우회를 고치지 않고 **없앴다**(재도입은 회귀로 못 박힘). 탐지 순서 지정경로 → PATH → 표준 설치 위치 → WSL 안이고 POSIX 절대경로는 `wsl.exe -e` 로 확장하되 **런타임 종류를 바꾸지 않아** 능력 협상이 그대로 돈다 → 웹 「답할 AI 있음」 ✅(PB-0008 실측)
- (09-04) 닫기=트레이·종료=우클릭 [종료] 를 **세 껍데기 공통 계약**으로 — 존재→**생존** 판정 통일(`_tray_alive`) · `Bridge.resident` 읽기전용화 · 첫 닫기 1회 안내(닫힘 이벤트를 **소유한** 두 껍데기에만 — 브라우저 껍데기의 추정 안내는 codex 2라운드가 되받아 되돌림). 실 Windows 하네스 `verify_embedded_close.py` 신설(하네스 결함 2건 선적발: **제목으로 창 특정**(트레이도 같은 제목의 창을 만든다) · **데몬 워커 절단**(마지막 단정이 결과 파일에 없었다))
- (09-04) 앱 창을 **WebView2 로 프로세스에 내장**(PR #1576) — 껍데기 3단(내장→브라우저→tkinter)
- (09-04) 상주를 두 껍데기 공통으로 재구성(웹 셸 주 경로에도 트레이 · 패널 문구는 `status.resident` 판정) — 하네스 결함 2건(테스트 행·문법파손 뮤턴트) 선적발
- (09-04) 상주 안내를 **주 표면**(앱 창이 여는 `index.html` 연결 패널)으로 이관 — `ai-connect.*` 는 별도 페이지라 주 경로 사용자가 못 봤다. 그 패널을 **구동**하다 `ReferenceError: _status is not defined` 적발(자유변수 → `openConnectModal()` 까지 전파 → **연결 창 자체가 안 열림**). 병렬 세션이 같은 증상을 방금 고쳤지만 **원인이 둘**이었다
- (09-04) 트레이 상주 + 자식 콘솔 창 제거(feature-0046) — 실 Windows 실측이 ctypes argtypes·`MF_DEFAULT` 2결함 적발, codex P1 4건 전부 수정, 뮤테이션 21/21
- (09-04) doc_sync — 신규 feature-0046 색인 배선(self-add 카드가 **orphan** 이었다) · feature 카운트 45→46 **7표면** · `shared/*.py` 18→19 2색인 · 릴리즈노트 09-03 블록 신설
- (09-03) Windows 네이티브 클라이언트 초판(ROADMAP ITEM-08) · 클라이언트 명칭 개명 + 명칭 SSOT · 연결 퍼널 계측(ITEM-00) · 연결 상태 3상태 · 아픈 러너 즉시 응답(150초→체감 0초) · 응답없는 AI 무한대기 폐쇄 · 연결 단계 체크리스트(ITEM-03·ITEM-06) · SPIKE-02 판정(conditional-go)
- (09-02) 러너 19모듈 분할 + 배포본 이중화 제거 · 러너 자기 갱신 + 런처 CA 핀 · Windows 명령줄 상한/cp949 · 능력 질의 가드(66→6.6초) · KB 근거 자동 주입 · `wiki/Flows/` 신설

## Active Threads
- ⚠ **`docs/SECURITY.md` 가 feature-0046 의 두 공격 표면을 모른다** — 미서명 exe 배포·로그인 대행 실행·러너 수신 / `dqa-connect://` 딥링크에 실리는 세션 베어러 토큰 + 마지막-등록자-승 스킴 + 하드 컷오버. 09-04 doc_sync 가 §50·§51 신설을 제안했으나 적대 검증이 §49.3 과의 상호작용 결함을 지목해 fail-closed 보류. **3창 연속 미해소**(09-08 재실측: `docs/SECURITY.md` 안 feature-0046 히트 **0** · 마지막 절은 여전히 §49.6). ⚠ 소유권은 doc_sync 가 아니다 — 같은 창의 뒤 판정(`meta/REVIEW.md` REV-20260904T190701 · ROADMAP ITEM-09 guard 6)이 **착수 cycle 의 몫**으로 옮겼다. 위협모델 절 신설은 미러 정합이 아니라 정본 저술이라 §18.8.1 적대 패널을 요구하고, doc_sync run 은 그 패널을 돌리지 않는다. 게다가 09-04 창이 **표면을 더 넓혔다** — 로컬 loopback HTTP 브리지(서비스 origin 에 열려 XSS 가 사용자 머신 프로세스 실행으로 이어진다, `tests/test_bridge_security.py` 21건이 잠금) · WebView2 프로세스 내 호스팅 · 업그레이드의 바로 가기·레지스트리 잔재 삭제 — 이므로 직전 §50·§51 초안은 이미 불완전하다.
- **연결 체크리스트 단계 수 불일치**: 정본 `TASK.md` 는 「5단계」, `_connect_steps.py` 의 `key` 는 **4개**.
- **개명·클라이언트의 라이브 미검증** — 실 OS 핸들러 재등록·옛 잔재 삭제는 사용자 머신 setup 재실행 후에만 관측된다. macOS 경로는 실측 수단 부재.
- **ITEM-08 후속 미완** — 자동시작으로 «바로 연결»(토큰을 저장하지 않아 로그온만으로는 연결 정보가 없다 — 토큰 보관 설계 선행) · 벤더 설치기 실행. **자동 업데이트는 09-07 에 섰다**(확인 후 적용, 자동은 홈 설정 토글·기본 꺼짐) — 남은 것은 웹 패널 표시(`static/**` → PB-0008 cycle)와 설치기 실제 실행 실측.
- **패널의 상주 문구는 아직 초기화 1회 스냅샷** (`client-bridge.js`) — 브리지는 매번 새로 판정하지만 화면 문장은 낡을 수 있다. 고치는 자리는 20초 ping 틱(1~2줄)이나 `static/**` 변경이 check #13 을 켜고 패널을 띄우면 `discover` 가 **사용자 AI 사용량**을 쓴다 → PB-0008 cycle 에 묶는다.
- **설치본 배포 채널은 섰다 (09-07)** — 리눅스 파이프라인이 `.exe` 를 만들지 못하는 것은 그대로이므로 **반입 경로**를 세웠다: Windows 에서 빌드 → `publish_release.py` 로 호스트 `artifacts/client-release` → compose 가 `/srv/client:ro` 로 붙임 → 서버가 **실물과 대조해 통과한 것만** 광고. 「연결 프로그램 받기」는 이제 실물이 있으면 뜬다. ⚠ 남은 것은 **설치기 실제 실행**(`/SILENT /RELAUNCH`) 미실측 — Windows 빌드 머신이 필요하다. ⚠ 릴리스 디렉토리 **첫 생성은 사람 몫**(없으면 docker 가 root 소유로 만들어 퍼블리시가 막힌다).
- **트레이 미실측** — 남은 것은 **탐색기 재시작(`TaskbarCreated`) 후 재등록** 하나다. 동결 exe 아이콘 추출 · **설치본을 실행한** 닫기(1건 유지)/종료(0건, 09-04 20:45) · 웹 셸 패널의 **PB-0008**(배포 `5ab60093` — 앱 창 안 패널 기동 · AI 3종 탐지 · 연결 버튼 활성 · WSL caps ✅)은 전부 실측 완료다.
- 「프로필 열린 상태에서 첨부 열기」는 backdrop 이 클릭을 먹어 실 브라우저 도달 불가 — jsdom 계약으로만 잠김.
- **취소 후 `submit_answer` 409 집행 미검증** — 러너가 제출 전에 하차하므로 그 경로에 닿지 않는다.
- **동기 inproc 경로(`_ask_impl`)의 첨부 쓰기는 세 번째 구현** — `shared/attachment_write.py` 합치기는 별도 cycle.
- **feature-0043 합본 테스트 14건·feature-0003 7건은 선재 order-dependent flake** (main 과 차집합 0, 격리 실행 PASS).
