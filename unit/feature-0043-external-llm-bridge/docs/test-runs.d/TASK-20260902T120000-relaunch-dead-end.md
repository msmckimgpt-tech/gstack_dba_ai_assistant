---
run_at: 2026-09-02T12:00:00+09:00
session: ai/claude-corp/feature-0043-stale-relaunch-dead-end
scope: 「업데이트 필요」가 재실행으로 풀리지 않는 막다른 길 + 컴포저 안내 문단 레이아웃
verdict: PASS (결함 라이브 정량 + 수정 + 뮤테이션 9종 KILL) — 구 런처 원격 복구는 불가로 명시
---

# Run — TASK-20260902T120000 재실행이 파일을 바꾸지 못할 때

## Environment: Windows-browser — **결함을 배포본에서 정량화**

`bin/win-browser.py` relay → 실 Windows Chrome, `https://localhost/` (배포 `2ffe054d`).

안내 문단을 켜 보고 입력창 폭을 잰다(끄고 되돌린다 — 읽기 성격의 측정):

| 관측 | 값 |
|---|---|
| 문단의 부모 | **`composer-box`** ← `display:flex` 한 줄 |
| 안내 꺼짐 → 입력창 | **1393px** |
| 안내 켜짐 → 입력창 | **922px** |
| **밀림** | **−471px (−34%)** · 문단이 같은 줄에서 **464px** 차지 |

사용자가 「대화창 UI가 무너진다」로 본 것이 이 수치다. 형제인 두 팝업은
`absolute`/`fixed` 라 줄에서 빠져 있고 이 `<p>` 만 정적 흐름이라 **플렉스 항목**이 된다.

## 근본 원인 실측 — 갱신이 왜 안 되는가

| 관측 | 값 |
|---|---|
| `~/.mysql-ai-bridge/launch.sh` | **10:16** 생성 · 자기 갱신 블록 **없음**(파일 직접 확인) |
| 갱신 블록이 들어간 커밋 | `342d1039` **10:44:26** |
| 러너 로그 11:28 재기동 | `run.start build=937b18be70eb` → `hb.stale_build` |
| 배포본 | `2ffe054d`(갱신 블록 포함) · 라이브 `bridge_setup.sh` 에 블록 **있음** |
| 러너 자기 갱신 기능 | **0건**(`execv`/self-update 부재) |

28분 차이다. 그래서 자동 실행이 정확히 동작해도 **같은 낡은 러너가 다시 뜬다** — 그리고
이미 설치된 그 런처를 서버가 원격으로 바꿀 통로는 없다.

## 검증

| 대상 | 결과 |
|---|---|
| `tests/test_relaunch_dead_end.py` **16건**(신규) | **PASS** |
| ├ node 실행 하네스(모듈을 실제로 구동, 8 시나리오) | PASS |
| └ 구조·조상사슬 단언(노드 없는 환경에서도 동작) | PASS |
| 뮤테이션 **9종**(M1~M9) | **전건 KILLED** |
| `feature-0043` + `feature-0003` 테스트 전량 (컨테이너) | **rc=0** |
| ruff (`feature-0003/src`, `feature-0043`) | clean |

### 뮤테이션이 잡은 설계 결함

M6(증거 해제 제거)이 처음엔 **SURVIVED** 했다 — 시나리오가 «갱신 성공 → 러너 꺼짐» 으로
끝나 `_isStaleNow` 가 거짓이 되면서 해제 없이도 통과했다(항진명제). 해제가 실제로 필요한
두 상태(«파일은 바뀌었는데 여전히 낡음» · «한 번 최신에 도달한 뒤 다시 낡음»)로 다시 쓰니
양쪽에서 KILLED.

## 시도했으나 채택하지 않은 검증

headless chromium 으로 `index.html` 을 직접 띄워 폭을 재려 했으나, JS 없이는 앱 셸이
서지 않아 입력창이 flex-grow 하지 않는다(수정 전/후 모두 182px 고정) — **결함을 재현하지
못하는 환경**이라 그 0px 는 근거가 되지 않는다. 레이아웃은 실 브라우저가 정본이다.

## POST-DEPLOY 실측 — 배포 `293a20c8`

`bin/win-browser.py` → 실 Windows Chrome, 하드 리로드 후.

| # | 관측 | 결과 |
|---|---|---|
| P1 | 안내 문단의 부모 | **`composer-wrap`** (입력창 줄 **밖**) |
| P2 | 안내 꺼짐 → 입력창 | 1393px |
| P3 | 안내 켜짐 → 입력창 | **1393px** — **밀림 0px** (배포 전 −471px) |
| P4 | 문단 자리 | 폭 1612px · 입력창 **위** (`noteAboveInput=true`) |
| P5 | 서빙 JS 새 심볼 | `_relaunchChangedNothing`3 · `_relaunchNoUpdate`8 · `_showRelaunchNoUpdate`3 · `MSG_RELAUNCH_NO_UPDATE`3 |
| P6 | `connect_status` | `runner_build` 필드 **존재** (러너 미대기라 `null` — tri-state 계약대로) |
| P7 | 서빙 JS ≟ 테스트한 소스 | 자산 스탬프 주입 **1줄**(`?v=dev` → `?v=502612dc9646`) 외 **완전 동일** |

P3 이 이 cycle 의 정본 관측이다 — 안내가 켜져도 입력창이 그대로다. P7 은 「테스트한 코드 =
배포된 코드」를 닫는다(자산 스탬프는 빌드가 주입하는 유일한 차이).

### 라이브에서 닫지 못한 것 — 정직하게

「같은 지문으로 재기동 → 새 안내」의 **사용자 눈 확인**은 하지 못했다. 그 상태가 성립하려면
계정에 **낡은 러너가 실제로 떠 있어야** 하는데 지금은 `listening=false` 다(제보자가 러너를
내려 둔 상태). 그 러너를 대신 띄우는 것은 사용자 환경을 손대는 일이라 하지 않았다.

그 경로는 node 실행 하네스가 모듈을 실제로 구동해 8 시나리오로 덮었고(뮤테이션 9종 KILL),
배포 실물이 그 소스와 동일함이 P7 로 확인됐다. 사용자가 다음에 연결하는 순간이 곧 그
관측 시점이다.

## 이월 (이 cycle 이 만들지 않은 것)

- **이미 설치된 구 런처의 원격 복구**: 불가능. 1회 재설치가 필요하고, 그 뒤로는 자동이다.
  이 변경은 그 1회가 **어디에 있는지 화면이 정확히 지목하게** 만든다.
- **러너 자기 갱신**: 같은 부트스트랩 한계라 지금 겪는 사용자에게 도움이 되지 않고,
  `bridge_agent.py` 는 현재 `feature-0043-runner-modularization` 세션의 hot path 다.
