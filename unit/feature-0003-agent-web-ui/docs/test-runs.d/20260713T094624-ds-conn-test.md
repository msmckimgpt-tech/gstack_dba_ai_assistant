### Run (2026-07-13) — ds-conn-test: 작업화면 제품 드롭업 데이터소스 라벨 '연결 테스트' 버튼 + 상단 단발성 토스트 (Major §12.3 — feature-0003 web/UI(app.js/admin.js/styles.css) + 백엔드 throttle(routers/admin_datasources.py)) — **Environment: Windows-browser**

- **REQ (`/_template:entry` arg-given)**: 채팅 작업화면 제품 목록에서 연결된 데이터소스의 연결 여부를 테스트하는 버튼(데이터소스 라벨을 클릭) + 관리 콘솔 '연결 테스트'처럼 단발성 Toast, 단 작업화면 토스트는 입력창을 가리지 않도록 화면 상단부터 표시.
- **설계 결정(AskUserQuestion 2026-07-13)**: ① 테스트 방식 = **A2** 관리자 엔드포인트(`POST /api/admin/datasources/{key}/test`) 재사용 + 남용 방지 프론트/백 재시도 텀. ② 상단 토스트 범위 = **C2** 작업화면 전체(`#toast` id 오버라이드, admin `#adminToast` 하단 불변).

- **pre-commit 자동 검증 (PASS)**:
  - `node --check` app.js PASS · admin.js(ES module) PASS · CSS brace 균형(1780/1780) · `py_compile` admin_datasources.py + 테스트 PASS.
  - 타깃 pytest `test_datasource_test_nonblocking.py` **6/6 PASS**(기존 4 + 신규 throttle 계약 2: 반복→429 throttled·다른 account/key 독립·404 무-throttle).
  - **전체 스위트 `feature-0002 + feature-0003` = 1902 passed, 2 skipped, 0 failed**(회귀 0, repo-agent 이미지).
  - `verify_profile_icon_consistency.mjs` 하네스에 `canOpenAdminConsole` 스텁 추가(무권한=display-only 경로 유지) — 컨테이너(jsdom) 실행 대상.
- **§18.8 적대 패널(3 렌즈, REV-20260713T094624-ds-conn-test)**: backend+security(BLOCKING/MAJOR 0)·frontend+QA(HIGH 테스트 하네스·MED-HIGH 더블클릭·MED 쿨다운 sentinel)·UX+a11y(MAJOR 중첩인터랙티브·at-rest 어포던스·접근가능한 이름) — **적발 결함 전건 반영 후 재검**. 상세 REVIEW.md.

- **Environment: Windows-browser — pre-commit 라이브 미수행 사유**: 정적 자산(app.js/admin.js/styles.css)이 web 이미지에 baked 라 서빙 자산은 merge + `make deploy-web`(web 재빌드) 후에만 갱신된다(리포 확립 패턴 — cache-buster 는 빌드 `inject_asset_stamp.py` content-hash 자동 주입, html 수기 bump 안 함). win-browser relay(172.26.144.1:9223) 가용 확인됨 → **POST-DEPLOY PB-0008 라이브 append**.
- **Pass/Fail: pre-commit 정적·문법·단위(1902) PASS · 라이브 = POST-DEPLOY**. CHECK#13 충족(웹 자산 변경에 이번 cycle Windows-browser Run + POST-DEPLOY 계획·미수행 사유 기록).

- **POST-DEPLOY PB-0008 라이브 검증 항목(append 예정)**:
  - AC-1: 작업화면 composer '+' 제품 드롭업 → 각 제품 행 데이터소스 라벨이 **버튼**(테두리·hover 틴트·min 24px) 으로 렌더(admin 권한 계정).
  - AC-2: 데이터소스 버튼 클릭 → **화면 상단**(topbar 아래) 단발성 토스트("…연결 테스트 중…" → "✓ 연결 성공 (Nms)" / "✗ 실패") — **입력창(composer)·드롭업 비가림**.
  - AC-3: 다중 DS 제품 → "N개 데이터소스" 버튼 클릭 → 요약 토스트 1개("N개 중 M개 연결 성공 …").
  - AC-4: 버튼 클릭이 제품 선택으로 전파 안 됨(메뉴/활성 제품 불변) · 키보드(Enter/Space)로도 테스트 트리거.
  - AC-5: 빠른 재클릭 → 프론트 쿨다운 토스트("N초 후 다시") 또는 백엔드 429 중립 토스트("너무 잦습니다").
  - AC-6: 관리 콘솔 `#adminToast` 하단 유지(회귀 0) · admin 데이터소스 '연결 테스트' 배지 프로브 429 무해(healthy 배지 유지).
  - AC-7: pageerror 0.
