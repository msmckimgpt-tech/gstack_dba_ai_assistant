---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0003-agent-web-ui
agent: qa-ux
timestamp: 2026-09-08T14:20:00+09:00
trigger: 라운드 2 확인 라운드 (§18.8 수렴 계약 (a)) — 게이트 유효성 + 화면 어휘 재감사
verdict: BLOCK
---

뮤테이션 주입 실험 + headless Chromium(playwright, 실 CSS, Noto Sans CJK KR) 실측.

## Half A — 라운드 1 P1 폐쇄 확인

CLOSED — `_fn_body` 주석 제거(무력화 실험으로 load-bearing 실증) · node 전제 정정(hard fail)
· 항진 음성대조군 · 배지 폭(340px 30종 전건 1행, 300px 은 예외 2종만 2행 = 예외 목록과 정확히
일치). PARTIALLY — census(조회 17 + 무조건 합산) · 진행/완료 동치(저장문구 가지가 1-표본) ·
백틱(툴팁 1줄 누락) · `intent`(payload 잔존).

### 1. Blocking issues

- **[1] (high) L7 가드의 모수가 `app.js` 한 파일뿐.** `progress.js:93` 을 수정 전 폴백으로
  되돌려도 **48건 전건 PASS**, 하네스도 rc=0. docstring 과 FUNCTION.md AC-4 는 「렌더러 전수」를
  선언한다.
  - Location: `tests/test_step_tool_syntax_leak.py:484`
  - Action: 모수를 `app.js` + `app/*.js` 합집합으로 넓히고 progress.js 주입 대조군을 추가하라.
- **[2] (high) `intent` 가 완료 답변·진행 폴링 payload 에 그대로 남는다.** 진행 경로에는
  `assert "intent" not in steps[0]` 가 있는데 완료 경로에는 없다.
  - Location: `src/routers/_conv_store.py:4228`
  - Action: `item.pop("intent", None)` + 완료 경로에도 대칭 단언.
- **[3] (high) census 폴백 무조건 합산 → `test_l2_census_…` 가 실패할 수 없다.**
  `modules.tools` import 를 강제 실패시켜도 census 25 로 그대로 PASS(실행 확인). 조회 산출은
  17종이고 6종이 오직 손 목록에서만 온다.
  - Location: `src/routers/_conv_store.py:6337`
  - Action: 폴백을 조건부로, 테스트는 **조회 산출**을 단언하게.

### 2. Cross-domain concerns

- **[4] 대기 말풍선·진행 요약이 라벨을 두 번 말한다** — 「테이블 구조 · 테이블 구조 단계」
  (라운드 1 수정이 만든 신규 결함). `work !== lbl` 는 `work === lbl + " 단계"` 를 못 잡는다.
- **[5] test-runs.d / REPORT.md 수치가 코드와 어긋난다** — 하네스 15항목·주입 8 FAIL·라벨
  30종·최장 7자·배지 폭 측정 완료. REPORT.md 는 (c) 폐기와 「(c) 를 좁혔다」를 같은 절에 함께
  적어 자기모순.
- **[6] 라벨 어휘 2종** — `scratch_reset`「임시 삭제」는 «임시로 삭제(되돌릴 수 있다)» 로
  먼저 읽힌다. `list_schemas` 배지「DB 목록」↔ 제목「사용자 스키마 목록」 명사 불일치.
- **[7] `scratch_import`/`scratch_sql` 콜러블이 인자를 받고도 대상을 버린다** — AC-4 가
  없앴다고 선언한 비대칭이 같은 표 안에 남았다.
- **[8] `_code_only` 가 줄머리 `//` 만 지운다** — 꼬리 주석·블록 주석 변형이 통과(실행 확인).
- **[9] L5 census 전수 모수가 «저장 문구» 가지를 안 태운다** — 정화 우회를 주입해도 전수
  테스트는 PASS, 1-표본만 FAIL.
- **[10] `test_side_panel_exclusive::test_s6` 가 node 설치로 실행 경로에 들어와** jsdom 유무로
  결과가 갈린다. 그 파일의 「node 없음」 docstring 은 이제 사실이 아니다(선재 실패 2건 —
  main 판 자산으로 되돌려도 동일).
- **[11] activity 툴팁만 백틱을 안 벗긴다.**

### 3. Challenge to current spec

규칙 (a) 에는 산문 예외가 없다 — 정상 제목 `설정값 {'theme': 'dark'} 이 든 컬럼을 확인한다` 가
서버·프런트 두 층에서 강등된다. JSON 컬럼이 일상 어휘인 도메인이라 (c) 를 죽인 논거가 약한
형태로 적용된다. 미지 도구 행은 여전히 무정보 행이다. `_LABEL_BUDGET = 6` 은 글자 수이지
폭이 아니다 — 「SQL 실행」 59.8px vs 「테이블 목록」 65.3px 로 같은 6자다.

### 4. Verdict

BLOCK
