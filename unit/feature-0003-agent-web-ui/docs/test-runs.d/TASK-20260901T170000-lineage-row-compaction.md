---
run_at: 2026-09-01T17:00:00+09:00
session: ai/claude/feature-0003-lineage-row-compaction
scope: 계보 내부 파일명 되풀이 제거 + 행 한 줄 간소화
verdict: PASS
---

## Run — TASK-20260901T170000-lineage-row-compaction (PRE-DEPLOY 라이브)

- Date: 2026-09-01 · Environment: **Windows-browser** (PB-0008)
- Target: 격리 컨테이너 `web-verify-rc` (static stamp `a3db80724568`), `https://localhost:18103`
- Result: **PASS** (한 줄 목표는 **부분 달성** — 아래 실측 수치로 경계를 명시)

## 1. 되풀이 제거 (사용자 지적 ①)

| 관측 | 값 |
|---|---|
| 버전 행의 파일명 요소(`.attach-list-version-name`) | **0** (종전: 버전 수만큼) |
| 계보 안내문(`.attach-list-versions-lineage`) | **0** |
| 화면에 뜨는 raw 상태 `uploaded` | **0** |

카드 하나에서 같은 파일명이 실리던 횟수: **머리 1 + 계보행 2 + 버전행 3 = 6회 → 1회**(머리).
파일명 확인 경로는 남았다 — 버전 행 `title`, 계보 행 `title`·`aria-label`·「원문 보기」.

## 2. 한 줄 간소화 (사용자 지적 ②) — 실측

행 높이(20px = 한 줄, 38~39px = 두 줄), 패널 폭별:

| 행 | 240px | 260px | 280px(기본) | 320px |
|---|---|---|---|---|
| **버전 행** (`v2 · AI 수정 · 8/31 · 최신 · 👁⬇🗑`) | 20 | 20 | **20** | 20 |
| 계보 행 — 사람 업로드 (`사용자 업로드 · 1KB · 8/31 · 상세 ▾ · ⬇🗑`) | 38 | 38 | **20** | 20 |
| 계보 행 — AI 수정본 (`AI 수정본 v2 ⤷ · 9KB +8KB · 8/31 · 버전 2개 ▾ · ⬇🗑`) | 56 | 39 | **39** | 39 |

**버전 행은 전 폭에서 한 줄**이 됐다 — 2줄이던 근거가 파일명이었고("한 줄에 몰면 파일명이
2자로 남는다") 그 파일명을 이 cycle 이 걷어냈기 때문이다.

**AI 수정본 행은 기본 폭에서 한 줄이 불가하다.** 실측 근거:

```
info 가용폭 206px   vs   필요폭 268px  (정체성 79 + 정보 135 + 액션 46 + 간격 8)
→ 62px 부족. 한 줄이 되려면 패널이 ~342px 이거나 토막을 빼야 한다.
```

토막을 빼면 각각 되찾는 폭: `버전 2개 ▾` ≈ 52px · `+8KB` ≈ 34px · `8/31` ≈ 30px.
**어느 것도 정보 손실이라 이번 cycle 에서 임의로 빼지 않았다** — 선택은 사용자 몫으로 남긴다.

이번에 되찾은 폭(정보 손실 0): 액션·배지 여백 축소 · 행 간격 8→4px · 레일 들여쓰기 12→9px ·
카드 좌우 패딩 7→5px · 분기 칩 문구→글리프(`⤷`, 말은 title/aria) · 크기·델타 한 토막화.
그 결과 **사람 업로드 행이 280px 에서 두 줄 → 한 줄**로 넘어갔다.

## 3. 캡처가 잡은 결함 1건

분기 칩을 글리프 한 자로 줄이자 파선 pill 안에 `⤷` 만 남아 **빈 동그라미처럼** 보였다
(`compaction.png`). 테두리·배경을 걷고 글리프 자체를 표식으로 바꿨다 — 「색 단독 의존 금지」
요건은 파선이 지던 역할을 **글리프**가 이어받아 유지된다.

## 4. 회귀

pytest `feature-0003`+`feature-0002`+`feature-0023` **5177 passed / 5 skipped**(신규 4건 +
계약 이전 6건) · `node --check` PASS.
⚠ `test_query_embed_visibility.py` 2건은 **main 기준선에서도 동일 실패** — 본 변경 무관.
