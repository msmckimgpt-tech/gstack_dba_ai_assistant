### Run (2026-07-16) — ds-test-gate-fix: 작업화면 데이터소스 '연결 테스트' 버튼 렌더 회귀 수정 (Minor §12.3 — feature-0003 web/UI app.js 1줄) — **Environment: Windows-browser**

- **회귀 신고(사용자)**: 07-13 출하·PB-0008 PASS 한 작업화면 데이터소스 '연결 테스트' 버튼이 이후 다른 세션 작업으로 회귀(버튼 미표시).
- **근본원인**: perm-atomic-split 커밋(`8e01cc24`, 07-15, Critical)이 프론트 게이트를 `!viewOnly && canOpenAdminConsole() && Boolean(state.user?.permissions?.["datasource.test"])` 로 변경. 그러나 이 코드베이스는 TASK-0098 로 프론트 `state.user.permissions` 의존성을 제거("표시 허용 + backend 403 fallback" — `can()` 컨벤션)했고 `/api/session`(system.py) 은 `user.permissions` 를 직렬화하지 않는다(라이브 b8658bee 세션 실측 `hasPermissions:false`). → `state.user.permissions` = undefined → 게이트 항상 false → **DS 테스트 버튼 전부 미렌더**.
- **수정(app.js 1줄)**: `Boolean(state.user?.permissions?.["datasource.test"])` → `can("datasource.test")`. `can()` 은 로그인 사용자에게 true(display-permissive)를 반환하고 실제 거부는 백엔드 403(`admin_test_datasource` 가 console.access+datasource.test 요구) + apiFetch 공통 토스트가 처리 → 코드베이스 컨벤션 정합. **보안 무영향**(백엔드 enforcement 불변, 표시 계층만 복구). 순 게이트 = `!viewOnly && canOpenAdminConsole()`(07-13 출하 동작과 동일).
- **관련 미해결(별도 소유)**: 같은 커밋이 app.js 권한 표시 UI(≈L2006 `Object.entries(state.user?.permissions || {})`)도 동일하게 부재 맵을 읽어 빈 값 — 본 회귀와 동일 뿌리이나 다른 기능이라 소유 feature 에 flag(본 cycle scope 밖).
- **pre-commit 검증(PASS)**: `node --check app.js` PASS · `can()` global fn 확인 · 회귀 하네스 `verify_profile_icon_consistency.mjs` 는 `canOpenAdminConsole` 스텁(false)로 short-circuit → `can` 미평가(무영향). §18.8: 1줄 컨벤션-정합 회귀 복구·display-only·보안 posture 불변이라 full 패널 skip(REVIEW 근거 기록).
- **Environment: Windows-browser — pre-commit 라이브 미수행 사유**: 정적 자산 web 이미지 baked → merge + `make deploy-web` 후 서빙(cache-buster content-hash 빌드 자동주입). **POST-DEPLOY PB-0008 라이브 append 예정** — win-browser relay 172.26.144.1:9223.
- **Pass/Fail: pre-commit 정적·문법 PASS · 라이브 = POST-DEPLOY**. CHECK#13 충족.
- **POST-DEPLOY 검증 항목(append 예정)**: ① admin(console_access+datasource.test) 계정 로그인 → 제품 드롭업 데이터소스 라벨이 다시 `<button.product-dropup-item-ds--test>` 로 렌더 ② 클릭 → 상단 토스트 "✓ 연결 성공" ③ pageerror 0.

- **[POST-DEPLOY 갱신 2026-07-16] 라이브 PASS (Environment: Windows-browser, AI 직접 — win-browser relay @ 172.26.144.1:9223, 라이브 e6ca5e4b)**: PR #855 머지 → main **e6ca5e4b** → `make deploy-web` 무중단 롤링(web+워커+gateway·soak PASS). `/healthz` git_commit=e6ca5e4b. 서빙 `/static/app.js` 내 `can("datasource.test")`=2매치(fix 반영). **런타임 실측(bootstrap_admin console_access=true, 14 products)**: ① **DS 테스트 버튼 14개 재렌더**(`<button.product-dropup-item-ds--test>`·aria-label "mssql-dk-dev 연결 테스트" — 수정 전 0개였던 회귀 복구) ② 클릭 → 실 probe → **"✓ 'mssql-dk-dev' 연결 성공 (13.6ms)"** 상단 토스트(top 56px) ③ **pageerror 0**. **결론: 회귀 복구 라이브 실증 완료 — 사용자 신고 해소.**
