---
doc_type: WIKI_OVERVIEW
scope: project
status: active
edit_policy: ai-maintained
source_of_truth: false
template_version: v3.13.0
domain: [navigation, wiki, synthesis]
ai_read_priority: 7
wiki_role: article
wiki_name: project
confidence: high
maturity: substantial
ai_generated: true
sources:
  - ../docs/PROJECT.md
  - ../docs/ARCHITECTURE.md
  - ../docs/DECISIONS.md
  - ../docs/STATUS.md
  - ./Index.md
---

# Wiki — Overview (living synthesis)

| 항목 | 값 |
|---|---|
| 분류 | `#wiki/overview` |
| 프로젝트 | MySQL DBA AI Assistant (`mysql_ai_delegated_dev`) |
| 정본 sources | [[../docs/PROJECT\|PROJECT]] · [[../docs/ARCHITECTURE\|ARCHITECTURE]] · [[../docs/DECISIONS\|DECISIONS]] · [[../docs/STATUS\|STATUS]] |
| Wiki layer | mirror (single page synthesis) |

## 목차

1. [개요](#1-개요)
2. [상세](#2-상세)
     - [2.1 핵심 영역](#21-핵심-영역)
     - [2.2 핵심 결정 (ADR)](#22-핵심-결정-adr)
     - [2.3 데이터 흐름](#23-데이터-흐름)
     - [2.4 진행 중인 흐름](#24-진행-중인-흐름)
3. [외부 source 합성](#3-외부-source-합성)
     - [3.1 인용 source](#31-인용-source)
     - [3.2 합성된 결론](#32-합성된-결론)
     - [3.3 미해결 contradiction](#33-미해결-contradiction)
4. [알려진 한계](#4-알려진-한계)
5. [다음 단계](#5-다음-단계)
6. [관련 문서](#6-관련-문서)
7. [둘러보기](#7-둘러보기)
8. [외부 link](#8-외부-link)
- [분류](#분류)

## 1. 개요

본 프로젝트는 **기존 `mysql_ai` 운영 자산을 AI 위임 개발 (`ai_delegated_dev`) 템플릿 구조로 이관한 실행형 사본** 이다. 자연어 입력 → LLM tool-call loop → **N 개 데이터소스 (MySQL·MSSQL)** 에 대한 read-only DBA 작업 자동화를 41 개 feature unit 디렉토리(feature 넘버 0001~0041 — feature-0016 은 metadata-graph + zd-pg-pause-caddy 2 슬라이스라 디렉토리 2개, feature-0018 은 디렉토리 없는 슬라이스로 feature-0003 에 코드 거주) + 단일 docker-compose 로 제공한다. 정책 정본 (`AGENTS.md`) + 도메인 정본 (`docs/`) + 기능 정본 (`unit/<id>/docs/`) 의 3-tier 문서 체계로 다중 AI 가 동시에 작업 가능.

> **2026-06 현재**: 초기 단일 MySQL replica → **멀티 데이터소스** (dialect 추상화 + envelope 암호화 registry + DB-단위 접근) 로 일반화. agent runtime + KB 는 Postgres 단독 정본 (2026-05-27 이관 완료). agent 실행은 ask-worker out-of-process 큐로 cutover.
> **2026-06-19~23**: ① **사용자 보안 보강 6종** (공유 링크 만료·로그인 시도 제한·감사 변조방지·LLM 사용량 한도·프롬프트 인젝션 방지·2단계 인증) 전체 완료 — [[../docs/SECURITY|SECURITY.md]] §7.2·§12~§15. ② **그룹 대화** (feature-0009) — 멤버십·`@assistant` 멘션·열람≠발화 분리·라이브 UX. ③ **NL→SQL 정확도 flywheel** (eval harness·샘플쿼리 few-shot·self-reflection·용어/ENUM 사전) — `docs/improvements/dba-ai-nl2sql/ROADMAP.md`.
> **2026-06-24**: ④ **관리 콘솔 메타데이터 거버넌스** (용어·ENUM·테이블/컬럼 설명·AI 자동완성, ITEM-11) 로 NL→SQL 컨텍스트 강화. ⑤ **feature-0010 Google Drive 연동 토대** — 계정별 OAuth 토큰 암호화 저장 + MCP 구성 seam (연동 미수행·비활성 scaffold). ⑥ **feature-0011 `shared/` 공통 코드 추출** (P5a — `model_catalog`·`config`·`db` 모듈 alias, `make test` 회귀 0).
> **2026-06-25**: ⑦ **그룹 대화 라이브 UX 확장** (feature-0009) — 사이드바 안 읽음/@멘션 배지(read cursor)·메시지 좌우 정렬·owner 멤버 추방/차단/해제·참가자 per-message 제품 선택·처리 중 composer 비잠금/1:1 인터럽트 재요청. ⑧ **관리 콘솔 프롬프트 자동화** (feature-0003) — 역할 '전체 제품 프롬프트'·프로필 '제품별 프롬프트' AI 자동 작성·제품 분석률 95% 시 제품프롬프트 자동완성·규칙 자동추가 DB insight 커버리지 UI·'지식베이스' 메뉴 재편. ⑨ **feature-0011 `shared/` 추출 P5a Step5 완료** — config·db·conn_health·datasources 소비처 마이그레이션 + alias shim 4종 전량 제거(회귀 0). ⑩ **안정성** — init "준비" 임베딩 지연 회귀 해소·요청량 한도 메시지 주체(서비스/계정) 구분·그룹 동시 처리 고착/입력 블로킹 해소·datasource 회로차단 안내 문구 명확화.
> **2026-06-26**: ⑪ **제품 선택 chip 처리 중 항상 활성화** (feature-0003) — composer 제품 선택 chip 을 "요청 처리 중" 에도 항상 활성·사용 가능하게(TASK-0047 turn-immutability 의 3계층 가드 — 프론트 시각 disable·프론트 reject·백엔드 PATCH 409 — 일괄 완화). 제품은 `/api/ask` enqueue 시 캡처되어 in-flight 답변은 비오염, 변경은 다음 요청부터 반영. RBAC/스키마/엔드포인트 무변경(timing 가드만 제거). ADR-WEB-0006. ⑫ **assistant 요청 2번 중복 처리 차단** (feature-0003/0002) — 워커모드 `/api/ask` 의 long-poll 연결이 web 재배포로 끊겨 사용자가 재전송하면 동일 페이로드 ask_job 2개 → 요청·답변 2회 처리되던 결함을 enqueue 멱등화(dedup NOT EXISTS + 활성 중복 시 기존 run attach) + app.js status 복구 재시도로 차단. 배포 검증 중 적발된 PG AmbiguousParameter 회귀(워커모드 신규 `/api/ask` 500)는 dedup 전용 파라미터+alias 격리로 해소.
> **2026-06-29**: ⑬ **답변 피드백 답변당 고유화** (feature-0003/0002) — 답변당 사용자별 고유 👍/👎 1개 강제 (마이그 0021/0022 + id_space 보강), 새로고침·대화 전환 후 중복 부여 차단. ⑭ **용어사전 대화 자율등록 + 역할 분리 + 유사어 참조** (feature-0002/0003, 관리 콘솔, 마이그 0023, ADR-20260629T101500) — 대화에서 용어를 자율 등록하고 등록·검토 역할을 분리하며 유사어를 참조. ⑮ **용어 검토 큐 IA 중첩** (admin) — 용어 검토 큐를 용어사전 하위 2차 보기 탭으로 중첩. ⑯ **첨부 wrong-bubble 엣지 하드닝** (feature-0003) — 첨부 영속 레이어 매칭 키에 `message_id_space` 추가 (정상 display 경로 동작 동일, fork/마이그 cross-space 엣지 하드닝). ⑰ **feature-0012 web-router-modularization 토대** — feature-0003 `app.py` 도메인별 `APIRouter` 점진 분할의 route-parity 안전망 + 의존성 audit (P5b, behavior-neutral 리팩터 토대). ⑱ **feature-0013 관계 다이어그램 (신규)** — assistant 가 flow/관계/구조 질문에 **mermaid 다이어그램**(ER·flowchart)으로 답하고, `table_relationships` 관계 저장소(FK introspection·대화 JOIN 학습, alembic 0024)로 관계를 지속 학습 (cross-cut: feature-0002 발화·저장소 / feature-0003 웹 렌더, PB-0008 라이브 PASS·배포 완료). ⑲ **메타데이터·공유 대화 화면 정리** (feature-0003) — 스키마 골격 결과 패널 접기·검색·페이지네이션·여백 압축(metadata-bs-collapse/flexclip/paging), 공유 대화 뷰 mermaid 렌더·전체폭 반응형·스크롤 가이드 뱃지(share-mermaid-responsive·point-scroll), 용어사전 역할 선택 UI 단일화·역할 미표시 수정(glossary-role-single-ui/fieldname-fix), 새 대화 사이드바 중복 제거(new-conv-dedup), diff 답변 누출 줄번호 정규화(diff-lineno-leak).
> **2026-06-30**: ⑳ **feature-0016 메타데이터 지식그래프 (신규, 대형)** — 관리콘솔 메타데이터(테이블·컬럼 설명)를 평면 나열에서 **탐색 가능한 지식그래프**로 승급. 관계형 테이블을 SSOT 로 두고 Apache AGE(PostgreSQL openCypher 확장)의 `metadata_kb` 그래프를 **재생성 가능한 투영**으로 동기화해, 관리콘솔 **그래프 뷰**(Cytoscape·fcose 카테고리 클러스터링·관련도 사이징·이웃조회 60x 인덱스)·검색·AI(`graph_navigate` tool, 8K 테이블 컨텍스트 초과 해소)가 같은 그래프를 공유. 커스텀 PG16 이미지(pgvector+pg_trgm+AGE 공존, alembic 0025) **운영 cutover 라이브 완료**(데이터 무손상, 무중단 롤링). per-datasource 투영(rag_objects ~21 ds·8,122 테이블)+scope 격리. cross-cut 코드 거주 feature-0002(코어·tool·insight)·0003(admin 그래프뷰). [[Features/feature-0016-metadata-graph]]. (※ feature-0016 번호는 metadata-graph 와 zd-pg-pause-caddy 두 슬라이스가 공유 — 사람 결정 보류.) ㉑ **메타데이터 관리 화면 정리** (feature-0003, frontend-only) — 스키마 설명 패널 list-detail 2단 재구성·데이터소스 선택 단일화(헤더 스코프 상속)·테이블 설명 인라인 입력(평면화·정렬)·스키마 가져오기 시 기존 설명 prefill+변경분만 저장. ㉒ **무중단 배포·운영 위생군** (feature-0014/0015/0016-zd/0017) — web 무중단 롤링 배포(Caddy LB web-a/web-b·자동 롤백·:18080 폐기→:443 단일)·insight-worker graceful 종료+MySQL online-DDL 게이트+백업 복원 리허설 cron·PG 재시작 무중단화(pgbouncer PAUSE 래퍼)·배포 빌드게이트 false-failure 수정. 라이브 컷오버/실증 완료. ㉓ **질문 의도 이해·끈기 개선** (feature-0002) — 능동 해석 지침을 1:1·그룹 모두에 주입(과도 재질문·스키마 추측 후 give-up·datasource 드리프트 억제)+식별자 대소문자 보존.
> **2026-07-01**: ㉔ **그래프 뷰 대규모 진화** (feature-0016) — 렌더러 canvas→**WebGL** 전환(대규모 스키마 프레임레이트 근본 대응)·**암묵(FK 미선언) 관계 추론+자기교정 엔진**(이름·구조 휴리스틱 추정 엣지=점선 → 대화 JOIN 관찰·프로브로 신뢰=실선 승격/파단=숨김, alembic 0026, ADR-002)·**AI 능동 분석**(앵커-상대 관련도 게이팅 ADR-003·alembic 0029 + 실시간 진행 패널)·ERD 컬럼 ordinal 세로배치(alembic 0027)·다수 그래프 UX/렌더 수정. ㉕ **메타데이터 탭 권한 5분할** (feature-0003, B안·Critical) — 단일 `kb.ingest.manual` 우산 권한을 `metadata.{glossary,enum,table,column}.manage`+`graph.read` 로 세분(담당자별 개별 위임, 비파괴·가역·마이그 0). ㉖ **feature-0012 라우터 모듈화 완료** — web `app.py` 148 route 핸들러 전량을 21개 도메인 `APIRouter` 로 byte-동치 추출(app.py ~29K→18,917줄, behavior-neutral, batch1-3 라이브 배포). ㉗ **좌측 대화 전환 방어** (feature-0003) — 대화 선택 시 대화창 미표시 방어 하드닝(convswitch opacity). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0012-web-router-modularization]].
> **2026-07-02**: ㉘ **그래프 뷰 후속 진화** (feature-0016) — 렌더링 엔진 **Cytoscape(WebGL)→AntV G6 v5(Canvas) 전면 교체**(ADR-004, 07-01 WebGL 전환을 대체하는 대형 기반 교체)+클러스터 다열 masonry/shelf-packing·**노드/스키마카드 우클릭 상세 상호작용**(kind별 컨텍스트 메뉴·관계 상세 패널 방향/신뢰·추정 w/근거/조인컬럼·중심 보기 지속 칩)·**초기 진입 가시성**(스키마-우선+줌클램프+미니맵)·검색 스키마 카드 badge 매칭·노드 펼침 논블로킹(ADR-005)·프리즈 잔존 해소(setData rebuild, ADR-006)·더블클릭 카메라 앵커-중심 애니 팬(ADR-008)·**추정/신뢰 관계 자기교정 파이프라인 미가동 근본수정**(config `__all__` 누락 NameError 로 insight 처리가 3일 조용히 정지했던 결함 포함 4중 원인 해소, ADR-007)·그래프 분석 전용 모델 claude-haiku 분리. ㉙ **AI 운영 관제 패널** (feature-0003, 신규) — `/api/admin/ai-ops` AI 운영 현황(관제) admin 화면 + LLM 계측 확장(main agent latency·최근 활동 cursor 페이징)·감사 화면 정리(카테고리 순서·툴팁·최근활동 상세 확장)·메타데이터(지식베이스) 권한 종속관계 그룹 게이트 계층화·최근활동 sentinel 대화 링크·첨부 개수 배지 stale 수정. [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]].
> **2026-07-03**: ㉚ **그래프 뷰 진화 2차** (feature-0016) — 관계 기반 배치(스키마 seriation·군집·barycenter, ADR-012)·유사 속성 그룹 영역화(affix family 휴리스틱+배경박스, ADR-013)·**제품(Products) 단위 카테고리 개요**(질의시점 MySQL SSOT 합성·AGE 미저장·마이그 0, ADR-014)·**자유 배치 persistence**(결정론 배치 위 드래그 offset 레이어, ADR-015)·**AI 분석 완료 노드 역할 시각 표식**(8종·Okabe-Ito 색·아이콘·범례, node-role-viz ADR-010, alembic 0031)·접힌 상태 관계 표시·상세 관계 컬럼 아코디언·중간버튼 카메라 팬+종속 드래그·더블클릭 팬 지연 제거(ADR-011)·운영현황 LLM target 관측(alembic 0032). ㉛ **데이터소스 평균 연결 응답 시간 + AI 운영 현황 지연 지표 재정의** (feature-0003) — 데이터소스 상세에 최근 N회 평균 연결 응답 시간(ds-avg-latency, conn_health 이동평균 재사용·추가 probe 0, PB-0008 PASS)·AI 운영 현황 지연(p50/p95)을 호출 왕복→단계 간 간격(step_gap_ms)으로 재정의(aiops-ttft, alembic 0033). ㉜ **공유 링크 참여 알림** (feature-0009) — 공유 링크로 새 멤버 참여 시 대화 내 '참여 알림' pill + 기존 멤버 unread(가입자 제외, LLM 히스토리 배제, 마이그 0). ㉝ **insight 안정성·부하 분산** (feature-0002/0007) — 동일구조 샤드 그룹화(대표 1회 LLM+형제 LLM-free 전파)·healthcheck false-negative 해소·probe 격리/scan skip/batched graph sync(TASK-0308)·insight LLM 요청-레벨 fallback(claude-corp→root→edge). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0009-group-conversation]].
> **2026-07-04**: ㉞ **그래프 뷰 대폭 확장** (feature-0016) — 의미 임베딩 클러스터링(bge-m3·kNN+union-find, Phase C·ADR-018·alembic 0035)·**크로스-데이터소스 관계**(시그니처 임베딩 후보+신뢰 게이팅, Phase B·ADR-019·alembic 0036·마젠타 점선)·카테고리 그룹(sim-group) 드래그/접기/리사이즈(ADR-020)·그래프 UX 7건(상세 nav·관계 단/더블·범례 3탭·중복분석 방어·ds 라벨)·z-order 의미 정합(`_METZ` bake, FUNCTION §13)·관계도 최상위 탭 분리+검색 부드러운 하이라이트. ㉟ **멤버 가시성 window 공유** (feature-0009, Critical) — '여기부터/여기까지'로 공유가 `[from,to]` window 만 노출하고 가려진 구간을 참여자 뷰·LLM recall·fork 전부에서 물리 배제(SECURITY §21/§21.4, alembic 0037, additive). ㊱ **말풍선 ☰ 메뉴 통합** (feature-0003) — 말풍선 액션(샘플 등록·분기·공유·'AI 로 고치기')을 kebab ☰ 로 통합(👍/👎만 외부). ㊲ **용도별 LLM 라우팅 분리** (feature-0007) — insight 주말/야간 gemma 고착 해소(대화·능동분석=항상 claude / 배치 insight=시각 기반, refresh/keepalive cron 24/7). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0009-group-conversation]].
> **2026-07-06**: ㊳ **대화 화면 추론 강도 선택** (feature-0003) — composer '+' 메뉴 "추론 강도" 4단계(낮음/일반/높음/매우 높음)·대화별 영구 저장·요청 단위 thinking budget override(일반=alias 기본), reasoning-effort Major·PB-0008 PASS. ㊴ **함수·프로시저 노드 + DB 단위 AI 능동 분석** (feature-0016) — `routine_objects` SSOT+AGE `Routine` 노드+정의 파싱 참조 엣지(alembic 0034)·전 ds routine backfill·DB(스키마) 단위 'AI 능동 분석'(재귀 0 보장·비용 가드, FUNCTION §15)·그래프 상세 nav/kind 필터/검색 보존/파라미터 수직(FUNCTION §16, graph-navfilter). [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0016-metadata-graph]].
> **2026-07-07**: ㊵ **런타임 설정 콘솔** (feature-0018, 신규 슬라이스 — 코드 거주 feature-0003/0002) — 관리 콘솔 시스템>설정에서 실행 타임아웃·모델별 추론 예산 조정·저장·live/restart 하이브리드 반영(`WebRuntimeSettings`·`admin_settings` 라우터·`system.runtime.read/write` RBAC+audit·`shared/runtime_settings.py` resolver, env-fallback). runtime-settings Major·PB-0008 PASS. **추론 강도별 예산**(낮음/높음/매우 높음, `reasoning_budget` low/high/max·'일반'=no-override) + pane UI 재설계(정렬 grid+commit-bar) 후속(d9516aee·a2fe4103). ㊶ **제품 카테고리 밴드 + 크로스-DB 관계 일반화** (feature-0016 §55) — 관계도 제품 카테고리 밴드(CAT/CATH/CATX)·크로스-DB 관계 일반화(xschema)·관계 수동 큐레이션(trust/break)·DB 단위 분석 재귀 전개·refine-not-override·back-refine(ADR-021, alembic 0038, PB-0008 PASS). **§56 후속**(ADR-022~023, 마이그 0) — 크로스-DB 루틴 참조 sync 근본수정(SAVEPOINT 행 격리·워터마크 게이트·ROUTINE_USES 채택·backfill/MSSQL label 정규화·케이스변형 멱등 회수, RC1~5)+§55 화살표 데이터흐름/능동분석 툴팁 정합, POST-DEPLOY e2e(크로스 1,041·case_purged 6,320·워터마크 전진). ㊷ **대화 답변 안정성** (feature-0002) — 대화 답변 경로 edge(gemma) 폴백 완전 차단(맥락 손실 해소, conv-audit FR-edge-fallback). ㊸ **지식베이스 채택 인박스 + ENUM 대화 자율수집 + 콘솔 IA 통합** (feature-0002/0003) — 대화 자율수집(용어+ENUM)을 통합 '채택 인박스'로 재구성(용어사전 0021/0023 대칭 ENUM 코드사전 신설·`kb.enum.curate`·alembic 0039·신뢰도/상태별 채택·거부·되돌리기+pending 배지) + 콘솔 IA 통합(채택 인박스·샘플 검수 최상위 탭→각 사전 하위 {목록/검토 큐} 2차 보기·5서브뷰 디자인 폴리시, UI 단독). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-08**: ㊹ **그래프 뷰 접힘 카드 연결선·상대 하이라이트·크로스 구분·LOD + 분석 기반 제품 분류 제안** (feature-0016 §57~59) — 관계도 스키마 카드 접힘 상태에서도 **연결선**(SCHEMA_REF 질의시점 1-hop 집계·렌더 3단 승격 컬럼→테이블→카드)·선택 노드 **상대 하이라이트**(1-hop 선명·나머지 dim)·크로스-DB ROUTINE_USES **마젠타** 색 구분·**중간 줌 LOD**(엣지 스파게티 축약, ADR-024)·스키마 골격 가져오기 MSSQL 라벨 케이스 정합(§58)·**분석 기반 제품 분류 'AI 제안→사람 승인'**(카테고리 밴드 매핑을 이름 규칙→분석 신호로 개선하되 접근 allowlist 자동기록 금지 위해 Pending-only 스테이징·환각 3중 게이트·데몬 기본 OFF, ADR-025 §59). 전건 POST-DEPLOY 라이브 실증. ㊺ **메타데이터 콘솔 UX·폴리시 + 제품 분류 승인 UI** (feature-0003) — 검토/검수 큐 행 클릭→우측 read-only 상세(선택 불가 해소)·ENUM "+코드 추가" pre-fill·샘플 검수 큐 mermaid 렌더·목록 가독성 + 디자인 폴리시 5건(필 위계·nesting·배지 accent) + 제품 관리 "✨ AI 분류 제안" 승인/거부 UI. [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-09~10**: ㊻ **그래프 뷰 대규모 성능·UX 진화 (§57.4~76)** (feature-0016) — 상대 하이라이트 신뢰성 완결(즉시·정확 점등·오-침강/dim 고착·유령 관계선 잔상 해소, §57.4~9·§64·§66, ADR-026~028)·대규모 성능·정리(스키마 펼침 세로폭주 해소 ADR-028·컬럼/뷰포트 컬링 ADR-029/032+컬링 노드 참조/상호작용 보존 ADR-037·배치정렬 위상서명 메모이즈 ADR-035·미니맵 전체이미지 재사용 ADR-036·극단 줌아웃 집계 카드는 §67 폐기 ADR-033)·상세 패널 사용관계 read/write 분리+관계행/미렌더 컬럼 카메라 이동+컬럼 선택(§68~72/§75)·상단 툴바 3존 통합+줌/상태 플로팅 오버레이·엣지 중간(휠)버튼 팬(§62)·AI 능동 분석 caveats 계약 재설계(§69/ADR-034 — 형식적 자기-불평 제거·실위험만; **T69.5 완수 07-13 PR #744·cc_data_main 715/715**). ㊼ **응답 지연 타임아웃 복구 모달 제거 + 추론 예산 상한 확대** (feature-0003/0018) — 작업 화면에서 화면 전체를 덮던 타임아웃 복구 모달 제거·모달/토스트 없는 조용한 자동 재연결(배포 4b6919ec·PB-0008 PASS) + (07-09) 관리 콘솔 모델별 추론 예산 상한 native 확대(Sonnet 128K·Haiku 64K)+추론↔본문 비율 슬라이더(feature-0018 코드 거주). ㊽ **MSSQL 인증 실패 순회 조기 skip + datasource cooldown** (feature-0002 §12.3) — insight 순회에서 MSSQL 로그인 실패(18456)를 조기 skip + datasource cooldown(기본 600s)으로 실패 로그인 반복·로그·I/O 억제(회복력; DB/객체별 권한거부는 로그인 성공이라 순회 계속, 마이그·경계 0). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-12**: ㊾ **web 라우터 모듈화 완결** (feature-0012, behavior-neutral 내부 리팩터) — feature-0003 `app.py` 모놀리스(~29K/148 route)를 **21개 도메인 `APIRouter` 로 byte-동치 전량 추출**(app.py→18,917줄, 잔여 `@app` 0) + ITEM-10(`web_context` 헬퍼 추출·모듈분리 -81%) + ITEM-11(잔여 인라인 authn DEFER 핸들러 실leak 13 DI-rework·keep-inline 정당 37 명시분류) 완료(라이브 4c203f9c, route-parity 골든 불변·프로덕션 응답 실측 동일). 브라우저 로그인 QA 사용자 사인오프 이관. 잔여 initiative ITEM-09(그래프 CSS/JS 세분화·외부 브랜치 blocked). [[Features/feature-0012-web-router-modularization]] · [[Features/feature-0003-agent-web-ui]].
> **2026-07-13**: ㊿ **그래프 렌더러 PixiJS v8 전면 교체 + 콘텐츠 밴드 + 메시지 편집 신규** — feature-0016 그래프 렌더 엔진을 노드 다수 팬(카메라 이동) 버벅임 근본 해소를 위해 **AntV G6 v5(Canvas)→PixiJS v8(WebGL) 전면 교체**(엔진-중립 SceneAdapter seam·GPU 상주 씬·G6 폴백 토글, §78)+미니맵 클램프/드래그·scene diff 오브젝트 풀(§79)·라벨 Text→BitmapText(§80·렌더 157ms→0.03ms)·상세 패널 하위 항목 hover 시각 효과(§81) — 대형 스키마(409 객체) 팬 60fps vsync 라이브 PB-0008 PASS. 카테고리 밴드 **'컨텐츠 단위' 그룹핑 실동작화**(mig 0040·DB단위 클러스터·함수/프로시저 합동·LLM 라벨·mutual-kNN)+p2(가짜 밴드 소멸·연관 밴드 인접). feature-0003 — 첨부 **사용자 재업로드 버전 관리**(해시 대조 체인·assistant diff 인지)·작업화면 데이터소스 **'연결 테스트' 버튼**·**그래프 뷰 권한 분리**(`metadata.graph.read` 를 '메타데이터 관리' 묶음에서 독립, Critical). feature-0002 — **describe_routine 도구**(저장 프로시저/함수 정의 조회)·**sql_guard read-only shape 과차단 보정**(최상위 UNION·읽기전용 SHOW 허용). **feature-0019 메시지 편집 (신규)** — 사용자가 보낸 메시지 수정(1:1=단순/요청 분기 재답변 `< n/m >`·공유=단순만), Phase 1 대화 내부 브랜치 트리 백엔드 기반(마이그 0041). [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]] · [[Features/feature-0019-message-editing]].
> **2026-07-14**: (51) **메시지 편집 Phase 1+2 완성·라이브 + 무중단 배포 커버리지 + 그래프/작업화면 UX + 답변 정확도** — **메시지 편집**(feature-0019) 사용자가 보낸 메시지 수정 완성·라이브(1:1=단순 수정 / 요청사항 수정 시 브랜치 재답변 `< n/m >` 페이징, 공유·그룹=단순 수정만·@assistant 호출 메시지 잠금; Phase 1+2 머지·마이그 0041·PB-0008). **feature-0020 무중단 배포 커버리지 완성**(내부) — deploy 스파인이 워커(insight/ask) 자동 롤아웃·bedrock-gateway surge replica 무중단 교체·caddy/alembic stale-image 가드까지 포괄. **그래프 뷰 UX**(feature-0003) — 첫 입장 조작 도움말 팝업·제품 카테고리 밴드 우클릭 메뉴·'AI 능동 분석 실행' 권한 분리(신규 하위 권한 `metadata.graph.analyze` 를 조회 `graph.read` 에서 독립, Critical §12.3). **작업 화면**(feature-0003) — 애니메이션 효과 on/off 설정·프로필 '계정' 탭 세분화(계정/알림/UI/사용 내역)·펼친 '결과 보기' 스크롤 보존. **답변 정확도**(feature-0002) — MySQL 시스템 변수(@@) 읽기 과차단 해소·첨부↔실DB grounding 모순·대소문자 false-missing 봉인·MSSQL cross-DB 발견(검색·describe)·첨부 갱신 새 버전 전달. [[Features/feature-0019-message-editing]] · [[Features/feature-0020-zd-deploy-all]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-15**: (52) **assistant 자가 적대(red-team) 리뷰 (feature-0021, 신규·Major) + 그래프 상세·우클릭·엣지 정합 + 권한 원자화 + ENUM 묶음 큐 + 답변 정확도** — **feature-0021 (신규)**: 답변을 전달하기 전 별도 fresh-context 리뷰어가 grounding·SQL·권한·완전성·정직성 5축으로 자가 적대(red-team) 검증(BLOCK 만 수정 유발·높음+ 재검증·전 경로 fail-open) + [세션,제품] 메모리 노트(`/shared/agent-notes` TTL) + 관리 콘솔 'AI 추론' 탭(`console.reasoning.read`), Claude Code find→verify·auto-memory·progressive disclosure 이식(alembic 0042, unit DECISIONS ADR-20260715T140000~2). **그래프 뷰**(feature-0003) — 클러스터 상세에 함수·프로시저 컨텐츠 카테고리 포함·전체 출력·카테고리별 접기(collapsible)·우클릭 3대상(스키마↔제품카테고리↔sim-group) hit-test/band-wins 정합·드래그 관계선 추종+per-frame 최적화. **권한**(feature-0003) — 권한 '접근' 카테고리 계층 재구성 + 최소 단위 원자화(추가/수정/삭제 분리)·레거시 묶음 grid 숨김(Critical). **ENUM**(feature-0003) — 코드사전 검토 큐 구조 묶음 승인 체크리스트+일괄 등록. **답변 정확도**(feature-0002) — 첨부↔실DB 커버리지 결정론 봉인(check_table_coverage)·스키마명 대소문자 drift 0행 봉인·model alias 누출 Bedrock 400 봉인·LLM 헬스 probe 오탐/throttle 수정. [[Features/feature-0021-redteam-review]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-21**: (53) **assistant PG 자율 작업공간 (feature-0022, 신규·Major) + 진행상황 실시간 전파 (feature-0003)** — **feature-0022 (신규)**: assistant 가 전용 PG DB `agent_scratch`(전용 role NOSUPERUSER·CONNECT 격리·대화별 스키마 `s_<hash>`)에서 외부 데이터소스 데이터를 governed 반입(execute_sql 신뢰경계 재사용)해 **cross-source JOIN·집계**, 도구 4종(scratch_import/sql/list/reset)·scratch_guard allowlist(파괴적 쓰기는 격리된 scratch DB 로만 한정·함수/뷰/pg_ 거부)·시간기반 TTL reaper(기본 24h) — 2026-07-21 라이브 활성(bootstrap+`AGENT_SCRATCH_ENABLED=1`·스모크 PASS; 기본값 OFF 게이트; 잔여=cross-source JOIN e2e 실증·관측 UI·대화별 role 승격 TASK-0011~13; feature-local ADR-SCRATCH-0001~0004). **진행상황 실시간 전파**(feature-0003) — 그룹/모니터링 대화를 열어둔 유휴 관찰자에게 타 사용자 assistant 진행상황·답변이 재로드·전환 없이 실시간 표시(frontend-only run-감지·백엔드 무변경·PB-0008 라이브). [[Features/feature-0022-agent-scratch-workspace]] · [[Features/feature-0003-agent-web-ui]].
> **2026-07-22**: (54) **외부 AI용 Conversation API 접근 (feature-0023, 신규·Critical)** — 외부 AI 가 세션 쿠키 없이 **Bearer API 토큰**으로 작업 화면 대화(`/api/ask` 등)를 호출하고, 그 대화를 **MCP 서버**로 tool 화(tool 4종). 토큰은 해시 저장·scope allowlist(`conversation.*`+`product.access.*`) ∩ 계정권한 · `*.any`/관리 네임스페이스 **절대 denylist**(scope 무관 → 관리 콘솔 원천 차단) · 저권한 서비스 계정 귀속으로 심층방어, 발급/폐기는 콘솔 밖 CLI(`bin/api-token-issue.sh`, web-a/web-b 자동감지). 2026-07-22 배포·라이브 e2e 통과(토큰→/api/ask 200·admin 403·무토큰 401·배포 66a48870 soak PASS)·단위 17 PASS·§18.8 적대 보안 리뷰(REV-20260722-0002 HIGH-1/2 수정). 코드 거주 feature-0003(web 인증·라우터)·feature-0002(agent_core ask), MCP 서버=feature-0023 unit. [[Features/feature-0023-conversation-api-access]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-07-23**: (55) **대화 폴더 — 프로젝트 워크스페이스 (feature-0024, 신규·Major/일부 Critical) + 대화목록·그래프·페이징 UX (feature-0003·0016·0019)** — **feature-0024 (신규)**: 좌측 대화 목록에 재귀 **폴더(프로젝트)** 도입 — 대화를 폴더로 조직·이동, 폴더 삭제해도 대화 보관(Trash·undo·부모/root 승격), 런타임 max-depth(기본 4·grandfathering·순환 방지), 폴더별 **커스텀 지침** ask-time 주입(요청자 폴더 기준). **엄격 per-user 격리**(폴더=개인 오버레이·`folder.*.own` 만·크로스-계정 노출 벡터 `folder.*.any` 폐지, Critical §12.3 privacy 수정) + conversation.create 보유 7역할 폴더 권한 부여 + UX 6종(무프롬프트 생성·인라인 이름변경·설정/이동 모달·DnD). Phase 1+2a 라이브 완결(PR #895/#899/#903/#908·POST-DEPLOY PB-0008 PASS·restore IDOR HIGH 수정 REV-20260723T060000). **대화목록·그래프·페이징**(feature-0003·0016·0019) — 날짜 그룹핑 적응형 트리(월/년 집계)·우클릭 보편 확장 메뉴·floating 메뉴 바깥클릭/ESC 닫힘 수정·관계도 '이 노드로 이동' 미렌더 노드 노출·'DB 전체 AI 능동 분석' 완결성/신선도 개선·긴 이전 대화 페이징 스크롤 보존. 코드 거주 feature-0002(스키마·alembic 0044·컨텍스트 조립)·feature-0003(라우터·RBAC·사이드바 UI). [[Features/feature-0024-conversation-folders]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0002-agent-core]].
> **2026-07-24**: (56) **워커 성능·병렬 처리 설정 (feature-0025, 신규·Major) + 대화/공유/관리 UX·모델 정합 다수 (feature-0003·0019·0007·0016·0002)** — **feature-0025 (신규)**: 백그라운드 워커(그래프 노드 분석·cluster_label)·사용자 답변 처리·지식베이스 임베딩의 **병렬도·처리 주기·배치 크기**를 관리 콘솔 '시스템 > 설정 > 성능·병렬 처리' 서브탭에서 조절 — feature-0018 runtime-settings 레지스트리/스냅샷 인프라 재사용(`performance` 그룹 10 knob). **모든 신규 동시성 기본값 = 1(현행 직렬과 byte-동치, opt-in)**·LLM 만 스레드 병렬/DB 직렬·[min,max] clamp 로 위험값(pgbouncer 풀 소진·LLM 한도) 차단·기존 `system.runtime.read/write` RBAC 재사용(admin·audit, 신규 authz 표면 0)·pgbouncer 40/200·PG 150 여력 상향. 2026-07-24 머지(PR #933·01c4ba44)·단위 48+회귀 166 PASS·§18.8 2-렌즈 적대 리뷰 SHIP(BLOCKING 0·MAJOR-1/2 in-cycle 수정)·feature-local ADR-0025-01~04·배포 후 PB-0008 라이브 검증 잔여. 코드 거주 feature-0002(워커 루프·runtime_settings 소비)·feature-0003(admin '성능·병렬' 서브탭)·shared(레지스트리)·docker-compose(인프라). **대화·공유·관리 UX**(feature-0003·0019·0016) — assistant 답변 SQL 코드블록 **구문 강조**·CSV 다운로드 **실제 배선**·신규 스크립트 첨부 전달·대화 목록 '···' 메뉴 순서(공유·이동·설정) 정합·'＋ 새 폴더' 바 → '새 대화' 우측 **폴더 아이콘 버튼**·공유 링크 뷰 대화 뱃지 **막대화**(클릭 위치 비례)+진입 시 **최신 메시지로 스크롤**·관리 그래프 노드 역할 **이모지 색 렌더** 수정·메타데이터 검토 큐 **datasource 필터**/자동승급 정합·'요청사항 수정' 재답변이 선택 **모델·추론강도 승계**. **모델·안정성**(feature-0007·0002·0021) — LLM **Sonnet 5** 라우팅 업그레이드(thinking adaptive)·요청 타임아웃 콘솔(AGENT_TIMEOUT_SEC) 동기화·도구 결과 하드캡 상향(프로시저 정의 절단 해소)·red-team 리뷰어 모델 정합·revise 지시 교정. [[Features/feature-0025-worker-parallelism]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0019-message-editing]] · [[Features/feature-0007-bedrock-llm-provider]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0002-agent-core]].
> **2026-07-27**: (57) **성능 관측 인프라 (feature-0026, 신규·Minor) — 측정 전용·사용자 가시 동작 변경 0 + 07-27 사용자향 UX·모델 (feature-0007·0003·0016)** — **feature-0026 (신규)**: 2026-07-27 전수 성능 조사(라이브 7일 병목 지도: 답변 E2E 평균 141s = agent LLM 87.8s/62% + red-team 파이프라인 46.4s/33%, AGE cypher 590만 호출/12.5d 가 PG 부하 지배)로 확인된 계측 사각을 additive·비파괴·fail-open 관측으로 메움 — ① web HTTP per-route 타이밍 미들웨어(순수 ASGI)+요청당 MySQL/PG 커넥션 카운터+admin 조회 `GET /api/admin/perf/http`(`console.aiops.read` 재사용·신규 권한 0) ② 워커 포함 전 LLM 호출 latency 백필(`agent_runtime.llm_usage.latency_ms` NULL→측정, 13 sites) ③ 답변 `duration_breakdown` 에 `redteam_ms`/grounding `init_detail`+post-answer KV `last_post_answer_ms`(기록 duration↔체감 격차 ~34s 정량화) ④ graph sync `duration_ms`+insight cycle node_analysis·관계 프로브 카운터 ⑤ `bin/perf-snapshot.sh` 전 신호 스냅샷 CLI(`artifacts/perf/<ts>/`) ⑥ Caddy edge access log(docker logs JSON). DB 스키마 변경 0(alembic 0)·UI 표면 0(JSON API 만·PB-0008 비대상)·병목 **개선** 자체는 후속 cycle. 커밋 396a9c67 머지·단위 9 PASS(회귀 0)·§18.8 3렌즈 패널·in-progress(배포+replica pg_stat_statements+라이브 실측 잔여). 코드 거주 feature-0002(`llm.py`·`agent_core.py`·`insight.py`·`metadata_graph.py`)·feature-0003(`perf_metrics.py`·`routers/admin_perf.py`)·feature-0006(`Caddyfile`)·shared(`perf_counters.py`)·repo-level(`bin/perf-snapshot.sh`). **07-27 사용자향 UX·모델**(feature-0007·0003·0016, POST-DEPLOY PB-0008 라이브 PASS) — 답변 모델 선택기에 최상위 tier **claude-opus**(value claude-opus-5) 추가(기본값 haiku 불변·비용 회귀 차단)·공유 대화 뷰 액션 하단 바 우측 재배치+조회수 상단 이동·관리 관계도 상세 패널 관련 노드 **DB 단위 접기/펼치기**+목록 '…외 N건' 상한 제거(전량 표시). [[Features/feature-0026-perf-observability]] · [[Features/feature-0007-bedrock-llm-provider]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0002-agent-core]].
> **2026-07-28**: (58) **성능 개선 3 feature (feature-0027 P0 답변 지연·feature-0028 web 계층·feature-0029 그래프 sync churn, 신규) — feature-0026 측정 근거 기반 내부 성능 개선·결과물/응답/인가/그래프 신선도 불변(사용자 가시 동작 0) + 07-28 사용자향 UX·모델 (feature-0016·0003·0002·0021)** — **feature-0027/0028/0029 (신규)**: 2026-07-27 병목 지도(답변 E2E 평균 141s·AGE cypher 가 PG 부하 지배)를 근거로 한 첫 성능 개선 — ⓐ **feature-0027**(P0·Minor~Major 경계): post-answer 큐레이션(topic/용어/ENUM LLM 3건)을 답변 KV terminal **이후**로 이동(체감 -25~35s)·grounding RO 연결 3→1·MySQL 버퍼풀 128MB→1G·Caddy gzip+immutable static(품질 영향 축=레드팀·thinking 불변) ⓑ **feature-0028**(web·Major 경계): `/api/ask_result` long-poll 워커 스레드 이관(이벤트 루프 정지 제거)·스냅샷 PG 연결 4~5→1(단일 왕복 번들)·권한 카탈로그 TTL 캐시+세션 LastSeenAt throttle·web MySQL 풀 opt-in ⓒ **feature-0029**(그래프 churn·Major 경계): 값 무변경 시 `updated_at` 미전진(alembic 0046 트리거·실측 churn 63%)·status 히스테리시스(trusted/broken 왕복 차단)·프로브 broken 부활 금지·공유 정점 중복 MERGE 제거(관계당 cypher 11→1~3) — 전부 비파괴·가역·in-progress(검증·배포 잔여). **07-28 사용자향 UX·모델**(feature-0016·0003·0002·0021) — 그래프 뷰 관계선·hover·클러스터·역할 배지 대규모 개선(역할 배지·'이웃 깊이(hop)' 예산 재설계 POST-DEPLOY PB-0008 라이브)·'사용 기록' 드릴다운·model.access RBAC(답변 모델 접근 계정별 통제)·자체 점검을 거친 답변이 원 요청에 답하도록 정확성 수정. [[Features/feature-0027-perf-latency-p0]] · [[Features/feature-0028-web-perf]] · [[Features/feature-0029-graph-churn]] · [[Features/feature-0026-perf-observability]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]] · [[Features/feature-0021-redteam-review]].
> **2026-07-29**: (59) **실행 타임아웃 임박 시 사용자 확인 후 연장 (feature-0030, 신규·Major) + 지식베이스 메타데이터 '제품' 스코프 전환 · 07-29 대화·그래프·자가검증 UX 다발 (feature-0003·0016·0021·0002)** — **feature-0030 (신규)**: 요청 처리가 실행 예산(관리 콘솔 `시스템 > 설정 > 실행 타임아웃` 파생값)의 **80%(설정 가능)** 에 도달하면 작업 화면 컴포저에 **비차단 배너**(탭이 백그라운드면 브라우저 알림)로 "이번 요청은 타임아웃과 무관하게 끝까지 추론할까요?" 를 묻고, 사용자가 승인한 **그 run 한정**으로 run 예산 컷을 해제해 답변을 완주시킨다(미승인 시 종전 타임아웃 동작 그대로 — 승인은 opt-in). AI 작업자가 콘솔 타임아웃을 낮춰 둔 상태에서 추론이 잘려 그때까지의 산출이 소실되던 마찰 해소. **개별 LLM 호출 상한(15분)은 의도적으로 유지** — 단일 호출을 무한정 열면 그 사이 '중단'·'즉시 답변'·`max_steps`·lease fencing 이 전부 무응답이 되기 때문(codex 적대 리뷰 P1-3). 연장 KV 시그널 5종은 `cancel`/`finalize` 패턴 미러링(run_id 엄격 짝 검증)·신규 권한 `conversation.extend.{own,any}`(`own` 만 `conversation.finalize.*` 보유 7역할에 1회 backfill·`any` 는 관리자 명시 부여)·콘솔 설정 3종(사용 기본 1=ON·임계 80%·추가 허용 상한 기본 0=무제한). codex 적대 리뷰 P1×4·P2×3 전건 반영(REV-20260729-0001)·신규 36건 PASS·회귀 0·배포 `80cc7aeb` + **PB-0008 라이브 PASS**(배너 노출·승인 전환·승인된 run 에 '중단' 2초 반영)·**in-progress**("승인이 예산 컷을 넘기게 한다"의 인과·미승인 종료 경로는 라이브 미재현 → 단위 테스트 커버, 프롬프트 발행이 루프 재진입 의존이라 임계 시점 발행 자체는 미보장 = codex P2-3 잔여). 코드 거주 feature-0002(`agent_core`·`memory` KV)·feature-0003(`POST /api/extend`·스냅샷·프론트 배너·권한)·shared(`runtime_settings`). **지식베이스 메타데이터 '제품' 스코프 전환**(feature-0003 Task-Cycle·코드 거주 0003/0002·POST-DEPLOY 라이브 실증 완료): 관리 콘솔 > 지식베이스 > 메타데이터의 스코프 축을 **데이터소스 → 제품**으로 전면 재구성 — 1제품↔N데이터소스(KR_LIVE·KR_QA 각 7 DS)에서는 등록분이 그 중 1개 DS 질의에만 주입되고, 1데이터소스↔N제품(mssql-qa-idc 를 5제품 공유)에서는 타 제품 메타데이터가 혼입되던 **주입층** 결함 해소(scope 키 `product.<ProductKey>` + 활성 제품 ContextVar·KB 읽기 seam 4곳 + `describe_table` 오버레이·자율수집 쓰기 제품 귀속(해소 실패 시 수집 중단 — common 폴백=누출)·`GET /api/admin/metadata/scopes` 신설·부트스트랩을 제품 접근DB allowlist 로 강제(밖은 404·카탈로그 미가용 503 fail-closed)·콘솔 선택기/목록/검토큐/스키마 골격 제품 축 — **그래프 뷰는 datasource 유지**·fact/RAG 축 무변경·신규 권한/스키마/마이그 0·expand-contract 이관 스크립트 `kb_scope_rescope.py`). 라이브 이관 update 5,130·delete 257(모호)·contract 후 잔여 0, PB-0008 — 제품 선택기 17건·**KR_LIVE 용어 85건 회복**(7 DS 중 1곳에 갇혀 있던 자산)·공유 datasource 3제품 경계 분리 + 교차 접근 404. **07-29 대화·그래프·자가검증 UX 다발**(feature-0003·0016·0021·0002, 대부분 POST-DEPLOY 라이브 실증 동반) — 조기 cid 전환 시 **모델 선택 유실**(sonnet→haiku 무음 강등) 봉인 · 첨부 목록 **append-only** 전환(삭제 UI 철회) + 이어지는 대화의 기존 첨부 **자율 참조**(`read_attachment` 도구·'모든 첨부' 토글 제거) · 대화 검색 **첨부 파일명 축** 추가(권한 스코프 게이팅) + 본문 검색 500(collation audit NameError) 근본 수정 · 대화 페이징 "요청이 너무 잦습니다" 블로킹 해소 · 제품 선택 드롭업 **방향키 순회 + Enter 선택** · 진행 폴링 영구 정지 → **지수 백오프 자가 회복**('처리 중' 말풍선 고착 근본 해소) · 그래프 상세 패널 **컬럼 미출력·시각 위계·헤더 라벨** 교정 + **상한 전수 감사**('개수를 줄여 출력'하던 데이터 절단 제거) · **자가검증·재검증 회차 원장**(다회차 폐기 사유 요약·회차별 실제 시각·대화 단위 접이식 콘솔, alembic `20260729_0048`) + 리뷰어 회귀 2건 교정 · **데이터플레인 연결 유휴/타임아웃 사망 시 자동 재연결**. 개발 인프라: `make test` 가 **라이브 PG·라이브 런타임 설정에 직결**됐던 근본 원인 차단(Makefile 격리 + 루트 `conftest.py` 2중 방어) → make test **0 failed**(이전 13건)·UI 카피 예산 게이트 채택(template v3.43.0). [[Features/feature-0030-ask-timeout-extension]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0021-redteam-review]] · [[Features/feature-0002-agent-core]].
> **2026-07-30**: (60) **분석 오케스트레이션 4 feature (feature-0031 접지 L0/L1 · feature-0033 클러스터 합성 요약 L2 · feature-0034 L2 의 대화 소비 · feature-0032 백그라운드 토큰 예산, 신규) — '추측의 요약'을 만들지 않도록 증거→합성→소비 순서로 층을 쌓고 그 자동 지출에 상한을 둔다 + 07-30 사용자 체감·운영 개선 bundle (feature-0016·0007·0003·0002·0025)** — 2026-07-30 진단이 사용자 지적("단발성 분석 후 유사 노드 재귀의 일차원 구조")을 라이브 실측으로 확증하면서 더 근본적인 공백을 찾았다: 분석 완료 노드 ~10,000(커버리지 14.6%)·월 `node_analysis` 콜 10,620 인데 **분석 입력의 데이터 실측 0** · **대화 답변 경로의 분석 참조 0**. 즉 병목은 재귀 구조가 아니라 **입력 접지 부재 + 소비 경로 부재**였다 — ⓐ **feature-0031**(Major, 접지 L0/L1): 운영 DB 에서 **통계만** 단계 수집(Stage 0 카탈로그=사용자 테이블 read 0 → Stage 3 정밀·하루 최대 1단계 승격·`ds` 자원 예산 게이트 위 fail-soft)해 `metadata_table_stats`/`metadata_column_stats`(alembic 0050)에 적재하고 노드 분석 payload 의 `evidence` 블록으로 주입, `NODE_ANALYSIS_PROMPT` 를 '이름 규칙 추론'→'증거 우선'으로 개정하고 thin 판정을 길이→항목 충족도로 재정의. **원시 샘플값은 저장·주입하지 않는다**(문자열 min/max 도 사실상 원시 1건이라 배제 → 길이 분포 + `value_pattern` 6종·DB CHECK 강제)·**LLM 호출 수 불변**(같은 콜에 더 나은 입력) ⓑ **feature-0033**(Major, 합성 L2): 의미 클러스터마다 "이 묶음이 함께 무엇을 하는가"를 2~4문장으로 합성해 `cluster_summaries`(alembic 0051)에 적재 — PK 는 pass 마다 재부여되어 churn 하는 `cluster_id` 대신 **멤버셋 지문**, 캐시 키는 멤버셋+L1 분석문+L0 증거 **3중 일치**, 요약은 `build_table_signature_text` 에 **절대 유입되지 않는다**(요약→재임베딩→재클러스터 순환 차단 불변식·테스트가 시그니처 빌더 소스를 단정), pass 당 40개·배치 6·live 정지 스위치 (라이브 818 클러스터·15,365 멤버·라벨 평균 9자) ⓒ **feature-0034**(Major, 답변 경로): 질문이 언급한 테이블이 속한 묶음의 요약 1~2건을 `agent_core._build_knowledge_context` 의 `TABLE GROUP SUMMARIES` 섹션으로 주입해 **RI-5**(쌓인 분석의 가치가 콘솔 열람에만 갇혀 있던 상태) 해소 — 사전 계산분만(런타임 합성·LLM 호출 0)·라벨로 조회(`cluster_id` churn 회피)+`(scope, effective_schema, label)` 3중 격리·`strpos`(ILIKE 가 테이블명의 `_` 를 와일드카드로 읽던 오탐 회피)·`_datamark_untrusted`+"지시 아님" 헤더·statement_timeout 1.5s·전 구간 fail-soft, 그리고 **근거 수 병기**(`(멤버 48개 중 40개 상세분석 근거)` / `(상세분석 근거 없음 — 이름·구조 기반 추정)`)로 추정이 실측 결론으로 오독되지 않게 했다(라이브 요약의 85%가 근거 0) ⓓ **feature-0032**(Major, 비용 게이트): T2 진입 전 점검에서 `TODOS.md` P3 종결 근거("라이브 100% edge → per-token 과금 없음")가 실측으로 **반전**(7일 Anthropic 8,654콜/55,567,176 토큰 vs edge 25콜 = 과금 lane 99.7%) → 사람이 요청하지 않은 자동 지출에 rolling 24시간 토큰 상한(`agent_runtime.llm_usage` 원천·진입점 3곳·커버리지 96%·60초 캐시·기본 2,000만 = 24h 실측 685만의 약 3배) + 콘솔 'AI 운영 현황' 막대·attention 2단계, **사용자가 기다리는 호출은 세지도 막지도 않는다**(fail-open 3중). 4건 모두 2026-07-30 머지·in-progress(배포·라이브 확인 잔여). **07-30 사용자 체감·운영 개선 bundle** — **feature-0016 컨텐츠 클러스터 근본 개선**: 사용자 리포트 2건(메일↔우편 유의어·경매 기록↔경매 시스템 거리)이 같은 뿌리였다 — 테이블 시그니처가 내용 없이 양식(boilerplate)에 지배돼 거리 지표가 역전(다른컨텐츠-같은양식 0.915 > 같은컨텐츠-다른양식 0.692 라이브 실측)됐고, 시그니처를 컨텐츠 전면·양식 중립으로 재작성 + 구조적 컨텐츠 주입(루틴 `used by`·FK `related`) + centroid complete-linkage 응집 병합·적응형 병합 하한·양식 교차 구조 브릿지·라벨 어휘 일관성(유의어 발명 금지)로 해소하고 배치를 1-D 체인→MDS 2-D boustrophedon 으로 바꿨다. **재클러스터 실측**: 루틴 밴드 4,572→2,669(-42%)·루틴 1~2멤버 966→41(-96%)·중복 라벨 584종→349종(-46%)·테이블 밴드 781→598(-23%), 우편 계열 12밴드/92멤버→1밴드/12멤버. + 시그니처 전수 재계산이 `ORDER BY` 자기-역행으로 영구 정체(잔여 46,726건 도달 불가)하던 결함 해소·임베딩 스로틀 상향(86% 유휴)·임베딩 타임아웃 cliff 해소(배치 100→25·타임아웃 60→300s — 타임아웃은 '느리다'가 아니라 '영영 안 온다'를 판정하는 값)·하이라이트 상태에서 밴드 집계 관계선이 항상 사라지던 결함 수정(`lit`/`litSelf` 가 `GB:` 밴드 키를 몰랐다) · **feature-0007 자동 로컬 LLM(gemma/edge) 강등 경로 전면 제거**(ADR-003): gateway 컨테이너 재생성 직후 34분간 외부 DNS 해석이 실패해 두 claude 계정이 도달 불가해지자 `-interactive` 체인만 조용히 gemma 로 서빙됐고(사용자 관측 "모든 LLM 요청이 edge"), edge 를 이미 배제한 `*-chat`·`*-meta` 는 같은 장애에서 정직하게 실패해 한 사건 안의 대조군이 됐다 → fallback 에서 `edge-fallback` 참조 전량 제거(체인은 `-root` 2계정 종단)·`AGENT_INSIGHT_OFFHOURS_MODEL` 기본값 공백화(ADR-002 결정 2 폐지)·정의는 되돌리기용 보존, 배포 후 라이브 확정 + 개발용 메타데이터 LLM `-meta` edge-free alias 봉인 · **feature-0002/0003 대화 신뢰성**: 재배포가 진행 중 답변을 삼키던 dead-air 봉인(worker identity 를 재생성 불변 role 기반으로·종료 시 lease 명시 반납·같은 role 죽은 인스턴스 짧은 창 회수 — 60일 실측 8건/8대화·dead-air 142~1,649초) + 요청 직후 말풍선 '시작 중…' 박제 해소(enqueue sentinel 을 추적 id 로 채택하지 않음 — 실제 run 으로의 정상 승계가 그룹 foreign-run 가드에 "남의 run"으로 오인되던 근인) + 중복 저장 억제 SQL 파라미터 캐스트로 봉인 C POST-DEPLOY 활성화 · **feature-0025 백그라운드 워커 공유 자원 예산·격리·계측**(T0/T0b/T0c — `shared/resource_budget.py` 자원 종류 단위 게이트 + 전역 kill-switch·`llm`/`ds`/`task` 3축·콘솔 'AI 운영 현황 > 워커 공유 자원' 표(PG 테이블·마이그·신규 라우트 0)·스냅샷 identity 재배포 불변화, ADR-0025-05~08) · **답변 지연 계측 심화**: `inference_ms` 내부 분해로 "미귀속 33초(23%)" 가설이 **스스로 반증**됐고(other_ms 235ms=1.6% — 그 33초는 inference 구간 안에서 도는 red-team 이었다), `init_ms` 프롤로그 계측+잔차 노출로 미귀속 85% 규명 착수(데이터플레인 connect 1,447ms) · 개발 인프라: 테스트→라이브 설정 오염 재발 차단(도달성 층 + fail-loud + 감지). [[Features/feature-0031-analysis-grounding]] · [[Features/feature-0032-llm-token-budget]] · [[Features/feature-0033-analysis-synthesis]] · [[Features/feature-0034-analysis-consumption]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0007-bedrock-llm-provider]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]] · [[Features/feature-0025-worker-parallelism]].
> **2026-07-31~08-03**: (61) **분석 오케스트레이션 잔여 3층 완결 (feature-0035 결정적 분석 플래너 ITEM-11 · feature-0036 분석문 사실성 판정 ITEM-10 · feature-0037 도메인 합성 L3 lazy ITEM-08, 신규) + 프론트엔드 모듈화 착수 (feature-0038, 신규·Critical) + 07-31~08-03 UX·운영·정직성 bundle (feature-0016·0003·0002)** — 07-30 이 쌓은 4층(접지→합성→소비→예산) 위에서 남은 질문 셋을 닫았다: *무엇을 먼저 분석할지* · *그 분석문이 사실인지* · *DB 단위 개요는 누가 언제 만드는지*. ⓐ **feature-0035**(Major, ITEM-11): 라이브 실측이 커버리지 **테이블 2,040/17,192 = 11.9%** 이고 그 11.9% 가 중요도와 무관하게(사용자가 우연히 클릭한 것) 뽑혀 있어 L1·L2·grounding 세 층의 품질 상한이 되고 있음을 보였다(클러스터 요약의 85%가 "상세분석 근거 없음") → 구조 변경이 없는 점검 사이클에 미분석 테이블을 `점수 = 대화 조인 이력 × 50 + 관계 차수 × 1`(동점은 **이름 오름차순** — tie-break 가 없으면 사이클마다 다른 테이블이 뽑혀 어떤 것은 영영 분석되지 않고 어떤 것은 반복 시드된다)로 순위화해 소량씩 시드한다. **LLM 을 쓰지 않는다**(신호가 이미 구조 데이터로 있고 결정적 규칙은 재현·설명 가능하며 비용 0 — LLM 이 값어치를 내는 자리는 계획이 아니라 분석과 합성이다)·**AGE 실시간 중심성도 쓰지 않는다**(multi-hop 라이브 최악 82초, 필요한 건 정확한 중심성 값이 아니라 순서)·큐잉은 재구현하지 않고 `enqueue_change_analysis` 를 `reason="coverage_priority"` 로만 구분해 재사용한다(자격·그래프 실재·cap·쿨다운·busy 가드 상속 — 자동 LLM 지출의 안전장치를 두 벌 만들면 서로를 모른다). 2중 상한(스키마당 3 · **사이클 9** — 시드가 effective-schema 루프 안이라 사이클 상한이 없으면 스키마 수만큼 곱해진다)·`object_key` prefix + `strpos(…)=1`(LIKE 는 식별자의 `_` 를 와일드카드로 읽어 타 스키마가 섞인다)·`DISTINCT` 필수(`rag_objects` 유니크 키에 `conversation_id` 포함). **배포 후 라이브 검증이 퇴화를 잡아냈다** — 관계 신호가 없는 스키마에서 선정이 이름 순으로 퇴화해 `daily_league_ranking_1_20240425` 류가 상위에 뽑혔고, 실측하니 mysql-f2b1fe2c288c 의 테이블 **1,464개 중 1,364개(93%)가 날짜 접미 파티션**이었다(그대로 두면 커버리지 숫자는 오르고 실제 이해는 늘지 않는다) → 파티션 계열 대표 1개만 후보로 남겨 web_ranking 미분석 **719 → 계열 11**, 파티션 없는 dblog 는 272 → 272 무영향. 라이브 선정 실측은 `dblog` 최고점 27 로 `item`·`guild`·`register` 선정. ⓑ **feature-0036**(Minor, ITEM-10): 1만여 건의 분석문이 L2 요약의 입력이 되고 대화 답변에 주입되는데(feature-0033·0034) 그 사실성을 아무도 확인하지 않았다 — 아래층이 틀렸는데 아무도 모르면 오류가 위로 전파되며 오히려 더 그럴듯해진다. feature-0031 이 증거를 만들었으니 이제 대조가 가능하다 → 분석문을 **L0 통계 증거와 대조**해 `supported`/`contradicted`/`unverifiable` + 판정을 가른 숫자를 지목하는 근거 한 문장을 `node_analysis_verdicts`(alembic **0052**)에 적재한다("이 컬럼이 고유 식별한다" 옆에 `distinct_est=12, sampled_rows=100` 이 있으면 그 서술은 틀렸다 — AI 의 말을 AI 가 평가하는 게 아니라 **실제 측정값과 대조**한다). **이 기능의 전부는 정직성이다** — LLM 오류·타임아웃·계약 위반·근거 없는 응답·증거 부재·저장 실패·예산 소진은 **전부 행을 만들지 않는다(= 미검증)**. `supported` 로도 `unverifiable` 로도 기록하지 않는다. 확인 도장이 잘못 찍히면 위층이 그것을 근거로 더 확신하게 되어 이 층은 없느니만 못하므로, fail-soft 가 "통과시킨다"가 아니라 **"침묵한다"** 다. 예외 하나 — 예산 모듈을 읽을 수 없으면 pass 를 **중단**한다(계량 장애로 기능이 멈추는 것보다 상한을 모른 채 자동 지출을 계속하는 쪽이 위험하다). `analysis_hash` 가 재판정 축(이전 판정 해시를 함께 조회 — "판정 행이 있으면 제외"로 두면 분석이 갱신돼도 옛 판정이 남아 재판정되지 않는다)·노드당 1행·pass 당 20건·**배치 없음(1건 1콜** — 배치하면 한 건의 오판이 다른 건으로 번진다)·호출측 `limit` 은 설정 상한을 넘을 수 없다(인자 우회는 콘솔 knob 을 거짓 컨트롤로 만든다). ⓒ **feature-0037**(Minor, ITEM-08 = analysis-orchestration ROADMAP 마지막 항목): 라이브 실측 클러스터 요약 **1,006건이 120개 스키마**에 걸쳐 있어(평균 8개/스키마) "이 DB 가 어떤 도메인인가"를 알려면 여전히 8개를 읽어야 했다 → 스키마 하나의 도메인 개요를 L2 요약들로부터 3~5문장으로 접어 `domain_summaries`(alembic **0053**)에 둔다(이미 한 번 접힌 것을 다시 접는다). 얼핏 모순인 두 제약 — 사전 전량 생성 금지(LazyGraphRAG: 120개를 미리 만들면 대부분 아무도 안 읽는다)와 답변 경로 런타임 합성 금지(ADR-0034-07: feature-0027 이 확보한 체감 -25~35s 를 잠식한다) — 을 **요청과 생성의 분리**로 함께 지켰다: grounding 이 조회하고 없으면 `request()` 만 남기고(LLM 0·지연 0·요청 UPSERT 는 읽기 전용 조회 커넥션이 아닌 별도 RW 연결로), insight tick 이 요청된 것만 `request_count` 우선순위로 합성한다(**사용이 곧 우선순위** — 첫 질문은 요약 없이 답하지만 120번의 헛된 합성을 치르지 않는다). 재생성 축 `cluster_set_hash`(라벨·요약·멤버 수·**근거 수** — 커버리지가 두터워지면 같은 문장이어도 도메인 그림의 신뢰도가 달라진다)는 payload cap 25 로 자르기 **전** 전체 집합(최대 300)으로 계산한다(잘린 뒤 해시하면 26번째 이후 클러스터의 변화를 영영 놓친다)·pass 당 3·**advisory lock 을 얻은 tick 에서만**(행 단위 claim 이 없어 lock 없이 돌면 여러 워커가 같은 스키마를 중복 합성)·빈약한 응답(30자 미만) 미저장. ⓐ~ⓒ 전부 `shared/config.py`·`runtime_settings.py` live 정지 스위치를 갖고 백그라운드 토큰 예산(feature-0032)·`acquire("llm")`·전역 kill-switch 하위이며, **0036/0037 은 배포·라이브 실측 잔여**다. ⓓ **feature-0038**(Critical 신규·PLAN-APPROVED 2026-08-03, ssot-consolidation ITEM-P5b 잔여): 프론트 모놀리스 3파일(admin.js **14,007줄** · app.js **13,165줄** · styles.css **9,328줄**, 08-03 실측)을 behavior-neutral 점진 추출로 도메인 모듈화하는 initiative — 백엔드 `app.py` 분할은 feature-0012 가 완결했으므로 범위 밖이고, 코드는 feature-0003 `src/static/**` 에 계속 거주하며 본 unit 은 계획·추적·검증 기록 홈이다(feature-0012 와 동일 패턴). **Cycle 1 완료·배포**: styles.css → `css/` 7파일(base·shell·chat·drawers·admin·profile·search-audit)로 원본 캐스케이드 순서를 보존해 물리 분할하고 순차 concat 이 원본과 **byte-identical**(cmp 기계 증명)·index/admin.html 단일 link 를 동일 순서 7-link 로 교체·PR #1124 머지(1da17988)·deploy-web RC=0(soak 통과)·POST-DEPLOY PB-0008 PASS·**롤백 리허설 Run-006(revert 왕복 diff 0)**. **Cycle 2 머지**: admin.js 의 LLM 사용량 pane·AI 운영 현황 pane 을 ES module 로 byte-동치 이동(`admin/{usage,aiops}.js`, admin.js **14,007 → 13,057줄(-950)** — 델타가 import 2줄·포인터 주석 2줄·export 접두 4개뿐임을 역재구성 parity 로 기계 증명), §18.8 적대 패널 BLOCK 흡수(`aiops.js` 자유 식별자 `$` = 운영 현황 pane 크래시 → export/import 배선 + `acorn-globals` free-vars 0/0 재검을 게이트 표준으로 편입)·경계 재절단으로 주석-코드 정합 3곳 복구. **JS 는 미머지 docker cp 사전 QA 가 불가**(ES module 스탬프 미주입 시 모듈 캐시 이중 인스턴스)라 배포·POST-DEPLOY PB-0008 이 완료 게이트이며, Cycle 3~10(admin.js/app.js 잔여 도메인) + Final(CONVENTIONS code-modularity 컨벤션 재발 방지)은 잔여다. **07-31~08-03 UX·운영·정직성 bundle** — **feature-0016 스키마 간 라벨 어휘 통일**(밴드는 그대로, 이름만 맞춘다): 컨텐츠 클러스터는 스키마 단위로 계산·라벨링되고 스키마 간 일관성은 LLM 프롬프트의 `existing_labels` **힌트에만** 의존했는데, 힌트는 강제가 아니라서 같은 게임 DB 의 사본 스키마들이 각자 라벨링되며 어휘가 갈렸다(거래 시스템/거래 처리, 아이템 관리/아이템 획득, 길드 신청/길드 가입이 centroid 유사도 **0.9998** 인데 이름만 달랐다). 임계는 측정해서 정했다 — mssql-06656002eda6(밴드 2,083 · 스키마 115 · 교차-스키마 213만 쌍): 같은 라벨 쌍 중앙 **0.949** / 다른 라벨 쌍 중앙 **0.672** · p95 0.788 · p99 0.848 → 기본값 **0.97** 은 다른-라벨 꼬리보다 한참 위라 "같은 클러스터인데 라벨만 다른" 경우만 잡는다. 설계 판단 셋: 병합하지 않는다(스키마가 다르면 각자 자기 DB 객체를 가리키므로 별개 밴드가 맞다)·union-find 를 쓰지 않는다(전이 병합은 무관한 어휘까지 뭉갠다 → 각 밴드는 직접 이웃만 보고 그중 가장 큰 밴드의 라벨을 따르는 **1-hop 결정론**)·스키마-내 충돌은 되돌린다. 부수 결함도 하나 막았다 — 요약 프롬프트는 label 을 입력으로 받는데 요약 캐시 키에 라벨이 없어 통일 전에 요약을 만들면 밴드는 '거래 시스템'인데 요약문은 '거래 처리'를 말하는 불일치가 영구 고착된다 → 생성을 통일 이후로 옮기고(AST 로 순서 잠금) 적대 리뷰 지적대로 label 을 요약 신선도 축에 추가했다. **라이브 검증(E.11, 배포본 4d5e6678)**: mysql-42371f8d92bc 라벨 62→56(-9.7%)·밴드 64→64·통일 8·요약 재생성 8, mssql-ba175631e9fc 라벨 150→130(-13.3%)·밴드 162→162·통일 23·스키마-내 중복 라벨 0 유지. **전량 전환 후(E.12 — 최대 scope 가 08-03 11:41 cadence 로 전환되어 전 scope 신 로직)**: 전역 루틴 밴드 **2,703 → 2,703(완전 불변)**·전역 루틴 라벨 종수 **1,648 → 1,279(-22.4%)**·우편 시스템 5밴드 → 0·메일시스템 1밴드 → 0 — 밴드가 1개도 변하지 않은 채 어휘만 줄어 "병합 없이 이름만 통일"이라는 설계 의도가 전역 규모에서 성립했다. · **feature-0016 그래프 시각 부정합 2건**(수치로는 안 드러나 라이브 화면을 확대해 찾은 종류): 멤버 1개짜리 "밴드" 3개가 경매 거래·46 과 같은 헤더·테두리·접기 토글을 갖추고 나란히 서 있던 것(원인 = 싱글턴 흡수가 `nm:` 을 예외로 뒀는데 그 근거인 "2차 attach 로 커질 수 있어"가 2차 attach 종료 시점엔 이미 성립하지 않음) → **3개 → 0개**('기타' 7→10 흡수) · 긴 이름이 알약 밖으로 나가던 것(`_metaRoutineStyle` 의 `labelMaxWidth` 176 고정 vs 칩 폭 `min(190, TW+rel×40)`=150 → 26px 넘침, 바로 위 `_metaTableStyle` 은 같은 이유로 이미 고쳐져 있었고 §45 가 루틴을 빠뜨렸다) → 우여백 **-3px → +14px**. 철회한 지적 1건(밴드 라벨 말줄임 방향 혼재는 "이 스템으로 시작/끝나는" 범위 신호라는 의도된 인코딩). 회귀 잠금은 pytest 소스 계약 4건이며 렌더 결과가 아닌 소스 형태를 재는 간접 계약이라는 한계를 REPORT 에 명시했다. · **metadata-graph 자동 재분석 계측 도달 규약 역전**: 배포 D+7 POST-DEPLOY 검토에서 직전 cycle 이 추가한 계측 2키(`auto_reanalysis_absorbed`/`_axis_dropped`)가 report 에는 기록되면서 insight_worker payload allow-list 미등재라 **7일간 운영자에게 한 줄도 도달하지 않은** 것을 발견했다(하필 실제 발동 DB 가 날짜 샤드 DB 라 그 cycle 의 critical 수정인 샤드 흡수 방어의 실효를 확인할 수단이 없었다 — 1라운드 리뷰 R7 "allow-list 등재 없이는 어떤 계측도 도달하지 않는다"의 재발). §18.8 적대 리뷰가 1차 보정(2키 등재 + 정규식 감시자)을 **BLOCK** 했다: 같은 클래스의 고아 카운터가 6건 더 있고 그중 넷은 FUNCTION.md 가 "관측된다"고 선언한 것들이며, 감시자 자체가 유실 3방식 중 2개(주석 처리·지역변수 이동 — 둘 다 이 파일의 실제 관용구)를 통과시킴을 mutation 으로 실측했다 → 리뷰 Challenge 를 채택해 규약을 **역전**했다: `_telemetry_sweep` 이 `scan_report` 의 미등재 스칼라를 payload 로 흘리고 **빼야 할 것만** `_TELEMETRY_SWEEP_DENY` 에 사유와 함께 명시해 "기록했다 ⇒ 도달한다"가 사람의 기억이 아니라 구조가 되게 했고(고아 6건 동반 해소·0 이어도 보여야 하는 핵심 지표는 명시 등재 유지 — sweep 은 truthy-only), 테스트를 소스 텍스트 검사 → 동작 검사로 교체했다(이 과정에서 baseline 이 unknown 으로 오집계되던 회귀를 발견해 `_AUTO_INFO_STATUSES` 로 분리). · **feature-0003 AI 운영 현황 '미분류 활동' 3종 재배치**: 관리 콘솔 `AI 운영 현황 > 운영 현황` 활동 카테고리 드릴다운에서 '미분류 활동'으로 떨어지던 항목들을 실제 소속으로 옮겼다 — 라이브 `llm_usage` 의 DISTINCT task 16종 중 3종이 taxonomy 미등록이었고(`analysis_verify` 1,258회 · `cluster_summary` 332회 · `domain_summary` 4회) 셋 다 insight 워커 분석 파이프라인 산출물이라 `ai.insight.analyze` 로 편입했다(검증 축인 `analysis_verify` 도 검증 대상·소비처가 인사이트 분석문이라 같은 카테고리 — 대화 파이프라인의 검증인 redteam 을 `ai.reasoning.aux` 에 둔 것과 같은 기준). **07-28 에도 같은 결함이 4종으로 발생한 반복 패턴**이라 dict 를 채우는 것으로 끝내지 않고 `_record_llm_usage` 의 task 리터럴(직접 호출 + 래퍼 경유 19종) **AST 전수 게이트**를 함께 넣었다 — f-string 파생은 파생 규칙을 선언해 산출 집합 동치를 요구하고, 정적으로 확정할 수 없는 경로(`*args`·`**kwargs`·지역변수·재바인딩·shadowing·sink 별칭 참조)는 통과가 아니라 **실패**로 떨어져 신규 계측이 taxonomy 등록 없이 들어오면 라이브 데이터가 쌓이기 전에 red 가 된다. POST-DEPLOY(PR #1121 머지본 main c0c6800f·web 스코프): 배포본 taxonomy **18종 → 21종**·세 task 가 `ai.other.unmapped` → `ai.insight.analyze` 로 이동을 web-a·web-b 양쪽에서 확인·미등록 self-surface 계약 유지·라이브 `admin_ai_ops(days=7)` 응답에 `ai.other.unmapped` 카테고리도 "미분류 AI 활동" attention 도 없음. **배포 판정은 파이프 종료코드가 아니라 서빙 주체의 SHA 로 했다**(워커는 미접촉 — taxonomy 는 web 관제 표시 전용이고 워커 소비 경로가 없다). · **feature-0002 conv-audit 4건(전건 배포 + 라이브 실증)** — ① **쿼리 리뷰 프레이밍 봉인**: 첨부가 '적용될 변경'일 때 **라이브 DB=BEFORE / 첨부 세트=AFTER** 라는 시간 방향 계약을 코드 권위선으로 항상 주입한다(변경이 스스로 만들어내는 차이 — 아직 없는 객체·바뀐 시그니처·스크립트가 추가할 키 — 는 적용 전제이지 결함이 아니며, 심각도 배지와 결함 목록은 적용 후에도 남는 문제에만 붙는다. 라이브 대조 강제와 0행≠부재 봉인은 유지). 근본은 프롬프트가 '실 DB 대조는 필수'만 정하고 **그 차이의 해석(시간 방향)을 정하지 않아** 프레임이 모델 재량이었던 것 — 라이브 A/B 대조쌍(동일 5파일 sha256 일치·동일 요청문이 3분 간격에 코드 리뷰 vs 현재-DB 부재 지적으로 갈림)·60일 SQL 첨부 96대화 중 12 distinct_conv. 배포(main 2ecfe4b5) 후 **PB-0008 실 Windows 브라우저 재현에서 프레이밍 축은 재현되지 않았다(= 봉인 작동)**: 도입부가 시간 방향을 명시하고 미배포 상태는 '적용 전제' 단일 절에만 기재되며 심각도 배지는 적용 후에도 남는 결함에만 붙고 배포 순서는 말미 Q&A 로 강등됐다. 다만 같은 답변에서 **미검증 부재 단정 1건**을 발견했다 — '적용 전제' 표가 약 148만 행 실존 테이블을 "현재 미존재"로 적었고 그 테이블을 조회한 도구 호출은 0건이어서 BEFORE 답변이 잡아냈던 대용량 PK 추가 리스크를 놓쳤다(과잉 불평이 과소 검증으로 뒤집힌 형태) → 원장 신규 항목으로 분리하고 원 항목은 **축 한정 verified(framing)** 로만 표기했다(PARTIAL PASS). ② **운영자 프롬프트가 삼킨 grounding 봉인을 코드 권위선으로 복구**: `compose_system_prompt` 은 운영자 `WebSystemPrompts` global row 가 있으면 코드 상수 `SYSTEM_PROMPT` 를 **통째로 대체**한다. 라이브 row 는 2026-06-18 자였고 그 이후 본문에만 추가한 grounding 규칙 5종(첨부↔실DB 양측 조회·0행≠부재·절단/완전성 신호·식별자 대소문자·`check_table_coverage` 유도)은 **프로덕션에 존재한 적이 없었다**(코드-append guidance 16,825자에도 부재 = 보완 경로 없음). 동시에 그 row 에는 REQ-20260714 가 환각 유발로 판정해 코드에서 제거한 두 지시가 한국어로 살아 동적 검증 지시와 정면 충돌했다 — 이것이 선행 cycle 이 진단한 A/B 대조쌍(동일 입력, 도구 **0회↔12회**)의 실제 기전이다. `_GROUNDING_AUTHORITY_DIRECTIVE` 를 신설해 5종을 코드-append 로 옮기고(배치는 **반환 직전** — 초기 parts 에 두면 뒤에 누적되는 운영자 scope row 가 다시 덮는다) 두 억제 지시를 언어 무관 의미로 지목해 무력화하며, override 는 첨부 검토 맥락으로 좁히고 보안·프라이버시 7종은 carve-out 했다. 운영자 row 는 stale 사본이 아니라 고유 PII·보안 정책을 담고 있어 덮어쓰기와 merge 구조 변경은 모두 배제하고 라이브에서는 문제의 2줄만 국소 교정했다(백업 보관). 배포(main 954adc87·4서비스 healthy) 후 배포본 ask-worker 실측: composed **13,604 → 17,830자**·필수 seal **9종 전량 LIVE**(수정 전 부재 확정이던 5종 전부 도달)·억제/첨부우선 지시 0건·carve-out·보안우선 문구 LIVE·운영자 마스킹 정책 보존·composed 가 계약으로 끝남(last-writer). census 테스트가 필수 seal 의 코드-append 존재·도달·작동조항 생존을 고정해 같은 유실을 구조적으로 차단한다. ③ **부하게이트가 재작성 방향을 못 줘 추론이 정체되던 마찰 봉인**: 무거운 쿼리 차단 메시지에 EXPLAIN 이 이미 아는 '왜 무거운가'(접근형태·미사용 인덱스·스캔 파티션 수)를 실어 원인별 재작성 지시를 주고, 전역 집계는 컬럼 축소나 LIMIT 으로 가벼워지지 않는다는 사실과 `approx_rows` 대안을 알려주며, 같은 대상에서 반복 차단되면 `confirm_heavy` 를 최후수단이 아닌 명시 선택지로 승격한다(임계·게이트 모드·confirm 신뢰 정책·MSSQL fail-closed 전부 불변 — 정당한 무거운 쿼리는 그대로 차단되고 진단만 붙는다). 동반: `EXPLAIN.rows` 는 LIMIT 을 반영하지 않는 스캔 상한이라 실제 n행만 읽는 순수 LIMIT 조회가 테이블 전체 행수로 오판·차단됐다(라이브 실측 `SELECT * FROM t LIMIT 5` → **13,903,018 추정**) → 조기 종료가 보장되는 형태에 한해 `est = min(est, n+offset)` 로 하향 보정한다(단일 SIMPLE plan row · 집계/WHERE/정렬/조인/서브쿼리 부재 · filesort·temporary 부재 · 주석 제거본의 문 끝 LIMIT). 근본은 선행 TASK-0304 가 게이트를 차단에서 재작성 코칭으로 바꿨으나 **그 코칭이 정적 일반론**이라 모델이 이미 적용한 조언을 다시 받고 같은 형태를 재제출해 한 대화에서 6연속 차단된 것(30일 10 distinct_conv / `execute_sql` 386건의 6.0%). 사용자가 지목한 'LIMIT 1인데 차단'된 쿼리는 집계라 전체 스캔이 맞았고(25건 중 24건이 집계) **표면 가설을 데이터로 기각한 뒤** 결함을 거부 자체가 아니라 거부 피드백으로 위치 재지정했다. 배포(main 97af7d27) 후 라이브 A/B 재현: 실행계획 진단 **0/6 → 4/4**·전역집계 불가 고지 0 → 3·`approx_rows` 대안 0 → 2·같은 대상 반복 차단 escalation 이 설계대로 2회 발동, 결정적으로 모델이 진단을 받고 대상을 작은 `TF_ErrorLog` 로 전환하고 범위를 좁혀 재작성에 성공해 **원 대화가 포기했던 조사(1062 PK 중복 · 1264 범위 초과 원인 규명)를 17 steps 6,078자 답변으로 완수**했다(정직한 한정: 차단이 0 이 되지는 않았고 임계 유지는 사용자 결정, LIMIT 상한 보정 레버는 이 재현에서 미발동). 재현은 콘솔 경로로 돌려 라이브 사용자 대화 테이블을 오염시키지 않았다. ④ **전제 절의 상태 단정을 '검증된 사실 또는 미확인' 으로 봉인**: 측정으로 시작했다 — 90일 SQL 첨부 대화 87건에서 객체 상태 단정 90건 중 **43건(47.8%)** 이 그 대화의 어떤 도구 결과에도 그 객체명이 없었고 `미확인` 표기는 **0건**이었다(라이브 1건은 약 148만 행 실존 테이블을 "현재 미존재"로 적어 대용량 PK 추가 마이그레이션 리스크를 놓쳤다 = ①의 PB-0008 발견과 같은 뿌리). 계약에서 "stated as fact" 를 빼고 **"모든 행은 검증된 사실이거나 미확인"** 으로 바꿨다 — 권한거부·스코프경고·미조회는 미확인이며, 0행은 정확한 식별자 + 도구가 커버리지를 명시한 경우에만 그 범위 내 부재로 말한다(과교정 방지). 미확인도 공짜가 아니라 결정이 걸린 객체는 최소 1회 조회 시도 의무를 둔다. **귀속도 정정했다** — 앞선 보고가 이를 자기가 만든 2차효과로 단정했으나 측정이 반증했다: 이 행동은 시간-방향 계약(07-30) 이전부터 광범위한 구조적 공백이고 '적용 전제' 절은 그 단정에 표 형태의 자리를 준 가중 요인이다. 감지기 `bin/measure-precondition-grounding.py` 를 신설해 재발을 측정으로 잡는다(지표가 아니라 **스크린**임을 명시·RO 트랜잭션·타임아웃·`ON_ERROR_STOP`·빈 결과 fail-closed), 적대 리뷰 3라운드가 성공 신호(미확인 카운터)가 영원히 0 이던 감지기 결함을 잡았기에 감지기 자체도 회귀 테스트 10건으로 고정했다. 배포(PR #1126 merge main 99d09137) 후 배포본 ask-worker 에서 계약 9요소 전부 LIVE·라이브 composed 19,764자 도달. **verified 로 닫지 않는다** — 90일 창은 과거 대화가 지배해 비율이 즉시 움직이지 않고, 미확인 표기가 0→2 로 처음 쓰인 것은 방향 신호이나 표본이 작다(완료 판정 = 새 리뷰 대화 축적 후 `--days 30` 재측정). ③④ 두 배포 모두 1차에 insight-worker 헬스 미도달로 워커군이 last-good 롤백된 부분 완료(DEPLOY_RC=2)를 겪었고 — 원인은 배포 창의 일시적 네트워크 버스트로 외부 datasource 다수 도달 불가여서 롤백본에서도 동일 증상 — 멱등 재실행으로 RC=0 해소하며 **배포 검증은 종료코드가 아니라 서빙 주체의 실제 SHA 로 한다**는 교훈을 남겼다. · **agent-core 질의 임베딩 강등 가시화**: feature-0034 계측이 `init` 의 87%가 `query_embed_ms`(**20,587ms**)임을 드러냈고 추적 결과 `AGENT_KB_QUERY_EMBED_TIMEOUT_SEC=20` 과 정확히 일치 — **성공한 느린 임베딩이 아니라 타임아웃 후 trigram 무음 강등**이었다. 사용자는 20초를 버린 뒤 벡터 검색 없이 답변을 받았고 분해에는 그 사실이 어디에도 없어 **2주간 미발견**이었다. 원인은 병렬 세션의 시그니처 전수 재계산(48,226건) 백필이 단일 CPU-bound 임베딩 백엔드를 포화시킨 것(조사 시점 이미 소진·pending=0). 처리량 노브는 같은 날 병렬 세션이 두 번 조정 중이라 **의도적으로 건드리지 않고** 빠져 있던 것을 채웠다 — `init_detail.query_embed_ok`(수치 1/0 — §3b 가 전 키를 `::float` 캐스트하므로 bool 금지, 소요가 아니라 잔차 leaf 에서 제외) 신설 + 타임아웃 **20 → 12s**(`shared/runtime_settings.py` spec default/minimum 동반) + `perf-snapshot` §3b-1 강등율·§3b 유령 행 제외. [[Features/feature-0035-analysis-planner]] · [[Features/feature-0036-analysis-verification]] · [[Features/feature-0037-domain-synthesis]] · [[Features/feature-0038-frontend-modularization]] · [[Features/feature-0016-metadata-graph]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]].
> **2026-08-04**: (62) **운영 정기 잡의 인-컨테이너 이관 (feature-0039, 신규·Major) + 프론트엔드 모듈화 ITEM-P5b 완결 (feature-0038, Cycle 3~10 + Final) + 08-04 사용자향 fix 4건 (feature-0003·0002)** — ⓐ **feature-0039 (신규, Major)**: 사용자 원 요청은 "crontab 내부에 `cd <repo> && …` 로 구성된 부분이 `root` 계정으로 실행되기보다 서비스 내부에서 동작되도록" 이었고 대상은 그 형태 **4줄 전부** 다 — 플랫폼 논리 백업(`0 3 * * *`) · 백업 복원 리허설(`30 3 * * 0`) · AGE 메타데이터 그래프 sync 증분(`*/30 * * * *`) / 전량(`17 4 * * *`). **왜 root 였는가가 설계를 결정했다**: 기존 래퍼(`bin/backup.sh` 등)가 전부 `docker exec` 로 DB 컨테이너에 들어갔고 이 호스트의 docker 소켓 접근이 root 에만 있어 "반드시 sudo(root crontab)로 실행" 이 요구됐다 — 잡 로직을 이미지 안(`/app/scripts/ops_*.sh`)으로 옮기면 소켓 의존 자체가 사라져 **root 권한의 유일한 근거가 소멸**한다(역방향인 "컨테이너에 docker 소켓 마운트" 는 컨테이너→호스트 root 승격 경로라 채택하지 않았고, 소켓을 마운트하지도 않는다). 신규 `ops_scheduler.py`(in-container cron 파서 + 루프) · `ops_backup.sh` · `ops_restore_rehearsal.sh` · `ops_graph_sync.sh` · `healthcheck_ops_scheduler.py` + compose `services.ops-scheduler`(agent 이미지 · `dbnet` 단독 · heartbeat healthcheck)로 스케줄 정본이 버전관리 밖 crontab → `docker-compose.yml` 환경변수(`OPS_SCHED_*`)로 이동했고, 산출물 경로·벽시계 시각·§82 동시성 가드는 이관 전과 동일하다. 세 제약이 구현을 좁혔다 — pg_dump 는 **서버 메이저와 동일**해야 해 PGDG `postgresql-client-16` 을 고정했고(Debian trixie 기본 client-17 산출물은 PG17 전용 `SET transaction_timeout` 을 넣어 PG16 복원을 rc=3 으로 깨뜨린다 — 사전 조사 실측), AGE sync 의 flock 은 호스트와 **같은 파일(inode 공유 bind-mount)** 이어야 `bin/routine-backfill.sh` 와 상호배제가 유지되며(미마운트 rc=1 fail-loud / 호스트 보유 rc=3 skip / 정상 rc=0 3케이스 실측), 호스트 cron 과 컨테이너 스케줄러를 병행하면 백업이 하루 2회 돌아 보존 회전이 절반이 되므로 이관은 **원자적**(배포 후 crontab 제거)이어야 한다. 부수로 이관 등가성 검증 과정에서 **AGE cutover 이후 5주간 FAIL 하고 있던 PG 복원 리허설 결함**을 발견·수정했다(`ag_catalog` 사전 프로비저닝 — 사용자 승인 하 범위 추가): 761MB 실백업 전량 복원 rc=0 **PASS**(PG 사용자 테이블 67개 · MySQL 32개)이고 mysqldump DDL 은 서버 내장본과 **바이트 동일**이었다. §18.8 codex 패널 [P1] 5건은 전건 흡수 — Makefile `up`/`start` 기동 목록 누락(신규·재부팅 환경에서 crontab 만 제거하면 4개 잡 전면 정지) · SIGTERM 시 진행 잡을 기다리지 않음(배포가 매번 recreate 하므로 백업 도중이면 그 회차 소실 → `OPS_SCHED_SHUTDOWN_GRACE_SEC=280` + `stop_grace_period` 5m) · 최종 경로 직기록으로 실패한 부분 디렉터리가 보존 회전에 계수돼 **정상 백업이 밀려남**(→ `<ts>.partial` staging 후 무결성 통과 시 rename · 회전은 완료 백업만 계수) · `../artifacts` 전체 쓰기 마운트(그 아래 라이브 DB 데이터가 있음 → `backups`·`metadata-graph` 2개로 축소) · `agent-common` `depends_on` 상속(무관한 bedrock-gateway·MinIO unhealthy 시 미기동 → mysql·postgres 로 override). [P2] 는 dom/dow 무제한 판정을 값 집합이 아니라 **문법(`*`)** 기준으로 교정한 오프바이원(`0 0 1-31 * 0` 이 매일이 아니라 일요일만 실행됐다)과 MySQL 암호를 argv → `--defaults-extra-file`(0600) 로 옮긴 것을 수용하고, 2건은 설계 의도로 명시 보류했다(같은 분 중복 발화 방지가 in-memory 인 것 = 4개 잡 전부 멱등 · healthcheck 가 루프 heartbeat 만 보는 것 = DB 를 healthcheck 에 엮으면 DB 순단이 스케줄러 재시작으로 번져 그 창의 잡을 통째로 잃는다). 검증은 pytest **29건** + 전 스위트 3,647건 실패 0 이며 `LEGACY_SPECS` 상수가 이관 전 crontab 4줄의 스케줄을 담아 무의식적 드리프트를 적색화한다. **머지(PR #1131)까지 완료이고 배포(`make deploy-workers` — ops-scheduler 롤아웃)와 호스트 root crontab 4줄 제거는 잔여**다. 정직한 후속 기록 — 주간 복원 리허설이 5주간 FAIL 이었는데도 아무도 몰랐다는 것은 `cron.log` 를 사람이 보지 않는다는 뜻이며, 리허설 FAIL 을 관리 콘솔 알림·헬스 신호로 승격하는 제안을 REPORT §8 에 남겼다(구현 미실행). ⓑ **feature-0038 ITEM-P5b 완결**: 08-03 착수한 프론트 모놀리스 분할이 Cycle 3(settings·audit) · 4(accounts·roles) · 5(products·datasources) · 6(metadata) · 7(app.js `type="module"` ES module 전환 — 유일한 비-기계적 변환 지점이라 단독 cycle 로 격리) · 8(auth·profile) · 9(sidebar) · 10(messages) + Final 로 닫혔다. 최종 구조는 styles.css(9,328) **소멸** + `css/` 7파일 · admin.js **14,007 → 4,804줄(-66%)** + `admin/` 9모듈(usage·aiops·settings·audit·accounts·roles·datasources·products·metadata, 계 9,354줄) · app.js **11,119줄** + `app/` 4모듈(auth·profile·sidebar·messages, 계 2,161줄)이고 10 cycle 전건 머지·배포·POST-DEPLOY PB-0008 PASS(PR #1124/#1125/#1128/#1129/#1130/#1132/#1133/#1141/#1142/#1143). **AC-3(오케스트레이터 ≤ ~3,000줄)은 부분 달성으로 정직 보고했다** — 잔여 admin.js 4,804줄(코어 유틸 · 계정/역할 공유 권한 grid 인프라 · pending/batch-apply 엔진 · 대시보드 · 탭 디스패치)과 app.js 11,119줄(composer/첨부 · progress 폴러 3계층 · 그룹 대화 · 검색 · 메시지 편집/피드백)의 차단 요인은 공유 가변 `let`(`_dqaDrag`·`_sidebarCatchupTimer` 류)의 **양방향 재할당 결합**이며, byte-동치 원칙 하에서 무리한 이동은 런타임 TypeError 라 잔류가 정답이고 다음 감축은 state 편입 mini-change(비-중립 · 별도 승인) 선행이 필요하다. Final 은 코드 무변경으로 재발 방지만 세웠다 — CONVENTIONS **§14 code-modularity**(프론트 임계 3,000/5,000줄 + 분할 방법 정본: byte-동치 · TDZ/binding-write 검사 · `acorn-globals` free-vars 게이트 · 합본 전환 · 검증 사다리) + ROADMAP ITEM-P5b done(백엔드 `app.py` 재착수 없음 문면 유지) + §18.8 패널 BLOCK 흡수(REPORT stale · 날짜 정본 08-04 정정 · AC 탈모호). 정적 자산 무접촉이라 배포는 생략했다. ⓒ **08-04 사용자향 fix 4건** — · **feature-0003 공유 링크 '대화에 참여' 버튼 표시**: 라이브 진단이 "버튼 누락" 이 아니라 **표시 조건** 임을 확정했다(공유 링크 Joinable=1 정상 · 로그인 열람자가 소유자 본인이면 서버 `can_join` = 로그인 && joinable && !already_member 가 false · `share.js` 가 `can_join` 단독 게이트라 사유 없이 숨김 · 라이브 서빙 자산에 버튼 DOM 이 있어 구버전 배포 가설은 배제). 사용자 결정으로 표시를 `is_authenticated && (can_join || joinable)` 로 넓혔는데 **적대 리뷰(codex)가 초안을 뒤집었다** — 표시만 넓히고 클릭을 그대로 join 으로 보내면 기존 **windowed 멤버**가 `stamp_member_visibility(is_new_member=False)` 로 가시 범위를 교집합 축소당하고 복구 경로가 없으므로(owner·full 멤버는 skip), 이미 멤버인 클릭은 join 을 호출하지 않고 `viewer.conversation_id`(멤버 한정 신규 응답 필드)로 직행하도록 설계를 바꿨다. 서버 인가 계약은 무변경(can_join 계산 · Joinable=0 → 403). POST-DEPLOY 898bc962. · **feature-0003 사용자별 시스템 프롬프트 자동 생성의 끊긴 배선 복구**: 호출 경로(엔드포인트 4종 · 프론트 버튼 · `compose_system_prompt` 3층 누적)는 전부 관통했고 끊긴 곳은 접지 신호였다 — 핵심(W1)은 **대화 요약 writer 의 완전 부재**로, `_refresh_summary_after_step`/`_after_ask` 의 유일 호출자였던 `agent_cli.py` 가 죽은 코드로 삭제되며(68ed7a76, 2026-06-02) 함께 끊겨 `agent_runtime.summary` 가 대화 296건에 **0행**이었고 제품·역할·개인 자동작성 3종의 "실제 분석 사례 요약" 접지가 한 번도 생성된 적이 없었다(`meta.summary_count` 항상 0 · 두 함수는 타 모듈 심볼 미해소로 호출 시 NameError 이기도 했다) → `refresh_conversation_summary()` + `_summary_deps()` 신설 · ask 당 1회 호출. 동반으로 topic 신호 오염 27.5%(placeholder·중복·인사·raw 절단) 정규화를 role/account/product 3경로 공통 적용 · 자동작성 LLM 실패가 서버 로그에 0줄이던 것을 warning 으로 · 역할 삭제 시 `WebSystemPrompts` 고아행 cascade(제품 경로엔 있었고 역할에만 부재)를 추가했다. POST-DEPLOY 5dc42e48. · **feature-0003 대화내역 발화자(사용자·assistant 제품) 사후 변경 차단**: 발화자를 렌더 시점의 대화 설정에서 파생하던 것을 **발화 시점 각인**으로 전환했다 — assistant 는 `renderMessages` 가 `state.pinnedProductId`(컴포저 제품 칩)로 만든 단일 값을 전 말풍선이 공유해 칩을 바꾸는 즉시 과거 답변 아바타가 전부 새 제품으로 바뀌었고(전환 토스트 "다음 답변부터 적용됩니다" 와 정면 배치), user 는 발신자 meta 를 그룹 발신에만 각인해 1:1 미각인 → FE 가 대화의 현재 owner 로 폴백 → fork 본(owner=복제자)에서 원저자 질문이 복제자 이름으로 표시됐다. assistant 미러 4경로(정상 · max_steps 초과 · 중단 보존 · 오류) 전부 각인 + fork/duplicate 의 `attribution_defaults` 로 원본 제품·원본 owner 고정 + PATCH `…/product` 가 바인딩 교체 직전 보정하며, 보정은 미각인 행 한정·추가만·fail-open 이고 추론분은 `attribution_inferred` 로 구분한다(§18.8 codex P1 4건 반영 — 부분 각인 행의 기존 meta 키 덮어쓰기 → `payload||existing`/`setdefault` · SELECT~UPDATE race → skip + 원문 대조 낙관적 가드 · 한 축 조회 실패가 다른 축 귀속까지 폐기 → 독립 best-effort · sender-id-only 타인 메시지의 owner 오귀속 → `사용자 <id>`). POST-DEPLOY ae9d34f6. · **feature-0002 요약 부트스트랩 교착 해소**: 위 writer 복구를 전량 배포한 뒤 배포본에서 실호출 검증하니 `saved=False` · `agent_runtime.summary` 여전히 0행이었다(로그 "load_memory_context: PG partial failure (summary=False msgs=True kv=True)") — `_read_runtime_pg` 의 호출 계약은 "None = 읽기 실패" 인데 `PgRuntimeBackend.load_summary` 가 **행 없음도 None** 으로 반환해 두 사건이 구분되지 않았고, 그 결과 요약이 아직 없는 대화 전부가 PG 읽기 실패로 분류되며 큐레이션 훅은 `conn=None` 으로 호출되므로 MySQL 폴백에서 예외 → 요약 미저장이었다. **요약이 없으니 읽기가 실패하고 실패하니 첫 요약을 영원히 못 쓰는 부트스트랩 교착**이 선행 cycle 의 writer 복구가 코드상 옳았음에도 라이브 실효가 0 이던 직접 원인이다 → 반환을 `Optional[str]` → `str`(행 없음/NULL 은 `''`)로 바꿔 실패 신호와 데이터 부재를 분리하고 `load_memory_context` 에 계약 주석을 남겼다(로직 무변경). 왜 단위 테스트가 못 잡았는지도 기록됐다 — 선행 cycle 테스트가 `load_memory_context` 를 통째로 스텁했는데 결함이 바로 그 함수의 PG/MySQL 분기에 있었다(배포 후 실호출 검증이 아니었으면 "고쳤다" 고 보고한 채 라이브는 그대로였을 사안). 이 건은 **자체 POST-DEPLOY 커밋이 없다**. [[Features/feature-0039-ops-scheduler]] · [[Features/feature-0038-frontend-modularization]] · [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0009-group-conversation]] · [[Features/feature-0002-agent-core]].
> **2026-08-05**: (63) **프론트 오케스트레이터 감축 후속 완결 (feature-0038 Phase A~B3) + 분석문 사실성 판정의 순환 차단·결과 노출 (feature-0036) + 첨부 변경 사실 코드-권위 봉인 (feature-0002) + P5a 종결 (feature-0011) + 하네스 red 전수 해소 (feature-0003)** — 신규 feature 0건, 전부 기존 feature 의 완결·정직성 정합이다. ⓐ **feature-0038 후속 Phase A~B3**: 본편이 \"byte-동치로는 더 못 줄인다\" 로 정직 보고한 잔여를 실측이 재정의했다 — 모듈-스코프 `let` 21개 × top-level 함수 297개 결합 매트릭스에서 도메인 경계를 넘는 양방향 재할당은 `_dqaDrag`(renderConversationList ↔ initialize) · `_sidebarCatchupTimer`(_scheduleSidebarCatchup ↔ handleLogout) **2건뿐**이었고, `_shareRangeEscHandler`·`_mentionMembers` 계열은 함수 집합 설계(attach/detach 쌍 동반 이동)로 state 편입 없이 해소됐다. 그 2건만 `state` 로 편입하는 비-중립 mini-change(Phase A, PLAN-APPROVED)를 선행한 뒤 사이드바(B1, -720줄)·composer(B2, -1,891줄)·progress(B3, 최다 회귀 영역이라 마지막)를 byte-동치 이동해 app.js **11,119 → 8,082줄(기점 대비 -27%)** + `app/` 6모듈(auth·profile·sidebar·messages·composer·progress)이 됐고, 4 PR(#1147~#1150) 전건 배포 + POST-DEPLOY PB-0008 PASS(B2·B3 는 실전송 완주 스모크 14/14)다. 잔여 admin.js 4,804줄은 계정/역할 공유 권한 grid·pending/batch-apply 엔진 등 공유 코어라 재실측 후 판단으로 남겼다. ⓑ **feature-0036 판정 순환 차단 + 결과 노출**: \"운영 현황에서 분석문 사실성 검증이 분석 작업의 대부분을 차지한다\" 는 사용자 관측이 발단이었고, 실측은 **대상 선정(행 단위)과 저장(노드당 1행)의 단위 불일치**를 지목했다 — 같은 노드에 여러 분석 run 의 done 행이 쌓이므로 A세대를 저장하면 B세대가 미판정으로 남고 B세대를 저장하면 A행이 지워져 다시 미판정이 되는 **무한 순환**으로, 7일간 **7,896콜**(배경 LLM 호출의 62.8%)을 태우고도 판정 행은 91개에 고정(노드당 평균 87회 재판정)돼 정보 증가가 0 이었다. done 행이 2개 이상인 노드만 반복하고 1개인 노드는 1회 후 조용해 대조군까지 성립했다. 대상도 `DISTINCT ON (scope_key, node_key)` 로 노드당 최신 1건으로 좁히고(ADR-0036-08) \"최신\" 기준을 `updated_at` 이 아니라 **`id`**(트리거가 모든 UPDATE 에서 `updated_at` 을 올려 과거 run 행이 재분석 행보다 최신으로 역전된다 · 상세 패널과 같은 기준이어야 판정 대상과 표시가 같은 문장을 가리킨다)로, 순회를 \"판정 오래된 것부터\"(미판정 우선만 두면 갱신된 분석문의 재판정이 굶는다)로 교정하니 시간당 148콜 → **~14콜**(25분 6콜/6판정, 낭비 0)이 됐다. 순환을 고쳐 비용은 90% 줄었으나 `node_analysis_verdicts` 를 읽는 코드가 0곳이라 효용이 0 이었으므로, 그래프 뷰 노드 상세 'AI 능동 분석' 박스에 **판정 배지 + 근거 한 줄**을 붙였다 — **표시도 해시가 일치할 때만 한다**(ADR-0036-09: 판정은 노드당 1행이라 분석문이 갱신되면 그 행은 지금 보이는 문장의 판정이 아니다 · 미판정·판정 실패·해시 불일치는 모두 \"표시 없음\"으로 수렴하고 운영자가 구분해야 할 것은 판정의 유무이지 없는 이유가 아니다), 증거 stage 0 은 표본이 0행이므로 라벨이 \"표본 통계와 …\" 가 아니라 **\"구조와 …\"** 가 된다(ADR-0036-10 — 라이브 판정 95건 중 82건이 stage 0 이라 이 구분이 곧 정직성이며, stage 승격 시 재판정하도록 대상 선정이 저장된 `evidence_stage` 와 현재 stage 를 비교한다). PB-0008 실측 PASS(stage 분기 · 판정 없는 노드 완전 미표시 · 나머지 문단 회귀 0). ⓒ **feature-0002 첨부 변경 사실 코드-권위 봉인**: 답변이 \"새로 첨부되거나 변경된 파일이 없습니다\" 라고 단정했는데 DB 실측은 v2 갱신 8 · 신규 4 · 이월 1 = 13(assistant 가 나열한 수와 일치)이었다. 컨텍스트는 정상 전달됐음을 배포본 ask-worker 재구성으로 **입증**(★신규 12 · 🔄v2 8 · FILE UPDATES diff 8건 · 섹션 38,013자)했으므로 근본은 전달 실패가 아니라 **부재 단정을 막는 코드-권위 규칙의 부재**(기존 봉인·LIVE-DB GROUNDING 은 DB 축 전용)와 표식이 72k자 프롬프트 중간(offset 25,840)에만 있던 위치였다 → `compose_system_prompt` 말미의 코드-권위 사실 블록(A) + 사용자 턴 매니페스트(C) + red-team 리뷰어 능동 검출(B) 3축 + 단일 사실 채널로 교차-대화 오염 차단. §18.8 backend/qa 패널이 **BLOCK** 을 냈고 [P1] 5 · [P2] 8 전건 흡수 후 뮤테이션 역검증 8종이 전건 KILLED 됐다(초판은 이 중 6종이 생존했다). PR #1155 배포 + 배포본 런타임 실증 완료이고 라이브 사용자 대화 실측만 다음 audit 의 corroboration 으로 이월했다. ⓓ **feature-0011 P5a 종결**: Step 6(feature 단위 Dockerfile 분리)을 **불채택으로 종결**했다(ADR-20260805T153000) — 분리의 전제였던 import 얽힘·경계 불명이 shared/ 추출 + CODEBASE_MAP §7 Known Gaps 로 이미 해소되고 그 뒤 배포 스파인(feature-0014/0020/0039)이 단일 이미지를 전제로 안정화됐으므로, 재개는 이미지 크기·보안 요구가 실측으로 등장할 때 별도 initiative 로 한다. 동반으로 P3-제외 로드맵의 유보 3건을 전수 실측해 100% 검증(wiki 백필 기완료 실증 · skeleton 기해소 · PB-0008 통합 불채택)하고 wiki/README §5.1 에 mirrors/sources 대상 범위를 명문화했다. ⓔ **feature-0003 하네스 red 전수 해소**(tests-only · `src/static` 접촉 0): `tests/verify_*.mjs` 39개 중 **23개가 red** 였다 — CI 비배선이라 조용히 썩던 상태로, feature-0038 REPORT §8 기록 12건에 전수 실측이 11건을 더 찾았다(Cycle 7~10 ESM 전환으로 새로 부러진 것 + jsdom 환경 소실). 원인 6유형(jsdom ESM /tmp 폴백 부재 · 소스-추출 eval 의 신규 의존 미주입 · admin.js ESM 전환으로 classic 주입 SyntaxError · asset-stamp 전환 이전의 cache-buster 토큰 단언 · 기능 진화 미추종 stale 앵커 · 정확 카운트 취약 단언)을 **현행 계약으로 갱신하되 검증 취지는 보존**(동어반복 금지)해 수리하니 **40/40 green** 이고, settings-notif 시나리오는 전 블록을 page 전역 호출 → DOM 이벤트 경유로 재작성해 실 Windows Chrome **20스텝 전건 OK**(알림 게이팅 매트릭스는 `verify_notify_gating.mjs` 신설로 CLI 이관). ⓕ **template v3.44.1**: worktree 격리 하네스 강제 최소버전을 v2.1.216+ → **v2.1.222+** 로 정정했다 — v2.1.210·v2.1.216 은 subagent git-재지정 3종만 막은 부분 수정이었고 격리 세션 자신이 main checkout 에 파괴적 git 명령을 거는 경로는 v2.1.221 까지 열려 있었으므로, 그 미만에서 F0 는 하네스가 강제하는 게이트가 아니다. [[Features/feature-0038-frontend-modularization]] · [[Features/feature-0036-analysis-verification]] · [[Features/feature-0002-agent-core]] · [[Features/feature-0011-shared-extraction]] · [[Features/feature-0003-agent-web-ui]].
> **2026-08-06~08-11**: (64) **첨부 워크벤치 성숙 — 버전 diff 구문색·줄 안 변경 구간·문서 원문 보기·분열 체인 병합 (feature-0003) + 리뷰어 발췌의 접지와 자가 검증 대기의 탈출구 (feature-0002) + 배포 창 전면 503 근본 해소 (feature-0014) + OAuth 계정 소진 우회·토큰 자동 회전 (feature-0007) + 익명 API 표면 축소 (feature-0003)** — 신규 feature 0건, 전부 기존 feature 의 성숙과 라이브 사고 대응이다. ⓐ **feature-0003 첨부 트랙**: 08-06 에 들어온 버전 diff 화면이 5 cycle 을 더 거쳐 실사용 가능한 도구가 됐다 — 구문 색은 저장소 단일 primitive `static/code-highlight.js`(SQL/JSON/YAML/XML/CSV/TSV · `textContent` 전용, `innerHTML` 경로 0)로 두고 `app.js` 의 로컬 키워드 목록을 삭제·import 했으며(복제가 곧 결함 기전), 라이브에서 keyword 만 색이 빠진 것은 팔레트가 아니라 **고아 `*/` 주석**이 CSS 파서에 셀렉터로 읽혀 다음 규칙을 삼킨 것이라 가드를 주석 구조에 걸었다. **줄 안(글자 단위) 변경 구간**은 서버 단독 계산으로 토큰 앵커(ASCII 단어/CJK 1자/공백 런) 뒤 자소 단위로 좁히고(토큰만 쓰면 식별자 전체가 칠해지고 문자만 쓰면 `SELECT`↔`INSERT` 가 색종이가 된다) 프론트는 구문 하이라이트 텍스트 노드를 문자 오프셋으로 쪼개 덧그리되 **세그먼트 총합이 셀 원문과 다르면 아무것도 그리지 않는다**. 비용 상한은 대리값이 아니라 실제 비용으로 잡았다(행 수 제한만으로는 411초 · 최종 3겹 컷에서 최악 입력 2997→5.3ms). 마크는 배경이 아니라 `box-shadow` 인데(배경 칠은 토큰 AA 대비를 깨고 `text-decoration` 은 탭 위에 0px 로 그려진다) `inset` 바가 `_` 자리와 겹쳐 `legacy_gy_pay` 가 `legacygypay`, `__init__` 이 `init` 으로 읽히던 사용자 지적을 바깥 그림자 `0 2px` 로 해소했다(글자 영역 마크색 픽셀 0 실측). 내용이 동일한 두 버전은 **맥락 축약이 "변경 주변만 남기는" 연산이라 남는 행이 0** 이어서 배너만 있는 빈 화면이었는데, `identical` 을 축약 대상에서 제외해 원문 전량을 렌더하고 "문서 원문" 이라는 **단어의 정직성**(행 상한 절단 · 1MB cap · `splitlines()` 가 흡수하는 줄 종단자 차이로 sha256 은 다른데 identical=true)을 `_identicalFlags` 한 곳으로 모았다. 이어 목록 행 자체를 열었다 — 내용을 보는 유일한 길이 비교 모달이고 그 진입점은 `versions.length > 1` 뒤라 **버전 하나뿐인 첨부는 내려받아야만 볼 수 있었던 것**을 `GET /api/attachments/{id}/source` + 원문 모달로 풀었고(권한·D21 게이트는 `/diff` 와 동형, 버전 파라미터를 두지 않아 식별 경로 비대칭을 차단), 성능 개선이 `len(raw) > cap` 절단 판정을 영원히 거짓으로 만들 뻔한 것을 DB 정본 `SizeBytes` 판정으로 막았다. 그 다음 사용자 보고("비교 버튼 유무에 따라 원문 버튼 위치가 뒤틀린다")는 오른쪽 정렬 flex 의 선행 구조라 빈 슬롯 예약 primitive 로 활성 목록·휴지통까지 3종을 함께 닫았다(패널의 수치는 실측 기각됐지만 "min-width 는 바닥이지 고정이 아니다" 라는 기전 지적이 옳아 글꼴 확대 A/B 로 재현 후 `flex: 0 0 22px` 로 고정 — 수치가 틀렸다고 기전까지 기각했으면 놓칠 결함이었다). 데이터 축에서는 이미 **갈라진 첨부 체인 155 row/62 논리파일/14 대화**를 병합 도구로 정리했고(dry-run 기본 · 스냅샷 + 사람이 실행 가능한 롤백 SQL · 사후 재검증), 라이브 적용이 자기 결함 2건을 드러냈다 — PG 미러만 단순 호출이라 중간 상태 UNIQUE 충돌이 fail-soft 로 삼켜져 29 row 가 옛 상태로 남았고(목록 read 는 PG 우선이라 화면이 어긋날 상태였고 사후 검증은 MySQL 기준이라 "잔여 0" 을 보고했다) 선재 live 중복 1건은 분열 조건에 걸리지 않아 살아 있었다. 다운로드 파일명 접미 규칙은 서버 정본 함수(keep/strip/force/auto) 한 곳으로 통일해 프론트 중복 규칙을 제거했다(규칙이 두 벌이라 AI 편집본이 `report_v2_v2.csv` 로 나갔다). ⓑ **feature-0002 리뷰어 접지·탈출구**: 첨부 맨 끝 줄을 근거로 한 **정확한** 답변에 red-team 이 grounding BLOCK 을 걸던 것(발췌가 head-only 라 인용 줄이 digest 에 없고 grounding 은 rederive 적격 축이 아니어서 옳은 답변에 "자가 검증 미해소" 배너가 찍혔다)을 인용 구간 앵커 + 문자 기준 coverage 권위 + 면책 경계 명시로 봉인했고, 배포 후 라이브 재현이 이중과금(창 단위 차감으로 실전달 862/1,200자)과 **초안이 논하는 바로 그 파일의 통째 강등**(첨부 id 고정 순서)을 잡아 union 회계 + 관련성 정렬 + 지분 비례 축소로 고쳤다. 리뷰어 digest 에 v(n-1)→v(n) diff 가 없어 "무엇이 바뀌었나" 에 정확히 답해도 무근거로 보였던 것과, 사용자가 완독을 명시하면 verify 패스가 면책 규칙을 어기고 honesty BLOCK 을 내던 것까지 닫고 같은 대화·같은 문구 재현으로 `verdict=pass`·0 block 을 확인했다. 별건으로 답변 본문이 9.6초에 끝났는데 리뷰어 무응답 때문에 전달이 **312초** 지연되고 그 구간에 취소·진행 표시가 전무했던 결함(신규 사용자 첫 턴이 2메시지로 종료)은 `abort_fn`·`progress_fn` 이 반복 수정 루프에만 걸려 있던 것이 근본이라 최초 검증 패스를 중단 가능한 폴링으로 바꾸고, 재검증 미완료 시 직전 BLOCK 을 '미해소' 로 단정하지 않게 했다. ⓒ **feature-0014 배포 창 전면 503**: 사용자 보고("최근 배포 과정 중 서비스가 멈춘다")를 엣지 로그로 추적해 **무중단 불변식이 실제로 깨져 있었음**을 확인했다 — 롤링 게이트가 컨테이너 내부 `/readyz` 까지만 보고 엣지가 그 replica 를 다시 LB 후보로 쓰는지는 보지 않아, Caddy 의 passive 격리(30s)가 풀리기 전에 다음 replica 를 내려 available upstream 이 0 이 됐다(6시간 창 `no upstreams available` 71건 · 전면 503 6회 × 12~17초). 판정을 2축(admin API `fails==0` + Caddy→replica `/livez` 실도달)으로 세우고 predrain 을 fail-closed 로 만든 뒤 **다음 배포 창에서 503 = 0 · 요청 172건 전부 200** 으로 실효를 확정했다(게이트가 실제로 격리 해제를 기다린 로그까지 확인). ⓓ **feature-0007 LLM 가용성**: 7일 쿼터가 100% 소진돼도 자격증명 파일은 유효해 30분마다 소진 계정이 1순위 slot 에 재주입되던 것을 저빈도 1-probe 소진 게이트로 우회하고(네트워크 오류는 fail-open — 도달성 장애를 소진으로 오판 금지), 그 뒤 **access token 이 CLI 세션에 의존해서만 회전**하던 구조를 선제 자동 회전으로 바꿨다(TTL ~8h · 야간·주말 공백에 계정이 탈락하고 다른 계정마저 소진되면 LLM 전면 중단 — 2026-08-07~11 실사례). 자격증명 저장소 쓰기라 적대 패널 2렌즈가 FAIL 을 냈고 P1 5건(`--check` 가 일회성 refresh token 소모 · 회전 예외로 1순위 slot 무음 정지 · 과거 만료 되쓰기 · `.bak` 심링크 · 백업 실패로 소모된 refresh token 유실) 전량 수정 후, 운영자 확인으로 계정 분리에 신뢰 경계가 없음이 명문화되며 P1 2건의 심각도가 "권한 경계" → "위생" 으로 재평가됐다(조치는 유지). ⓔ **익명 API 표면**: 외부 AI 가 로컬 파일 없이 라이브 HTTP 응답만으로 낸 지적 9건 중 8건이 사실로 판정돼 익명 3경로(`/api/session`·`/api/llm/health`·`/api/api-vault/options`)를 최소 응답으로 축소하고(세 경로 모두 `docs/SECURITY.md §7` allowlist 미등재 = 정책-코드 drift) 엣지 보안 헤더를 일괄 부착했으며(HSTS 는 `/trust` 평문 CA 번들 배포와 충돌해 의도적 제외) 데이터플레인 RO 계정으로 `DB_USER=root` 폴백을 제거했다. 자체 적발로 `api-vault/options` 의 예외 경로가 **fail-open**(DB 를 불능으로 만들 수 있는 요청자가 오히려 전체 카탈로그를 받는 구조)이었던 것도 함께 닫았다. ⓕ **08-06 창**(직전 sync 가 카드·hot·Log 에만 반영하고 이 타임라인에는 남기지 않은 구간): 대화 첨부 수명주기 개방(`attach-manage` — **Critical**, 삭제·복구 인가를 열람 경계에서 분리 + `ADR-20260729T163000-attach-append-only` supersede) · 첨부 버전 diff 신설(임의 쌍·다단계) + 후속 5 cycle · 폴더 단위 첨부/중복 스킵 UX/편집본 체인 통합 · 공유 링크 발화자 배지 · 배경 dismiss 단일 primitive · feature-0002 `update_attachment` 도구(첨부 전달을 답변 출력 예산과 분리) + `read_attachment` 완전성 계약 — 상세는 각 카드 §3. [[Features/feature-0003-agent-web-ui]] · [[Features/feature-0002-agent-core]] · [[Features/feature-0014-zero-downtime-deploy]] · [[Features/feature-0007-bedrock-llm-provider]] · [[Features/feature-0009-group-conversation]] · [[Features/feature-0006-lan-proxy-access]].
> **2026-08-11~08-12**: (65) **외부 AI 도구 표면 — 추론 주체 반전 (feature-0041, 신규·Critical) + 역할 기반 DB 객체 탐색 (feature-0040, 신규·Major) + 무중단의 마지막 구멍과 배포 스파인 *밖* 재생성 봉인 (feature-0020) + 일시 LLM 장애에서 작업 보존·추론 재개 (feature-0002) + 첨부 .md 문서 렌더·말풍선 diff 진입·한/영 자판 교차 검색·메타데이터 pane 재구성 (feature-0003)** — 신규 feature 2건. ⓐ **feature-0041 (신규·Critical)**: 지금까지 외부 AI 는 feature-0023 의 `ask` 로 **우리 LLM 에게 추론을 맡길** 수만 있었다. 이 표면은 방향을 뒤집어 외부 사용자의 AI 가 **자기 계정 LLM 으로 직접 추론**하면서 데이터소스·RAG·메타지식만 가져가게 한다(LLM 비용은 호출자 부담·서비스는 자격증명 무보관). OAuth AS(DCR·authorize=세션 쿠키 필수·token·revoke) + P0 도구 9종 REST 로 열되 관문 순서를 **토큰→상한→스코프→실행→각인→원장** 으로 고정하고 `P0_TOOLS` allowlist 로 도달면 자체를 고정했으며, **원장 기록이 실패하면 결과를 반환하지 않는다**(거절·게이트도 원장에 남는다). 전송은 MCP stdio 와 HTTP/SSE 두 어댑터인데 **판단 로직은 0** — 토큰 무보관(요청 헤더 전달)·리다이렉트 시 Authorization 전달 금지·HTTP 는 loopback 기본. 발견 자료(매니페스트 `tool_surface`·큐레이션 OpenAPI 7 path)는 "익명=static contract, 인스턴스 데이터 0" 을 테스트로 못박았고, codex 적대 리뷰가 **L4 권한 비대칭이 교차 테넌트 노출**(client_id 는 DCR 로 공유 가능한 앱 식별자인데 상대 계정 id·권한 겹침을 응답에 실었다)을 잡아 응답에서 완전 제거·원장 전용으로 되돌렸다. 위협 모델은 `docs/SECURITY.md §44`(신뢰경계 반전) 신설. 1차 `a68fbbac`·2차 `e1372f32` 라이브 배포 + POST-DEPLOY 12항/8항 실측. 잔여는 관리 콘솔 '외부 도구 한도' 탭(사용자 결정으로 별도 cycle — admin.js 동시 편집 충돌 + PB-0008 하드 게이트)과 사람 1회 인가가 필요한 전 구간 e2e. ⓑ **feature-0040 (신규·Major)**: assistant 의 구조 탐색이 테이블·컬럼·인덱스·FK·루틴까지였고 트리거·이벤트·SQL Server Agent 작업·뷰·시노님·시퀀스는 **도구도 그래프도 없었다**. 분류 축을 벤더 객체명이 아니라 **역할**(6종 taxonomy SSOT)로 잡아 DBMS 확장에 열어 두고(사용자 결정), `search_db_objects`/`describe_db_object` 2종 + `db_objects` SSOT(alembic 0054) + AGE `DbObject`/`OBJECT_USES`(참조·실선)/`OBJECT_ON`(소유·파선) 로 그래프 뷰에 편입했다. 핵심 불변식은 **미지원 ≠ 부재** — 지원상태 4종(SUPPORTED/UNSUPPORTED/PRIVILEGED/DELEGATED)으로 0행을 "없다"로 뭉개는 허위 부재를 타입 레벨에서 차단하고 `msdb` 접근은 한 함수의 고정 질의로만 한다. 테스트가 실결함 3건(DbObject 클러스터 강등·routine 오판·cross-DB 스니펫 열 소실)을 적발했고 실 Windows Chrome 150 WebGL 실증까지 마쳤고 **라이브 배포 완료**(doc_sync 08-13 실측: alembic `0054` 적용 · `db_objects` 339행 · 그래프 자산 라이브 byte-identical) — 잔여는 POST-DEPLOY PB-0008 시각검증뿐. ⓒ **feature-0020**: 무중단은 web 에만 완성돼 있었다 — 워커·gateway 는 아무 상태도 보지 않고 `stop_grace_period` 만료 시 SIGKILL 했는데 실측(ask_jobs 30일 363건 p50 81s·p95 691s·**60초 초과 66%** vs drain 60s)이 그 타이머가 분포 안쪽임을 보였다. surge replica 는 신규 요청만 흡수하고 실행 중 run 은 프로세스 간 이전이 불가능하므로 "옮긴다"가 아니라 **"붙어 있는 게 없을 때 바꾼다"** 로 풀어(30일 중 busy 2.1%) ask_jobs running + web active_streams 를 폴링하는 **fail-closed quiesce 게이트**를 교체 직전에 걸었다. 이어진 2차 사고가 더 근본적인 구멍을 드러냈다 — 문제의 gateway recreate 는 `deploy-web.sh` 를 **경유하지 않았고**(배포 lock mtime 이 직전 배포 그대로), 즉 게이트가 구조적으로 볼 수 없는 경로였다 → 게이트 구현을 `bin/lib/quiesce.sh` 단일 정본으로 뽑고 `bin/safe-recreate.sh`(배포 밖 재생성의 인가된 진입점)·Makefile up/down 전치·`bin/recreate-audit.sh`(raw docker compose 는 막을 수 없으니 스탬프↔컨테이너 StartedAt 대조로 **보이게** 만들어 사후 적발)로 봉인했다. ⓓ **feature-0002**: 같은 사고의 사용자 쪽 얼굴 — 배포가 gateway 를 recreate 하면서 in-flight LLM 호출이 SIGKILL 되자 앱이 그 예외로 run 을 통째로 terminal 종결해, 7분 40초를 기다린 사용자가 LLM 5라운드·도구 10회 분량 조사를 잃고 `Connection error.` 원문만 받았다(90일 corroboration 18 대화). 1차로 같은 라운드 재호출(누적 messages 무손실·대기 중 1초 주기 취소 폴링·탈출구가 재시도보다 우선)을 넣었고, 2차 실사례에서 **실패한 것은 재시도가 아니라 예산**임이 드러나(2회×4.5초 vs 게이트웨이 부재 48초) 값싼(요청 미도달) 실패 전용 예산(6회·cap 30s·누적 120s)과 **재개 재큐**(`requeue_ask_job_for_resume` — 누적 도구 결과는 `core_messages` 에 있고 로더가 replay 하므로 재claim 이 이어받는다·**재큐 분기는 KV terminal 기록 앞**)로 확장했다. 429 는 도달해서 거부된 것이므로 값싼 부류에서 제외한다. ⓔ **feature-0003**: 말풍선 수정본 첨부 칩에 `⇄` diff 진입(preselect 는 체인에 남아 있는 직전↔이 버전 — `thisVer-1` 하드코딩은 중간 버전 삭제 체인에서 무음 오표시가 된다) · 첨부 `.md` 를 구문 하이라이트로 연 뒤 사용자 재지시("구문 색이 아니라 실제 마크다운 구성으로")를 받아 **마크다운 렌더 토글**로 정정(선행 작업은 유지 — 원문·diff 는 줄 대조가 목적이라 렌더하면 기능이 사라진다) · **한/영 자판 교차 검색**(`ㅎㅋ`→`gz`, `rmffhqjf`→`글로벌` — 제시된 예시 4개가 전부 실제 제품이었고 종전엔 "검색 결과가 없습니다"뿐이었다. 같은 매핑표 primitive 2벌로 클라이언트 13곳·서버 6경로, 후보 첫 항목이 항상 원문이라 회귀 0 을 기계 단언) · '+ 데이터베이스 추가' 목록이 체크마다 밀리던 것(실측 단일 38.3px·연속 3회 114.3px)을 **스크롤 보정이 아니라 편집기 순서 교체**로 구조적 0 화 · 메타데이터 pane 을 "이 pane 만 앱 토큰·계약을 안 쓰는 8건"으로 환원해 단일 표 surface 로 재구성(DOM 셀렉터·dataset 계약 무변경).

## 2. 상세

### 2.1 핵심 영역

| Feature | 책임 | 카드 |
|---|---|---|
| feature-0001-platform-runtime | MySQL 8.0 / DAB 설정, SQL 유틸리티, replica 연결 정책, redo log 1 GiB | [[Features/feature-0001-platform-runtime\|카드]] |
| feature-0002-agent-core | agent core / 멀티 데이터소스(MySQL·MSSQL) / dialect / Postgres KB / insight worker | [[Features/feature-0002-agent-core\|카드]] |
| feature-0003-agent-web-ui | FastAPI Web UI + 관리콘솔(데이터소스·대시보드·LLM사용량) + audit + 첨부 + ask-worker | [[Features/feature-0003-agent-web-ui\|카드]] |
| feature-0004-browser-automation | Playwright 기반 browser 자동화 service | [[Features/feature-0004-browser-automation\|카드]] |
| feature-0005-qa-mcp | MCP test + QA script | [[Features/feature-0005-qa-mcp\|카드]] |
| feature-0006-lan-proxy-access | Caddy TLS + Windows LAN proxy + `X-Forwarded-For` trust 정책 | [[Features/feature-0006-lan-proxy-access\|카드]] |
| feature-0007-bedrock-llm-provider | AWS Bedrock (Claude) gateway + per-user OpenAI key 폐기 | [[Features/feature-0007-bedrock-llm-provider\|카드]] |
| feature-0008-windows-browser-testing | 실제 Windows 브라우저 AI 자동 검증 (PB-0008 · ADR-0029) | [[Features/feature-0008-windows-browser-testing\|카드]] |
| feature-0009-group-conversation | 그룹 대화 — 멤버십·`@assistant` 멘션·열람≠발화 분리·라이브 UX | [[Features/feature-0009-group-conversation\|카드]] |
| feature-0010-google-drive-integration | 계정별 Google Drive 연동 토대 — OAuth 토큰 암호화 + MCP 구성 seam (비활성 scaffold) | [[Features/feature-0010-google-drive-integration\|카드]] |
| feature-0011-shared-extraction | 공통 코드 `shared/` 추출 리팩터 (model_catalog·config·db·conn_health·datasources) — **P5a 종결(2026-08-05)**: alias shim 4종 전량 제거로 추출이 완성됐고 Step 6(feature 단위 Dockerfile 분리)은 **불채택 종결**(분리 전제가 shared/ 추출 + CODEBASE_MAP §7 로 해소·이후 배포 스파인 0014/0020/0039 가 단일 이미지를 전제로 안정화 · 재개는 이미지 크기·보안 요구가 실측으로 등장할 때 별도 initiative — ADR-20260805T153000-p5a-closeout) | [[Features/feature-0011-shared-extraction\|카드]] |
| feature-0012-web-router-modularization | feature-0003 `app.py` 도메인별 `APIRouter` 점진 분할 토대 (P5b — route-parity 안전망 + 의존성 audit, behavior-neutral) | [[Features/feature-0012-web-router-modularization\|카드]] |
| feature-0013-relationship-diagrams | flow/관계 질문에 mermaid 다이어그램 답변 + `table_relationships` 관계 저장소 (FK introspection·대화 JOIN 학습) | [[Features/feature-0013-relationship-diagrams\|카드]] |
| feature-0014-zero-downtime-deploy | web 무중단 롤링 배포 — Caddy LB(web-a/web-b) 뒤 2-replica 한 번에 하나씩 재시작 + 배포 스파인 `bin/deploy-web.sh`(자동 롤백·:18080 폐기→:443 단일) | [[Features/feature-0014-zero-downtime-deploy\|카드]] |
| feature-0015-zd-hygiene-backup | 백엔드/DB 무중단 위생 — insight-worker graceful 종료 + MySQL online-DDL 게이트 + 백업·복원 리허설 cron | [[Features/feature-0015-zd-hygiene-backup\|카드]] |
| feature-0016-metadata-graph | 메타데이터 지식그래프 — 관계형 SSOT→AGE `metadata_kb` 투영 + 관리콘솔 그래프 뷰(검색·이웃) + `graph_navigate` AI 도구 | [[Features/feature-0016-metadata-graph\|카드]] |
| feature-0016-zd-pg-pause-caddy | PG 재시작 무중단화 — pgbouncer PAUSE 래퍼(`pg-restart.sh`) + deploy-web.sh Caddyfile reconcile | [[Features/feature-0016-zd-pg-pause-caddy\|카드]] |
| feature-0017-deploy-build-gate | 배포 스파인 빌드 게이트 false-failure 수정 — 이미지 정합 검증(snap-docker metadata-race) | [[Features/feature-0017-deploy-build-gate\|카드]] |
| feature-0019-message-editing | 메시지 편집 — 1:1 대화 내부 브랜치 트리(단순 수정 / 요청 수정 분기 재답변 `< n/m >`) + 공유(그룹) 단순 수정(@assistant 잠금) · Phase 1+2 라이브 | [[Features/feature-0019-message-editing\|카드]] |
| feature-0020-zd-deploy-all | 무중단 배포 커버리지 — deploy 스파인(`bin/deploy-web.sh`) 확장: 워커(insight/ask) 자동 롤아웃 + bedrock-gateway surge replica 무중단 교체 + caddy/alembic stale-image 가드 | [[Features/feature-0020-zd-deploy-all\|카드]] |
| feature-0021-redteam-review | assistant 답변 자가 적대(red-team) 리뷰 — 답변 전달 전 fresh-context 5축 검증(grounding/SQL/권한/완전성/정직성·강도 게이팅·fail-open) + [세션,제품] 메모리 노트(TTL) + 관리 콘솔 'AI 추론' 탭 (코드 거주 0002/0003/shared) | [[Features/feature-0021-redteam-review\|카드]] |
| feature-0022-agent-scratch-workspace | assistant PG 자율 작업공간 — 전용 DB `agent_scratch`(대화별 격리 스키마)에서 외부 데이터소스 반입·cross-source JOIN·TTL(기본 24h) + scratch_guard allowlist (2026-07-21 라이브 활성·기본값 OFF 게이트·코드 거주 0002/shared) | [[Features/feature-0022-agent-scratch-workspace\|카드]] |
| feature-0023-conversation-api-access | 외부 AI용 Conversation API — 세션 쿠키 없이 **Bearer 토큰**으로 대화 API(`/api/ask`) 호출 + 대화를 tool 화하는 MCP 서버(tool 4종) · scope allowlist ∩ 계정권한 · 관리 네임스페이스 절대 denylist(→ 관리 콘솔 차단) (2026-07-22 배포·라이브 e2e 통과·코드 거주 0003/0002) | [[Features/feature-0023-conversation-api-access\|카드]] |
| feature-0024-conversation-folders | 대화 폴더(프로젝트 워크스페이스) — 사이드바 재귀 폴더로 대화 조직·이동·폴더 삭제해도 대화 보관(Trash·undo·승격)·런타임 max-depth·폴더별 커스텀 지침 ask-time 주입·엄격 per-user 격리 (2026-07-23 라이브 완결 Phase1+2a·코드 거주 0002/0003) | [[Features/feature-0024-conversation-folders\|카드]] |
| feature-0025-worker-parallelism | 워커 성능·병렬 처리 설정 — 백그라운드 워커(노드 분석·cluster_label)·사용자 답변·KB 임베딩의 병렬도·처리주기·배치크기를 관리 콘솔 '시스템 > 설정 > 성능·병렬 처리' 서브탭에서 조절(feature-0018 runtime-settings 재사용·기본 동시성 1=현행 직렬 byte-동치 opt-in·LLM만 병렬/DB직렬·`system.runtime.*` RBAC 재사용·clamp 방어) (2026-07-24 머지·§18.8 SHIP·배포후 PB-0008 잔여·코드 거주 0002/0003/shared) | [[Features/feature-0025-worker-parallelism\|카드]] |
| feature-0026-perf-observability | 성능 관측 인프라 — web HTTP per-route 타이밍·요청당 DB 커넥션 카운터·워커 LLM latency 백필(13 sites)·답변 파이프라인 단계 계측(redteam/post-answer/grounding)·graph-sync duration·`bin/perf-snapshot.sh`·Caddy access log (측정 전용·사용자 가시 동작 0·additive/fail-open·신규 권한/스키마/UI 0·2026-07-27 머지·in-progress 배포 잔여·코드 거주 0002/0003/0006/shared) | [[Features/feature-0026-perf-observability\|카드]] |
| feature-0027-perf-latency-p0 | P0 성능 개선 1차 (feature-0026 병목 지도 기반) — post-answer 큐레이션(topic/용어/ENUM LLM 3건) 답변 KV terminal 후 이동(체감 -25~35s·결과물·scope 귀속 불변)·grounding RO 연결 3→1·MySQL 버퍼풀 128MB→1G·Caddy gzip+immutable static (품질 영향 축=레드팀·thinking 불변·비파괴/가역·인가/스키마 0·위험도 Minor~Major 경계·2026-07-28 구현·검증/배포 진행·코드 거주 0002/0001/0006/bin) | [[Features/feature-0027-perf-latency-p0\|카드]] |
| feature-0028-web-perf | web 계층 성능 (feature-0026 지목 병목) — `/api/ask_result` long-poll 워커 스레드 이관(이벤트 루프 정지 제거)·스냅샷 PG 연결 4~5→1(단일 왕복 번들)·권한 카탈로그 TTL 캐시+세션 LastSeenAt throttle·web MySQL 연결 풀 opt-in (응답 shape·인가 경계 불변·additive/가역 env 토글·위험도 Major 경계·2026-07-28·코드 거주 0003) | [[Features/feature-0028-web-perf\|카드]] |
| feature-0029-graph-churn | 그래프 sync churn 근절 — 메타데이터 그래프 incremental sync 무의미 재투영 3종 제거: 값 무변경 시 `updated_at` 미전진(upsert·신호·트리거 alembic 0046·실측 churn 63%)·status 히스테리시스(trusted/broken 왕복 차단)·프로브 broken 부활 금지·공유 정점 중복 MERGE 제거(관계당 cypher 11→1~3) (그래프 신선도·자기교정 보존·가역·위험도 Major 경계·2026-07-28·코드 거주 0002) | [[Features/feature-0029-graph-churn\|카드]] |
| feature-0030-ask-timeout-extension | 실행 타임아웃 임박 시 사용자 확인 후 연장 — 요청 처리가 실행 예산(콘솔 `실행 타임아웃` 파생값)의 80%(설정 가능)에 도달하면 작업 화면 컴포저에 비차단 배너(+탭 백그라운드면 브라우저 알림)로 '계속 추론할까요?'를 묻고, 승인한 그 run 한정으로 run 예산 컷을 해제해 답변을 완주(미승인=종전 타임아웃 동작). 개별 LLM 호출 상한 15분은 의도적으로 유지 — '중단'·'즉시 답변'·`max_steps`·lease fencing 응답성 보존(codex P1-3). 신규 권한 `conversation.extend.{own,any}`(own 만 finalize 보유 역할 1회 backfill)·콘솔 설정 3종(기능 게이트 기본 1=ON·임계 80%·추가 허용 상한 0=무제한) (위험도 Major·2026-07-29 머지·배포 80cc7aeb + PB-0008 라이브 PASS·in-progress·코드 거주 0002/0003/shared) | [[Features/feature-0030-ask-timeout-extension\|카드]] |
| feature-0031-analysis-grounding | 노드 분석 접지 — 운영 DB 에서 **통계만**(원시 샘플값·문자열 min/max 배제, `value_pattern` 6종 CHECK) 단계 수집(Stage 0 카탈로그(사용자 테이블 read 0)~Stage 2 표준 표본까지 하루 최대 1단계 승격(주간은 Stage 1 상한)·Stage 3 정밀은 운영자 승인 knob 전용·`ds` 자원 예산 게이트 위 fail-soft) → `metadata_table_stats`/`metadata_column_stats`(alembic 0050) 적재 → 노드 분석 payload 의 `evidence` 블록 주입 + `NODE_ANALYSIS_PROMPT` '증거 우선' 개정 + thin 판정 길이→항목 충족도 (LLM 호출 수 불변·시그니처 미유입·위험도 Major·2026-07-30 머지·in-progress 배포 검증 잔여·코드 거주 0002/shared) | [[Features/feature-0031-analysis-grounding\|카드]] |
| feature-0032-llm-token-budget | 백그라운드 LLM 토큰 예산 — 사람이 요청하지 않은 자동 지출에 rolling 24시간 상한(`agent_runtime.llm_usage` 원천·진입점 3곳 게이트=노드 분석 tick claim 전/클러스터 유지보수 pass/제품 분류 pass·커버리지 96%·60초 캐시·기본 2,000만) + 콘솔 'AI 운영 현황' 막대·attention 2단계. **사용자가 기다리는 호출은 집계·차단 양쪽에서 제외**·fail-open 3중 (`TODOS.md` P3 종결 전제가 실측에서 반전 — 과금 lane 99.7%·위험도 Major·2026-07-30 머지·in-progress·코드 거주 shared/0002/0003) | [[Features/feature-0032-llm-token-budget\|카드]] |
| feature-0033-analysis-synthesis | 클러스터 합성 요약(L2) — 의미 클러스터마다 "이 묶음이 함께 무엇을 하는가"를 2~4문장으로 합성해 `cluster_summaries`(alembic 0051)에 적재. PK=멤버셋 지문(`cluster_id` churn 회피)·캐시 키 3중 일치(멤버셋+L1 분석문+L0 증거)·요약은 클러스터링 시그니처에 **유입 금지**(재임베딩 순환 차단 불변식·테스트가 소스 단정)·pass 당 40개·배치 6·feature-0032 예산 배치마다 재확인·live 정지 스위치·routine 클러스터 1차 제외 (라이브 818 클러스터·15,365 멤버·라벨 평균 9자·위험도 Major·2026-07-30 머지·in-progress·코드 거주 0002/shared) | [[Features/feature-0033-analysis-synthesis\|카드]] |
| feature-0034-analysis-consumption | 분석 산출물의 대화 소비 — `agent_core._build_knowledge_context` 에 `TABLE GROUP SUMMARIES` 섹션 신설(질문이 언급한 테이블 → 그 테이블의 클러스터 라벨 → `cluster_summaries` 조회 → 근거 수와 함께 주입, 매칭 0건이면 섹션 생략). RI-5(분석 1만여 건이 답변 경로에서 참조 0) 해소·사전 계산분만(LLM 호출 0)·라벨 조회+`(scope, effective_schema, label)` 3중 격리·`strpos`(ILIKE `_` 와일드카드 회피)·`_datamark_untrusted`·statement_timeout 1.5s(SET+RESET)·전 구간 fail-soft·`cluster_summary_ms` 계측 (위험도 Major(답변 경로)·2026-07-30 머지·in-progress·코드 거주 0002/shared) | [[Features/feature-0034-analysis-consumption\|카드]] |
| feature-0035-analysis-planner | 결정적 분석 플래너 — 미분석 테이블을 중요도 순으로 자동 시드(`점수 = 대화 조인 이력 × 50 + 관계 차수 × 1`, 동점은 이름 오름차순·**LLM 0**·AGE 실시간 중심성 미사용(multi-hop 라이브 최악 82초)). 큐잉 미재구현(`enqueue_change_analysis` + `reason="coverage_priority"`)·2중 상한(스키마당 3 · 사이클 9 — 시드가 effective-schema 루프 안이라 사이클 상한이 load-bearing)·`object_key` prefix + `strpos(…)=1`(LIKE 의 `_` 와일드카드 회피)+`DISTINCT`·파티션 계열 대표 1개 축약(라이브 1,464 중 1,364=93% 날짜 접미 → web_ranking 미분석 719→11) (커버리지 2,040/17,192=11.9% 가 L1·L2·grounding 품질 상한이던 것을 해소·위험도 Major·2026-07-31 머지·PR #1104 배포 + 라이브 선정 실측(`dblog` 최고점 27 → item·guild·register)·후속 파티션 수정 배포 잔여·코드 거주 0002/shared) | [[Features/feature-0035-analysis-planner\|카드]] |
| feature-0036-analysis-verification | 분석문 사실성 판정 — 분석문을 L0 통계 증거와 대조해 `supported`/`contradicted`/`unverifiable` + 근거 문장 + 증거 깊이를 `node_analysis_verdicts`(alembic 0052)에 적재. **판정 실패는 행을 만들지 않는다**(LLM 오류·계약 위반·근거 없음·증거 부재·저장 실패 = 미검증 — fail-soft 가 "통과"가 아니라 "침묵": 확인 도장이 잘못 찍히면 위층이 그걸 근거로 더 확신한다). 예산 모듈 미가용만 방향이 반대로 pass 중단·`analysis_hash` 재판정 축(노드당 1행)·pass 당 20·1건 1콜·호출측 `limit` 이 설정 상한을 넘지 못함 . **2026-08-05**: 배포 후 라이브에서 판정 **무한 순환**을 발견·차단했다 — 대상 선정은 행 단위인데 저장은 노드당 1행이라 같은 노드에 쌓인 여러 분석 run 의 done 행이 서로를 미판정으로 되돌려, 7일간 7,896콜(배경 LLM 62.8%)을 태우고도 판정 행은 91개에 고정(노드당 평균 87회 재판정·정보 증가 0)됐다. 대상도 `DISTINCT ON (scope_key, node_key)` 로 노드당 최신 1건으로 좁히고 \"최신\" 기준을 `updated_at` 이 아니라 **`id`**(트리거가 모든 UPDATE 에서 `updated_at` 을 올려 과거 run 이 역전된다), 순회는 판정 오래된 것부터(미판정 우선만 두면 재판정이 굶는다)로 교정 → 시간당 148콜 **→ ~14콜**(ADR-0036-08). 이어 판정을 그래프 뷰 노드 상세에 **배지 + 근거 한 줄**로 노출했다 — 표시도 해시가 일치할 때만 하고(ADR-0036-09), 증거 stage 0 은 표본이 0행이라 \"표본 통계와 …\" 가 아니라 **\"구조와 …\"** 라벨이 된다(ADR-0036-10 · 라이브 판정 95건 중 82건이 stage 0) · PB-0008 실측 PASS (위험도 Minor·2026-07-31 머지·2026-08-05 순환 차단+노출 배포·in-progress·코드 거주 0002/0003/shared) | [[Features/feature-0036-analysis-verification\|카드]] |
| feature-0037-domain-synthesis | 도메인 합성(L3, lazy) — 스키마 하나의 도메인 개요를 그 스키마의 L2 요약들로부터 3~5문장 합성해 `domain_summaries`(alembic 0053)에 적재. 요청(`requested_at`)과 생성(`generated_at`)을 분리해 사전 전량 생성 금지(LazyGraphRAG)와 답변 경로 런타임 합성 금지(ADR-0034-07)를 함께 지킨다 — grounding 조회가 없으면 요청만 남기고(LLM 0·지연 0·요청 UPSERT 는 RO 조회 커넥션이 아닌 별도 RW 연결) insight tick 이 요청된 것만 합성. 재생성 축 `cluster_set_hash` 는 payload cap 25 이전 전체 집합(최대 300)으로 해시·pass 당 3·advisory lock 을 얻은 tick 에서만(행 단위 claim 부재)·30자 미만 미저장 (라이브 클러스터 요약 1,006건/120 스키마=평균 8·위험도 Minor·2026-07-31 머지·in-progress 배포·라이브 합성 실측 잔여·코드 거주 0002/shared) | [[Features/feature-0037-domain-synthesis\|카드]] |
| feature-0038-frontend-modularization | 프론트엔드 모듈화(ssot-consolidation ROADMAP ITEM-P5b 완결, 2026-08-04) — 프론트 모놀리스 3파일(admin.js 14,007 · app.js 13,165 · styles.css 9,328줄, 2026-08-03 실측)을 behavior-neutral 점진 추출로 도메인 모듈화하는 initiative unit. 코드는 feature-0003 `src/static/**` 거주(feature-0012 선례의 계획·추적·검증 홈 패턴)·big-bang 금지·cycle 당 단일 PR + 게이트(make test + PB-0008 + 롤백 리허설 + plan-review Critical). Cycle 1 배포 완료(styles.css → `css/` 7파일 순차 concat byte-identical(cmp 기계 증명)·PR #1124/1da17988·deploy-web RC=0·PB-0008 PASS·롤백 리허설 revert 왕복 diff 0) · Cycle 2 머지(admin.js 사용량·AI운영현황 pane → `admin/{usage,aiops}.js`, 14,007→13,057줄(-950)·역재구성 parity 증명) — JS 는 사전 cp QA 불가라 배포·POST-DEPLOY PB-0008 이 완료 게이트. **Cycle 3~10 + Final 로 완결(2026-08-04)**: styles.css(9,328) 소멸 + `css/` 7파일 · admin.js 14,007 → **4,804줄(-66%)** + `admin/` 9모듈(계 9,354줄) · app.js **11,119줄** + ES module 전환 + `app/` 4모듈(계 2,161줄), 10 cycle 전건 머지·배포·POST-DEPLOY PB-0008 PASS(PR #1124~#1143 10건). AC-3(오케스트레이터 ≤~3,000줄) 부분 달성 정직 보고 — 차단 요인은 공유 가변 `let` 의 양방향 재할당 결합(byte-동치 원칙 하 이동 불가·후속은 state 편입 mini-change 승인 선행). 재발 방지 = CONVENTIONS §14 code-modularity(임계 3,000/5,000줄 + 분할 방법 정본)·ROADMAP ITEM-P5b done·Final 은 코드 무변경으로 배포 생략. **후속 Phase A~B3 완결(2026-08-05, PLAN-APPROVED)** — 차단 요인이던 공유 가변 `let` 은 결합 매트릭스 실측(모듈-스코프 `let` 21개 × top-level 함수 297개)에서 도메인 경계를 넘는 양방향 재할당이 `_dqaDrag`·`_sidebarCatchupTimer` **2건뿐**임이 드러나, 그 2건만 `state` 로 편입하는 비-중립 mini-change(Phase A) 후 사이드바(B1)·composer(B2)·progress(B3) 를 byte-동치 이동해 app.js **11,119 → 8,082줄(기점 대비 -27%)** + `app/` 6모듈(auth·profile·sidebar·messages·composer·progress)이 됐다(PR #1147~#1150 전건 배포·POST-DEPLOY PASS). 잔여는 공유 코어인 admin.js 4,804줄의 재실측 판단 (위험도 Critical·PLAN-APPROVED 2026-08-03/2026-08-05·feature_status review(2026-08-05)·코드 거주 0003) | [[Features/feature-0038-frontend-modularization\|카드]] |
| feature-0039-ops-scheduler | 운영 정기 잡 인-컨테이너 이관 — 호스트 root crontab 의 `cd <repo> && bin/*.sh` 4줄(백업 `0 3 * * *` · 복원 리허설 `30 3 * * 0` · AGE 그래프 sync 증분 `*/30 * * * *` / 전량 `17 4 * * *`)을 `ops-scheduler` 컨테이너 서비스로 이관. 기존 래퍼가 전부 `docker exec` 였고 이 호스트의 docker 소켓이 root 전용이라 root crontab 이 요구됐던 것 — 잡을 서비스 안으로 옮겨 소켓 의존을 없애면 **root 권한 근거가 소멸**한다(역방향인 소켓 마운트는 컨테이너→호스트 root 승격 경로라 배제·마운트도 안 함). 스케줄 정본이 버전관리 밖 crontab → `docker-compose.yml` `OPS_SCHED_*` 로 이동하고, DB 접속은 `dbnet` 클라이언트로 전환하되 pg_dump 는 PGDG `postgresql-client-16` 으로 서버 메이저에 고정(client-17 산출물의 `SET transaction_timeout` 이 PG16 복원을 깨뜨림 — 실측)·AGE sync flock 은 호스트와 같은 inode bind-mount 로 `routine-backfill.sh` 와 상호배제 보존·산출물 경로·시각·가드 불변. 부수로 AGE cutover 이후 5주간 FAIL 하던 PG 복원 리허설 결함 수정(`ag_catalog` 사전 프로비저닝, 사용자 승인 하 범위 추가) → 761MB 실백업 전량 복원 PASS(PG 67·MySQL 32 테이블). §18.8 codex 패널 P1 5건 전건 흡수(Makefile 기동목록 누락 · SIGTERM 즉시 종료 → grace 280s/stop_grace 5m · 최종경로 직기록 → `.partial` staging·완료본만 회전 · `../artifacts` 전체 쓰기 마운트 → 2개로 축소 · `depends_on` 상속 → mysql/postgres override)·pytest 29건·전 스위트 3,647건 실패 0 (위험도 Major·2026-08-04 머지 PR #1131·feature_status review·**배포(ops-scheduler 기동) + root crontab 4줄 제거 잔여**·코드 거주 0002) | [[Features/feature-0039-ops-scheduler\|카드]] || feature-0040-db-object-explorer | 역할 기반 DB 객체 탐색 — 트리거·이벤트·SQL Server Agent 작업·뷰·시노님·시퀀스를 벤더 객체명이 아니라 **역할**(6종 taxonomy SSOT)로 분류하는 `search_db_objects`/`describe_db_object` 도구 2종 + `db_objects` SSOT(alembic 0054) + AGE `DbObject`/`OBJECT_USES`/`OBJECT_ON` 그래프 편입. 지원상태 4종(SUPPORTED/UNSUPPORTED/PRIVILEGED/DELEGATED)이 **미지원 ≠ 부재**를 타입 레벨에서 보장 | [[Features/feature-0040-db-object-explorer\|카드]] |
| feature-0041-external-ai-tool-surface | 외부 AI 도구 표면(추론 주체 반전) — 외부 사용자의 AI 가 자기 계정 LLM 으로 직접 추론하며 데이터소스·RAG·메타지식에 접근하는 표면(비용은 호출자 부담·자격증명 무보관). OAuth AS(DCR·authorize·token·revoke) + P0 도구 9종 REST(관문 순서 토큰→상한→스코프→실행→각인→원장 고정·원장 실패 시 결과 미반환) + MCP stdio / HTTP·SSE 어댑터(판단 로직 0) + `WebAiTasks` 미제출률 원장 · `docs/SECURITY.md §44` | [[Features/feature-0041-external-ai-tool-surface\|카드]] |

### 2.2 핵심 결정 (ADR)

41-feature 위에 다음 결정이 누적되어 현재 시스템을 형성한다:

- **ADR-0019** ([[Decisions/ADR-0019-web-audit-events|mirror]]) — `WebAuditEvents` 단일 테이블 + `record_audit_event` dispatcher + 16 endpoint hook + 4 RBAC 권한
- **ADR-0021** ([[Decisions/ADR-0021-kb-postgres-rbac|mirror]]) — KB Postgres 분리 + `agent_kb_rw` / `agent_kb_ro` 2-layer RBAC
- **ADR-0024** ([[Decisions/ADR-0024-postgres-database-isolation|mirror]]) — 단일 Postgres cluster + 별 database (`agent_kb` / `agent_drag`)
- **ADR-0025** ([[Decisions/ADR-0025-m5-cleanup|mirror]]) — M5 cleanup 14-day window + Stage A/B/C boundary
- **ADR-0026** ([[Decisions/ADR-0026-bedrock-llm-provider|mirror]]) — per-user OpenAI key → service-managed AWS Bedrock (Seoul region)
- **ADR-0027** ([[Decisions/ADR-0027-agent-runtime-pg-schema|mirror]]) — agent_runtime Postgres schema 설계 + search_path 전역 변경 금지 + AR-M4 read cutover
- **ADR-0028** ([[Decisions/ADR-0028-runtime-mysql-cleanup|mirror]]) — runtime 6 테이블 MySQL cleanup 14-day window + Stage A/B/C (Phase 2 완료 2026-05-27)
- **ADR-0029** ([[Decisions/ADR-0029-windows-browser-testing|mirror]]) — 실제 Windows 브라우저 AI 자동 검증 (PB-0008, feature-0008)
- **ADR-0030** ([[Decisions/ADR-0030-ssrf-guard-toggle|mirror]]) — datasource SSRF 사설망 경계 env 토글 (Major 보안 저하, 사용자 승인)
- **ADR-0022 / ADR-0023 / ADR-0025-pgvector** — TASK-0094 첨부 multi-cycle: MinIO storage + sandbox MySQL user 4종 + PGVector
- **Unit-level 설계** — 멀티 데이터소스 (ADR-CORE-0002/03/04) · fork hybrid (ADR-WEB-0005) · ask-worker (ADR-WEB-0004) 는 unit `DECISIONS.md` 정본 ([[Decisions/_Index#21-unit-level-설계-결정-repo-adr-와-별개|MOC §2.1]]).

[[Decisions/_Index|전체 Decisions MOC]] 참조.

### 2.3 데이터 흐름

```
User → Caddy/web (TLS) → FastAPI (feature-0003)
                              ├→ MySQL agent_memory (web* RBAC/audit/auth)
                              ├→ MinIO (첨부 storage)
                              └→ /api/ask → ask_jobs 큐 → ask-worker
                                    └→ agent loop (feature-0002)
                                          ├→ Bedrock gateway → Claude (feature-0007)
                                          ├→ Postgres agent_kb (KB + runtime, pgvector)
                                          └→ 데이터소스 N (MySQL·MSSQL, registry+allowlist+dialect)
```

> **2026-05-27**: agent runtime state (`agent*` 10 테이블) 가 MySQL → Postgres 이관 완료. MySQL `agent_memory` 에는 `web*` 18 테이블만 잔존.
> **2026-06**: data plane 이 멀티 데이터소스 (MySQL·MSSQL) 로 일반화 + ask-worker 큐 cutover.

자세한 그림과 trust boundary 는 [[Architecture/Data-Flow|Data Flow]] 참조.

### 2.4 진행 중인 흐름 / 최근 완료

- **사용자 보안 보강 6종 (2026-06-19, 전체 완료)** — ① 공유 링크 시간 기반 만료 (SECURITY §7.2) · ② 로그인 시도 제한 (계정 잠금 + IP throttle, §12) · ③ 감사 로그 변조방지 (SHA-256 해시 체인 + 검증 + off-DB 앵커, §13) · ④ LLM 사용량 한도 (역할 기본 + 계정 특수, daily/monthly, 사전 게이트 429 fail-open, §SECURITY §6) · ⑤ AI 프롬프트 인젝션 방지 (datamarking + 명령-계층, §14) · ⑥ 2단계 인증 (TOTP, self-service + 관리자 해제, §15). 전건 outside-voice 적대 보안 리뷰 통과. [[../docs/SECURITY|SECURITY.md]].
- **그룹 대화 (feature-0009, 2026-06-19~23)** — 한 대화에 여러 멤버 + `@assistant` 멘션. **열람 ≠ 발화** 권한 분리 (멤버는 datasource 권한 없이 열람만, `@assistant` 발화는 발신자 RBAC 게이트). 공유 링크 '참여 허용'으로 join 일원화. 라이브 UX (적응형 폴링·`@`멘션 자동완성·발신자 Identicon 아바타·피멘션 알림+하이라이트) 배포 완료. S5(run cap·actor 귀속)·S6(스레드) deferred. [[Features/feature-0009-group-conversation|카드]].
- **NL→SQL 정확도 flywheel (2026-06-19~23, 진행 중)** — `dba-ai-nl2sql` ROADMAP 드레인: ITEM-01 평가 harness(RAGAS+LLM-judge) · ITEM-02+03 샘플쿼리 few-shot 저장소(pgvector, datasource-scoped, 예시-only 주입) · ITEM-04 데이터소스 비즈니스 컨텍스트 필드 · ITEM-07 self-reflection 자가수정 루프 · ITEM-10 용어/ENUM 코드사전. 효과 A/B 는 임베딩(titan) 복구 후 harness 측정. [[concepts/nl2sql-flywheel]] · `docs/improvements/dba-ai-nl2sql/ROADMAP.md`.
- **insight-worker 병목 진단·수정 (TASK-0305, 2026-06-23)** — "제품 DB 파악 진전 없음" 2축 분리: 축A 커버리지(RO 로그인 per-DB GRANT 누락 → 운영 GRANT 가 1차 해결) · 축B 처리량(fingerprint casefold·force_scan 진전기반 backoff·실패사유 telemetry). [[concepts/insight-worker]].
- **멀티 데이터소스 (2026-06)** — Stage 1 P1 (multi-MySQL) + datasource registry 암호화 (TASK-0205) + DB-단위 접근 (TASK-0206) + datasource-aware insight (TASK-0219) 배포·라이브검증 완료. MSSQL 실연결 검증 (제품90). 다음 = Stage 2 MSSQL 전면 / P2 UI. [[concepts/multi-datasource]].
- **데이터소스 연결 격리 + 연결상태 3색 (2026-06)** — per-datasource **circuit breaker** (TASK-0247, `AGENT_DB_CONNECT_TIMEOUT_SEC` 10s 로 단일 직렬 ask-worker starvation 차단) + insight-worker 연결 회복력 (TASK-0255) + `conn_health` 모니터의 **3-state 분류 (정상 #16a34a / 불안정 #dc2626 / 끊김 #6b7280)** 가 작업화면·관리콘솔 양면에 노출 (TASK-0250/0261/0282). 느린 타-리전 datasource 도 작업화면 사용 가능 (`should_fast_fail` = down 한정). [[concepts/datasource-registry]].
- **데이터소스 라벨/키 분리 → Id surrogate (TASK-0277)** — `DatasourceKey` (rename 가능 라벨) 바인딩이 rename 시 고아되던 결함을 stable `WebDatasources.Id` surrogate FK 로 근본수정 (3 테이블 DatasourceId backfill + Id-구동 cascade rename). [[concepts/datasource-registry]].
- **첨부 메타 MySQL → PG cutover (TASK-0279)** — 첨부 4 테이블을 MySQL `agent_memory` → PG `core_attachments` (+부속) 로 cutover (읽기 전환·MySQL 쓰기 롤백안전망). id 권위 = MySQL, dual-write fail-soft, backfill `--verify` diff=0 게이트.
- **관리 콘솔 성숙 (2026-06)** — 대시보드 위젯화·CloudWatch (TASK-0210/0218), LLM 사용량 대시보드 (TASK-0163~0202), 제품 insight 완료율·DB별 파악내용 (TASK-0223/0242/0243), 권한 편집기 점진적 공개 + 단일열 tree (TASK-0257~0270), 보관 대화 탭 정합 (TASK-0277), 제품 아이콘 편집 오버레이 (TASK-0283).
- **답변 diff 블록 (TASK-0256)** — assistant 가 첨부/쿼리 리뷰·편집 시 변경을 markdown ```diff 블록으로 제시 + 웹 UI 가 라인별 +/- 색 렌더 (`enhanceDiffBlocks`). 프롬프트 자동작성 SSE 스트리밍 (TASK-0233/0254, stick-to-bottom).
- **ask-worker out-of-process (2026-06)** — `ask_jobs` 큐 + ask-worker 서비스 cutover (TASK-0169). orphan-on-redeploy 제거 + 요청 즉시 취소/재요청 (TASK-0241). [[concepts/ask-worker-queue]].
- **storage Postgres 단일화 (완료 2026-05-27)** — KB (Phase 1, ADR-0021/0025) + runtime (Phase 2, ADR-0027/0028) 이관 완료. 첨부 메타 (TASK-0279) 까지 PG 로 이동. 잔여 = `web*` RBAC/audit/auth 테이블.
- **자동화 폐기 → 수동 PR 흐름** — ADR-0018 (`ai-*` 워크플로 폐기, 단순화).

## 3. 외부 source 합성

### 3.1 인용 source

본 vault 의 *템플릿 패턴* 자체는 다음 외부 source 에 매핑되어 있다 (`AGENTS.md §21.10` 출처 표 정본):

- [Karpathy LLM Wiki gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) (33k stars) — raw / wiki / schema 3-layer
- [SamurAIGPT/llm-wiki-agent](https://github.com/SamurAIGPT/llm-wiki-agent) (2.7k) — `wiki-ingest` 10-step / `wiki-query` 4-step / `wiki-lint` 6-category
- [AgriciDaniel/claude-obsidian](https://github.com/AgriciDaniel/claude-obsidian) (5.5k) — vault skeleton
- [namu.wiki 편집지침](https://namu.wiki/w/%EB%82%98%EB%AC%B4%EC%9C%84%ED%82%A4:%ED%8E%B8%EC%A7%91%EC%A7%80%EC%B9%A8/%EC%9D%BC%EB%B0%98%20%EB%AC%B8%EC%84%9C) — namu-style 사람 facing 형식

`wiki/sources/` 디렉토리는 도메인 source summary 누적 영역이며 현재는 placeholder (raw/ 큐레이션 시 ingest 예정).

### 3.2 합성된 결론

> 합성: 본 프로젝트는 *기존 운영 자산의 구조 이관* 으로 시작 (ADR-0012~0014) → *Plan-Review-Execute + playbook 도입* (ADR-0016) → *GitHub 자동화 폐기 + 단순 PR 흐름* (ADR-0018) → *audit subsystem + KB Postgres 분리 + Bedrock 통합* (ADR-0019/0021/0026) → *첨부 multi-cycle* (ADR-0022/0023/0025) → *storage Postgres 단일화* (ADR-0027/0028, 2026-05) → *멀티 데이터소스 (MySQL·MSSQL) + envelope 암호화 registry + DB-단위 접근* (ADR-CORE-*, 2026-06) → *관리 콘솔 성숙 + ask-worker out-of-process* 로 진화했다. 단일 MySQL DBA 도구에서 **다중 엔진 데이터소스 분석 플랫폼**으로 범위가 확장된 것이 2026-06 의 핵심 서사.

### 3.3 미해결 contradiction

- ADR-0025 가 정본 `docs/DECISIONS.md` 안에 *두 entry* 로 존재 (M5 cleanup, 2026-05-22 + PGVector attachment RAG, 2026-05-21) — wiki mirror 는 `ADR-0025-m5-cleanup` 과 `ADR-0025-pgvector-attachment-rag` 두 page 로 분리해 양쪽 정본 참조 보존.

## 4. 알려진 한계

- *통합 테스트 자동화 부재* — `repo/tests/integration/` 가 구조만 갖춤. ([[../docs/ARCHITECTURE|ARCHITECTURE §7]])
- *Bedrock model deprecation drift* — `litellm_config.yaml` 의 versioned model ID 가 AWS rotation 에 끌려간다 (ADR-0026 Consequences).
- *MSSQL synonym/view = GRANT hard boundary* — DB-단위 allowlist 가 synonym/view 의 underlying object 까지 강제하지 못함 (accepted-risk, [[concepts/db-level-access]]).
- *SSRF 사설망 경계 OFF* — 운영이 `AGENT_DATASOURCE_SSRF_GUARD_ENABLED=0` (사내 사설망 전제). 메타데이터 IP 는 하드차단 유지 ([[Decisions/ADR-0030-ssrf-guard-toggle]]).
- *wiki layer 의 stale risk* — 정본 변경 시 mirror 가 자동 update 되지 않는다. v3.13.0 의 `check #12` 는 WARN-only. (본 갱신이 2026-06-16 → **2026-06-23** drift 를 해소 — 그룹 대화 feature-0009·보안 6종·NL2SQL flywheel 반영.)
- *보안 hardening — 사용자 보강 6종 완료 (2026-06-19)* — 로그인 시도 제한·2단계 인증(TOTP)·공유 링크 만료·LLM 사용량 한도·감사 변조방지·프롬프트 인젝션 방지 구현 완료 ([[../docs/SECURITY|SECURITY.md]] §6·§7.2·§12~§15). 잔여 위협모델(예: 분산 botnet·LLM 인젝션 확률적 완화 한계)은 SECURITY.md 에 정직 기록.

## 5. 다음 단계

- **NL→SQL flywheel 후속** — ITEM-05 하이브리드 검색 · ITEM-06 reranker · ITEM-08 fix-with-AI · ITEM-09 heavy-query plan+approve · ITEM-11 거버넌스 포탈(샘플 등록 UI) · ITEM-12 retrieval tuning. 효과는 ITEM-01 harness 로 측정 게이트. [[concepts/nl2sql-flywheel]].
- **그룹 대화 S5/S6** — per-conversation run cap·`llm_usage` actor 귀속·LLM 화자 라벨(S5) → 풀 스레드 UI + 스레드별 run 병렬화(S6). [[Features/feature-0009-group-conversation|카드]].
- **멀티 데이터소스 Stage 2 / P2** — MSSQL 전면 (§9/§10 합격선) + datasource UI (P2). [[concepts/multi-datasource]].
- **메인 loop tier 라우팅** — LLM 사용량 대시보드 follow-up: 메인 추론의 edge/auto tier 라우팅 부재 (현재 claude 만). [[Decisions/ADR-0026-bedrock-llm-provider]].
- **Phase 3 web\* 이관** — `web*` 18 테이블 (RBAC/audit/auth) Postgres 이관 (미진행).
- **Sprint 4 D RAG** — `agent_drag` namespace + PGVector compose service 추가 (ADR-0024 / ADR-0025-pgvector).

## 6. 관련 문서

- [[wiki/Index|Index — MOC]]
- [[wiki/Architecture/Overview|Architecture Overview]]
- [[wiki/Features/_Index|Features MOC]]
- [[wiki/Decisions/_Index|Decisions MOC]]

## 7. 둘러보기

- 상위: [[wiki/Index|Index]]
- 하위 영역: [[wiki/Architecture/Overview]] · [[wiki/Features/_Index]] · [[wiki/Decisions/_Index]] · [[wiki/concepts/_Index]] · [[wiki/entities/_Index]] · [[wiki/Glossary/_Index]]
- sibling: [[wiki/Log|Log.md]] · [[wiki/README]]

## 8. 외부 link

- [Karpathy LLM Wiki gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)
- [namu.wiki 편집지침](https://namu.wiki/w/%EB%82%98%EB%AC%B4%EC%9C%84%ED%82%A4:%ED%8E%B8%EC%A7%91%EC%A7%80%EC%B9%A8/%EC%9D%BC%EB%B0%98%20%EB%AC%B8%EC%84%9C)
- [AWS Bedrock Anthropic models — Seoul region](https://docs.aws.amazon.com/bedrock/latest/userguide/models-supported.html)

## 분류

`#wiki/overview` · `#status/active` · `#confidence/high` · `#maturity/substantial`
