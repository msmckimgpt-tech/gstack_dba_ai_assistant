---
doc_type: WIKI_HOT_CACHE
last_updated: 2026-08-19
---

# Hot Cache

## Last Updated
2026-08-19

## Key Recent Facts
- 08-19 창은 **신규 머지 0건**(origin/main HEAD = 08-17 sync 커밋 `74316049`)이라 정합 대상이 전부 **prior-window drift** 였다. 그중 가장 무거운 것은 내용 누락이 아니라 **markdown 행 구분자 1문자**였다 — `Architecture/Overview.md` §2.3 과 `overview.md` §2.1 에서 feature-0039 행 끝과 feature-0040 행 시작이 개행 없이 `||` 로 붙어, **텍스트는 다 있는데 feature-0040 이 자기 행으로 렌더되지 않고 앞 행의 잉여 열로 파싱**됐다. 그래서 이름 집합 대조(42/42 PASS)로는 잡히지 않고 **렌더 행 수 대조(41/40 vs 42)** 로만 드러난다 — 카운트 검증 recipe 에 "이름 집합" 과 "행 수" 를 **둘 다** 두어야 한다는 실증이며, 직전 08-17 run 이 이 항목을 detect-and-report 로 이월했던 것을 이번에 해소했다. 함께 정정한 것은 `overview.md` §2.1 feature-0042 행 누락 · `Architecture/Overview.md` §2.4 feature-0008 의존 행 누락(정본 §6 에만 있던 backfill) · `overview.md` §5 와 `concepts/nl2sql-flywheel.md` 의 nl2sql 측정 blocker·벡터 차원·엣지 tier 폐지 stale 서사 · 릴리즈노트 08-14 블록의 무거운 조회 안내 누락 1항목.
- 08-14~08-16 사용량·첨부 화면 UX 묶음 + 대화 런타임의 대기·전달 봉인 + 외부 AI 표면 실사용 정합 + 바쁜 시간대 배포 완결 + 분석 요청 경제성 판정(feature-0003 9 cycle / 0002 6 / 0041 5 / 0020 / 0022 / **0042 신규 1** — `ls -d unit/feature-*` **41→42**): **feature-0003** — ⓐ `usage-metric` 전환 결함은 기전이 셋이고 **전부 헤드리스 green·라이브 red** 였다(signature 에 분해모드·모델집합이 들어 있어 '요청' 과 모델별은 **세그먼트 키 집합 자체가 다름** → 일자 집합으로 축소하고 세로 구성 변화는 0 높이 접기/자라기로 흡수 · 범례 유무로 스크롤바가 사라져 폭이 12px 흔들리는데 **폭이 signature 에 남는 한 그 자체로** 재생성 → viewBox·축·막대 in-place 좌표 재배치 · 삽입 직후 rAF 1회로는 시작 스타일 확정 전에 두 값이 같은 프레임 → 강제 reflow). §16.6 "렌더 타이밍·레이아웃 축은 헤드리스를 1차 그물로만" 의 실증이고, ③ 의 검사가 헤드리스에서 **뮤턴트도 통과시키는 vacuous** 임을 정본에 적었다. ⓑ "프로필에도 정합하게" 를 복제가 아니라 **정본화**로 — 지표 정의를 `static/usage-metrics.js` 하나에 두고 관리·프로필 두 번들이 import(관리의 복제 정의 제거·공유는 정의까지, 렌더는 각자), 지표 4→8종·전환 시 재조회 0. ⓒ 사용 기록 표 정렬·페이지네이션을 관리→프로필 양 판에. 정렬 키는 **화면 표시값** 기준(주체 열은 raw sentinel 이 아니라 번역 라벨), 정렬·페이징은 받은 결과셋 안에서(열마다 API 파라미터를 늘리면 표시축↔질의축 동기화 실패 지점이 생긴다), 첫 화면은 종전과 동일이라 무회귀. PB-0008 이 **기본 50행 아래에 페이저가 묻혀 첫 화면에 안 보이던 것**을 잡았고, 적대 리뷰가 `title='…'` **속성 인젝션**(관리 콘솔은 타인 제목을 렌더 = stored 경로 실재)과 sticky 열 머리 미부착을 잡았다. ⓓ 카드 넘침은 보고가 "좁은 화면" 이었지만 **900px 에서도 6건** — 원인은 폭이 아니라 균등 분배였고, 트랙 값을 **라이브 패딩 실측**으로 재확정했다(하네스 패딩 16px 이 라이브 32px 보다 관대해 150px 뮤턴트 생존 → 배포 후 1열). 검증 안 되는 방어(container query·폰트 축소)는 **제거**. ⓔ 첨부 계보를 **작성 주체별 root 체인으로 분기**(assistant 수정본이 사용자 최신본을 supersede 하지 않는다 — 이 가드가 없으면 목적의 정반대) + `lineages` API 축 + `## FILE VERSION LINEAGES` 프롬프트 계약 + 계보 간 diff(양쪽 **개별 인가**·같은 대화·같은 파일명 스코프). 적대 P1 은 로직이 아니라 **진입점 부재**(버튼 노출 조건 `verNum > 1` 을 놓쳐 v1↔v1 에서 버튼이 안 떴는데 하네스 18건이 모달 안쪽만 봤다). ⓕ 첨부 `CreatedAt` 로컬→**UTC**(모순 234→0 · 저장만 옮기면 화면이 9시간 이르므로 **전송 계약도 같이** 이동 · 상한은 ALTER 이전 확정 · 저장소별 마커 2개 · 원자 선점). ⓖ 실행 단계 패널 시각·간격·누적(`step.created_at` 이 전 경로에 이미 노출 → **backend 무변경**·과거 대화 소급). **feature-0002** — ⓐ 조기 종료 run 이 **0.12초 만에 error** 인데 화면은 23분 대기(조기 종료 12경로 vs KV 마감 3갈래 비대칭·60일 22 job/20 대화=7.4%) → 3중 봉인. ⓑ per-attempt 상한 900s 가 **성공 지연 꼬리 안쪽**(p95 238.5s·max 854s)이라 정상 추론이 전량 폐기되던 것을 스트리밍으로(같은 상한이 **chunk 간 간격**을 잰다) — **반환 계약 유지로 소비처 3곳 무변경**·조용한 EOF=`_LLMStreamIncomplete`. 후속이 관측 수단을 정정(운영 로거 WARNING 이라 `logger.info` 는 라이브 미잔존 — 실증에서 본 건 probe 의 `basicConfig`). ⓒ provenance 게이트의 **빈 경로**(이미지·csv/xlsx 샘플 행) 봉인, 히스토리 replay 는 과차단 회피로 **한계 채택**(`SECURITY.md §47.4` self-add). ⓓ 08-16: 변경-인지 4축이 **브라우저 pill 하나**에 걸려 있어 전환·새로고침이면 동시에 꺼지던 것을 **클라 신호 ∪ 서버 파생**(기준선=직전 턴 전달 스코프, `None`↔`[]` 구분 · 판정=첨부 id 단조성이라 tz 비의존)으로. **feature-0041** — DCR 이 표준 클라이언트 이름을 400 거절해 **자동 연결 자체가 불가**(allowlist→denylist 반전) · 구조 조회 3종이 **어떤 인자로도 성공 불가** · `execute_sql`(P1) 개방 과정에서 **08-12 배포부터의 스코프 격리 결함** 발견(단일 바인딩에서 `list_schemas` 가 타 대화 첨부 샌드박스 노출 — 계약을 docstring 으로 호출측에 미루면 지켜지지 않는다) · AC-7 답변 보존 당일 출하(`WebAiTasks` 전용 컬럼 — 원안은 47개 파일 파급으로 기각) · 정본 REPORT 의 **사실과 반대인 보안 서술**("도달면 0") 청산. **feature-0020** — "조용해지기를 기다린다" 대신 **"받는 쪽을 먼저 세운다"**(surge→drain 1800s→교체), 자가 검증이 `ps -q` 기반 관측 결함 2건("보유 N→0" 이 어떤 상황에서도 나오는 문장)을 잡았다. **feature-0022** — 분기 3 경로가 문맥만 복사해 **유령 테이블**을 참조하던 것을 CTAS 이월 + 매 턴 실재 목록 주입(TTL 만료 재개까지 동시 차단). **feature-0042(신규)** — 코드 0건, 산출물은 판정: batch 두 해석 기각 → 후보 3개 등재 → **등재 직후 전제 재실측**으로 형제 dedup 자체 기각(이름 중복 69.3% vs 내용 distinct 99.6~100%) · prompt caching 은 4,096 토큰 미만 **무음 실패** 재현 후 실현 가능 확정 · ITEM-15 는 방향 반전 뒤 스스로 BLOCKED. [[feature-0042-analysis-dedup]]
- 08-13 첨부·대화 마찰의 하루 안 종결 + 그래프 조작 체감 개선 + 공유 대화 첨부 LLM 경계 재정의 + 외부 AI 인증 URL 화(feature-0003 8 cycle / 0016 / 0041 · **신규 feature 0**): **feature-0003** — ⓐ `graph-expand-perf`(Major·perf): 체감 지연을 추정 없이 실 Chrome CDP `Profiler`+`longtask` 로 귀속했고 세 축이었다 — 오브젝트 풀 diff 의 `nodeSig` 가 `style` 을 통째로 직렬화해 **x/y 를 서명에 포함**(masonry 재균형으로 형제가 이동하면 "다른 요소" 판정 → 538개 전량 destroy→re-create, 비용의 정체는 라벨 재생성) · 더블클릭 판별 340ms 타이머가 **네트워크까지 미뤄** 클릭 후 ~500ms 무작업 대기 · 배치-정렬 메모이즈 위상 서명에 **컬럼 포함**(겨냥한 조작이 매번 캐시를 날림 · 주석은 "컬럼은 자연 제외" 라고 **사실과 반대로** 적혀 있었다). 위치를 뺀 `nodeShapeSig`(모양 같으면 재배치 — 자식이 전부 로컬 좌표계라 표시 결과 불변이 구조적으로 보장)·컬럼 GET 선-fetch(promise 공유로 선재 중복 왕복 1건 제거)·`_metaTopoSig` Column 제외 → 클릭→펼침 **915→556ms** · 블로킹 **344→133ms** · `drawMs` **210→35ms** · 재생성 **537→21**, 라이브 n=3 재현(544/134/32.8/21). 트윈 360ms 는 **의도된 연출이라 미변경**(성능으로 위장해 연출을 지우지 않는다 — 조정은 사용자 판단으로 표면화). ⓑ `rail-async-relayout`(Minor): mermaid 가 `Promise` 뒤에 SVG 를 넣어 배치 **이후에** 높이가 뛰는데 rail 재배치 트리거 5곳에 "콘텐츠의 비동기 성장" 이 없었다. 대조군이 이미 저장소 안(공유 뷰 `share.js` = ResizeObserver)이고 **메인 채팅만 미적용**(§16.7 G8 적용면 확장). 관찰 대상은 컨테이너가 아니라 **메시지 row**(`.messages` 는 flex 자식이라 콘텐츠가 늘어도 자기 box 불변 → RO 미발화) · pin 해제는 `scroll` 이 아니라 **`pointerdown`**(스크롤 앵커링의 자동 조정과 사용자 조작을 scroll 값으로는 원리적으로 구분 불가 — 초판이 만든 라이브 회귀를 PB-0008 이 잡았다) · settle 600ms + ceiling 8s. 오차 **86.58→0.11px** · 진입 gap **1,631→0px**. ⓒ `attach-list-name-sort`(Minor): `ORDER BY Id` → **두 read 경로(MySQL 정본·PG mirror)가 합류한 뒤 파이썬 한 함수**의 자연 정렬(collation 차이가 곧 "경로마다 다른 순서"), 휴지통은 `LIMIT 200` **절단과 정렬 분리**(이름순 절단은 방금 지운 파일을 휴지통에서 없앤다), ZIP 은 체인 그룹 사이만 재배열. 첫 PB-0008 대상이 **이미 알파벳순**이라 무의미했던 것을 39첨부 혼재 대화 A/B 로 교체한 것이 교훈. ⓓ `attach-source-compare`(Minor): 원문 모달의 비교 진입점 0 과 비교 모달의 "같은 버전 = 본문 0" 이 **한 사실에 두 화면이 다르게 답하던** 비대칭 → in-place 전환 + `/source` 가 체인을 함께 반환(presign 없는 `/versions` **부분집합**) + fail-soft + 방향 정규화 + 목록 버튼 기본쌍 최초↔최신. 변경의 절반이 **복제 제거**(`_bodyState`·`_renderSourceBody`)이고 회귀 잠금은 문자열이 아니라 **함수 정의 개수**. ⓔ `usage-metric-charts`(Major·alembic 0056): "가능하다면 cache hit 도" 의 전제를 코드 전에 실측해 `llm_usage` 캐시 컬럼 부재 + `cache_control` 미전송 = **캐시 hit 자체가 없음**을 확인하고 범위를 사용자에게 되물었다(계측만 붙였다면 영원히 0). `prompt_tokens 5039 = 순수입력 37 + 캐시쓰기 5002`(캐시는 입력의 **부분집합**) 발견으로 비용식 정정 — 안 고치면 캐싱 활성화 순간 최대 10배 과대 계상. 종결은 화면이 아니라 **DB 실조회**(`cache_write 72,865` → `cache_read 72,865`). ⓕ `folder-dnd-shared-group`(Minor): 백엔드·'···' 메뉴는 이미 허용, **사이드바 드래그 게이트만** `mine` 로 좁혀진 표시-집행 불일치 1지점 → `isFolderScopedConversation`(owner ‖ is_member) 로 폴더 파티션과 드래그가 같은 판정 공유. ⓖ `member-scope-gates` → `member-leave-branch`(**감사 결론 자기 정정**): `can(permission)` 은 인자를 버리고 로그인 여부만 반환한다(display-permissive TASK-0098) → 선행 cycle 의 "확정 결함" 중 중단·즉시답변·연장은 **원래 막히지 않았고** "연장 배너가 떠도 승인 불가 → 타임아웃" 은 사실이 아니다. 실효는 오도 안내 2건 + 같은 `can()` 때문에 죽어 있던 **설정 팝업의 보관/나가기 분기**(서버는 self-leave 를 허용하는데 멤버에게 나갈 UI 경로가 없었다) → `isOwnConversation ‖ canOpenAdminConsole` 로 복원. 문자열 `includes` 로 "그 함수를 쓴다" 만 잠그던 테스트가 **거짓 PASS 2회** 를 낸 것도 주석 제외·행위 기반으로 교체(`LRN-20260813-display-permissive-can-invalidates-gate-audit`). ⓗ `group-attach-scope-window`(**Critical**·사용자 승인): 열람·다운로드는 전원 공유인데 **LLM 주입만 발신자 본인**(CSO F1, 2026-06-19)이던 비대칭이 라이브에서 "버그 발생;;" 후 대화 종료까지 만들었다(첨부 보유 그룹 대화 **6/6** = 빈도가 아니라 구조). 스코프를 대화 전체로 열되 **공유창 window 게이트**(은닉 구간 실재 시 발신자 한정·fail-closed) + 판정 단일 정본 `shared/share_window.py` 를 web 스코프 해소와 agent_core 주입 게이트가 공유(choke-point 2곳 — 한 곳만 고치면 다른 곳이 막는다). 첨부 `created_at` 이 메시지와 **다른 시간축**(라이브 +9h · 로컬시각이 UTC 로 라벨링돼 저장)이라 시각 비교를 버리고 **메시지 id/joined_at 축의 은닉 구간 실재 여부**로 게이트(정밀도는 낮지만 오판 방향이 항상 안전). injection 은 업로더 라벨 + **데이터-전용 계약** 코드 권위 주입으로 대체 통제(ADR-20260813T183000 · `docs/SECURITY.md §47`). **feature-0016 `expand-camera-anim` + 자기 회귀 종결**: 단일클릭 컬럼 펼침엔 카메라 보정이 **전혀 없었고** 스키마 펼침은 반대로 **항상 중앙 focus** 였다 → keep-in-view(안전영역 = 짧은 변의 12%·24~120px · 벗어날 때만 잔여 26% ease-out · 안전영역보다 큰 요소는 **중심 기준**, 아니면 카메라가 끝없이 밀린다) 4경로 배선 + 재배치 **360ms 이동 트윈**(`setData` 직전 좌표 스냅샷 → `draw()` 뒤 출발 위치로 되감기 · **모델 무접촉**이라 배치 메모이즈·미니맵 서명·fit 무오염 · `_tweenPos` 를 관계선 끝점·hit-test·bounds 가 공유). 비용 상한 = reduced-motion·씬 교체(겹침<30%, 분모는 큰 쪽)·노드 600·**관계선 1500**(프레임 비용의 지배항은 노드가 아니라 선). codex 4라운드가 상호작용 결함 연속 적발(상태 변경이 트윈 참조를 무효화 → 노드는 멈추고 선만 보간 · hit-test 최종 좌표 기준이라 **보이는 자리는 안 눌리고 빈 목적지가 눌림** · 카메라가 목적지를 추종해 **애니메이션 내내 화면 밖** = 요구의 정반대) → 전건 가드 + 헤드리스 1155→**1264 PASS** + **뮤테이션 포착력 실증**. **라이브가 자기 회귀 1건을 잡았다** — 스키마 펼침의 중앙 focus 를 keep-in-view 로 바꾼 것이 큰 combo 에서 "이동 0" 을 내 상태줄은 "9개 펼침" 인데 화면엔 카드만 남았다(**요청 범위를 넘어 바꾼 것이 회귀의 원인**) → 1줄 되돌림 + 배선 계약 테스트(EA.8) + POST-FIX 같은 조작 대조 확정. **feature-0041 3차 출하 → 기동 수정 → 콘솔 분리 → 인증 접근성**: HTTP/SSE 를 `ext-tool-mcp` 로 **라이브 기동** + 엣지 `/api/ai/mcp`(전송 계층에 verifier 가 없어 Bearer 는 도구 실행 시점에만 검사되므로 엣지가 `Authorization` 존재 자체를 요구해 익명 스트림 차단 · Caddy 익명 401 라우트를 `handle` **밖**에 두면 adapt 결과에서 `reverse_proxy` 뒤로 밀려 무력화되는 것을 자체 적발). 상한 4종은 bespoke `admin.js` 탭 대신 `runtime_settings` 슬라이스(그 파일을 다른 활성 브랜치 2개가 편집 중). codex 의 핵심 지적은 **"콘솔이 존재하지 않는 방어를 표시"** — 동시 실행 상한은 소비처 없고 미제출 상한은 미집행이고 RPM 은 `open_task` 미배선: 노출된 knob 은 그 자체로 "이 방어가 있다" 는 주장이므로 미구현은 삭제하고 나머지에 집행을 붙였다. 이어 **문서가 사실을 앞지른 사례 3연속** — 컨테이너 미기동(이미지 의존·SDK 2.0·TLS upstream) · 콘솔 오배치(상한이 '실행 타임아웃' 패널에 섞임, PB-0008 적발) · **미로그인 사용자는 인가를 시작할 방법 자체가 없었다**(`/login` 404). 마지막을 닫으며 RFC 8414/9728 discovery 4경로 + 401 `WWW-Authenticate` + **동의 화면**(서명 consent token·세션 결합·nonce 1회 — SameSite=Lax + GET 발급의 링크 클릭 탈취 경로 동반 차단) + 발급 페이지 `/ai/connect` + `safeNextTarget`(문자열 검사로는 `/\evil.com` 이 통과하는데 브라우저가 `\` 를 `/` 로 정규화해 외부로 나가므로 **URL 해석 결과의 origin** 비교)로 URL 접속 방식을 완성 — 사용자는 주소만 등록하고 로그인 후 [허용] 만 누른다. 안내 정본을 "주소만 등록" 으로 바꾸고 런북은 개발자 회귀용으로 격하.
- 08-11~08-12 외부 AI 도구 표면 신규 + 역할 기반 DB 객체 탐색 신규 + 무중단의 마지막 구멍·배포 밖 재생성 봉인 + 일시 LLM 장애의 작업 보존·재개(feature-0041 / 0040 신규 · 0020 · 0002 · 0003 · **신규 feature 2**): **feature-0041(Critical 신규)** — feature-0023 의 `ask` 는 외부 AI 가 **우리 LLM 에게 추론을 맡기는** 축이었다. 이 표면은 방향을 뒤집어 외부 사용자의 AI 가 **자기 계정 LLM 으로 직접 추론**하면서 데이터소스·RAG·메타지식만 가져가게 한다(비용은 호출자 부담·자격증명 무보관). OAuth AS(DCR·authorize=세션 쿠키 필수·token·revoke) + P0 도구 9종 REST 의 관문 순서를 **토큰→상한→스코프→실행→각인→원장** 으로 고정하고 `P0_TOOLS` allowlist 로 도달면 자체를 고정했으며 **원장 기록이 실패하면 결과를 반환하지 않는다**(거절·게이트도 원장). 전송 어댑터 2종(MCP stdio · HTTP/SSE)은 **판단 로직 0** — 토큰 무보관·리다이렉트 시 Authorization 전달 금지·HTTP 는 loopback 기본. 발견 자료는 "익명=static contract, 인스턴스 데이터 0" 을 테스트로 못박았고, codex 리뷰가 **L4 권한 비대칭의 교차 테넌트 노출**(client_id 는 DCR 로 공유 가능한 앱 식별자인데 상대 계정 id·권한 겹침을 응답에 실었다)을 적발해 원장 전용으로 되돌렸다. `docs/SECURITY.md §44` 신설 · 1차 `a68fbbac`/2차 `e1372f32` 라이브 배포 + POST-DEPLOY 12항/8항. **feature-0040(Major 신규)** — 트리거·이벤트·SQL Server Agent 작업·뷰·시노님·시퀀스는 탐색 도구도 AGE 정점도 없었다. 분류 축을 벤더 객체명이 아니라 **역할**(6종 SSOT)로 잡아 DBMS 확장에 열고(사용자 결정) 도구 2종 + `db_objects` SSOT(alembic 0054) + `DbObject`/`OBJECT_USES`(실선)/`OBJECT_ON`(파선) 그래프 편입. 불변식은 **미지원 ≠ 부재**(SUPPORTED/UNSUPPORTED/PRIVILEGED/DELEGATED — 0행을 "없다"로 뭉개지 않는다) · `msdb` 는 한 함수 고정 질의만. 실 Chrome 150 WebGL 실증까지 마쳤고 **라이브 배포 완료**(doc_sync 08-13 실측: alembic `0054` 적용 · `db_objects` 339행 · 자산 라이브 byte-identical) — 잔여는 POST-DEPLOY PB-0008 시각검증뿐. **feature-0020** — 무중단은 web 에만 완성돼 있었다(ask_jobs 30일 p50 81s·p95 691s·**60초 초과 66%** vs drain 60s = 매 배포가 진행 중 run 다수를 죽였다). surge 는 신규 요청만 흡수하고 실행 중 run 은 프로세스 간 이전이 불가하므로 "옮긴다"가 아니라 **"붙어 있는 게 없을 때 바꾼다"**(30일 중 busy 2.1%) → fail-closed quiesce 게이트. 2차 사고는 그 recreate 가 **`deploy-web.sh` 를 경유하지 않았음**을 드러냈고(배포 lock mtime 이 직전 배포 그대로) 게이트를 `bin/lib/quiesce.sh` 단일 정본 + `bin/safe-recreate.sh` + Makefile up/down 전치 + `bin/recreate-audit.sh`(raw docker compose 는 막을 수 없으니 **보이게** 만든다)로 봉인했다. **feature-0002** — 같은 사고의 사용자 쪽 얼굴: in-flight LLM 이 SIGKILL 되자 앱이 run 을 통째로 terminal 종결해 7분 40초 조사가 소실됐다(90일 18 대화). 1차 같은-라운드 재호출, 2차에서 **실패한 것은 재시도가 아니라 예산**(2회×4.5초 vs 게이트웨이 부재 48초)이라 값싼(요청 미도달) 실패 전용 예산(6회·cap 30s·누적 120s) + **재개 재큐**(누적 도구 결과는 `core_messages` 에 있고 로더가 replay·**재큐 분기는 KV terminal 앞**). 429 는 도달해서 거부된 것이라 제외. **feature-0003** — 말풍선 첨부 칩 `⇄` diff 진입(preselect 는 체인에 남은 직전↔이 버전 · `thisVer-1` 하드코딩은 중간 버전 삭제 시 무음 오표시) · `.md` 구문 하이라이트 → 사용자 재지시로 **마크다운 렌더 토글**로 정정(선행 유지 — 원문·diff 는 줄 대조가 목적) · **한/영 자판 교차 검색**(`ㅎㅋ`→`gz`·`rmffhqjf`→`글로벌`, 예시 4개가 전부 실제 제품인데 종전엔 "검색 결과가 없습니다"뿐 — 매핑표 primitive 2벌·클라 13곳·서버 6경로·후보 첫 항목이 항상 원문이라 회귀 0 기계 단언) · '+ 데이터베이스 추가' 밀림(38.3px/연속 114.3px)을 **편집기 순서 교체**로 구조적 0 화(스크롤 보정은 위쪽 행 제거 시 원리적으로 실패) · 메타데이터 pane 을 "이 pane 만 앱 토큰·계약을 안 쓰는 8건"으로 환원해 단일 표 surface 재구성(DOM 셀렉터·dataset 계약 무변경).
- 08-07~08-11 첨부 워크벤치 성숙 + 배포 창 전면 503 근본 해소 + OAuth 계정 가용성 자동화 + 익명 API 표면 축소(feature-0003 / 0002 / 0014 / 0007 · **신규 feature 0**): **feature-0003 첨부 트랙(5축)** — ⓐ 버전 diff 가 5 cycle 더 성숙했다: 저장소 단일 primitive `static/code-highlight.js` 로 구문 색(SQL/JSON/YAML/XML/CSV/TSV · `textContent` 전용)을 넣고 `app.js` 의 로컬 키워드 목록을 삭제·import 했으며(복제가 곧 결함 기전), 라이브에서 keyword 만 색이 빠진 원인은 팔레트가 아니라 **고아 `*/` 주석**이 CSS 파서에 셀렉터로 읽혀 `.code-tok-keyword` 를 통째로 삼킨 것이었다(jsdom CSSOM 은 깨진 파일도 13 규칙으로 인식해 기존 가드를 통과시켰다 → 가드를 주석 구조에 걸었다). **줄 안(글자 단위) 변경 구간**은 서버 단독 계산으로 토큰 앵커 후 자소 단위로 좁히고(토큰만 쓰면 식별자 전체가 칠해지고 문자만 쓰면 `SELECT`↔`INSERT` 가 색종이가 된다) 프론트는 문자 오프셋으로 쪼개 덧그리되 세그먼트 총합이 셀 원문과 다르면 아무것도 그리지 않는다. 비용 상한은 대리값이 아니라 실제 비용으로 잡았고(행 수 제한만으로는 411초 → 3겹 컷에서 최악 2997→5.3ms) 마크는 배경이 아니라 `box-shadow` 인데 `inset` 이 `_` 자리와 겹쳐 `legacy_gy_pay` 가 `legacygypay`, `__init__` 이 `init` 으로 읽히던 사용자 지적을 바깥 그림자 `0 2px` 로 해소했다. 내용 동일 화면은 **맥락 축약이 "변경 주변만 남기는" 연산이라 변경 0 이면 남는 행도 0** 이어서 배너만 있는 빈 화면이었고, `identical` 을 축약 대상에서 제외해 원문을 렌더하면서 "문서 원문" 이라는 단어의 정직성(행 상한 절단 · 1MB cap · `splitlines()` 가 흡수하는 줄 종단자 차이 → sha256 은 다른데 identical=true)을 `_identicalFlags` 한 곳으로 모았다. ⓑ 목록 행을 열었다 — 내용을 보는 유일한 길이 비교 모달이고 진입점은 `versions.length > 1` 뒤라 **버전 하나뿐인 첨부(대다수)는 내려받아야만 볼 수 있었다** → `GET /api/attachments/{id}/source` + 원문 모달(권한·D21 게이트는 `/diff` 와 동형 · 버전 파라미터를 두지 않아 식별 경로 비대칭 차단) · ranged read 로 성능을 고치면 `len(raw) > cap` 절단 판정이 영원히 거짓이 되는 것을 DB 정본 `SizeBytes` 판정으로 막았다 · `stats.lines_partial`(200,000줄 파일이 "95,326줄" 로 표기되던 것). 이어 "비교 버튼 유무에 따라 원문 버튼 위치가 뒤틀린다" 는 오른쪽 정렬 flex 의 선행 구조라 빈 슬롯 예약 primitive 로 활성 목록·휴지통까지 3종을 함께 닫았다(패널 수치 24-25px 는 실측 기각, 기전 지적은 옳아 글꼴 확대 A/B 로 재현 후 `flex: 0 0 22px`). ⓒ 이미 갈라진 첨부 체인 **155 row/62 논리파일/14 대화**를 병합 도구로 정리(dry-run 기본 · 스냅샷 + 사람이 실행 가능한 롤백 SQL · 사후 재검증 exit 2)하고 라이브 적용이 자기 결함 2건을 드러냈다 — PG 미러만 단순 호출이라 중간 상태 UNIQUE 충돌이 **fail-soft 로 삼켜져 29 row 가 옛 상태로 잔존**(목록 read 는 PG 우선이라 화면이 어긋날 상태였고 사후 검증은 MySQL 기준이라 "잔여 0" 을 보고했다) · 선재 live 중복 1건은 분열 조건에 걸리지 않아 살아 있었다(판정에 `live>1` 추가). PB-0008 목록 31→22행 · 손실 0. ⓓ 다운로드 파일명 접미 규칙을 서버 정본 함수(keep/strip/force/**auto**) 한 곳으로 통일하고 프론트 중복 규칙 `_versionedFilename` 제거(규칙 두 벌이 AI 편집본을 `report_v2_v2.csv` 로 내보낸 기전 · AC-AMU-5 superseded) + 첨부 날짜 compact 표기(`CreatedAt` 은 MySQL `NOW()` 기반 KST naive 라 그대로 파싱 — 선행 `restorable_until` UTC 보정의 반대 사례). ⓔ 그룹 대화 **기능 첫 사용 1회 가이드 툴팁**(팝업 금지 = 사용자 결정 · 계정 단위 소진 키 · 닫기 ≠ 소진) — 라이브가 잡은 건 로직이 아니라 **핸들러 실행 순서**(버블 단계라 먼저 등록된 멘션 AC 핸들러가 AC 를 닫은 뒤 돌아 Esc 양보가 오판)였고 capture 등록으로 고쳤다. 선행 61 PASS 는 경쟁 핸들러가 없어 vacuous 였다. **feature-0002(2축)** — 첨부 맨 끝 줄을 근거로 한 **정확한** 답변에 red-team 이 grounding BLOCK 을 걸던 것(head-only 발췌 + grounding 은 rederive 적격 축이 아님 → 옳은 답변에 "자가 검증 미해소" 배너)을 인용 구간 앵커 + 문자 기준 coverage 권위로 봉인하고, 라이브 재현이 잡은 이중과금(실전달 862/1,200자)과 **초안이 논하는 바로 그 파일의 통째 강등**을 union 회계 + 관련성 정렬 + 지분 비례 축소로 고쳤으며, 리뷰어 digest 에 버전 diff 가 없어 정확한 답변이 무근거로 보이던 것과 완독 요구가 verify 패스에서 면책되지 않던 것까지 닫아 같은 대화 재현에서 `verdict=pass`·0 block 을 확인했다. 또 답변 본문이 9.6초에 끝났는데 리뷰어 무응답으로 전달이 **312초** 지연되고 그 구간에 취소·진행 표시가 전무했던 결함(`abort_fn`·`progress_fn` 이 반복 수정 루프에만 배선)을 중단 가능 폴링으로 고쳤다. **feature-0014** — 사용자 보고("배포 중 서비스가 멈춘다")가 사실이었고 **무중단 불변식이 실제로 깨져 있었다**: 롤링이 내부 `/readyz` 까지만 보고 엣지 후보 복귀를 안 봐서 Caddy passive 격리(30s)가 풀리기 전에 다음 replica 를 내려 available upstream 이 0 이 됐다(6시간 창 `no upstreams available` 71건 · 전면 503 6회 × 12~17초 · duration 전부 5.01s = `lb_try_duration`). 판정 2축(admin API `fails==0` + Caddy→replica `/livez`) + predrain fail-closed 후 **다음 배포 창 503 = 0 · 172요청 전부 200**. **feature-0007** — 7일 쿼터 100% 소진에도 자격증명 파일이 유효해 30분마다 소진 계정이 1순위 slot 에 재주입되던 것을 저빈도 1-probe 게이트로 우회(네트워크 오류 fail-open)하고, access token 이 CLI 세션에 의존해서만 회전하던 구조를 선제 자동 회전으로 바꿨다(TTL ~8h · 08-07~11 에 LLM 전면 중단 실사례). **feature-0003 익명 API** — 외부 AI 무인증 감사 지적 9건 중 8건 사실 판정 → 익명 3경로 최소 응답화(`docs/SECURITY.md §7` allowlist 미등재 = 정책-코드 drift) · 엣지 보안 헤더(HSTS 는 `/trust` 평문 CA 번들과 충돌해 제외) · `agent_ro` 로 `DB_USER=root` 폴백 제거 · 자체 적발 `api-vault/options` **fail-open**. **배포 파리티**: 컨테이너 이미지 = `d56367c8`(= HEAD) · 서빙 `release-notes-data.js` `generated: "2026-08-06"` = **편집 전** 브랜치 파일과 일치(본 run 이 `"2026-08-11"` 로 갱신 — 반영은 wrapper post-merge 배포 몫) → 델타 44 커밋 전부 라이브.
- 08-06 대화 첨부 수명주기 개방 + 첨부 전달의 출력 예산 분리 + 배경 dismiss 단일 primitive(feature-0003 / 0002 · **신규 feature 0**): **feature-0003 첨부 트랙** — ⓐ `attach-manage`(**Critical** — 파괴적 삭제 + 인가 경계 변경, 사용자 승인으로 `ADR-20260729T163000-attach-append-only` **supersede**): 목록에서 첨부를 지우고(버전 단위 / 체인 전체 · 기본은 항상 좁은 쪽) retention 창(기본 30일) 안에서 휴지통으로 되살리며 대화 첨부 전량을 ZIP/개별로 내려받는다. 설계의 전부는 **삭제 인가를 열람 경계에서 분리**한 것 — 종전 `_account_can_access_attachment` 는 그룹 멤버 전원을 통과시켜 제3자가 남의 첨부를 지울 수 있었다(append-only 철회가 "별도 판단" 으로 이월했던 미해결 이슈). 이제 삭제·복구는 `conversation.attachment.upload.any` 또는 `upload.own` + (업로더 본인 ∨ 대화 소유자)이고, 두 핸들러가 열람 헬퍼를 **호출하지 않음을 AST 로 단정**한다(열람 경계 자체는 종전대로 멤버 전원). soft-delete(`DeletePending=1`) 후 실 객체 삭제는 기존 reconciliation worker 가 하고, 최신 버전을 지우면 `VersionNumber` 최대값을 승격(`SupersededAt=NULL`)해 "이 버전만 삭제" 가 체인 전체 소멸이 되지 않게 한다. ZIP 은 상한(512MB) 초과 시 **부분 ZIP 을 만들지 않고 413**(무음 절단 금지)·zip-slip 제거·이름 충돌 시 id 부여이고, 개별 다운로드는 presigned MinIO URL 이 내부 endpoint 호스트라 외부 브라우저가 못 여는 문제 때문에 앱-내부 URL manifest 로 준다. ⓑ `attach-version-diff`(Major) + 후속 5 cycle — 버전 체인(`RootAttachmentId`/`VersionNumber`)과 체인 조회는 이미 있었지만 **비교 그 자체**가 없었다(`MetaJson.version_diff` 는 업로드 시점 직전↔신규 1쌍만 담고 프론트 diff 렌더가 0 이라 v1↔v3 은 데이터로도 화면으로도 불가능). `GET /api/attachments/{id}/diff` + 전용 모달(`static/app/attach-diff.js`)로 임의 쌍·다단계를 좌우 2열/단일열 토글로 렌더하고, 후속이 열 폭 `<colgroup>` 정본화 · 모달 확대·줄번호 여백·중앙선 드래그·gap 국소 전개 · 높이 고정→상한 · 상호작용 스크롤 보존·문단 단위 하이라이트를 얹었다. 마지막 `attach-diff-unified-bg` 는 **자기 회귀 수정** — 줄 배경을 `.has-content` 로 좁힐 때 `_renderSplit` 에만 클래스를 부여해 단일열의 내용 있는 추가/삭제 줄이 전부 중립 filler 를 받았다(색이 사라진 것보다 나쁘게 **의미가 반대로 뒤집혔다**). 자동 44축이 전부 PASS 인 채 나갔고, 잡은 것은 배포 후 확대 캡처 판독이었다 → 재발 클래스("렌더러가 둘인데 규칙을 한쪽만 따름")를 헤드리스 B9/B9b + mjs A1d 정적 대칭 가드로 잠갔다. ⓒ `attach-multi-upload`(Major) — 사용자 보고 "내용이 다른데 '동일한 파일'로 블로킹" 에서 **판정 자체는 정확했다**(로컬 22개 sha256 전량 일치 실측 · mtime 만 갱신). 실재한 결함은 주변이었다: `#attachFileInput` 에 `multiple` 부재(22회 반복 선택) · change 핸들러가 `files[0]` 만 처리 · `.composer-wrap` drop 이 `#chatPane` **자손**이라 같은 drop 이 버블링돼 첫 파일이 2회 업로드되고 두 번째가 dedup 에 걸리는 **오탐 토스트** · 중복 스킵을 빨간 에러 토스트로 알려 "차단당했다" 로 읽힘 · assistant 편집본이 `<원본>_v<n>` 이라는 다른 이름으로 저장돼 체인 스코프 `(conv, account, OriginalFilename)` 가 갈리며 한 논리 파일이 **두 체인으로 분열**(라이브 9쌍). 전량 순차 업로드 + 배치 요약 1회(정보 톤 "이미 최신입니다(내용 동일) — 건너뜀") + drop 위임 + 편집본 파일명 **원본 승계**(LLM filename 무시 → 실행파일류 확장자 승격이 정의상 불가능해져 SEC-1 강화)로 해소하고 버전 구분은 표시 계층(`_v{n}`)에만 둔다. **feature-0003 표시·상호작용** — `share-sender-nickname`(Major): 공유 링크 대화 내역의 발화자가 `roleLabel(msg.role)` 로만 채워져 전원 '사용자' 고정이라 그룹 대화를 공유하면 누가 무엇을 말했는지 구분할 수 없었는데, 라이브 익명 호출 실측에서 `meta.sender_username`/`sender_account_id` 가 **이미 payload 에 실려 나오고 있었다**(백엔드 무변경 · 표시 계층 단일 결함). `senderLabel(msg)` 4단 폴백(닉네임 → `사용자 <id>` → 대화 소유자명 → `사용자`)으로 배지·point rail 툴팁/aria 를 단일 출처로 통일하되, §18.8 흡수로 **사후 추론 각인(`attribution_inferred`)은 확정 라벨로 렌더하지 않고**(fork 본 공유 시 원본 대화 owner 이름이 익명 화면에 등장하던 경로 차단) 소유자명 폴백은 **1:1 확인 시에만** 허용한다(그룹 legacy 행 오귀속 — payload `conversation.is_group` 불리언 1개 + fail-closed 게이트). `modal-backdrop-dismiss` → `modal-dismiss-siblings`(Minor): DOM `click` 의 target 은 mousedown/mouseup 두 지점의 **공통 조상**이라 패널 안에서 누르고 배경에서 떼도(지침 textarea 드래그 선택) target 이 backdrop 으로 **승격**돼 닫혔다 — 사용자 관점에선 "down 만 해도 / up 만 해도 종료". 판정을 pointerdown+pointerup **2단 계약**(두 이벤트 target 이 모두 backdrop)으로 옮기고 실행은 `click` 단계에서 해 ghost click 을 흡수하며, 터치·펜의 implicit pointer capture 는 배경에서 시작한 제스처에 한해 해제한다. 이어 같은 판정이 9곳에 흩어져 6곳 `click`·3곳 `mousedown` 으로 굳어 있던 것을 **저장소 단일 primitive `static/modal-dismiss.js`** 로 통일했다 — ESM 번들이 작업화면(`app.js`)/관리콘솔(`admin.js`) **둘**이라 primitive 를 app 쪽에 두면 admin 은 복제할 수밖에 없고 **그 복제가 애초에 이 결함을 만든 기전**이다. 회귀 잠금은 파일 목록 하드코딩이 아니라 `src/static/**/*.js` 재귀 walk + 표기 변형 무관 detector. **feature-0002** — `update_attachment` 도구(Major, `FR-attach-delivery-truncated-by-output-cap`): 첨부 갱신본을 답변 본문 블록으로 실으면 그 payload 가 답변 서술과 **같은 출력 창(`max_tokens`)을 두고 경합**한다. 관측 대화에서 6파일 전문이 `completion_tokens=100,000` 상한에 정확히 도달해 1건만 전달됐는데, 잘리기 전에 쓰인 "6개 전부 갱신했습니다" 는 그대로 남았다 — `finish_reason` 은 코드 전체에서 **한 번도 읽히지 않았고**, red-team 은 materialize **이전**에 돌아 실제 전달 결과를 구조적으로 볼 수 없었다. 증상 대응(감지·리뷰·순서)이 "전달 payload 가 출력 예산을 공유한다" 는 전제를 남긴다는 사용자 지적을 받아 **구조 개선**으로 재설계: 파일마다 독립 턴의 창을 쓰는 도구(턴당 상한 20 · 블록 폴백 5) + unified-diff `patch` 전달의 **fail-closed 적용기**(문맥 불일치 · ±400줄 안 모호한 다중 일치 · hunk 겹침/역순 · **잘린 패치**(머리말 선언 길이 ↔ 본문 크기 불일치) · 문맥 줄 없는 hunk → 하나라도 걸리면 전체 미적용) + `finish_reason=="length"` 를 **초안 확정 시점에 latch**(red-team 의 revise/rederive 가 값을 덮으므로 리뷰 이후 재조회 금지) + 리뷰어 `DELIVERY FACTS`(블록 경로 전달은 리뷰 이후 materialize 라 **floor** — 총량으로 읽으면 정직한 혼합 턴을 BLOCK). **가드는 분기하지 않는다** — 도구도 블록 경로와 같은 materialize(대화·소유권·kind·용량·명명·확장자)를 태우고 스코프는 `read_attachment` 와 동일 집합이며 bounded 발신자에겐 도구가 노출되지 않는다. §18.8 [P1] 4·[P2] 9·[P3] 6 전건 흡수 중 결정적인 건 **도구로 만든 첨부가 다운로드 칩에 안 나오던 것**(`MetaJson.message_id` 미바인딩 → `result["tool_delivered_attachment_ids"]` 를 답변 저장 후 워커·web inproc 양쪽에서 바인딩) — 기능 무효이자 새 허위 완료 채널이었다. 뮤테이션 9/9 KILLED · `docs/SECURITY.md §39`. 선행 `read_attachment` 완전성 계약(08-05, Major)은 결과가 **읽은 범위의 완전성을 스스로 진술**하게 하고 모든 수치를 실제 전달분에서 파생시킨다(문자 상한이 줄 중간을 자르면 조각줄 폐기 — 종전엔 자르기 전 청크 길이를 돌려줘 `남은 0줄 미열람` 같은 **정량화된 허위**를 냈고 이어읽기 시작점이 전달분보다 앞서 **어떤 호출로도 오지 않는 구멍**이 됐다). 끝으로 `aiops-hygiene`·`label-namespace`(Minor 2건): `domain_synthesis` 계측이 dict 라 `_telemetry_sweep` 이 통째로 버려 **운영자에게 한 줄도 도달하지 않던 것**(이 워커 네 번째 재발 — auto_reanalysis 2회·analysis_verify·domain_synthesis)을 명시 payload 등재 + `ran`/`attempted` 분리로 고쳤고, L3 도메인 합성이 스키마의 클러스터 요약을 **전량** 재료로 써 재구성으로 죽은 클러스터가 섞이던 것(저장 90행/887멤버 ↔ 실제 84/770 · 라이브 `domain_summaries` 5건 전부 해당)을 살아있는 라벨 집합으로 필터한다. 판정 축은 시간이 아니라 **존재** — 같은 라벨의 여러 행이 "버전" 이 아니라 동시 생존하는 다른 클러스터라 "최신 1건" 처방은 직전 cycle 에서 **반증돼 철회**됐고(정합 편차 339→645 로 1.9배 악화), 라벨 저장처가 멤버 종류로 갈려(`rag_objects`=테이블 · `routine_objects`=루틴) 한쪽만 보면 다른 쪽이 전멸한다(`rag_objects` 단독 50.6% 사망 판정 → 루틴 합산 **3.4%**, 버려질 뻔한 933행 중 870행이 살아있는 루틴 클러스터 · MSSQL 은 루틴이 압도적). 조회 실패는 합성 skip(fail-closed), 라벨 0 은 로그(head-of-line 정지 식별). **배포 실측**: 델타로 바뀐 static 14종의 라이브 서빙본이 `origin/main` 과 byte-identical(빌드 주입 `?v=<hash>` 정규화 후)이고 5서비스 healthy → 08-06 작업 전부 라이브. [[feature-0003-agent-web-ui]] · [[feature-0002-agent-core]]
- 08-05 프론트 오케스트레이터 감축 후속 완결 + 분석문 판정의 순환 차단·결과 노출 + 첨부 변경 사실 봉인 + P5a 종결 + 하네스 red 전수 해소(feature-0038 / 0036 / 0002 / 0011 / 0003 · **신규 feature 0**): **feature-0038 Phase A~B3** — 본편이 "byte-동치로는 불가" 로 남긴 AC-3 잔여를 결합 매트릭스 실측(모듈-스코프 `let` 21개 × top-level 함수 297개)이 재정의했다: 도메인 경계를 넘는 양방향 재할당은 `_dqaDrag`(renderConversationList ↔ initialize) · `_sidebarCatchupTimer`(_scheduleSidebarCatchup ↔ handleLogout) **2건뿐**이고 `_shareRangeEscHandler`·`_mentionMembers` 계열은 함수 집합 설계로 해소된다 → 그 2건만 `state` 편입(Phase A, 비-중립 mini-change·PLAN-APPROVED 08-05, 계약 가드 `verify_state_intake.mjs`)한 뒤 사이드바(B1 -720)·composer(B2 -1,891)·progress(B3, 최다 회귀 영역이라 마지막)를 byte-동치 이동해 app.js **11,119 → 8,082줄(-27%)** · `app/` 6모듈(auth·profile·sidebar·messages·composer·progress), PR #1147~#1150 전건 배포·POST-DEPLOY PASS(B2/B3 는 실전송 완주 스모크 14/14). 잔여 admin.js 4,804줄은 공유 코어(권한 grid·pending/batch-apply)라 재실측 사안. **feature-0036** — "운영 현황에서 판정이 분석 작업의 대부분" 이라는 사용자 관측에서 출발해 **대상 선정(행 단위) ↔ 저장(노드당 1행) 단위 불일치**로 판정이 무한 순환하던 것을 확정했다: 같은 노드에 여러 분석 run 의 done 행이 쌓여 A세대 저장 시 B세대가 미판정으로 남고 B세대 저장 시 A행이 지워져 다시 미판정 → 7일 **7,896콜**(배경 LLM 62.8%)에 판정 행은 91개 고정(노드당 평균 87회 재판정·정보 증가 0), done 행 2개↑ 노드만 반복하고 1개인 노드는 조용해 대조군까지 성립. 대상도 `DISTINCT ON` 노드당 최신 1건 + "최신"=**`id`**(트리거가 모든 UPDATE 에서 `updated_at` 을 올려 과거 run 이 역전 · `get_node_analysis` 와 같은 기준) + 순회는 판정 오래된 것부터(미판정 우선만 두면 재판정 starvation) → 시간당 148콜 **→ ~14콜**(25분 6콜/6판정, 낭비 0). 이어 `node_analysis_verdicts` 를 읽는 코드가 0곳이라 효용 0 이던 것을 그래프 뷰 노드 상세에 **배지 + 근거 한 줄**로 노출 — **해시 일치일 때만 표시**(ADR-0036-09 · 미판정·실패·불일치는 모두 표시 없음으로 수렴) · 증거 stage 0 은 표본이 0행이라 "구조와 …" 라벨(ADR-0036-10 · 라이브 95건 중 82건이 stage 0) · stage 승격 시 재판정 · PB-0008 PASS. **feature-0002** — "새로 첨부되거나 변경된 파일이 없습니다" 단정(DB 실측 v2 8·신규 4·이월 1)의 근본은 컨텍스트 미전달이 아니라(배포본 재구성으로 ★신규 12·🔄v2 8·diff 8건·38,013자 정상 입증) **부재 단정을 막는 코드-권위 규칙 부재**(기존 봉인은 DB 축 전용) + 표식이 72k자 프롬프트 중간에만 존재 → A(말미 코드-권위 블록)+C(턴 매니페스트)+B(리뷰어 능동 검출) 3축, §18.8 **BLOCK** 의 P1 5·P2 8 전건 흡수 후 뮤테이션 8종 전건 KILLED(초판은 6종 생존), PR #1155 배포 + 배포본 런타임 실증 완료(라이브 사용자 대화 실측만 다음 audit 이월). **feature-0011** — P5a Step 6(feature 단위 Dockerfile 분리) **불채택 종결**(ADR-20260805T153000: 분리 전제가 shared/ 추출 + CODEBASE_MAP §7 로 해소되고 이후 배포 스파인 0014/0020/0039 가 단일 이미지를 전제로 안정화 · 재개는 이미지 크기·보안 요구 실측 시 별도 initiative) + P3-제외 로드맵 유보 3건 전수 실측 100% 검증. **feature-0003** — `verify_*.mjs` 39개 중 **23 red**(CI 비배선으로 조용히 썩음 — feature-0038 REPORT §8 기록 12 + 전수 실측 추가 11)를 원인 6유형별로 현행 계약 갱신(검증 취지 보존·동어반복 금지)해 **40/40 green** + settings-notif 시나리오 전 블록을 page 전역 호출 → DOM 이벤트 경유로 재작성해 실 Windows Chrome **20스텝 OK**. **template v3.44.1** — worktree 격리 강제 최소버전 v2.1.216+ → **v2.1.222+**(v2.1.221 까지 격리 세션이 main checkout 에 파괴적 git 을 걸 수 있어 그 미만에서 F0 는 강제 게이트가 아니다). [[feature-0038-frontend-modularization]] · [[feature-0036-analysis-verification]] · [[feature-0002-agent-core]] · [[feature-0011-shared-extraction]] · [[feature-0003-agent-web-ui]]
- 08-04 운영 정기 잡의 인-컨테이너 이관 + 프론트엔드 모듈화 ITEM-P5b 완결 + 사용자향 fix 4건(feature-0039 신규·Major / feature-0038 Cycle 3~10 + Final / feature-0003·0002): **feature-0039** — 호스트 root crontab 의 `cd <repo> && bin/*.sh` **4줄**(백업 `0 3 * * *` · 복원 리허설 `30 3 * * 0` · AGE 그래프 sync 증분 `*/30 * * * *` / 전량 `17 4 * * *`)을 `ops-scheduler` 컨테이너 서비스로 옮겼다. 기존 래퍼가 전부 `docker exec` 였고 이 호스트의 docker 소켓이 root 전용이라 root crontab 이 요구됐던 것 — 잡을 이미지 안(`/app/scripts/ops_*.sh`)으로 옮기면 소켓 의존 자체가 사라져 **root 권한의 유일한 근거가 소멸**한다(역방향 소켓 마운트는 컨테이너→호스트 root 승격 경로라 배제 · 소켓을 마운트하지도 않는다). 스케줄 정본은 crontab(버전관리 밖) → `docker-compose.yml` `OPS_SCHED_*`, 산출물 경로·벽시계 시각·§82 가드는 불변. 제약 3종이 구현을 좁혔다: pg_dump 는 서버 메이저와 동일해야 해 PGDG `postgresql-client-16` 고정(client-17 산출물의 PG17 전용 `SET transaction_timeout` 이 PG16 복원을 rc=3 으로 깨뜨림 — 실측) · flock 은 호스트와 같은 inode bind-mount(미마운트 rc=1 / 호스트 보유 rc=3 / 정상 rc=0) · 호스트 cron 병행 시 백업이 하루 2회 돌아 보존 회전이 절반이라 이관은 원자적(배포 후 crontab 제거). 부수로 **AGE cutover 이후 5주간 FAIL 하던 PG 복원 리허설 결함**을 발견·수정(`ag_catalog` 사전 프로비저닝, 사용자 승인 하 범위 추가) → 761MB 실백업 전량 복원 PASS(PG 67 · MySQL 32). §18.8 codex 패널 P1 5건 전건 흡수(Makefile 기동목록 누락 · SIGTERM grace 280s/stop_grace 5m · `.partial` staging·완료본만 회전 · `../artifacts` 마운트 2개 축소 · `depends_on` override) · pytest 29 · 전 스위트 3,647건 실패 0. **머지(PR #1131)까지이며 배포 + 호스트 root crontab 4줄 제거는 잔여.** **feature-0038** — Cycle 3~10 + Final 로 ITEM-P5b 완결: styles.css(9,328) 소멸 + `css/` 7파일 · admin.js **14,007 → 4,804줄(-66%)** + `admin/` 9모듈(계 9,354줄) · app.js **11,119줄** + ES module 전환 + `app/` 4모듈(계 2,161줄), 10 cycle 전건 머지·배포·POST-DEPLOY PB-0008 PASS(PR #1124~#1143 10건). AC-3(오케스트레이터 ≤ ~3,000줄) 부분 달성 정직 보고 — 공유 가변 `let`(`_dqaDrag` 류)의 양방향 재할당 결합이 byte-동치 이동을 막아 잔류가 정답이고 후속은 state 편입 mini-change(비-중립·별도 승인) 선행. 재발 방지 = CONVENTIONS **§14 code-modularity**(프론트 임계 3,000/5,000줄 + 분할 방법 정본) · Final 은 코드 무변경으로 배포 생략. **08-04 fix 4건** — 공유 링크 '대화에 참여' 표시 조건 확대(적대 리뷰가 초안을 뒤집음: 이미-멤버 클릭은 join 미호출 · `viewer.conversation_id` 직행 = windowed 멤버가 `stamp_member_visibility(is_new_member=False)` 로 가시 범위를 교집합 축소당하는 경로 차단) · 시스템 프롬프트 자동 생성 배선 복구(**대화 요약 writer 완전 부재** — 유일 호출자 `agent_cli.py` 가 죽은 코드로 삭제되며(68ed7a76, 06-02) 끊겨 `agent_runtime.summary` 가 296대화에 0행 · 자동작성 3종의 사례 요약 접지가 한 번도 생성된 적 없음 → `refresh_conversation_summary()` 신설 + topic 오염 27.5% 정규화 3경로 공통 + 실패 로깅 + 역할 cascade) · 발화자 사후 변경 차단(렌더 시점 대화 설정 파생 → 발화 시점 각인 — 제품 칩 변경이 과거 답변 아바타를 전부 바꾸고 fork 본에서 원저자 질문이 복제자 이름으로 표시되던 것) · **feature-0002 요약 부트스트랩 교착 해소**(`PgRuntimeBackend.load_summary` 가 행 없음도 None 으로 반환해 "None = 읽기 실패" 계약과 충돌 → 요약 없는 대화 전부가 PG 실패로 분류되고 큐레이션 훅 `conn=None` → MySQL 폴백 예외 → 요약 미저장 = writer 복구가 라이브 실효 0 이던 직접 원인. `Optional[str]` → `str`(부재=`''`)로 실패 신호와 데이터 부재 분리 · 선행 cycle 테스트가 `load_memory_context` 를 통째로 스텁해 단위 테스트가 못 잡았음도 기록).(feature-0035/0036/0037 신규 = `docs/improvements/analysis-orchestration/ROADMAP.md` **ITEM-11·10·08 종결** · feature-0038 신규 = `ssot-consolidation` ITEM-P5b 잔여): 07-30 이 쌓은 4층(접지→합성→소비→예산) 위에서 남은 질문 셋을 닫았다 — *무엇을 먼저 분석할지* · *그 분석문이 사실인지* · *DB 단위 개요는 누가 언제 만드는지*. ① **feature-0035 결정적 분석 플래너**(Major): 라이브 커버리지 **테이블 2,040/17,192 = 11.9%** 가 중요도와 무관하게 편향돼(사용자가 우연히 클릭한 것) L1·L2·grounding 세 층의 품질 상한이던 것을, `점수 = 대화 조인 이력 × 50 + 관계 차수 × 1`(동점 이름 오름차순)로 **LLM 없이** 순위화해 소량씩 시드. AGE 실시간 중심성 미사용(multi-hop 라이브 최악 82초 — 필요한 건 값이 아니라 순서)·큐잉 미재구현(`enqueue_change_analysis` + `reason="coverage_priority"` 로 자격/cap/쿨다운/busy 가드 상속)·2중 상한(스키마당 3 · **사이클 9 = load-bearing**, effective-schema 루프 안이라 없으면 스키마 수만큼 곱해짐)·`strpos(…)=1` prefix + `DISTINCT`. **배포 후 라이브 검증이 퇴화를 잡았다** — 관계 신호 없는 스키마에서 이름 순 퇴화(mysql-f2b1fe2c288c 테이블 1,464 중 **1,364=93%가 날짜 접미 파티션**) → 계열 대표 1개 축약(web_ranking 미분석 719→11 · dblog 272→272 무영향), 라이브 선정 실측 `dblog` 최고점 27 → item·guild·register. ② **feature-0036 분석문 사실성 판정**(Minor): 분석문을 L0 통계와 대조해 `supported`/`contradicted`/`unverifiable` + 판정을 가른 숫자를 지목하는 근거 문장을 `node_analysis_verdicts`(alembic **0052**)에. **이 기능의 전부는 정직성** — 판정 실패(LLM 오류·타임아웃·계약 위반·근거 없음·증거 부재·저장 실패·예산 소진)는 **행을 만들지 않는다(= 미검증)**, fail-soft 가 "통과"가 아니라 **"침묵"** 이다(잘못 찍힌 도장은 위층을 더 확신하게 만들어 이 층을 없느니만 못하게 한다). 예외 하나: 예산 모듈 미가용이면 pass **중단**. `analysis_hash` 재판정 축·노드당 1행·pass 20·**1건 1콜**(배치하면 오판이 번진다)·호출측 `limit` 이 설정 상한을 못 넘음. ③ **feature-0037 도메인 합성 L3 lazy**(Minor): 라이브 클러스터 요약 **1,006건/120 스키마**(평균 8 — "이 DB 가 어떤 도메인인가"에 여전히 8개를 읽어야 했다)를 스키마당 3~5문장으로 접어 `domain_summaries`(alembic **0053**)에. 사전 전량 생성 금지(LazyGraphRAG)와 답변 경로 런타임 합성 금지(ADR-0034-07)를 **요청/생성 분리**로 함께 지킴 — grounding 은 없으면 `request()` 만(LLM 0·지연 0·요청 UPSERT 는 RO 조회 커넥션이 아닌 별도 RW), insight tick 이 `request_count` 순으로 합성(**사용이 곧 우선순위**)·재생성 축 `cluster_set_hash`(라벨·요약·멤버 수·근거 수)는 payload cap 25 **이전** 전체 집합(최대 300)으로 해시·pass 3·advisory lock tick 한정·30자 미만 미저장. **0036/0037 은 배포·라이브 실측 잔여.** ④ **feature-0038 프론트엔드 모듈화**(Critical·PLAN-APPROVED 2026-08-03): admin.js **14,007** · app.js **13,165** · styles.css **9,328줄**(08-03 실측)을 behavior-neutral 점진 추출로(코드는 feature-0003 `src/static/**` 거주·본 unit 은 계획·추적·검증 홈 = feature-0012 패턴·big-bang 금지). Cycle 1 배포 완료(styles.css → `css/` 7파일 순차 concat **byte-identical** cmp 증명 · PR #1124/1da17988 · deploy-web RC=0 · PB-0008 PASS · 롤백 리허설 revert 왕복 diff 0) · Cycle 2 머지(admin.js 사용량·AI운영현황 pane → `admin/{usage,aiops}.js`, 14,007→**13,057줄(-950)**·역재구성 parity 증명·§18.8 BLOCK 흡수 = `aiops.js` 자유 식별자 `$` 로 운영 현황 pane 크래시 → export/import 배선 + `acorn-globals` free-vars 0/0 게이트 표준화)이나 **JS 는 사전 docker cp QA 불가(모듈 캐시 이중 인스턴스)라 배포·POST-DEPLOY PB-0008 이 완료 게이트**. **동일 창 UX·운영·정직성 bundle** — feature-0016 **스키마 간 라벨 어휘 통일**(밴드 병합 없이 이름만: 임계 0.97 을 측정으로 결정 — 같은 라벨 쌍 중앙 0.949 / 다른 라벨 쌍 중앙 0.672·p99 0.848, 213만 교차-스키마 쌍 · union-find 대신 1-hop 결정론 · 요약 생성을 통일 이후로 옮기고 label 을 요약 신선도 축에 추가. 라이브 E.11 라벨 -9.7%/-13.3%·밴드 불변, E.12 전량 전환 후 전역 루틴 밴드 **2,703→2,703 완전 불변**·라벨 종수 **1,648→1,279(-22.4%)**·우편 시스템 5밴드→0) · feature-0016 **그래프 시각 부정합 2건**(멤버 1개짜리 "밴드" 3→0 — 싱글턴 흡수의 `nm:` 예외 근거가 2차 attach 종료 시점엔 이미 무효 / 칩 라벨 넘침 `labelMaxWidth` 176 고정 vs 칩 폭 150 → 우여백 -3px→+14px) · **자동 재분석 계측 도달 규약 역전**(계측 2키가 report 엔 있고 payload allow-list 엔 없어 **7일간 운영자 도달 0** = 1라운드 R7 재발 → §18.8 이 1차 보정을 BLOCK(고아 카운터 6건 더·감시자가 유실 3방식 중 2개를 통과함을 mutation 실측) → `_telemetry_sweep` 이 미등재 스칼라를 흘리고 **빼야 할 것만** `_TELEMETRY_SWEEP_DENY` 에 명시해 "기록했다 ⇒ 도달한다"를 구조로) · feature-0003 **AI 운영 현황 '미분류 활동' 3종 재배치**(라이브 `llm_usage` DISTINCT task 16종 중 3종 taxonomy 미등록 — `analysis_verify` 1,258회·`cluster_summary` 332회·`domain_summary` 4회 → `ai.insight.analyze`. 07-28 4종 반복 패턴이라 `_record_llm_usage` task 리터럴 **AST 전수 게이트**(래퍼 19종·f-string 파생 동치·정적 미확정 경로는 fail) 동반. POST-DEPLOY taxonomy 18→21종·web-a/web-b 양쪽 확인·`ai.other.unmapped` 카테고리 소멸) · feature-0002 **conv-audit 4건 전건 배포+라이브**: ⓐ 쿼리 리뷰 프레이밍 봉인(라이브 DB=BEFORE/첨부=AFTER 시간 방향을 코드 권위선으로 — PB-0008 재현에서 프레이밍 축 미재현 = 봉인 작동이나 같은 답변에서 **미검증 부재 단정 1건**(148만 행 실존 테이블을 "현재 미존재", 조회 0건) 발견 → 축 한정 verified(framing)·PARTIAL PASS) ⓑ **운영자 프롬프트가 삼킨 grounding 봉인 복구**(`compose_system_prompt` 이 운영자 `WebSystemPrompts` global row 존재 시 코드 `SYSTEM_PROMPT` 를 통째 대체 — 라이브 row 가 2026-06-18 자라 이후 추가한 grounding 5종이 **프로덕션에 존재한 적 없었고**, 동시에 REQ-20260714 이 제거한 억제 지시 2건이 한국어로 생존해 충돌 = 선행 A/B 대조쌍 도구 **0회↔12회**의 실제 기전. `_GROUNDING_AUTHORITY_DIRECTIVE` 코드-append·반환 직전 배치·보안/프라이버시 7종 carve-out·census 테스트 고정 → 배포본 composed **13,604→17,830자**·필수 seal **9종 전량 LIVE**) ⓒ 부하게이트 재작성 방향(차단 메시지에 EXPLAIN 진단+원인별 재작성 지시+`approx_rows` 대안+반복 차단 시 `confirm_heavy` 명시 승격, `EXPLAIN.rows` 가 LIMIT 미반영 스캔 상한이라 순수 LIMIT 조회가 전체 행수로 오판·차단되던 것(라이브 `LIMIT 5` → **13,903,018 추정**)을 `est=min(est, n+offset)` 하향 — 라이브 A/B 재현에서 진단 **0/6→4/4**·전역집계 고지 0→3·escalation 2회, 모델이 대상 전환·범위 축소로 재작성 성공해 **원 대화가 포기한 조사를 17 steps 6,078자로 완수**) ⓓ **전제 절 '검증된 사실 또는 미확인' 봉인**(90일 87대화 객체 상태 단정 90건 중 **43건=47.8%** 가 도구 결과에 객체명 부재·`미확인` 표기 **0건** → 계약에서 "stated as fact" 제거, 권한거부·스코프경고·미조회=미확인, 0행은 정확한 식별자+도구 커버리지 명시 시에만 범위 내 부재. **귀속 정정** — 자기 2차효과 단정을 측정이 반증(07-30 시간-방향 계약 이전부터 구조적 공백). 감지기 `bin/measure-precondition-grounding.py`(스크린 명시·fail-closed·회귀 10건) 신설. 배포본 계약 9요소 LIVE·composed 19,764자·미확인 0→2 는 방향 신호이나 표본 작아 **verified 로 닫지 않음** = `--days 30` 재측정이 완료 판정) · **agent-core 질의 임베딩 강등 가시화**(feature-0034 계측이 init 의 87%가 `query_embed_ms` **20,587ms** 임을 드러내고 `AGENT_KB_QUERY_EMBED_TIMEOUT_SEC=20` 과 정확히 일치 — **성공한 느린 임베딩이 아니라 타임아웃 후 trigram 무음 강등**이었고 사용자는 20초를 버린 뒤 벡터 검색 없이 답변받았으며 분해에 그 사실이 없어 **2주간 미발견**. 원인 = 병렬 세션 시그니처 전수 재계산 48,226건 백필이 단일 CPU-bound 임베딩 백엔드 포화. `init_detail.query_embed_ok`(1/0·bool 금지·잔차 leaf 제외) 신설 + 타임아웃 **20→12s** + perf-snapshot §3b-1 강등율). 두 배포 모두 1차에 insight-worker 헬스 미도달로 워커군 롤백(부분 완료·원인은 배포 창 외부 datasource 도달 불가로 코드 무관) 후 멱등 재실행 RC=0 — **배포 검증은 종료코드가 아니라 서빙 주체의 실제 SHA 로 한다**. [[feature-0035-analysis-planner]] · [[feature-0036-analysis-verification]] · [[feature-0037-domain-synthesis]] · [[feature-0038-frontend-modularization]] · [[feature-0016-metadata-graph]] · [[feature-0003-agent-web-ui]] · [[feature-0002-agent-core]]
- 07-30 분석 오케스트레이션 4 feature 신규(feature-0031/0032/0033/0034 — `docs/improvements/analysis-orchestration/{RESEARCH,ROADMAP}.md` ITEM-04·05·06 → 12 → 07 → 09, 전부 Major·in-progress·**배포 검증 대기**): 라이브 실측이 병목을 재정의했다 — 분석 완료 노드 ~10,000(커버리지 14.6%)·월 `node_analysis` LLM 콜 10,620 인데 **분석 입력의 데이터 실측 0 · 대화 답변 경로의 분석 참조 0** 이었고, 프롬프트가 "이름 규칙에서 가장 그럴듯한 의미를 추론·단정"을 지시해 1만여 건의 추측이 정본 노릇을 하고 있었다(병목은 사용자 진단의 '재귀 구조'가 아니라 **입력 접지 부재**). 끊긴 사슬을 4층으로 이었다: ① **접지 L0/L1**(feature-0031 — 운영 DB 에서 **통계만** 수집(원시 샘플값 미저장·미주입 — DBAutoDoc 근거 "이득은 통계에서 나오고 샘플값에서 나오지 않는다")·`ds` 예산 게이트·Stage 하루 1단계 승격+운영자 승인·payload `evidence` 블록·`_analysis_is_thin` 을 길이→항목 충족도, alembic **0050** additive) ② **합성 L2**(feature-0033 — 클러스터마다 "이 묶음이 함께 무엇을 하는가"를 2~4문장으로 사전 합성. 라벨 평균 9자가 이름이면 이건 설명·캐시 키=멤버셋+L1+L0 3중 지문·요약은 클러스터링 시그니처에 **미유입**(유입 시 전량 재임베딩 폭주)·pass 당 40 상한, alembic **0051** `cluster_summaries`) ③ **소비**(feature-0034 — 질문이 언급한 테이블 → 그 테이블의 클러스터 라벨 → `cluster_summaries` 를 `_build_knowledge_context` 에 주입·**근거 수 동반**으로 추정/실측 구분·매칭 0건이면 섹션 생략·사전계산분만(런타임 합성 0)·`_datamark_untrusted` 인젝션 차단·`cluster_summary_ms` 계측) ④ **지출 상한**(feature-0032 — `agent_runtime.llm_usage` 원천 rolling 24h **백그라운드** 토큰 cap. 사용자가 기다리는 호출은 집계도 차단도 안 함·fail-open 3중(상한 0·조회 실패·모듈 부재)·기본값=실측 일평균 5배라 정상 운영 무영향·콘솔 'AI 운영 현황' 현황 막대+소진/임박 attention). 전 층 live knob 정지 스위치 보유. [[feature-0031-analysis-grounding]] · [[feature-0032-llm-token-budget]] · [[feature-0033-analysis-synthesis]] · [[feature-0034-analysis-consumption]]
- 07-30 feature-0016 컨텐츠 클러스터 품질 + 임베딩 파이프라인 처리량(사용자 리포트 "과세분화·유의어 분산·라벨 이름 순서대로 나열"): ① 클러스터 유사도 지표가 **컨텐츠가 아니라 '양식'에 지배**되던 근본 결함 수정 + 과세분화·배치·상세패널 부정합 개선(시그니처 content-forward 보강) ② 시그니처 전수 재계산이 **선택-순서 결함으로 영구 정체**하던 문제 해소 ③ 임베딩 병목 2단 — 스로틀(`AGENT_KB_EMBEDDING_BATCH_MAX_ROWS` 100→1000: 지속 용량 42,657건/h 대비 설정 상한 6,000건/h·실처리 5,400건/h = **86% 유휴**) 해소 후 병목이 **타임아웃 cliff** 로 이동(텍스트가 길어져 평균 210자·p95 464자 → 100건 배치가 60~120s 로 불어나 클라이언트 60s 를 넘겨 **매번 완료 직전 폐기**, 버려진 요청도 백엔드에서 계속 연산 → 처리량이 느려지는 대신 0 이 되어 큐 8,371 이 25분 고착) → 배치 100→25·타임아웃 60→300s·`MAX_ROWS`→600, 회귀 계약을 "요청당 작업량 × 4 ≤ 타임아웃"으로 재정의("서브배치 ≤ N회"는 노출량을 재던 틀린 불변식이라 폐기) ④ 재임베딩 부하 귀속 정정(AWS Bedrock 아님 — **로컬 embed-ollama**) ⑤ 하이라이트 상태에서 밴드 집계 관계선이 항상 사라지던 결함 수정 + R3 밴드 집계선 픽셀 검증 PASS. **재클러스터 결과 실측(E.6, 기준선 07-30 22:00 동일 쿼리)**: 루틴 밴드 4,572→**2,669**(−42%)·루틴 1~2멤버 밴드 966(21%)→**41(1.5%)**(−96%)·루틴 평균 멤버 6.56→**10.36**(+58%)·루틴 중복 라벨 584종/7,600쌍→**349종/4,067쌍**(−46%)·테이블 밴드 781→**598**(−23%). 배치가 이름 아닌 관계를 따르는지도 검증 — 4 스키마 전부에서 현행 MDS(centroid serpentine) 배치의 인접 밴드 centroid 유사도가 라벨 이름순·옛 루틴명순보다 높다(+0.014~+0.040, 전체 평균 대비 +0.033~+0.083). '메일↔우편' 사례: `우편` 계열 12밴드/92멤버 → **1밴드/12멤버**. [[feature-0016-metadata-graph]]
- 07-30 워커·라우팅·계측 위생 다발(feature-0025·0007·0002·0003): **feature-0025 T0/T0b/T0c** — `shared/resource_budget.py` 신설로 백그라운드 워커 공유 자원(`llm`·`ds`·`task`) 예산 게이트 + 전역 kill-switch + 콘솔 '워커 공유 자원' 표(게이트 없는 상한 knob 은 노출 금지 = 거짓 컨트롤 금지 ADR-0025-06·`pg` 는 커넥션 수명↔예산 수명 결합 미해결로 미등재·web 요청 경로는 예산 밖 ADR-0025-05·PG 테이블/마이그/신규 라우트 0 ADR-0025-08), 스냅샷 파일명을 HOSTNAME→재생성 불변 role(`AGENT_WORKER_ROLE > AGENT_SESSION > HOSTNAME`)로 바꿔 재배포마다 누적되던(라이브 3개) 결함 수정. **feature-0007 edge-free 라우팅**(ADR-003) — 18:00 게이트웨이 재생성 직후 34분간 외부 DNS 해석이 실패해 두 Claude 계정이 도달 불가해지자 edge 폴백이 남은 체인만 조용히 gemma 로 서빙됐다(사용자 관측 "모든 LLM 요청이 edge"). 계정 quota 가 아니라 **도달성 장애**였고, 결함은 장애 자체가 아니라 그것이 **조용한 품질 저하로 번지는 구조** → litellm fallbacks 의 edge 참조 전량 제거·`AGENT_INSIGHT_OFFHOURS_MODEL` 기본값 비움(ADR-002 결정 2 폐지, 로직은 보존)·회귀 잠금 5건·개발용 메타데이터 LLM 의 `-meta` alias 강등 경로도 봉인·배포 후 라이브 확정(9c4e9935). **feature-0002 재배포 dead-air 봉인**(conv-audit) — worker 소유자 id 가 `gethostname()` 기반이라 `--force-recreate` 후 boot self-reclaim 의 `claimed_by` 일치가 항상 0행이어서 "배포로 죽은 job"의 자가회수가 구조적으로 불가능했고 전역 stale 창(실측 450s)을 통째로 기다렸다(60일 실측 8건/8대화·dead-air 142~1,649초·3건 최종 error·재실행이 같은 말을 두 줄로 9대화) → 종료 시 lease 명시 반납 + role 기반 identity + 같은 role 의 죽은 인스턴스를 짧은 창(`ROLE_STALE_SEC` 60s)으로 회수(전역 `STALE_SEC` 불변). **agent-core 지연 계측** — `inference_ms` 내부 분해(`llm_ms`/`tool_ms`/`tool_top`/`other_ms`)가 자기 가설을 반증했다: 미귀속 33초(23%)는 숨은 오케스트레이션이 아니라 `task='redteam'` 으로 기록되는 red-team 이었고 실 잔차는 235ms(1.6%). 다음 표적은 `init_ms` 평균 4,921ms 중 **미귀속 4,171ms(85%)**(데이터플레인 connect 1,447ms 확인) — **잔차 노출이 핵심**, 없으면 다음 블라인드스팟이 또 숨는다. **feature-0003 테스트→라이브 설정 오염 재발 차단** — 07-29 격리를 머지한 10분 뒤 재발해 하루 방치되고 사용자가 다시 복구·재보고했다. 파일 방어(Makefile env·tests/conftest.py)는 이미 분기된 worktree 에 소급되지 않으므로 `make test` 를 전용 compose 프로젝트(`-p repo-unittest`)로 띄워 라이브 mysql/pgbouncer 가 DNS 해석조차 되지 않게 하고(CLI `-p` 가 외부 `COMPOSE_PROJECT_NAME` 을 이김) 루트 `conftest.py` 로 도달성 층·fail-loud·감지를 추가. [[feature-0025-worker-parallelism]] · [[feature-0007-bedrock-llm-provider]] · [[feature-0002-agent-core]] · [[feature-0003-agent-web-ui]]
- 07-29 feature-0003 progress-enqpre-handoff(Major, 사용자 재보고 2차): 요청 직후 말풍선이 **'시작 중…' 에 박제**되고 대화 전환-복귀로만 풀리던 결함 — 선행 progress-poll-resilience(폴러 사망 축)와 **원인이 독립**. 워커 모드 `/api/ask` 는 enqueue 시점에 `enqpre-<uuid>` **sentinel** 을 KV run_id 로 선기록하고(enqueue~claim 갭의 '처리중' 가교 + TASK-0241 orphan clobber 가드용) ask-worker 가 claim 하면 **claim 별 실제 run_id** 로 덮어쓴다. 전송 직후 첫 폴(0ms)이 그 sentinel 을 `progressRunId` 로 고정하면 실제 run 으로의 **정상 승계**가 feature-0009 foreign-run 가드에 "남의 run" 으로 오인돼 **매 응답이 버려졌다**. 라이브 재현으로 확증: 서버가 `steps 3→6→7` 을 정확히 보내는데 프론트 `progressSteps` 는 **끝까지 0**, 상태 라벨만 첫 응답에서 굳고 경과시간만 흐른다. 워커 claim 이 tick 1~2초 뒤라 첫 응답이 sentinel 인 것이 정상 타이밍 = **사실상 매 요청 재현**. 해소: sentinel 을 **추적 id 로 채택하지 않는다**(`_adoptRunId`, choke-point=`resetProgressTracking` 로 전송·복원·attach 전 경로 커버) + 가드 2중 방어. **그룹 foreign-run 불변식 무손상**(가드는 "실제 run vs 실제 run" 에만). 신규 27 PASS(수정 전 18 FAIL)·회귀 0·`make test` FAILED 0. 교훈: 선행 cycle 의 PB-0008 관측에 이미 `runId: enqpre-…` + `stepText: 시작 중…` 이 찍혀 있었는데 "아직 단계 없음" 으로 읽어 같은 증상을 두 번 고쳤다 — **관측 로그의 정체불명 id 를 그 자리에서 추적할 것**. [[feature-0003-agent-web-ui]]
- 07-30 feature-0016 analysis-retry-resilience(Major, 사용자 리포트 "AI 능동 분석이 주기적인 네트워크 단절로 중단되면 네트워크가 다시 연결되더라도 아무런 작업이 이루어지지 않는다"): 근본 원인은 재시도 계정의 **부재**였다 — `llm_node_analysis` 가 네트워크·타임아웃·429·빈 응답을 전부 `None` 으로 평탄화하고, `process_pending` 이 그 `None` 을 즉시 terminal `failed` 로 기록하며, stale 회수(lease 900s)는 `running` 만 대상이라 복구 후 되살아날 행이 없었다(라이브 확증: jobs done 10,215 / failed 710 중 LLM 사유 130건이 07-27 mssql-…d884 49 · 07-29 mysql-…92bc 43 두 장애 창에 뭉침 · **pending 0** · run 전량 done = 화면상 완료로 보임). 조치 4층: ① 실패 분류(`classify_node_analysis_failure` — bad_model/context_length 만 permanent) + 일시 실패는 `attempts+1`·`next_attempt_at=now()+LEAST(BASE×2^attempts, MAX)` 로 pending 되돌림, **`runs.failed` 미증가**(run 이 running 유지 → 복구 시 사람 개입 없이 완주), 상한 4회 도달분만 terminal (alembic **0049** additive: attempts/next_attempt_at/error_kind + 부분 인덱스) ② **run liveness heartbeat**(대기 잡 보유 running run 의 updated_at 매 틱 갱신 — 없으면 backoff 대기 run 이 lease 를 넘겨 stale 오판 → 재트리거가 중복 run 생성, load-bearing) ③ 연속 일시 실패 3회면 claim 을 **canary 1건**으로 축소(단절 창 큐 소모 차단)·성공 1건으로 즉시 복귀 ④ 굳은 실패 회수(`retry_failed_jobs` 단일 CTE — attempts=0 리셋 + runs.failed 차감 + status='running' 복원, 대상은 transient·레거시 LLM 실패만이라 'verification-cleanup(취소)' 580건·permanent 배제) + `scripts/node_analysis_retry_failed.py`(기본 dry-run) + 진행 패널 "재시도 대기 N (hh:mm)"·회수 버튼(`POST …/graph/analyze/retry`, 실행 권한 재사용·신규 권한 0) + 폴 **240회(=10분) cap 제거**→적응 백오프(진전 2.5s / 무진전 10→30s / 실패 2.5→30s, 총 6h 상한). codex 적대 P1 0·P2 4건 전건 흡수(컬럼 부재 영구캐시→600s 재-probe · dry_run limit 미적용 → retried/eligible/capped 분리 · 파싱 실패 태그 분리 · 프론트 in-flight 응답 경쟁). 신규 40 pytest + 25 헤드리스 PASS · make test 전 스위트 EXIT=0. **PB-0008 라이브 + 라이브 잔여 130건 회수는 배포 후 잔여**. [[feature-0016-metadata-graph]] · [[feature-0002-agent-core]] · [[feature-0003-agent-web-ui]]
- 07-29 지식베이스 메타데이터 '제품' 스코프 전환(Major·feature-0003 Task-Cycle·코드 거주 0003/0002·**POST-DEPLOY 라이브 실증 완료**): 관리 콘솔 > 지식베이스 > 메타데이터의 스코프 축을 **데이터소스 → 제품**으로 전면 재구성. 라이브 실측상 축이 **양방향으로** 어긋나 있었다 — 1제품↔N데이터소스(KR_LIVE·KR_QA 각 7 DS)에서는 등록분이 그 중 1개 DS 질의에만 주입되고, 1데이터소스↔N제품(mssql-qa-idc 를 5제품 공유)에서는 타 제품 메타데이터가 혼입됐다. **표시층이 아니라 주입층 결함**이라 저장·읽기·콘솔 3면을 모두 제품 축으로 통일: scope 키 `product.<ProductKey>` 신설 + 활성 제품 ContextVar(`shared/config`) · KB 읽기 seam 4곳 + `describe_table` 오버레이 제품 축(**fact/RAG 축은 무변경**) · 자율수집 쓰기도 제품 귀속(제품 해소 실패 시 수집 중단 — common 폴백=누출) · `GET /api/admin/metadata/scopes` 신설 + 부트스트랩을 제품 접근DB allowlist 로 강제(밖은 404·카탈로그 미가용 503 fail-closed·호출자 datasource override 제거) · 콘솔 스코프 선택기/목록/검토큐/스키마 골격 제품 축(**그래프 뷰는 datasource 유지**) · expand/contract(배포↔이관 창 레거시 ds-scope 꼬리 읽기 + 이관 스크립트 `scripts/kb_scope_rescope.py` 7테이블·백업 강제·23505 한정 중복 병합·`--verify-contract`) · ENUM self-heal sweep 제품 스코프화(단일 DS 제품 한정 — 오삭제 방지). 신규 권한 0·스키마/마이그 0·make test 전 스위트 PASS·codex 적대 리뷰 10라운드(P1 12건+P2 3건 흡수→최종 0). 라이브: 이관 update 5,130·delete 257(모호)·백업 5,387행·contract(`AGENT_KB_LEGACY_DS_SCOPE_READ=0` + 4서비스 재기동) 후 잔여 0, PB-0008 — 제품 선택기 17건·**KR_LIVE 용어 85건 회복**(7 DS 중 1곳에 갇혀 있던 자산)·공유 datasource 3제품 경계 분리(CC 17/DK 35/FH 8) + 교차 접근 404·이관 귀속 정확도(DK_QA 153/1,046·WEB_QA 1,926·WEB_G_QA 879). [[feature-0003-agent-web-ui]]
- 07-29 대화·그래프·자가검증 UX 다발(feature-0003·0016·0021·0002 — 대부분 POST-DEPLOY 라이브 실증 동반): **모델 선택 유실 봉인**(조기 cid 전환 시 sonnet→haiku 무음 강등) · 대화 검색 **첨부 파일명 축** 추가(권한 스코프 게이팅) + 본문 검색 500(collation audit NameError) 근본 수정 · 대화 페이징 "요청이 너무 잦습니다" 블로킹 해소(rate-limit 스코프 분리·부하 클라이언트 분산) · 그래프 상세 패널 **컬럼 미출력·일부 누락** 근본 수정 + 목록 행 **시각 위계·리듬·정렬** 교정 + 위계 **헤더 라벨 재설계**(레벨별 단일 크기·예약 행 완전 수용) + **상한 전수 감사**(feature-0016 — '개수를 줄여 출력'하던 데이터 절단 제거) · feature-0021 **자가검증·재검증 회차 원장**(다회차 폐기 사유 요약 노출·회차별 실제 시각·대화 단위 접이식 콘솔, alembic `20260729_0048`) + 리뷰어 회귀 2건 교정(현재 턴 발화만 보고 답변 붕괴·continuation 과교정 가드) + 추론 탭 안내 문구 축약 · feature-0002 **데이터플레인 연결 유휴/타임아웃 사망 시 자동 재연결**(마찰 원장 FR-dataplane-conn-stale-no-reconnect 배포·라이브 실증 종결). **개발 인프라**: `make test` 가 운영 `.env` 상속 + compose 네트워크 잔존으로 **라이브 PG·라이브 런타임 설정에 직결**됐던 근본 원인 차단(Makefile `TEST_ISOLATION_ENV` + 루트 `conftest.py` 2중 방어 · 콘솔 타임아웃 900→90 무음 롤백의 주체가 자기 저장소 테스트였음을 `WebAuditEvents` 150건으로 입증) → make test **0 failed**(이전 13건) · RO 연결 헬퍼 저하(None) 계약 정본화 + UI 카피 예산 게이트(`bin/ui-copy-budget.py`·verify-completion 배선·template v3.43.0) 채택. [[feature-0003-agent-web-ui]] · [[feature-0016-metadata-graph]] · [[feature-0021-redteam-review]] · [[feature-0002-agent-core]]
- 07-29 feature-0003 progress-poll-resilience(Major, 사용자 재발 보고): 요청 전송 후 assistant 말풍선이 '처리 중'에서 갱신되지 않고 **대화를 전환-복귀해야만** 되살아나던 결함 근본 해소. 원인은 단일 버그가 아니라 **회복 타이머가 전부 사라지는 구조** — ① `pollProgress` 가 연속 3회 실패에서 재스케줄을 영구 포기(롤링 배포 창·4초 fetch 타임아웃·수십 초 순단이면 흔히 도달) ② 유휴 감지기의 dormant 판정 근거가 `progressRunId`/`pendingBubble` 인데 **둘 다 폴러가 죽어도 남는 잔여값**이라 감지기도 영구 dormant ③ `loadHistory` processing 분기가 감지기를 아예 정지. 수정: 폴링은 포기 대신 **지수 백오프**(8→16→32s, 상한 60s)로 계속 재시도하고, dormant 판정을 **"폴러 생존"**(`progressPoller`/`progressPollInFlight`) 사실로 교정하며, 감지기를 처리 중에도 무장해 **폴러 watchdog** 으로 승격(폴러 생존 시 fetch 0회 = 비용 0, 끊기면 `loadHistory` 로 자동 회복). baseline 분기의 `runId !== progressRunId` 비교 제거가 "죽은 폴러가 추적하던 바로 그 run" 을 회복 대상에서 배제하던 마지막 자물쇠였다. + 재가시(`display_status`·`pendingBubble` 근거 보강)·`online` 재무장. 신규 34 PASS(수정 전 코드에선 12 FAIL 로 가드 유효성 실증)·기존 `verify_run_detect_poll` 은 **옛 결함을 고정하던 단언 6건**을 새 계약으로 갱신(28→30). 프론트 단독·백엔드/RBAC/스키마 변경 0. [[feature-0003-agent-web-ui]]
- 07-29 feature-0003 product-picker-keynav(Minor, frontend-only): 작업화면 제품 선택 드롭업의 **키보드 경로**를 닫았다 — 검색칸에서 `↓` 로 검색 결과에 진입, `↑`/`↓` 로 순회(검색 필터로 숨은 항목·선택 불가한 열람전용 행은 건너뜀), **최상단 `↑` 는 검색칸으로 복귀**, `Enter` 는 기존 `_select` 그대로 재사용(신규 선택 경로 0). 종전엔 검색으로 좁혀도 항목이 `tabindex=0` 뿐이라 Tab 으로만 도달했고 `↓` 는 페이지 스크롤만 했다. 포커스 이동은 메뉴 자신의 `scrollTop` 만 보정(sticky 검색칸 높이 차감·`scrollIntoView` 미사용)하고 `.product-dropup-item:focus-visible` 로 커서를 보인다. IME 가드는 `isComposing` + 레거시 `keyCode 229` 둘 다. **동반 수정**: 항목 선택으로 닫을 때 `openProductDropup` 이 문서에 건 Escape/바깥클릭 리스너가 남아 닫힌 뒤 `Escape` 가 포커스를 chip 으로 튕기던 **기존 결함**을 `closeProductDropup` 단일 해제 지점(`_productDropupDetach`)으로 흡수(codex P2). 검증은 실 `renderProductDropupMenu`/`openProductDropup`/`closeProductDropup` 원문 + 실 CSS 를 chromium 에 태운 헤드리스 27건. 백엔드·RBAC·스키마 0.
- 07-29 feature-0003 attach-append-only(Minor, 사용자 지시): 첨부 목록은 **append-only** — 한 번 대화에 올라간 파일은 UI 에서 빼지도 지우지도 않는다. 직전 cycle 이 넣었던 삭제 UI(pill × 실삭제 + 목록 행 ×)를 **철회**하고, ×는 서버에 아무것도 만들지 않은 항목(`staged`·`failed`)에만 남긴다 (`uploading` 제외 — in-flight abort 미배선이라 '취소'가 거짓 어포던스가 되고, 삭제 수단 없는 append-only 에서 그 대가는 회수 불가). 프론트의 `DELETE /api/attachments` 호출 0건(엔드포인트는 백엔드 존치). 안내 = "첨부는 대화에 계속 쌓입니다". 선행 이월 이슈(그룹 첨부 삭제 권한이 업로더가 아니라 대화 소유자/멤버)는 UI 경로 소멸로 **제품 표면에서 무효**. POST-DEPLOY 실증(cb06a4e3): 삭제 컨트롤 0건·첨부 2건 누적·메타줄 말줄임 원복. ADR-20260729T163000-attach-append-only.
- 07-29 feature-0003 attach-full-scope(Major): 첨부가 걸린 대화를 **이어서 요청하면 assistant 가 기존 첨부에 접근하지 못하던** 마찰 해소 — 참조 범위의 결정 주체를 프론트 selection(D16 minimum exposure)에서 **대화 자체**로 이관(`_resolve_conversation_attachment_scope`, 활성 첨부 전량·상한 200). 진입 경로(새로고침·랜딩 복귀·pending)에 무관하게 같은 집합이 보인다. **보안 경계 불변** — 그룹 발신자 스코프(CSO F1)·ConversationId(IDOR)·공유창 억제 유지(그룹 개방은 사용자 결정으로 미채택). 인라인 상한(텍스트 20/턴) 밖 파일은 신설 **`read_attachment`** 도구로 모델이 자율 조회(권한 경계=스코프 집합 재사용·datasource-free·datamark 구획·첨부 있는 대화만 노출). 자가 적대 리뷰어에는 본문 미주입 첨부를 `ALSO ATTACHED` 매니페스트로 전달(예산 상한 0.35 선점 — 발췌 부재를 '창작'으로 오판하던 축 차단). **'이 대화의 모든 첨부 사용' 토글 제거**(백엔드 미사용 no-op 이었다)+참조범위 안내 1줄, 첨부 pill `×`를 **실삭제**로 연결(로컬 제거는 서버가 되살렸다). 회귀 차단: 동기 ingest 대기=이번 턴 한정·vision 정렬 ASC→DESC. §18.8 패널 4 BLOCK+1 CONCERN 전건 해소·신설/수정 50건 PASS·신규 실패 0. 코드 거주 0003(라우터·프론트)·0002(agent_core·tools·redteam).
- 07-29 feature-0030-ask-timeout-extension(Major 신규): 요청 처리가 실행 예산의 80%(콘솔 조절)에 도달하면 작업화면 컴포저 위 **비차단 배너**(+백그라운드 탭이면 브라우저 알림)로 '계속 추론할까요?'를 묻고, 승인한 **그 run 한정**으로 run 예산 컷을 해제해 답변을 완주시킨다(미승인=종전 타임아웃 동작 그대로). AI 작업자가 콘솔 타임아웃을 낮춰둔 상태에서 추론이 잘려 작업 내용이 소실되던 마찰 해소. **개별 LLM 호출 상한(15분)은 유지** — 단일 호출을 무한정 열면 그 사이 '중단'·'즉시 답변'·lease fencing 이 전부 무응답이 되기 때문(codex 적대 리뷰 P1-3). 신규 권한 `conversation.extend.{own,any}`(own 만 finalize 보유 역할에 1회 backfill·any 는 관리자 명시 부여)·KV 시그널은 cancel/finalize 패턴 미러링(run_id 엄격 짝 검증)·콘솔 설정 3종(사용/임계%/추가 허용 상한 기본 0=무제한). codex 리뷰 P1×4·P2×3 전건 반영·신규 36건 PASS·회귀 0. 코드 거주 0002(agent_core·memory)·0003(라우터·스냅샷·프론트·권한)·shared(runtime_settings).
- 07-28 성능 개선 3 feature(feature-0027/0028/0029 신규·in-progress·feature-0026 병목 지도 기반 첫 성능 개선·결과물/응답/인가/그래프 신선도 불변=사용자 가시 동작 0): **feature-0027 P0 답변 지연·자원**(Minor~Major 경계·코드 거주 0002/0001/0006) post-answer 큐레이션(topic/용어/ENUM LLM 3건) 답변 KV terminal **후** 이동(체감 -25~35s)·grounding RO 연결 3→1·MySQL 버퍼풀 128MB→1G·Caddy gzip+immutable static(품질 영향 축=레드팀·thinking 불변) · **feature-0028 web 계층**(Major 경계·코드 거주 0003) `/api/ask_result` long-poll 워커 스레드 이관(이벤트 루프 정지 제거)·스냅샷 PG 연결 4~5→1(단일 왕복 번들)·권한 카탈로그 TTL 캐시+세션 LastSeenAt throttle·web MySQL 풀 opt-in(응답 shape·인가 불변·env 토글 가역) · **feature-0029 그래프 sync churn 근절**(Major 경계·코드 거주 0002) 값 무변경 시 `updated_at` 미전진(upsert·신호·트리거 alembic 0046·실측 churn 63%)·status 히스테리시스(trusted/broken 왕복 차단)·프로브 broken 부활 금지·공유 정점 중복 MERGE 제거(관계당 cypher 11→1~3, 그래프 신선도·자기교정 보존). 전부 비파괴/가역·검증·배포 잔여.
- 07-28 그래프·사용 기록·모델 권한 UX(feature-0016·0003·0002): 관리 콘솔 그래프 뷰 **관계선·hover·클러스터·역할 배지 대규모 개선**(역할 배지·'이웃 깊이(hop)' 예산 재설계 POST-DEPLOY PB-0008 라이브 실증)·프로필/관리 **'사용 기록' 드릴다운**·**model.access RBAC**(답변 모델 접근 계정별 통제). 사용자 가시 표면 — 코드 거주 0016(그래프)·0003(사용 기록 UI·모델 접근 RBAC)·0002(사용 기록 백엔드 `llm_usage.target_scope`).
- 07-28 feature-0023 conversation-quality-controls(Major): 외부 AI 작업자가 **대화 품질 5축**(모델·추론 강도·제품 데이터소스 스코프·폴더 커스텀 지침·첨부)을 직접 조정. 실제 간극은 조정 수단이 아니라 **발견**이었다 — 모델/제품이 계정별 RBAC(`model.access.*`·`product.access.*`)로 다르게 열려 목록 없이는 추측→400/403. 인증 필수 **`GET /api/ai/capabilities`**(신규 권한 코드 0)가 계정별 라이브 값 제공, 목록은 작업화면 선택기와 **동일 필터 함수** 재사용(표시-집행 정합), 익명 매니페스트에는 포인터만 실어 SEC-20260724 "익명=static contract·인스턴스 데이터 0" 불변식 보존. 큐레이션 OpenAPI +6 path/+3 스키마·가이드 §4.7·llms.txt·MCP tool 4→11(품질 7·stdlib multipart). 토큰 안전 기본 scope 에 `folder.` 확장(`.own` 2개만·절대 denylist 무변경·**기존 발급 토큰 무회귀**). `/api/ask` body 계약 불변(제품 힌트=신규 대화 한정, TASK-0047 race 가드 보존). 회귀 2586 passed/0 failed. SECURITY §25.1.
- 07-24 feature-0025 worker-parallelism(Major 신규·머지 PR #933/01c4ba44): 백그라운드 워커(노드 분석·cluster_label)·답변·KB 임베딩 **병렬도·주기·배치**를 관리 콘솔 '시스템>설정>성능·병렬 처리' 서브탭에서 조절(feature-0018 runtime-settings 재사용·performance 10 knob). **기본 동시성 1=현행 직렬 byte-동치 opt-in**·LLM만 병렬/DB직렬·`system.runtime.*` RBAC 재사용(신규 표면 0)·clamp·pgbouncer 40/200·PG 150. 단위48+회귀166 PASS·§18.8 SHIP·배포후 PB-0008 잔여·ADR-0025-01~04·코드 거주 0002/0003/shared/compose. + 07-24 사용자 UX(0003·0019·0007·0016): SQL 강조·CSV 다운로드·새 폴더 버튼·공유 막대/진입 스크롤·그래프 이모지·검토 큐 ds 필터·재답변 모델 승계·Sonnet 5·타임아웃 동기화.
- 07-23 feature-0024 conversation-folders(Major/일부 Critical 신규·**2026-07-23 Phase1+2a 라이브 완결**): 좌측 대화 목록에 재귀 **폴더(프로젝트 워크스페이스)** — 대화를 폴더로 조직·이동, 폴더 삭제해도 대화 보관(Trash·undo·부모/root 승격), 런타임 max-depth(기본 4·grandfathering·순환 방지), 폴더별 **커스텀 지침** ask-time 주입(요청자 폴더 기준). **엄격 per-user(owner-scope) 격리** — `folder.list.own`/`folder.manage.own` own-only(크로스-계정 노출 벡터 `folder.*.any` 폐지, Critical §12.3 privacy 수정)·restore IDOR(HIGH) 봉인. conversation.create 보유 7역할 folder.*.own 동적 backfill. 4 슬라이스(conversation-folders·folder-privacy·folder-perms-broaden·folder-ux 6종: 무프롬프트 생성·인라인 rename·설정/이동 모달·DnD). PR #895/#899/#903/#908·POST-DEPLOY PB-0008 각 슬라이스 PASS. Phase 2b(폴더 파일·datasource 자동스코프) 이연. 코드 거주 0002(스키마·alembic 0044·컨텍스트)·0003(라우터·RBAC·사이드바)·0009(그룹멤버 배정)·shared. feature-local REVIEW REV-20260723T060000/T170000.
- 07-23 feature-0016 analysis-completeness(Major, 사용자 리포트 mysql-local/log_v2): 'DB 전체 AI 능동 분석' 미완결 근본 개선 — ① **컬럼 인벤토리 lazy introspection**(부트스트랩 미수행 datasource 도 Table 잡 처리 시 INFORMATION_SCHEMA→column_descriptions **누락분-only** insert(DO NOTHING, 큐레이션 불변)+Column 정점 MERGE → §55 직계 컬럼 분석·payload 컬럼 컨텍스트 실동작, `AGENT_NODE_ANALYSIS_COLUMN_INTROSPECT_CAP`) ② 분석 완료→**재클러스터 신선도 배선**(임베딩 신선도 due — 6h cadence 비대기·진행 run/임베딩 드레인 유예·라벨 캐시 키에 시그해시 합성·변경분 AGE targeted 투영 `project_cluster_props`) ③ 프론트 stale 클러스터 병합 수정(`"in"` 체크 — 캔버스 밴드↔상세 패널 정합) ④ 전부 완료 시 **'전체 재분석(refine)'** confirm 경로(only_missing:false — "분석 대상 없음" 차단 해소). §18.8 적대 리뷰 FAIL(B1: datasource 해석이 MEMORY_DB 미지정으로 프로덕션 무효)→전량 흡수·타깃 40+전체 스위트 PASS. 코드 거주 0002(modules)·0003(graph-ctxmenu)·shared(config).
- 07-22 feature-0023 conversation-api-access(Critical 신규·**배포·라이브 e2e 통과**): 외부 AI 가 세션 쿠키 없이 **Bearer API 토큰**으로 작업 화면 대화(`/api/ask`) 호출 + 대화 tool 화 **MCP 서버**(tool 4종). 토큰=해시 저장·scope allowlist ∩ 계정권한·`*.any`/관리 네임스페이스 **절대 denylist**(→ 관리 콘솔 차단)·저권한 서비스 계정 귀속, 발급/폐기=콘솔 밖 CLI(`bin/api-token-issue.sh`·web-a/web-b 자동감지). 라이브 e2e 토큰→/api/ask 200·admin 403·무토큰 401(배포 66a48870 soak PASS)·단위 17 PASS·§18.8 리뷰 REV-20260722-0002. 코드 거주 0003·0002·MCP=feature-0023 unit.
- 07-21 feature-0022 agent-scratch-workspace(Major 신규·**2026-07-21 라이브 활성**) + feature-0003 진행상황 실시간 전파: **assistant PG 자율 작업공간** — 전용 DB `agent_scratch`(전용 role NOSUPERUSER·CONNECT 격리·대화별 스키마 `s_<hash>`)에서 외부 데이터소스 governed 반입(execute_sql 신뢰경계 재사용)→cross-source JOIN, 도구 4종(scratch_import/sql/list/reset)·scratch_guard allowlist(root 명시+CREATE/DROP=TABLE/INDEX·함수/뷰/pg_ 거부)·시간기반 TTL reaper(24h)·`AGENT_SCRATCH_*` 설정. 백엔드 코어+단위 22 PASS·회귀 0·**2026-07-21 라이브 활성**(bootstrap+`AGENT_SCRATCH_ENABLED=1`·스모크 PASS·run_sql 수정; 기본값 OFF 게이트; 잔여=cross-source JOIN e2e 실증 TASK-0011·관측 UI·대화별 role 승격). 코드 거주 0002/shared·repo-level bootstrap, feature-local ADR-SCRATCH-0001~0004. · **진행상황 실시간 전파**(feature-0003) — 그룹/모니터링 대화를 열어둔 유휴 관찰자에게 타 사용자 assistant 진행상황/답변이 재로드·전환 없이 실시간 전파(frontend-only run-감지 폴러·백엔드 무변경, PB-0008 라이브). 코드 거주 0003.
- 07-16 그래프 검색·상세 내비 + 콘솔 IA 재구성(feature-0003·0016·0021): 그래프 뷰 검색이 컨텐츠 카테고리·AI 능동 분석 텍스트까지 매칭(graph-search-content-match)+검색어 갱신 시 상세 패널 검색 결과 리스트(graph-search-detail-panel)+결과 패널 2단 접기·검색 이력(뒤로/앞으로)·설명 간결화(graph-search-panel-groups) · 상세 패널 [뒤로/앞으로] 스크롤 보존·바 sticky·비hover 반투명 페이드+focus-fade fix(graph-detail-nav) · 컨텐츠 카테고리 헤딩 hover-pan(타깃=첫 멤버 노드) · 관리 콘솔 'AI 추론' IA 재구성 — 감사>'AI 운영 현황' 단일 탭+서브탭[LLM 사용량|운영 현황|추론]/설정>프롬프트 단일 항목+서브탭[전역|지침|스킬](bindPaneSubtabs)·guidance 권한 system_prompt.global.read 재사용·console.reasoning.read 감사 재배치·서브탭 바 sticky(SECURITY §23 정정 동반). 전건 POST-DEPLOY 라이브. 코드 거주 0003·0016·0021.
- 07-15~16 그래프 상세·우클릭·엣지 + 권한 원자화 + ENUM 묶음 큐 + agent-core 정확도(feature-0003·0002): 그래프 클러스터 상세에 함수/프로시저 컨텐츠 카테고리 포함·전체 출력·카테고리별 접기(cluster-detail routines/cap/fulllist/collapse)·우클릭 3대상 hit-test/band-wins/sim-group 정합(ctxmenu hittest/band-priority/content-category)·노드/제품 카테고리 드래그 시 관계선 즉시 추종+per-frame 재그림 최적화(edge-follow-drag/drag-perf)·도움말 팝업 줄바꿈/반응형 · 관리 콘솔 권한 최소단위 원자화(추가/수정/삭제 분리·레거시 묶음 grid 숨김)+'접근' 카테고리 계층(perm-atomic-split/category-hier, backfill 마커) · ENUM 코드사전 검토 큐 구조 묶음 승인 체크리스트+일괄 등록(enum-review-bundle) · agent-core check_table_coverage 도구·스키마명 server-case drift 0행 봉인·model alias 누출 Bedrock 400 봉인·LLM 헬스 probe 오탐/throttle 수정. 전건 POST-DEPLOY 라이브. 코드 거주 0003·0002.
- feature-0021 자가 적대 리뷰(07-15, Major 신규): 답변 전달 전 **red-team 자가 검증** — fresh-context haiku 리뷰어 5축(grounding/SQL/권한/완전성/정직성)·BLOCK 만 수정 유발(≤1회)·높음+ 재검증·전 경로 fail-open. [세션,제품] 노트 `/shared/agent-notes`(TTL 7d/30d reaper)·REDTEAM_* 런타임 설정·alembic 0042 `agent_runtime.redteam_reviews`·콘솔 'AI 추론' 탭(`console.reasoning.read`)+설정>'AI 자가 리뷰'. 코드 거주 0002(redteam/agent_notes/guidance_registry·choke-point 훅)·0003(admin_reasoning INCLUDE_ORDER=240)·shared(runtime_settings redteam 그룹). Claude Code find→verify·auto-memory·progressive disclosure 이식 (unit DECISIONS ADR-20260715T140000~2).
- feature-0019 메시지 편집(07-14, Major 완성·라이브): **사용자가 보낸 메시지 수정** — 1:1=단순 수정 / 요청사항 수정 시 브랜치 재답변(`< n/m >` 페이징), 공유(그룹)=단순 수정만(@assistant 호출 메시지 잠금·본인 발신 IDOR 게이트). Phase 1(엔드포인트+프론트+보안)+Phase 2(그룹/공유+@assistant 잠금) 머지·마이그 0041 적용·브랜치 recall 로더 hotfix·**PB-0008 라이브 PASS**. append-only 대화 위 대화-내부 브랜치 트리(has_branches 게이트·비분기 byte-identical·단위 11 PASS). 코드 거주 0002(브랜치 트리·recall)·0003(엔드포인트·프론트). [bee8a96d Phase1·1cf7784b Phase2·08443ae1 hotfix·3f9c55ba PB-0008] (정본 REPORT prose 는 checkpoint 3 까지라 Phase 2 라이브 lag — git+PB-0008 이 진실)
- 07-14 그래프·작업화면 UX + 답변 정확도(feature-0003·0002): feature-0003 — 그래프 뷰 **첫 입장 조작 도움말 팝업**(중간버튼 커서 표식·mis-position CSS 주석 `*/` hazard 근본수정)·**제품 카테고리 밴드 우클릭 전용 메뉴**·작업 화면 **애니메이션 효과 설정**(복원+인앱 on/off)·프로필 **'계정' 탭 세분화**(계정/알림/UI/사용 내역)·펼친 **'결과 보기' 스크롤 보존**(세로/가로)·**그래프 'AI 능동 분석 실행' 권한 분리**(신규 하위 권한 `metadata.graph.analyze` 를 조회 `metadata.graph.read` 에서 독립·LLM 호출/KB 갱신 특권 별도 통제·무권한 시 버튼 미표시, Critical §12.3·A안 최소권한 backfill 없음). feature-0002 — MySQL **시스템 변수(@@) 읽기 과차단 해소**·**첨부↔실DB grounding 모순·식별자 대소문자 false-missing 봉인**(+부분 미리보기 전수 단정 방지·LLM 요청-레벨 오류 분류)·**MSSQL cross-DB 메타데이터 발견**(검색·describe, admin)·**첨부 갱신 새 버전 전달**. 전건 POST-DEPLOY PB-0008/라이브. 코드 거주 0003·0002.
- feature-0020-zd-deploy-all(07-14, Major 신규): **무중단 배포 커버리지 완성** — deploy 스파인(bin/deploy-web.sh) 확장: 워커(insight/ask) 자동 롤아웃(`mysql-ai-agent:<sha>` build-once 핀·healthy+GIT_COMMIT 게이트·agent last-good 롤백 — 구 WARN-only divergence 대체)+bedrock-gateway 드리프트 시 **surge replica** 무중단 교체(profile deploy-surge·DNS alias·steady-state 비용 0)+caddy 이미지 드리프트 reconcile+alembic 직접호출 stale-image 가드(선행 재빌드)+워커 healthcheck timeout 30s(16h unhealthy 오탐 해소)+라이브 적용 미커밋 compose 튜닝 정식 커밋(live-truth). `make deploy-all`/`deploy-workers`/`ask-worker-*`·make up ask-worker 기동 공백 수정. 동반: feature-0014 TLS preflight cold-host 무메시지 사망·STATE dry-run 실기록·롤백 후 :current 태그 오염(M-2) 수정, RUNBOOK §9 stale 정정. §18.8 패널 MAJOR5/MINOR7 전건 반영. 코드 거주 0014(스파인)·0002(이미지)·0007(gateway).
- feature-0016 graph-pixi(07-13, §78~81 · MAJOR): 그래프 렌더 엔진 **AntV G6 v5(Canvas)→PixiJS v8(WebGL) 전면 교체**(엔진-중립 SceneAdapter seam·GPU 상주 씬·G6 폴백 토글) — 노드 다수 팬 버벅임 근본 해소(대형 409 객체 팬 60fps vsync 라이브 PASS)+미니맵 클램프/드래그·scene diff 풀(§79)·라벨 BitmapText(§80·렌더 157ms→0.03ms)·상세 hover 시각 효과(§81). 카테고리 밴드 **콘텐츠 단위 그룹핑 실동작화**(mig 0040·LLM 라벨·mutual-kNN)+p2(가짜 밴드 소멸·연관 밴드 인접). 코드 거주 0003(graph-core/renderer-pixi)·0002.
- 07-13 오후 기타: feature-0003 첨부 **사용자 재업로드 버전 관리**(해시 체인·assistant diff 인지)·작업화면 데이터소스 **연결 테스트 버튼**(ds-conn-test)·**그래프 뷰 권한 분리**(`metadata.graph.read` 를 kb.ingest.manual 묶음에서 독립, Critical) · feature-0002 **describe_routine 도구**+**sql_guard read-only shape 과차단 보정**(최상위 UNION·읽기전용 SHOW) · **feature-0019 메시지 편집 신규**(Phase 1 대화 내부 브랜치 트리 백엔드·마이그 0041, 엔드포인트/프론트 대기).
- feature-0016 graph-perf-uxpolish(07-09~10, §57.4~76, feature-local ADR-026~037): 그래프 뷰 **대규모 성능·정리 + 상세 내비·강조 안정화** — 스키마 펼침 세로폭주 해소(ADR-028)·컬럼/뷰포트 컬링+컬링 노드 참조/상호작용 보존(ADR-029/032/037)·배치정렬 위상서명 메모이즈(줌인 근본해소 882노드 7.4×, ADR-035)·미니맵 전체이미지 재사용(ADR-036); 극단 줌아웃 집계 카드는 §67 폐기(ADR-033) · 상세 패널 사용관계 read/write 분리·선택노드/관계행/미렌더 컬럼 카메라 이동·컬럼 선택(§68~72/§75)·중복 관계 병합(§69) · 상단 툴바 3존 통합+줌/상태 플로팅 오버레이 · 상대 하이라이트 즉시·정확 점등·유령 관계선 잔상 제거(§57.4~9·§64·§66)·엣지 중간버튼 팬(§62) · **AI 능동 분석 caveats 계약 재설계(§69/ADR-034)** — 형식적 자기-불평 제거·실위험만 짚기, **T69.5 완수(07-13 PR #744, cc_data_main 재생성 715/715·옛 자기-불평 사실상 0, 사용자 원 리포트 해소)**. 코드 거주 0003(admin.js)·0002(node_analysis/llm). PB-0008 라이브 육안 다수 잔여.
- feature-0003 ask-timeout-nonblocking(07-09): 작업 화면 응답 지연 시 **화면 전체 덮던 타임아웃 복구 모달(z-9999) 제거 → 모달·토스트 없는 조용한 자동 재연결**. 취소/즉시답변 컴포저 인라인 버튼 상시(TASK-0157/0158). §18.8 R1 MAJOR(H1 신규대화 early-cid in-flight 키 이관) 동반수정. 배포 4b6919ec·POST-DEPLOY PB-0008 win-browser PASS.
- feature-0003/0018 reasoning-budget-per-model(07-09): 관리 콘솔 **모델별 추론 예산 상한 native 확대**(Sonnet 128K·Haiku 64K max_tokens)+추론 강도별 예산 모델별 분리+추론↔본문 비율 슬라이더(feature-0018 코드 거주 0003, `WebRuntimeSettings`).
- feature-0002 mssql-auth-cooldown(07-10, §12.3, 마이그 0): insight-worker 순회에서 **MSSQL 로그인 실패(18456) 조기 skip + datasource cooldown**(기본 600s, `AGENT_INSIGHT_AUTH_COOLDOWN_SEC`) — 반복 재연결·로그·I/O 억제(복구권위=TTL 만료). DB/객체별 권한거부(916/229/297)는 로그인 성공이라 순회 계속. 코드 거주 0002(insight.py·shared/config.py).
- feature-0012 완결(07-12, behavior-neutral): app.py 모놀리스 → **21 도메인 APIRouter 로 148 route 핸들러 전량 byte-동치 추출**(app.py ~29K→18,917줄) + ITEM-10(web_context 헬퍼 추출·모듈분리 -81%)+ITEM-11(잔여 인라인 authn DEFER 핸들러 실leak 13 DI-rework·keep-inline 정당 37) 완료(라이브 4c203f9c). 브라우저 로그인 QA 사용자 사인오프 이관. 잔여 ITEM-09(그래프 CSS/JS·외부 브랜치 blocked).
- feature-0016 product-classify-suggest(07-08, §59/ADR-025, 마이그 0): **분석 기반 제품 분류 'AI 제안→사람 승인' 파이프라인** — 카테고리 밴드의 스키마→제품 매핑을 이름 규칙에서 분석 신호(테이블 구성·node_analysis 요약)로 개선하되, 매핑 테이블(WebProductDatabases)이 **에이전트 접근 allowlist 겸용**이라 LLM 산출은 Pending(RuleId NULL·Reason 'ai_suggest:<conf>') 적재까지만·사람이 제품 관리에서 승인(Source='ai')/거부. 환각 차단 3중 게이트(스키마 실재·연결 제품 화이트리스트·MIN_CONF 0.6)·데몬 기본 OFF(AGENT_PRODUCT_CLASSIFY_AUTO). POST-DEPLOY 라이브 실증(dry-run 11→10건 Pending 적재·근거 문자열 동봉, 예: PayShopPurchase/InAppBilling→로그 DB 0.95). 코드 거주 0002(product_classify/llm)·0003(admin_products/admin UI·제품 관리 "✨ AI 분류 제안" 승인 UI).
- feature-0016 graph-edge-visibility(07-08, §57/ADR-024, 마이그 0): 그래프 뷰 **접힘 카드 연결선**(SCHEMA_REF 질의시점 1-hop 스키마-쌍 집계·cap 400·렌더 3단 승격 컬럼→테이블→SC 카드, 양쪽 접힘일 때만)·**상대 하이라이트**(선택 1-hop 선명·비인접 dim, rebuild-bake §33)·크로스-DB ROUTINE_USES **마젠타** 색 구분(직교 인코딩)·**중간 줌 LOD**(줌<0.35 ∧ 엣지>120 무상태 FK 축약·의미 신호 보존). POST-DEPLOY PB-0008 라이브 PASS(SCHEMA_REF count 라벨·상대 dim 358/361·마젠타 51·LOD dropped 520). + §58 스키마 골격 MSSQL 라벨 `normalize_db_label` 정합+AccountDB rekey·gateway mem 2g 영속화. 코드 거주 0002·0003.
- feature-0002/0003 kb-adoption-inbox + console-IA(07-07): 대화 자율수집을 통합 **채택 인박스**로 재구성 — 용어사전(0021/0023) 대칭 **ENUM 코드사전 자율수집**(`_enum_autopropose`·threshold≥0.9·`kb.enum.curate`·alembic 0039 비파괴)+신뢰도/상태별 채택·거부·되돌리기 UI+pending 배지. **콘솔 IA 통합**(채택 인박스·샘플 검수 최상위 탭→각 사전 하위 {목록|검토 큐} 2차 보기·5서브뷰 디자인 폴리시, UI 단독·RBAC 0). **추론 강도별 예산**(`reasoning_budget` low/high/max·'일반'=no-override)+런타임 pane UI 재설계 동반. 코드 거주 0002/0003.
- feature-0016 graph-sync-crossdb §56(07-07, ADR-022~023, 마이그 0): 루틴(함수/프로시저) sync 견고화 — SAVEPOINT 행 격리(실패 행만 롤백)·워터마크 전진 게이트·**크로스-DB ROUTINE_USES 참조 채택**(qualifier 4규칙+external_tables 실재검증)·thin '연결 정보 없음' 동치·backfill/MSSQL store label lower 정규화+케이스변형 행 멱등 회수(RC1~5). POST-DEPLOY e2e(errors 0·step_failures 0·크로스 ROUTINE_USES 1,041·case_purged 6,320·워터마크 전진)+§55 화살표 데이터흐름 정합·능동분석 지침 플로팅 툴팁. 코드 거주 0002/0003.
- feature-0018 runtime-settings(07-07, Major, 코드 거주 feature-0003/0002): 관리 콘솔 **시스템>설정 런타임 설정** — 실행 타임아웃(AGENT_TIMEOUT_SEC·MCP_TIMEOUT_SEC live)·모델별 추론(thinking) 예산 조정·저장·live/restart 하이브리드 반영. `WebRuntimeSettings` + `routers/admin_settings.py`(GET/PUT/DELETE, `system.runtime.read/write` RBAC+audit, autocommit=False 원자화) + `shared/runtime_settings.py` 레지스트리/resolver(env-fallback 으로 배포 .env 존중, override 없으면 byte-동치). §18.8 3렌즈 SHIP(env-fallback BLOCKING 수정)·신규 테스트 41. **배포·PB-0008 PASS**(audit action 등록 write-path hotfix 포함).
- feature-0003 reasoning-effort(07-06, Major, 코드 거주 feature-0003/0002): 대화 화면 composer '+' 메뉴 **사용자 지정 추론 강도**(낮음/일반/높음/매우 높음)·대화별 영구 저장(KV)·비-thinking 모델 비활성. `/api/ask` 가 명시 레벨을 요청 단위 `extra_body.thinking.budget_tokens` override('일반'=no-override=alias 기본, sonnet 강등 회귀 방지), `litellm_config` 무변경. **배포·PB-0008 PASS**.
- feature-0009 share-visibility-window(07-04, Critical, SECURITY §21/§21.4): "여기부터/여기까지" **멤버 가시성 window 공유** — 공유가 `[from,to]` window 만 노출, 가려진 구간을 참여자 라이브 뷰·LLM recall·fork 전부에서 물리 배제(프롬프트 인젝션으로도 추출 불가). `conversation_members` window 컬럼 + `has_restricted_members` 게이트(alembic 0037, additive) + recall/display 양 loader fail-closed 필터 + fork 3-store 교집합 clip + join stamp never-widen. `has_restricted_members=false` 대화는 필터 완전 우회(무회귀). 말풍선 ☰ 메뉴 통합 동반(코드 거주 feature-0003). owner-answer 표시태그(display-단)·PB-0008 라이브 시각검증 잔여.
- feature-0003 ds-avg-latency + aiops-ttft(07-03): 데이터소스 상세 '연결 상태'에 **평균 연결 응답 시간**(conn_health 성공 probe 이동평균 `AGENT_CONN_AVG_WINDOW`=20 재사용·추가 probe 0·마이그 0, PB-0008 PASS) + AI 운영 현황 **지연 KPI 재정의**(p50/p95 를 호출 왕복 `latency_ms`→라운드 사이 간격 `step_gap_ms`, alembic 0033, cross-cut 0002 계측; 잔여 배포 후 PB-0008+트래픽 축적).
- feature-0009 gc-join-notice(07-03): 공유 링크 참여 시 대화 내 '참여 알림' pill + 기존 멤버 unread +1(가입자 제외) — core_messages 이벤트 행 + 표시 store system pill 이중기록, `__event__` sentinel 로 LLM 히스토리 배제(assistant 오응답 방지), 익명 공유뷰 event/username 비노출. 마이그 0·인가 경계 불변.
- feature-0002/0007 insight 안정성·부하 분산·용도별 라우팅(07-03~04): 동일구조 샤드 그룹화(대표 1회 LLM+형제 LLM-free 전파·flag rollback)·긴 cycle healthcheck false-negative 해소(heartbeat throttle)·부하 분산(probe 격리·circuit-open scan skip·batched graph sync, TASK-0308)·요청레벨 LLM fallback(claude-corp→root Max→edge gemma, litellm 3-deployment)·용도별 라우팅 분리(대화·능동분석=항상 claude / 배치 insight=시각 기반, refresh/keepalive cron 24/7, ADR-002 feature-local). (07-07) 대화 답변 경로 edge(gemma) 폴백 완전 차단(맥락 손실 해소, conv-audit FR-edge-fallback). config-only·인가 경계 불변.
- feature-0016 graph-category-recursive-refine(07-07, §55/ADR-021/alembic 0038): ① 그래프 뷰 **제품 카테고리 밴드**(WebProductDatabases 질의시점 합성 schema_products → CAT/CATH/CATX 계층, 밴드별 shelf-pack·헤더 드래그·접기·미분류 후미) ② 관계 추론 **xschema**(같은 DS 다른 DB, 기본 ON)+MSSQL 3-part 프로브(dbo 가드)+관계 수동 큐레이션 API/UI(trust/break)+XDS flip ③ DB 단위 분석 **재귀 전개**(시드 depth0·per-seed anchor_key·예산×12)+refine-not-override(previous_analysis 전역)+**back-refine**(thin 재-pending pass_no+1, cap30)+suggested_links candidate ④ 시그니처 백필 정체(3% 고착) 근본수정(미처리 우선). §18.8 패널 BLOCKING2·MAJOR4·MINOR3 수정(REV-20260707T100744). **배포·PB-0008 완료**(0bd73169: 밴드 6종 라이브·큐레이션 왕복·재귀+back-refine 8/8 실증·백필 497→1,175 가동). 코드 거주 0002·0003.
- feature-0016 graph-navfilter-routine(07-06): 그래프 뷰 개선 5건 — 상세 패널 뒤로/앞으로(view-typed: 노드·클러스터·관계), 노드 종류 필터(🔗/ƒ/⚙ 토글=빌드 제외 재배치·영속·스키마 그래프 전용), 검색 변경/클리어 시 구성 보존(additive overlay·pristine 회수·base 복원), DB 단위 분석에 Routine 시드 포함(schema_routine_keys·테이블 우선 cap·payload params), 루틴 파라미터 수직 배치(XR:/RP: 서브노드+상세 패널). §18.8 패널 MAJOR 3 수정. **배포·PB-0008 완료**(07-06: 842b9ecf, AC-1~5 전 항목 라이브 PASS — Routine 잡 7 done·⚙ 마커·분석문, products 검색→클리어 복귀, 엣지 토글 배치 불변, pageerror 0). 코드 거주 0002·0003.
- feature-0016 routine-dbanalysis(07-06, resume 완결): ① 함수·프로시저 노드 **전 datasource 가시화** — 원인=ds 커버리지(20중 4 적재, cadence 전파 중) → `bin/routine-backfill.sh` 결정론 backfill(레지스트리 MEMORY_DB·prune-safety: backfill 복수 스키마 prune=False + worker `prune=(label==schema)`) ② **DB(스키마) 단위 AI 능동 분석** — enqueue_schema_analysis(root=Schema·depth1 시드·재귀0=budget 캡·only_missing·cap 200/500·dry_run→confirm→진행 패널). §18.8 패널 BLOCKING 1·MAJOR 2 등 반영, 테스트 16+57 PASS. **배포·검증 완료**(07-06: d1951b7e 롤링+worker 재빌드, 라이브 backfill 2,201/4 ds — 0행 스키마 3곳 회수, PB-0008 PASS — ƒ/⚙ 198 렌더·e2e done 2/2·재귀 0·reused/noop·pageerror 0). 코드 거주 0002·0003.
- feature-0016 graph-funcproc-uxfix(07-03): **함수·프로시저 노드**(routine_objects SSOT — INFORMATION_SCHEMA introspect + 정의 파싱 참조 read/write, AGE `Routine`/`HAS_ROUTINE`/`ROUTINE_USES`, alembic 0034, ƒ/⚙ 보라 칩·보라 잔점선) + 미니맵 패널 리사이즈 추종 수정(G6 inline left/top→CSS 앵커) + AI 능동 분석 **참조 컬럼의 부모 테이블 same-depth 승격**(ADR-017 — "테이블까진 분석", 앵커 게이팅으로 심화 억제)·'재분석' 버튼 제거·**hover 지침 popover**(runs.user_prompt → 앵커 토큰 + LLM user_intent 자율 반영). 코드 거주 feature-0002·0003. ADR-016·017.
- feature-0016 graph-reltrace(07-03): 그래프 뷰 **접힌 상태 관계 표시 + 관계 클릭 추적 + AI 능동 분석 연동**. ① 테이블 더블클릭 전까지 관계 미표시 이슈 해소(백엔드 schema_tables 가 스키마 내 REFERENCES 반환 + 프론트가 끝점을 컬럼 미렌더 시 소속 테이블로 승격·dedupe → 접힌 테이블 간 관계 렌더) ② 상세 패널 관계 클릭 → 대상 테이블·컬럼 추적(`_metaGraphTraceRelation` 이웃로드·컬럼전개·카메라 focus) ③ AI 능동 분석 결과에도 추적 가능 관계 행. 비파괴·마이그 0. 코드 거주 feature-0002·0003.
- feature-0016 그래프 뷰 07-02 후속 진화(merged; PB-0008 라이브 PASS=ctxmenu·초기뷰·검색 badge, G6 v5 렌더러 교체·masonry·카메라 팬·rel-selfheal 은 배포/PB-0008 라이브 잔여): 렌더링 엔진 **Cytoscape(WebGL)→AntV G6 v5(Canvas) 전면 교체**(ADR-004 — 07-01 WebGL 전환을 대체하는 대형 기반 교체, 프레임레이트·외곽선)+클러스터 다열 masonry/shelf-packing·**노드/스키마카드 우클릭 상세 상호작용**(kind별 컨텍스트 메뉴 — 상세·관계 상세·확장 1~3hop·중심 보기·컬럼 펼침·AI 능동 분석·FQN 복사 + 관계 상세 패널 방향/신뢰·추정 w/근거/조인컬럼 + 중심 보기 지속 칩)·**초기 진입 가시성**(스키마-우선+줌클램프+미니맵)·검색 스키마 카드 badge 매칭·노드 펼침 논블로킹(ADR-005)·프리즈 잔존 해소(per-node→setData rebuild, ADR-006)·더블클릭 카메라 앵커-중심 애니 팬(ADR-008)·그래프 분석 전용 모델 claude-haiku 분리. ADR-004~008 feature-local.
- feature-0016 rel-selfheal(07-02): 신뢰/추정 관계 자기교정 파이프라인 **미가동 근본수정** — config `__all__` 누락 NameError 가 insight 스키마 처리 전체를 3일 조용히 정지시킨 결함(D1b) 포함 4중 원인 해소(주기 cadence 6h·스키마-slot 규약 MSSQL=DB명·파서 qualifier·uniqueid PK·AGE 앵커링). §18.8 적대 패널 backend FAIL(MAJOR 6) 전량 수정(instance-scan 커서 DB별 분리·write-back 스키마 한정·PK≡PK 차단·프로브 오류 negative). 단위 61 PASS. 배포+데이터 정정(2행 정규화·고아 3노드 회수·재sync) 동반. ADR-007.
- feature-0003 AI 운영 관제 패널 신규(07-02, merged): `/api/admin/ai-ops` — **AI 운영 현황(관제) admin 화면 + LLM 계측 확장**(main agent latency·최근 활동 cursor 페이징 '더 보기'·pane 세로 스크롤). 감사 화면 정리(카테고리 순서 재구성·항목 툴팁·최근활동 클릭 상세 확장, audit-nav-ux). 최근활동 시스템 sentinel 대화 링크 깨짐 수정(aiops-conv-link-fix). 메타데이터(지식베이스) 권한 종속관계 그룹 게이트 계층화(metadata-perm-hier, SECURITY §19). "+" 메뉴 첨부 개수 배지 대화 전환 후 stale 수정(attach-count-scope). 권한 `console.aiops.read`(admin 전용). 코드 거주 feature-0003.
- feature-0016 그래프 뷰 07-01 대규모 진화(merged): 렌더러 canvas-2D→**WebGL** 전환(대규모 스키마 프레임레이트 근본 대응·외곽선 선명화 — 07-02 AntV G6 v5(Canvas) 로 대체됨, ADR-004)·**AI 능동 분석 앵커-상대 관련도 게이팅**(ADR-003, alembic 0029)+실시간 진행 패널(우측·세션독립 재개)·**ERD 컬럼 ordinal 세로배치**(alembic 0027, compound 박스 내 겹침 차단)·단일클릭 컬럼 펼침/접힘·상세 패널 드래그 리사이즈·노드 드래그 엣지 유지·클러스터명 WebGL 오버레이 정합. **메타데이터 탭 권한 5분할**(B안, feature-0003 인가, Critical/PLAN-APPROVED): `kb.ingest.manual`→`metadata.{glossary,enum,table,column}.manage`+`graph.read`(비파괴·우산 함의·마이그 0, SECURITY §19). 코드 거주 feature-0002(엔진)·0003(admin UI). 잔여 PB-0008 T5.4·eval A/B T5.1.
- feature-0012 P5b 전체추출 완료(07-01, merged): app.py 148 route 핸들러 전량 21개 도메인 APIRouter byte-동치 추출(app.py ~29K→18,917줄, 잔여 @app 0, helpers+DI seam+include_router). batch1-3 라이브 배포·검증(main c031b5d, 응답 byte-동치). 잔여 web_context 헬퍼 추출·프론트(admin.js/app.js) 분할·Final QA·batch4 재배포.
- feature-0016 implicit-edges(07-01, merged, cycle `ai/claude/feature-0016-implicit-edges`): 그래프 뷰의 **FK 미선언 암묵 JOIN 관계**를 명명 규칙으로 추론(source='inferred', candidate)하고, 정적 confidence 와 분리된 **동적 weight** 를 (a)성공한 대화 JOIN 사용(관찰)+(b)insight 워커 실데이터 겹침(EXISTS) 프로브(능동)로 강화/감쇠하는 자기교정 엔진. 양성 점근 상승·음성 고정 감산(전 구간 비대칭 보장)→ weight≤0.15 broken(주입·그래프 삭제), ≥0.85+양성누적 trusted, FK 권위적 불변. alembic 0026 비파괴 ADD. 노출: AI context digest broken 제외+weight 정렬+추정/신뢰 태그, AGE REFERENCES weight/status 투영·broken 회수, admin UI 신뢰=실선/추정=점선/파단=숨김+범례+배지. §18.8 적대 패널 1+4+3 findings 전건 수정(REV-20260701-0002). 단위 38 PASS·verify-completion PASS. 라이브 e2e 는 cutover 된 AGE 스택(T6.10/PB-0008). 코드 거주 feature-0002·0003.
- feature-0016 메타데이터 지식그래프 신규(06-30, 대형, PR#477/#479/#480/#482/#484): 관리콘솔 메타데이터를 Apache AGE(openCypher) `metadata_kb` 그래프(관계형 SSOT 의 재생성 가능 투영)로 승급 — 그래프 뷰(Cytoscape·fcose 클러스터링·관련도 사이징·이웃 60x 인덱스)·검색·`graph_navigate` AI 도구(8K 테이블 컨텍스트 초과 해소). 커스텀 PG16 이미지(pgvector+pg_trgm+AGE, alembic 0025) cutover 라이브 완료(데이터 무손상·무중단 롤링). per-datasource 투영(~21 ds/8,122 테이블). 엣지 희소(게임 DB FK 미선언) — 노드 그래프·그룹핑 즉시 가치. cross-cut 0002(코어·tool)·0003(admin UI). ※번호 0016 은 metadata-graph + zd-pg-pause-caddy 2 슬라이스 공유(사람 결정 보류).
- web 무중단 배포·운영 위생군 라이브 완료(06-30, feature-0014/0015/0016-zd/0017): web 무중단 롤링 배포(Caddy LB web-a/web-b·자동 롤백·:18080 폐기→:443 단일, 0014) + insight-worker graceful 종료·MySQL online-DDL 게이트·백업 복원 리허설 cron(0015) + PG 재시작 무중단화 pgbouncer PAUSE 래퍼(0016-zd, RW 무에러 실증) + 배포 빌드게이트 snap-docker metadata-race false-failure 수정(0017, 전 web 배포 차단 해소).
- 메타데이터 관리 화면 정리(feature-0003, 06-30, frontend-only): 스키마 설명 패널 list-detail 2단 재구성·데이터소스 선택 단일화(헤더 스코프 상속)·테이블 설명 인라인 입력(평면화·정렬)·스키마 가져오기 시 기존 설명 prefill+변경분만 저장. 질문 의도 이해·끈기 개선(feature-0002, 능동 해석 지침 1:1·그룹 주입+식별자 대소문자 보존).
- feature-0013 관계 다이어그램 신규(06-29, PR#462/#469): assistant 가 flow/관계/구조 질문에 mermaid(ER·flowchart)로 사용자 DB 구조를 답함 — 웹 UI mermaid 렌더(vendor v10.9.3, sanitize-후 `securityLevel:'strict'` + graceful fallback), `_MERMAID_DIAGRAM_GUIDANCE` 발화(feature-0002), `table_relationships` 관계 저장소(alembic 0024 — FK introspection·대화 JOIN 학습) + knowledge digest 주입. 후속 mermaid render orphan/erDiagram "Syntax error" 봉인(rd-h2/h3), PB-0008 라이브 검증 PASS·배포 완료(rd-h6). cross-cut 코드 거주 feature-0002·0003.
- 메타데이터·공유 대화 화면 정리(feature-0003, 06-29 저녁): 스키마 골격 결과 패널 페이지네이션(`META_BS_PAGE_SIZE=30`, 가시성 윈도우 — 저장/AI일괄 전체수집 불변식 유지)·여백 압축(metadata-bs-paging), flex-shrink 클리핑 수정(metadata-bs-flexclip). 공유 대화 뷰 mermaid 렌더 + 전체폭 반응형(share-mermaid-responsive) + 스크롤바 가이드 뱃지·클릭 스크롤 단축 EaseOutExpo(point-scroll-easeoutexpo). 용어사전 역할 선택 UI 단일화 + 역할 미표시(필드명 role_key→key) 수정(glossary-role-single-ui/fieldname-fix). 새 대화 첫 전송 사이드바 중복 제거(new-conv-dedup). diff 답변 누출 줄번호 prefix 정규화(diff-lineno-leak).
- 메타데이터 부트스트랩 결과 패널 접기+검색 재설계 + 잘림 cache-buster 수정(feature-0003, 06-29): 직전 mssql-db 수정의 460px 캡 제거가 배포는 됐으나 `admin.html` 의 `styles.css?v=`/`admin.js?v=` cache-buster 미bump 로 브라우저가 stale CSS 를 계속 로드 → "여전히 잘림". cache-buster bump(→20260629-metadata-bs-collapse)로 해소. 또 수백 테이블을 전부 펼쳐 평면 렌더하던 여백 과다를 **테이블별 기본 접힘 한 줄 헤더(caret+이름+입력상태 힌트) + 테이블명 검색/필터 + 모두펼치기/접기** 로 재설계(사용자 결정). 접기·필터는 시각 토글만 — 입력값 DOM 보존, 저장·AI 일괄생성이 전체 수집(회귀 0). 서브탭 전환 재렌더 mode 불일치(pre-existing)는 follow-up. metadata-bs-collapse.
- 메타데이터 부트스트랩 MSSQL database 차원 수정(feature-0003, 06-29): "테이블 설명 > 스키마 골격 가져오기"가 MSSQL 에서 `database=None`→중립 tempdb(shared/db.py `_connect_mssql` 보안 기본값) 임시테이블(`#…`)을 노출하던 "테이블 명칭 모두 오류" 근본 수정. 부트스트랩 unit 엔진분기(MySQL=schema/MSSQL=database via `list_server_databases`, 시스템 DB·스키마·센티넬 필터), MSSQL 선택 DB 연결 평탄수집(저장 schema_name=database — 4계층→3-키, 사용자 결정), suggest grounding 동일분기, 프론트 '데이터베이스/스키마' 라벨, `.admin-meta-bootstrap-result` max-height 제거(패널 내부 잘림 해소, 테이블·컬럼 공통). AI 자동완성은 기구현·깨진 골격에 grounding 해 무력화됐던 것. REQ-20260629T114221.
- 용어사전 대화 자율등록+역할분리+유사어(feature-0002/0003, 06-29): 대화에서 용어 자율 등록 + 등록·검토 역할 분리 + 유사어 참조(관리 콘솔 메타데이터 거버넌스, 마이그 0023, ADR-20260629T101500). 용어 검토 큐는 용어사전 하위 2차 보기 탭으로 중첩(admin IA).
- 답변 피드백 답변당 고유화(feature-0003/0002, 06-29): 답변당 사용자별 고유 👍/👎 1개 강제(마이그 0021/0022 + 고유성 키 id_space 보강) — 새로고침·대화 전환 후 중복 부여 차단.
- 첨부 wrong-bubble 엣지 하드닝(feature-0003, 06-29): 첨부 영속 레이어 매칭 키에 `message_id_space` 추가(ec39a60). 정상 display 경로 동작 동일 — fork/마이그 cross-space 엣지 하드닝.
- feature-0012 web-router-modularization 토대(06-29, PR#456): feature-0003 `app.py` 도메인별 `APIRouter` 점진 분할 토대 — route-parity 안전망 + 의존성 audit(P5b, behavior-neutral).

## Recent Changes
- doc_sync 08-19(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): `74316049`(08-17 sync, META-0059) 이후 **신규 머지 0건** — HEAD 가 그 sync 커밋 자체라 delta 전량이 prior-window drift. reconcile-first **파리티 갭 0**(서빙 `release-notes-data.js` = 브랜치 blob byte-identical 257,063B · `/healthz` 200)이라 배포 수리 대상 아님. wiki feature 카운트 **42 유지**(참조 8건 전건 이미 42 · 문면 변경 0). 갱신: `Architecture/Overview.md` §2.3 **행 분리**(0039↔0040 `||`) + §2.4 feature-0008 의존 행 신설 · `overview.md` §2.1 **행 분리 + feature-0042 행 신설**(렌더 40→42) + §5 다음 단계 2건 · `concepts/nl2sql-flywheel.md` 2건 · `Features/feature-0040-*.md` frontmatter · `docs/ARCHITECTURE.md` §4 · `docs/DOC_REGISTRY.md` secret 건수 · `docs/SECURITY.md` 첨부 읽기 경로 · 본 hot 3구획 prepend · Log append. 동반 operational(feature-0003 별도 commit): 릴리즈노트 08-14 블록 17→18항목(+summary 정합, `generated` 불변). **noChange**: STATUS(`gen-status --check` rc=0 · worktree note 실측 일치) · DECISIONS · CONVENTIONS · `Features/_Index.md` · 카운트 8참조. **detect-and-report(이월 29건)**: 적대검증 미도달 finding — `Decisions/_Index.md` ADR 색인·건수 · `Index.md` ADR 범위 · `Glossary/_Index.md` 건수 · `README.md` sources · `PROJECT.md` compose 서비스·배포 정책·런타임 · `Features/feature-0040-*.md` 상태 문면·self-link·placeholder heading · `_Index.md` 0042 상태 · `overview.md` §2.4 흐름·§4 한계 참조 · `hot.md` 500자 규약 · `DOC_REGISTRY.md` stale 목록.
- doc_sync 08-17(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): `6d4fdd87`(08-14 sync, META-0058) 이후 델타 **57 커밋(non-merge)** 정합 — git-date 는 08-14 가 대부분이고 `a639272c`·`65c34fff` 2건만 08-16. **신규 feature 1개**(feature-0042 분석 요청 경제성 — 조사·검증 cycle·코드 0건)라 wiki feature 카운트를 **41→42** 로 전진(ground-truth 실측 `ls -d unit/feature-*` = 42 = `ls wiki/Features/feature-*.md` = `_Index.md` 표 42행 · 갱신 **8참조/5파일**: `Index.md` 요약·MOC / `Features/_Index.md` 헤더·개요 / `Architecture/Overview.md` 헤더 / `Architecture/Module-Map.md` MOC 포인터 / `overview.md` §1 서두·§2.2 ADR 서두 — `hot.md`·`Log.md` 의 41 은 과거 서술이라 **소급 수정 금지**). 이번 창 wiki self-add 는 **1건뿐**(`63808675` → `Features/feature-0042-analysis-dedup.md` 카드 76행)이라 색인·서사 전량이 doc_sync 몫이고, **POST-DEPLOY 이월 0**(라이브 6종 전부 `:6c7413cf` = HEAD = origin/main · `/healthz` 200 · 서빙 릴리즈노트 sha 정확 일치 → 델타 57 커밋 전건 라이브). 갱신: `Features/_Index.md` **feature-0042 행 신설 + 5행 backfill**(0002·0003·0020·0022·0041) · 카드 6종(`feature-0003` §2.4 신규 공유 정본 모듈 1 + §3 특징 8 bullet · `feature-0002` §3 특징 5 bullet · `feature-0041` §2 상태·§3 책임 경계(답변 보존 경로 신설)·§6·§8 · `feature-0020`·`feature-0022` §2/§4/§5/§7 · `feature-0042` §6(후속 `47b5c891` 의 안전망 확정 반영)·§7 — self-add 카드 본문은 재작성 안 함) · `overview.md` §1 **(67)** + §2.2 서두 · `Architecture/Overview.md` §2.3 **feature-0042 행 신설 + 5행 delta** · §2.4 **feature-0042 의존 행 신설**(관측·판정 전용, 코드 의존 0) + 0020·0022 확장 · `Index.md` 2참조 · 본 hot 3구획 prepend · Log append. **detect-and-report**: `Architecture/Overview.md` §2.3 의 feature-0039↔0040 행이 한 줄에 `||` 로 붙어 **표가 깨져 있다**(선재 drift·행 분리 필요) · `unit/feature-0003-agent-web-ui/docs/TASK.md` 동일 heading 중복 2쌍(병렬 세션 머지 잔재·정본 내부 모순이라 report-only) · feature-0042 는 **slug 와 산출물이 어긋난다**(기각된 1순위 후보 유래 — 실제 범위 정본은 FUNCTION.md §1).
- doc_sync 08-14(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): `807fce6f`(08-13 sync, META-0057) 이후 델타 **27 커밋(non-merge)/머지 23건** 정합 — git-date 전부 2026-08-13. **신규 feature 0개**라 wiki feature 카운트 **41 유지**(오케스트레이터 ground-truth 실측 · `ls -d unit/feature-*` = 41 · 카운트 참조 전건 이미 41 → 문면 변경 0). 이번 창 wiki self-add 는 **1건뿐**(`89b7cd54` → `Features/feature-0041-*.md` §7 접속 방법 + §8 이력)이라 나머지는 전부 doc_sync backfill이고, **POST-DEPLOY 이월이 0**(모든 cycle 이 같은 날 배포·실측·종결 — 실측 기록 전용 커밋 9건). 갱신: `Features/feature-0003-agent-web-ui.md` §2.4 신규 static 자산 1 bullet + §3 특징 6 bullet(그래프 펼침 성능 귀속 · rail 비동기 재배치 · 첨부 이름순+원문 화면 비교 · 사용량 차트 지표 선택기+캐시 계측 · 폴더 DnD+멤버 게이트 자기 정정 · 공유 첨부 LLM 경계 **Critical**) · `Features/feature-0016-metadata-graph.md` §2 (2026-08-13) 델타 bullet + 마지막 갱신 08-13 + §7 이력 · `Features/feature-0041-external-ai-tool-surface.md` §2 상태(3차 출하 후속·인증 접근성 반영·잔여 정정) + §7 접속 방법(URL 방식·discovery·`/ai/connect`) + §8 이력 · `Features/_Index.md` 3행 · `overview.md` §1 (66) · `Architecture/Overview.md` §2.3 2행(0016·0041) + §2.4 `feature-0041` 의존에 **feature-0006 신규 편입**(엣지 익명 401 게이트가 Caddyfile 에 집행되면서 생긴 실제 의존) · `hot.md` 3절 각 1 bullet prepend · `Log.md` append. report-only: `Architecture/Overview.md` §2.3 feature-0003 행이 1줄 요지로 잔존(이번 창 발생 drift 아님) · feature-0016 카드 §2 의 07-14~07-29 미backfill 잔존 · feature-0041 잔여 2건(사람 1회 인가 e2e · P1 도구 판단) · feature-0016 미측정 2건(트윈 중간 프레임 시각 캡처 · 이탈-추종 라이브 재현) · group-attach bounded 축소 라이브 미실측 · member-gates D-1~D-3 사용자 결정 대기.
- doc_sync 08-13(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): `27407779`(08-12 sync, META-0056) 이후 델타 **26 커밋(non-merge)/머지 22건** 정합 — git-date 전부 2026-08-12(`1f9a5943` 만 08-11). **신규 feature 2개**(feature-0041 외부 AI 도구 표면·Critical / feature-0040 역할 기반 DB 객체 탐색·Major)라 wiki feature 카운트를 **39→41** 로 8참조/5파일 일괄 전진(`overview.md` §1 서두·§2.2 / `Features/_Index.md` 헤더·개요 / `Index.md` 요약·MOC / `Architecture/Overview.md` 헤더 / `Architecture/Module-Map.md` MOC 포인터 / `Module-Map.md` 포인터). 이번 창 wiki self-add 는 2건뿐 — `Features/feature-0041-*.md`(380e056f, 계획 시점 stub) · `Features/feature-0040-*.md`+`_Index.md`+`Log.md`(ed5d4bb8) — 이고 `_Index.md` self-add 는 **MOC 표가 아니라 `## 분류` 뒤에 3열로 붙어** 있어 5열 표로 이관했다. 나머지(overview §1 (65)·§2.1 · Architecture §2.3/§2.4 · Module-Map · Index · hot · Log)는 전량 doc_sync backfill.
- doc_sync 08-12(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): `00431585`(08-07 sync, META-0055) 이후 델타 **44 커밋(non-merge)/머지 33건** 정합 — 실작업일은 08-07 후반 25건 + 08-11 19건이고 **08-08~08-10 은 커밋 0**. **신규 feature 0개**라 wiki feature 카운트 **39 유지**(8참조/5파일 ground-truth 재대조 PASS · `ls -d unit/feature-*` = 39 · `ls wiki/Features/feature-*.md` = 39). **델타가 `wiki/` 를 한 줄도 건드리지 않아(self-add 0) 이번 창 wiki 는 전부 doc_sync backfill** — `Features/feature-0003-agent-web-ui.md` §3 5 bullet(diff 성숙 5 cycle · 원문 보기+액션 열 정렬 · 체인 병합/접미 규칙/날짜 compact · 가이드 툴팁 · 익명 API 축소 **Critical**) · `Features/feature-0002-agent-core.md` §3 2 bullet(발췌 앵커링 + 라이브 후속 2 cycle · 자가 검증 탈출구) · `Features/feature-0007-bedrock-llm-provider.md` §3 2 bullet(소진 게이트 + 하드닝 · 토큰 자동 회전 **Critical**) · `Features/feature-0014-zero-downtime-deploy.md` §7 이력 1건(엣지 롤링 게이트 + POST-DEPLOY 503=0) · `Features/feature-0006-lan-proxy-access.md` §3 2 bullet(엣지 보안 헤더 · 롤링 결합 계약 — `Caddyfile` 거주지) · `Features/feature-0009-group-conversation.md` §3 1 bullet(가이드 툴팁 = 기능 정본 0009 · FE 거주 0003) + `overview.md` §1 **(64) 2026-08-06~08-11**(직전 run 이 08-06 창을 카드·hot·Log 에만 반영하고 타임라인에는 남기지 않아 생긴 prior-window 공백을 ⓕ 로 함께 흡수) + `hot.md`/`Log.md`. **noChange**: 카운트 8참조 · `Features/_Index.md` MOC 표 · `overview.md` §2.1 표 · `Architecture/Overview.md` §2.3 기능맵/§2.4 의존성(신규 feature 0 · 행 body 는 한 줄 라벨 규약) · `wiki/Decisions/*`(창 내 신규 ADR 0건 — 프로젝트·feature-local 양쪽 `git diff` 0). `docs/*` 축·릴리즈노트는 별 축 소관, `index.html`/`admin.html` 캐시버스터 무편집(소스 `?v=dev` 고정 + 빌드 content-hash 자동주입, ITEM-09). (doc-sync-20260812-010301)
- doc_sync 08-07(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): a072729c(08-06 sync, META-0054) 이후 델타 23 커밋(non-merge)/머지 PR 20건·git-date 전부 08-06(36c3f0da 만 08-05 19:26) 정합 — **신규 feature 0개**라 wiki feature 카운트 **39 유지**(8참조/5파일 ground-truth 재대조 PASS: overview §1 서두·§2.2 / Features/_Index 헤더·개요 / Index 요약·MOC / Architecture/Overview 헤더 / Module-Map MOC). 갱신은 전부 **feature 카드 body backfill** — `feature-0003-agent-web-ui` §2.4 신규 static 모듈 2종 + §3 특징 5건(attach-manage Critical · attach-version-diff 체인 · attach-multi-upload · share-sender-nickname · 배경 dismiss primitive) · `feature-0002-agent-core` §3 특징 3건(update_attachment 도구 · read_attachment 완전성 계약 · 계측 도달 규약/라벨 현재-유효성) + hot 갱신 + Log append. `Architecture/Overview.md` §2.3 은 feature-0002/0003 행 body 가 한 줄 라벨이고 §2.4 deps 서술도 08-06 델타로 의미 변동 없음 → **noChange**. docs/STATUS 인덱스 행·docs/ROUTEMAP·docs/SECURITY §39/§21.7·docs/CODEBASE_MAP 은 feature 커밋 self-add → 재생성 안 함. index.html/admin.html 편집 0(cache-buster 는 소스 `?v=dev` 고정 + 빌드 content-hash 자동주입, ITEM-09). (doc-sync-20260807-010301)
- doc_sync 08-06(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): aa46e677(08-05 sync, META-0053) 이후 델타 15 커밋(non-merge)/PR 14건(머지 커밋 13 + squash 직접커밋 1 = template v3.44.1 `8b0eefa5`)·git-date 전부 08-05 정합 — **신규 feature 0개**라 wiki feature 카운트 39 유지(8참조/5파일 ground-truth 재대조 PASS) + Arch §2.3 기능맵 표 body·Features/_Index 표·overview §2.1 표 전부 실제 unit 목록과 diff 0. 갱신은 전부 stale 정정 — `Features/_Index.md` feature-0038 상태 `in-progress → review` + "ITEM-P5b 잔여" → 완결(**prior-window drift**: 직전 sync 가 overview·Arch 만 고치고 MOC 행을 빠뜨렸다) · overview §2.1 + Arch §2.3 의 0038(후속 Phase A~B3, app.js -27%)·0036("라이브 판정 실측 잔여" → 순환 차단 ADR-0036-08 + 판정 배지 노출 ADR-0036-09/10 배포 완료)·0011(P5a 종결 ADR-20260805T153000) · Arch §2.4 feature-0036 deps 에 feature-0003 추가(cycle 3 이 `graph-ctxmenu.js`·`admin.html` 접촉) · overview §1 (63) 2026-08-05 타임라인 + hot/Log. `wiki/README.md` 는 owning feature self-add(803a5ecd) → 재작성 안 함. feature 카드(0002/0003/0011/0036/0038)·ADR 미러는 본 축 범위 밖으로 핸드오프. index.html/admin.html 편집 0(cache-buster 는 소스 `?v=dev` 고정 + 빌드 content-hash 자동주입, ITEM-09). wiki-lint 22·ssot-lint 4 WARN-only baseline 불변.
- doc_sync 08-05(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): 34ee7448(08-04 sync, META-0052) 이후 델타 36 커밋/17 머지·git-date 전부 08-04 정합 — 신규 feature 1개(feature-0039 운영 정기 잡의 인-컨테이너 이관, Major·PR #1131) 색인 + wiki feature 카운트 38→39 8참조/5파일 전수 sweep + overview §2.1 표 1행 · §1 (62) 2026-08-04 타임라인 + Architecture/Overview §2.3 기능맵·§2.4 의존성 1행씩(deps = 0002/0015/0016/0020, FUNCTION §9) + **feature-0038 stale 정정**(overview §2.1 · Arch §2.3 의 "ITEM-P5b 잔여 / Cycle 3~10 잔여 / in-progress" → 10 cycle 완결·CONVENTIONS §14·AC-3 부분 달성) + hot/Log 갱신. feature 카드·MOC 행·Log feature entry·CODEBASE_MAP·LEARNINGS·STATUS 행은 feature 커밋 self-add → 재생성 안 함. index.html/admin.html 편집 0(cache-buster 는 소스 `?v=dev` 고정 + 빌드 content-hash 자동주입, ITEM-09). 라이브 서빙 파리티 갭 0.
- doc_sync 08-04(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): 2df27587(07-31 sync, META-0051) 이후 델타 26 커밋(non-merge)/머지 25건 정합 — 신규 feature 4개(feature-0035 결정적 분석 플래너 ITEM-11 · feature-0036 분석문 사실성 판정 ITEM-10 · feature-0037 도메인 합성 L3 lazy ITEM-08 = **analysis-orchestration ROADMAP 종결** · feature-0038 프론트엔드 모듈화 = ssot-consolidation ITEM-P5b 잔여) 색인: wiki feature 카운트 **34→38** 8참조/5파일(overview 서두·§2.2 ADR 서두·_Index 헤더/개요·Index 요약/MOC·Architecture Overview 헤더/범위·Module-Map MOC)·_Index §2 표 4행·overview §2.1 표 4행 + §1 **(61) 2026-07-31~08-03** 타임라인·Architecture §2.3/§2.4 backfill(양 표 body 가 0034 에서 끊겨 있었음)·Log append·hot 갱신(잔존 actionable thread 보존·append/prepend 만). feature 카드 4장은 feature 커밋 self-add(`wiki/Features/*.md` 실측 38장) → 재생성 안 함. 타임라인·hot fact 는 신규 feature 4개 + 07-31~08-03 UX·운영·정직성 bundle(feature-0016 라벨 어휘 통일 E.11/E.12·그래프 시각 부정합 2건·자동 재분석 계측 도달 규약 역전 / feature-0003 AI 운영 현황 미분류 활동 3종 재배치 + AST 전수 게이트 / feature-0002 conv-audit 4건 = 쿼리 리뷰 프레이밍·grounding 코드 권위선·부하게이트 재작성 방향·전제 절 검증된-사실-또는-미확인 / agent-core 질의 임베딩 무음 강등 가시화)을 함께 반영. 무변경 정직: overview §2.4 '진행 중인 흐름'은 2026-06 서사 목록이라 창 단위 backfill 관행 없음(미변경). report-only: feature-0035 TASK-0009(파티션 축약 후속 배포) 미체크 상태에서 §2.1/카드 문면은 '배포 잔여'로 표기·Log.md 2중 포맷 regime 공존(선언 포맷 `[ISO] op | role | path | note` vs 하단 `## [date] type | note` 롤업). **로컬 commit 만·push/merge/deploy 는 wrapper**. (doc-sync-20260804-010301)
- doc_sync 07-31(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): 7fd73e3e(07-30 sync, META-0050) 이후 델타 52 커밋(31 non-merge) 정합 — 신규 feature 4개(feature-0031 접지 L0/L1 · feature-0032 백그라운드 토큰 예산 · feature-0033 L2 합성 요약 · feature-0034 L2 답변 grounding = 분석 오케스트레이션 ITEM-04·05·06/12/07/09) 색인: wiki feature 카운트 30→34 8참조/5파일(overview 서두·§2.2 ADR 서두·_Index 헤더/개요·Index 요약/MOC·Architecture Overview 헤더/범위·Module-Map MOC)·overview §2.1 표 4행+§1 (60) 2026-07-30 타임라인·Architecture §2.3/§2.4 backfill(표 body 0030 에서 끊김)·docs/ARCHITECTURE §4/§6 backfill·docs/STATUS 신규 4행(gen-status 재생성 — 4건 전부 frontmatter `feature_status` 보유)·Log append·hot 갱신(잔존 thread 보존). feature 카드 4장은 feature 커밋 self-add → 재생성 안 함. 동반 operational(feature-0003 별도 commit): 사용자향 릴리즈노트 창 블록(cache-buster `?v=dev` 고정·빌드 content-hash 자동주입이라 수동 bump 없음). 무변경 정직: docs/DECISIONS(델타 내 4자리 프로젝트 ADR 0 — feature-local 만 증가). report-only: feature-0031 TASK 체크리스트 lag(REPORT/카드/git 대비) · ROADMAP ITEM-07/09 가 머지 후에도 `pending`+`feature_id: feature-0032-analysis-synthesis`(실제 0033/0034). **로컬 commit 만·push/merge/deploy 는 wrapper**. (doc-sync-20260731-010301)
- doc_sync 07-16(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): 07-15 03:03 sync(8e6b2b76) 이후 37 머지 정합 — feature 카운트 20→21(feature-0021 신규) 8곳(_Index 헤더·개요·overview 서두/§2.2·Index 요약/MOC·Architecture Overview 헤더·Module-Map)·overview §2.1 feature-0021 행+2026-07-15 서사(52)·Architecture §2.3/§2.4 feature-0019/0020/0021 backfill(pre-existing drift 동반 정합)·Log per-feature 엔트리·hot. SECURITY §23 신설(feature-0021 'AI 추론' admin observability boundary 색인). 무변경 정직: SECURITY 권한/CONVENTIONS/ROUTEMAP 는 07-15 perm 커밋(8e01cc24) 반영·DECISIONS/STATUS(gen-status --check PASS) noChange. 동반 operational(feature-0003 별도 commit): 릴리즈노트 07-15 블록 7항목(generated 07-15)·cache-buster `?v=dev` 고정(빌드 inject_asset_stamp.py content-hash 자동주입). **로컬 commit 만·push/merge/deploy 는 wrapper**. (doc-sync-20260716-010501)
- feature-0020 cycle(07-14): docker-compose(워커 hc timeout 30s·gateway stop_grace 120s·bedrock-gateway-surge profile 서비스·live-truth 튜닝 커밋)·deploy-web.sh(워커/gateway phase·scope 플래그·state key=value)·alembic-migrate.sh(upgrade/stamp 선행 재빌드 가드)·Makefile(deploy-all/deploy-workers/ask-worker-*·up 기동)·RUNBOOK §9 개정·STATUS/ARCHITECTURE/wiki 카드+_Index 19 active.
- doc_sync 07-14(스케줄, ULTRACODE·landing/배포 소유=wrapper 위임): 07-13 10:30 sync(bafad813) 이후 07-13 오후 델타(#746~#770) 정합 — feature-0016 그래프 렌더러 PixiJS v8 전면 교체(§78~81)+콘텐츠 밴드(mig 0040)·p2·feature-0003 첨부 버전 관리·ds-conn-test·graph-perm-split(Critical)·feature-0002 describe_routine·sql_guard read-only 보정·feature-0019 메시지 편집 신규(Phase 1). STATUS 인덱스(0002/0003/0016 행 07-13+요지·feature-0019 신규 행·sources·gen-status --check PASS)·ARCHITECTURE feature-0016 렌더러 PixiJS+§78~81 마커·SECURITY §19 graph.read 함의 정정(우산=편집 4키만)·wiki(_Index 카운트 17→18+feature-0019 행·feature-0016 렌더러·overview 카운트+㊿+§2.1 행·hot·Log·카드 0016/0003/0002). 동반 operational(feature-0003 별도 commit): 릴리즈노트 data.js 07-13 블록 +7항목(generated 07-13) — cache-buster `?v=dev` 고정(빌드 inject_asset_stamp.py content-hash 자동주입, 수기 bump 불요). content-cluster·§77 은 착지 브랜치가 이미 Log 기록·feature-0019 카드 기존재. 무변경 정직: DECISIONS noChange(§78 등 feature-local ADR·graph-perm-split B안 승인). **landing/배포 소유=wrapper 위임 → 로컬 commit 만·push/merge/deploy 는 wrapper**. (doc-sync-20260714-020501)
- doc_sync 07-13(수동, attended): 직전 07-10 스케줄 doc_sync 미landed(wrapper landing 실패·133 커밋 stale)를 **콘텐츠 harvest 후 supersede** — origin/main(c02d81e0) 기준 fresh 재구성으로 07-09~13 창 정합. 사용자향은 07-09~10 국한(07-11~13 은 feature-0012 라우터 모듈화 완결·ITEM-09 그래프 CSS/JS·META 툴링 등 전부 behavior-neutral). ① feature-0016 그래프 §57.4~76 대규모 성능/UX(뷰포트 컬링·세로폭주·미니맵·상세 read/write·카메라 이동·툴바 3존·하이라이트 안정화, ADR-026~037) ② feature-0003 응답 지연 타임아웃 모달 제거+(07-09) 추론 예산 상한 확대 ③ feature-0002 MSSQL 로그인실패 조기 skip+cooldown ④ **§69 AI caveats — 07-10 run 이 유보(T69.5 미완)했으나 07-13 PR #744 T69.5 완수(cc_data_main 715/715)로 사용자 릴리즈노트 편입** ⑤ (색인) feature-0012 완결(07-12). STATUS 인덱스 0002/0003/0016 행 07-10·0012 review/07-12(rollup 0·passthrough·gen-status --check PASS)·ARCHITECTURE feature-0012 완결·`docs/RELEASE_NOTES` 07-09/07-10 블록·wiki(카드 0016/0012·overview ㊻~㊾·hot·Log). 동반 operational(feature-0003 별도 commit): 릴리즈노트 data.js 07-10 블록 7항목(§69 포함, generated 07-10) — cache-buster `?v=dev` 는 빌드 `inject_asset_stamp.py` content-hash 자동주입이라 수기 bump 불요. 무변경 정직: SECURITY/DECISIONS noChange(경계 0·ADR feature-local)·카운트 noChange(카드 18=unit 18). landing/배포 소유=스킬 → PR→merge→deploy-web. (doc-sync-20260713)
- doc_sync 07-08(스케줄, ULTRACODE): 07-07 23:52 직전 sync 이후 07-08 머지 델타 정합 — ① feature-0016 §57 graph-edge-visibility(ADR-024 — 접힘 카드 연결선·상대 하이라이트·크로스 마젠타·중간 줌 LOD) ② §58 tableaxis-case(MSSQL 라벨 정합+AccountDB rekey·gateway mem 2g) ③ §59 product-classify-suggest(ADR-025 — 제품 분류 AI 제안→승인) ④ feature-0003 메타데이터 콘솔 UX 4건+폴리시 5건+제품 분류 승인 UI. STATUS 인덱스 0002/0003/0016 행·RELEASE_NOTES 07-08 블록·wiki(hot·카드 0016/0003·overview ㊹㊺·Log). 동반 operational(feature-0003 별도 commit): 릴리즈노트 data.js 07-08 블록 + cache-buster. 무변경 정직 보고: _Index/Index/feature 카운트 noChange(신규 feature 0 — 17 active/카드 18)·concepts noChange(§59=기존 metadata 도메인 내부)·DECISIONS noChange(ADR-024~025 feature-local). (doc-sync-20260708-230501)
- doc_sync 07-07 2차(스케줄, ULTRACODE): 같은 날 11:34 sync(04bf4a87/dfc64728) 이후 11:37~19:33 머지 델타 정합 — ① KB 채택 인박스+ENUM 자율수집(`kb.enum.curate`·alembic 0039) ② 콘솔 IA 통합+5서브뷰 폴리시 ③ 추론 강도별 예산 ④ §56 그래프 sync 견고화(ADR-022~023) ⑤ feature-0007 OAuth cron 정적화. STATUS 인덱스 0002/0003/0007/0016 행·RELEASE_NOTES 07-07 tail 블록·wiki(hot·카드 0003/0016·overview ㊸)·meta/REVIEW. 동반 operational(feature-0003 별도 commit): 릴리즈노트 data.js 07-07 +2항목(코드값 채택·추론 강도별 예산)+cache-buster `20260707b-rn-0707`. SECURITY noChange(kb.enum.curate 대칭 확장·경계 0, feature cycle 승격 권고)·DECISIONS/ARCHITECTURE noChange·카드 18 안정. 8-agent adversarial-verify wf_731a14ae(2 catch 교정: 용어 릴리즈노트 중복·§56 ADR split). (doc-sync-20260707-230501)
- doc_sync 07-07(수동, attended): 마지막 landed 릴리즈노트 5b1481bb(07-02) 이후 **07-03·07-06 두 doc_sync 미landed분 통합 + 07-04/07-07 추가분** 정합(69 머지 창). 07-03·07-06 미방치 worktree 는 stale-base·불완전·미검증이라 **rebase-salvage 대신 콘텐츠 harvest 후 supersede** — 현재 origin/main(3d2d070c) 기준 fresh 재구성. STATUS 인덱스 feature-0002/0003/0007/0009/0016 행 date+요지(인덱스 모델·rollup 0)·ARCHITECTURE §4(feature-0003 aiops-ttft 앵커 0032/0033+런타임 설정, feature-0009 window, feature-0016 07-03~07 UX+§55)·RELEASE_NOTES 07-03/04/06/07 블록 tail append·wiki(overview ㉚~㊷·hot Key Facts 6+본 항목·Log·카드 0002/0003/0007/0009/0016)·meta/REVIEW. 동반 operational(feature-0003 별도 commit): 릴리즈노트 data.js 07-03/04/06/07 블록(generated 07-07) + cache-buster `20260707-rn-0707`. SECURITY noChange(share-visibility-window §21/§21.4 는 소속 feature-0009 정본이 이미 반영; 07-03~07 나머지 신규 RBAC/경계 0 — additive 컬럼·gateway 내부 라우팅·feature-0018 권한은 소속 정본). DECISIONS noChange(ADR-010~021 feature-local·ADR-002 feature-0007 local). Index/_Index noChange(카드 18 안정). ssot-lint 4 WARN(기존 secret) 불변. feature-0018 은 독립 unit 디렉토리 없이 feature-0003 코드 거주 → 신규 wiki 카드 불요.
- doc_sync 07-02(2305): 07-01 23:44 직전 sync(bb63b9f8) 이후 07-02 머지 backfill — STATUS 0003/0016-metadata-graph 행(date+요지 1구)·ARCHITECTURE §4(feature-0016 렌더러 G6 v5 교체 정정+07-02 진화, feature-0003 AI 운영 관제 패널 색인)·SECURITY 신규 §20(AI 운영 관제 패널 admin observability 경계)·RELEASE_NOTES 07-02 블록 2건·wiki(0016·0003 카드·overview ㉘㉙·hot·Log) 정합. 동반 operational(feature-0003): 릴리즈노트 data.js 07-02 블록 + cache-buster 20260702-rn-0702. DECISIONS noChange(ADR-004~008 feature-local). 기능 카운트 무변경(18 dir/17 id)·ssot-lint 4 WARN 불변.
- doc_sync 07-01(2305): 06-30 23:51 직전 sync 이후 07-01 머지 backfill — STATUS 0003/0012/0016-metadata-graph 행·ARCHITECTURE §4/§6·SECURITY §19(그래프 RBAC 5분할)·RELEASE_NOTES 07-01 블록 2건·wiki(0016·0012 카드·_Index·Index·overview ㉔~㉗·Architecture/Overview·hot) 정합 + 잔존 stale '13' 2건 정정. 동반 operational(feature-0003): 릴리즈노트 07-01 블록 7항목 + cache-buster 20260701-rn-0701. DECISIONS noChange(ADR-002/003 feature-local). ssot-lint 4 WARN 불변.
- doc_sync 06-30(2305): 06-30 10:31 직전 sync 이후 머지 backfill — feature-0016-metadata-graph 전 문서 누락분 신설(STATUS 행·ARCHITECTURE §4/§6·wiki 카드/_Index/overview/Architecture Overview·SECURITY §19 그래프 질의면 색인) + 무중단군(0014/0015/0016-zd/0017) STATUS/ARCHITECTURE §6 의존맵·docs/RELEASE_NOTES 운영자 블록 + feature-0002/0003 06-30 행 + feature 카운트 13→17(디렉토리 18) 5곳 정합. 동반 operational: 릴리즈노트 06-30 블록 10항목 + cache-buster bump(20260630b-rn-0630).
- doc_sync 06-30: 06-29 13:35 직전 sync 이후 머지분 정합 — feature 카운트 12→13(Index/_Index/overview/Architecture Overview)·ARCHITECTURE/overview 기능맵·의존맵 feature-0013 행·STATUS §5 카운트·feature-0013 카드 reality(stub→active, PB-0008 PASS)·`concepts/nl2sql-flywheel.md` §2 ITEM-08/11 출시 반영. 동반: 릴리즈노트 06-29 블록 6항목 추가(3→9) + cache-buster bump(20260630-rn-0630) → web 재배포.
- 제품 chip 처리 중 항상 활성화(f049fee, 06-26): 처리 중 제품 선택을 막던 3계층 가드(`renderProductChip` 시각 disable·`setActiveProduct` busy reject·`update_conversation_product` PATCH 409) 제거 — chip 항상 활성, 변경은 다음 요청부터(in-flight 답변은 enqueue 시 캡처된 product 로 실행). RBAC/스키마 0 변경. ADR-WEB-0006.
- ask-dedup-idempotency(0818b0a·3595ea3, 06-26): `enqueue_ask_job(dedup_message=...)` INSERT NOT EXISTS(`_ACTIVE_SLOT_PREDICATE` 재사용·stale 제외)+`find_active_dup_ask_job` 로 활성 중복이면 새 job·sentinel 없이 기존 run attach; app.js `/api/ask` 실패 시 status 복구 0.7s×3 재시도. 배포 검증 중 dedup NOT EXISTS 가 INSERT SELECT(varchar)와 파라미터 공유 → AmbiguousParameter 로 워커모드 신규 `/api/ask` 전부 500 회귀 → 전용 파라미터 + 테이블 alias `d` 격리로 수정.
- shared/ P5a Step5 완료: 4개 alias shim(model_catalog 제외 config·db·conn_health·datasources) 소비처를 `shared.*` 로 직접 마이그레이션 후 `modules/*` shim 전량 제거.
- 그룹 대화 06-25: 안 읽음/@멘션 배지·메시지 좌우 정렬·owner 멤버 추방/차단(앞선 06-24 참여자 roster·보관 설정팝업·참여자 나가기·'참여 허용' owner-only 게이트 위에).
- 그룹 대화 06-25 잔여(doc_sync): 참가자 per-message 제품 선택·발화(PR#440)·처리 중 composer 비잠금/1:1 인터럽트 재요청/그룹 중복차단(PR#438·#444)·@assistant 발신자 표시 정정(PR#437)·datasource 회로차단 사용자 안내 문구 분리(PR#439, feature-0002).
- 그룹 대화 06-25 후속(gc-optimistic-sender-attrib, 머지): optimistic(전송 직후) 발신자 표시 정정 — 비-owner 참가자 메시지가 처리 중 동안 owner 로 잘못 표시되던 깜빡임 제거(`_selfSenderMeta()`를 optimistic 2지점에 부여). frontend display-only.
- 그룹 대화 06-25 잔여(doc_sync, sync a29a2f0 이후): ① 안 읽음 배지 미감소 최종 근본원인 해소 — 읽음 커서/unread 집계가 `core_messages.id` 공간인데 FE 가 보낸 `last_read_message_id` 는 표시 store `messages.id` 공간이라 disjoint → 핸들러가 requested 무시·항상 `MAX(core_messages.id)` 로 전진(read-idspace-fix). 선행 read 500(`modules.db`→`shared.db`)·읽음 커서 전진 누락(refreshWorkspace/재선택)·입력창 즉시 클리어 보정 포함. ② 공유 대화 join 불가 수정 — PG named-param 타입 모순(AmbiguousParameter) 해소(group_members). ③ assistant SQL dialect 교정(MySQL DS T-SQL thrashing 차단 `_MYSQL_DIALECT_GUIDANCE`)+그룹 히스토리 발신자 맥락 라벨 `[이름]:`(feature-0002). ④ 좌측 대화 전환 크로스페이드(fade-out/in + 가속·reduced-motion no-op, frontend).

## Active Threads
- **doc_sync 이월 29건(2026-08-19 · report-only)** — 08-19 run 의 적대검증이 예산상 축별 상위 4건까지만 도달해, 나머지 29 finding 은 **정본 재검증 없이 적용하지 않았다**(미검증 제안을 색인에 밀어넣지 않는 규율). 다음 run 이 우선 처리할 것: ① `wiki/Decisions/_Index.md` 의 타임스탬프 ADR·ADR-0031 미색인 + ADR 건수 문면 ② `wiki/Index.md` ADR 범위 표기 ③ `wiki/Glossary/_Index.md` 항목 수 ④ `docs/PROJECT.md` compose 서비스 목록·배포 정책·런타임(Postgres) 서술 ⑤ `wiki/Features/feature-0040-db-object-explorer.md` 상태 문면(배포 전 시점 잔존)·self-link·placeholder heading ⑥ `wiki/Features/_Index.md` feature-0042 상태 ⑦ `wiki/overview.md` §2.4 진행 흐름(6월 고정)·§4 한계의 절 참조 ⑧ `wiki/hot.md` 실제 분량 vs 500자 규약. 각 finding 의 제안문·정본 근거는 워크플로 `wf_fea1dd76-a75` 산출에 있으나 **재검증 없이 신뢰하지 말 것**(같은 run 에서 1건은 refuted 됐다).
- 08-14~08-16 창 **잔여 = 원리적 미측정 · 정본 상태 전이 · 다음 audit 재측정**(feature-0041 · feature-0020 · feature-0042 · conv-audit 원장): 이 창도 **POST-DEPLOY 이월이 0** 이다(델타 57 커밋 전건이 라이브 `6c7413cf`). 그래서 열린 것은 배포가 아니다. ⓐ **feature-0041**: 08-14 에 P1 `execute_sql` 개방과 AC-7 답변 보존이 출하되면서 종전 잔여 2건 중 하나가 닫혔고, 남은 것은 **사람 브라우저 로그인·동의가 필요한 전 구간 e2e** 1건뿐이다(절차서 `docs/E2E_RUNBOOK.md` 완비 · 인가 지점이 "신원 = 우리 로그인 세션" 축의 생성점이라 **설계상 자동화 불가**) → 실행 후 TEST.md §3 Run 기록으로 AC-1 종결. ⓑ **feature-0020**: surge 교대의 핵심 주장인 "바쁠 때 기다리지 않고 완결" 궤적은 **아직 미관측**이다 — 배포 시점 running=0(유휴)이었고, 내려간 본체는 구 이미지라 구 drain 시맨틱(60s)으로 동작해 **신 예산(1800s)은 다음 배포부터** 적용된다. 다음 바쁜 시간대 배포가 자연 검증이다. 카드의 '마지막 갱신' 이 정본 `feature_status_date`(2026-07-14)에 묶여 있어 08-12·08-14 슬라이스는 **정본 상태 전이 미반영**(feature-cycle 소관). ⓒ **feature-0042**: ITEM-15(프롬프트 캐시 재구성)는 `BLOCKED: verification-sample-insufficient` — 선행 안전망은 '출력 계약 회귀 테스트 신설' 로 확정됐으나(`47b5c891`) **구현은 별도 cycle** 이고, 미확정 2건(구독 사용량 한도 회계에 캐시 읽기 0.1× 반영 여부 = `usage`·헤더 미노출로 프로브 판정 불가 · `_record_llm_usage` 가 캐시 필드를 저장하지 않아 **효과 관측 수단 자체가 없다**)이 남는다. ⓓ **conv-audit 원장**: `FR-early-return-kv-never-finalized`·`FR-attach-change-signal-client-only`·`FR-llm-attempt-cap-inside-latency-tail` 등 다수가 `fixed:deployed:unverified-live` — 배포·기계 검증은 끝났고 **실사용자 corroboration 재측정이 다음 audit 몫**이다. 그 외 정직한 잔여: 첨부 변경-인지의 공유창 확대 과표시(오차 방향이 봉인 대상과 반대라 tz 비의존을 지키고 **수용**·AC-2 명시) · 계약 도입 이전 대화의 legacy `[]` 기준선(한 턴 뒤 자기 치유) · 업로드→삭제→복구 사이 표식 유실(선재 갭) · 첨부 `CreatedAt` UTC 전환의 **실브라우저 화면 칩 미실측**(PB-0008) · 사용 기록 표의 열 폭 재계산(≤4%)·브라우저 콘솔 에러 미수집 · provenance 게이트의 **히스토리 replay 미신호**(넓히면 그룹 대화 상시 차단이라 한계로 채택) · `OBS-attach-chain-multiple-live-heads`(라이브 0건이라 고치지 않고 관측 항목 등재 — 승격 조건 = 지표 ≠ 0).
- 08-13 창 **관측·결정 잔여**(feature-0041 · feature-0016 · feature-0003): 이 창은 **POST-DEPLOY 이월이 0** 이다 — 8개 feature-0003 cycle · feature-0016 · feature-0041 이 전부 같은 날 배포·라이브 실측·종결까지 갔고 실측 기록 전용 커밋이 9건이다. 그래서 열린 것은 배포가 아니라 **원리적으로 미측정인 축과 사용자 결정**뿐이다. ⓐ **feature-0041**: 사람 브라우저 로그인·동의가 필요한 **전 구간 e2e 실행**(절차서 `docs/E2E_RUNBOOK.md` + 스크립트 완비 · 인가 지점이 "신원 = 우리 로그인 세션" 축의 생성점이라 **설계상 자동화 불가**) → 실행 후 TEST.md §3 Run 기록으로 AC-1 종결 · P1 이후 도구(`execute_sql` 등)는 사용자 결정(2026-08-12)에 따라 **운영 데이터를 보고 판단**(근거 = `tool_call_usage` 원장의 도구별 호출 분포·미제출률·한도 도달 빈도)이므로 원장 축적 대기. ⓑ **feature-0016**: 트윈 중간 프레임의 시각 캡처는 **CDP relay 가 호출 사이 렌더러를 재워** 360ms 창을 샘플링할 수 없어 미측정(프레임 간격 실측 16.7ms=60fps vsync 는 **연속 5프레임 단일 표본**이라 구간 전체의 부드러움으로 일반화하지 않는다) · "재배치로 화면을 벗어날 때의 추종" 분기는 라이브에서 조건이 발생하지 않아 헤드리스 계약(C2·C9)으로만 검증됐다 → **체감 부드러움과 이탈-추종은 사용자 육안 확인이 남은 항목**. ⓒ **feature-0003**: `graph-expand-perf` 의 **재배치 이동 트윈 360ms 조정 여부 = 사용자 판단**(개선으로 처리 시간이 줄어 556ms 중 360ms 가 연출이 됐다 — 성능으로 위장해 연출을 지우지 않는 원칙상 doc_sync·feature 가 임의로 줄이지 않는다) · 같은 cycle 이 원장에 남긴 **선재 결함 2건**(상세 조회의 세대 가드 부재 · 상세 backfill 과 펼침의 `/columns` 중복 — 둘 다 `main` 에 이미 있고 본 변경이 만든 경로가 아니다) · `group-attach-scope-window` 의 **bounded 축소는 라이브 미실측**(라이브에 은닉 구간이 실재 하는 멤버가 없어 조건이 생성되지 않는다 — 단위 테스트로만 증명) + 첨부 버전의 **사용자별 트리 분기**(assistant 독자 계보)는 스키마 마이그레이션·UNIQUE 제약 변경·백필·프론트 트리 UI 를 수반해 **후속 cycle 로 분리** + 첨부 `created_at` 시간축 왜곡(+9h)이 fork 의 `_attachment_outside_window` 에도 영향을 주는 **별도 결함**으로 원장 등재 · `member-gates` D-1~D-3(dead `buildPermissionPills` 삭제 후보 · 대화 검색 배지가 멤버 대화를 "타 계정" 으로 표기하는 라벨 불일치 · 관리자 `.any` 열람 대화의 '이동' 이 서버 배정 성공에도 폴더 하위에 렌더되지 않는 **무음 실패** — 숨김 vs 파티션 확대)은 **사용자 결정 대기** · 제목 입력이 멤버에게 활성이고 서버 403 인 것은 display-permissive 컨벤션상 **의도된 현 동작**(테스트로 고정 — 더 정확히 하려면 per-code `can()` 도입 = TASK-0098 결정을 되돌리는 별 작업).
- 08-11~08-12 창 **배포·잔여 cycle 대기**(feature-0040 · feature-0041 · feature-0020): **feature-0040 은 라이브 배포까지 완료됐고 잔여는 POST-DEPLOY PB-0008 시각검증뿐** — doc_sync 08-13 실측으로 배포 전제가 모두 해소됐다: alembic 라이브 head `0055`(= 저장소 MAX_MIGRATION, 즉 `0054` 적용됨) · `db_objects` **339행 충전**(미충전 아님) · 그래프 자산(`graph/graph-roleviz.js`·`graph-core.js`)이 라이브에서 브랜치 blob 과 byte-identical · web 이미지 `105fa2b7`·agent 이미지 `e1372f32` 모두 `ed5d4bb8` 포함. 다만 **정본은 아직 이 사실을 모른다** — `feature_status: review` 이고 REPORT §1/§5 는 "코드·단위검증 완료 / 라이브 시각검증·배포 미수행"·"배포 전제 alembic 0054 적용 필요"에 정지, TEST.md POST-DEPLOY 4항도 전건 미체크다(커밋 시점 스냅샷이 배포보다 앞선 lag). 정본 갱신과 PB-0008 시각검증은 feature-cycle 소관(doc_sync 는 색인만 실측 정합). **feature-0041 은 1차 `a68fbbac`·2차 `e1372f32` 배포 완료**지만 3건이 열려 있다 — ⓐ 관리 콘솔 '외부 도구 한도' 탭은 사용자 결정으로 **이번 출하에서 제외**(admin.js 를 다른 활성 브랜치 2개가 동시 편집 중 + `visual_verification_scope: always` 라 PB-0008 이 하드 게이트여서 붙이면 나머지 3건 출하까지 묶인다. 상한 자체는 runtime 기본값으로 이미 집행 중이라 기능 공백은 아니다) → **별도 cycle** ⓑ 인증된 전 구간 e2e 는 인가에 사람 브라우저 로그인·동의가 필요해 **설계상 자동화 불가**(사람 1회) ⓒ P1 이후 도구(`execute_sql` 등)는 원 계획대로 별도 단계. **feature-0020 은 08-12 에 quiesce 게이트 2 cycle(ed257c1e·9f43c3ca)이 머지·실측됐는데 정본 TASK 는 `feature_status_date: 2026-07-14` 에 정지** — `docs/STATUS.md` 비고도 07-14 문면이라 상태 재판정은 feature-cycle 소관(doc_sync 는 passthrough). 더불어 `bin/recreate-audit.sh` 는 raw `docker compose` 우회를 **막지 못하고 사후 적발만** 하므로 운영 규약(인가 2경로만 사용)이 계속 필요하다.
- 08-07~08-11 창 **잔여 관측·결정 대기**(feature-0003 api-exposure-hardening · feature-0003 attach-diff · feature-0002): 이번 창은 배포·실증이 이례적으로 촘촘하다 — 컨테이너 이미지가 `d56367c8`(= HEAD)이고 델타 44 커밋이 전부 라이브이며 PB-0008/POST-DEPLOY 실측 기록 커밋이 **9건**(접미 토글 · 구문 하이라이트 · identical 원문 · 줄 안 마크 · `_` 분리 3× 확대 캡처 · 원문 보기 7축 · 액션 열 좌표 단일값 · 가이드 툴팁 8/8 · 엣지 게이트 503=0) + feature-0002 라이브 실측·종결 기록 5건이다. 열린 것은 ⓐ `20260811T1830-api-exposure-hardening` 의 **§18.8 적대 패널 미수행**(세션 제약 — REVIEW 에 잔여 리스크로 정직 표기)과 **공인 IP 노출 차단 = 사용자 결정 대기**(차단이 서비스 중단을 수반), 그리고 `agent_ro` 는 **컨테이너 재시작 시점부터** 효력 ⓑ feature-0003 `20260807T0620-attach-diff-unified-bg` 의 '배포 + PB-0008 재검증' 체크박스가 여전히 미체크 — 후속 cycle 들의 PB-0008 이 같은 모달을 라이브에서 재측정했으나 그 항목 자체는 정본에서 닫히지 않았다(정본 갱신은 feature-cycle 소관) ⓒ feature-0002 `TASK-20260806T1600` 의 '미실측 잔여'(모델이 `read_attachment` 를 호출하지 않아 완전성 계약 축은 관측 불가)와 부수 발견 2건 처리 방향 결정, `TASK-20260806T110000-aiops-hygiene`·`T170000-label-namespace` 의 라이브 재합성 확인. 직전 창 thread 의 ⓑ(feature-0002 `update_attachment` 라이브 실측)는 **08-07 PB-0008 도구 채택 4/4 · 완주 4/4 · 다운로드 칩 4/4 렌더로 해소**됐다(정본 TASK 체크 완료).
- 08-06 첨부 트랙 **라이브 관측 잔여**(feature-0003 attach-diff · feature-0002 attach-delivery-tool·도메인 합성 2건): **배포는 실측으로 완료**됐다 — 델타로 바뀐 static 14종의 라이브 서빙본이 `origin/main` 과 byte-identical(빌드 `?v=<hash>` 정규화 후) · `/healthz` 200 · web-a/web-b/ask-worker/insight-worker/ops-scheduler healthy. 남은 것은 배포 **후** 관측 축뿐이고, 정본 체크박스 기준으로 ⓐ feature-0003 `20260807T0620-attach-diff-unified-bg` 의 PB-0008 단일열 확대 캡처 재판독(자동 44축이 전부 PASS 인 채 회귀가 나갔으므로 지각 축은 사람 판독이 게이트) ⓑ feature-0002 `TASK-20260806T1600` 의 라이브 실측(모델이 실제로 `update_attachment` 를 채택하는지 · 다중 파일 전달 완주 · **다운로드 칩 노출**(패널 P1 재현 항목) · 패치 적용 성공률) ⓒ `TASK-20260806T110000-aiops-hygiene`·`TASK-20260806T170000-label-namespace` 의 라이브 재합성 확인 — 세 건이 미체크로 남아 있다. 정본 체크박스 갱신은 **feature-cycle 소관**(doc_sync 는 detect-and-report).
- feature-0039 ops-scheduler(08-04 머지 PR #1131 · `feature_status: review` · **Major**): **배포·cutover 완료(2026-08-05 실측)** — `ops-scheduler` 컨테이너 healthy · 그래프 sync 증분 30분 주기 rc=0(00:30·01:00), 호스트 root crontab 의 해당 4줄은 이관 주석으로 대체돼 제거(병행 실행 창 없음). 정본 TASK §3 `TASK-20260804T104800-cutover` · §7 (deploy-backed) 체크박스만 미체크로 남아 문서↔현실 lag 은 feature-cycle 소관. 서버 메이저 업그레이드 시 Dockerfile `postgresql-client-16` 핀 동반 필수(누락 시 복원 불가가 조용히 재발 — ADR-20260804T104200). 후속 후보: 주간 복원 리허설 FAIL 을 관리 콘솔 알림·헬스 신호로 승격(5주간 FAIL 방치가 근거) · AGE 그래프(`metadata_kb`) 덤프·복원 왕복 행 수 대조 · `bin/routine-backfill.sh` 도 서비스 내부로 이관할지 검토.
- 분석 오케스트레이션 ROADMAP **종결(07-31)**: 잔여 3항목이 전부 구현·머지됐다 — ITEM-11=feature-0035(결정적 플래너) · ITEM-10=feature-0036(분석문 사실성 판정) · ITEM-08=feature-0037(L3 도메인 합성 lazy). 아래 07-30 항목의 "ITEM-08/10/11 전부 pending" 은 **그 시점 기준**이며 현재는 해소. **잔여는 배포·라이브 실측**: feature-0035 는 PR #1104 배포 + 라이브 선정 실측 완료이나 후속 파티션 계열 축약(TASK-0009 `후속 verify → PR → 머지 → 배포`) 미체크 · feature-0036 은 배포·라이브 판정 실측 완료(TASK-0008 [x])에 이어 08-05 판정 무한 순환 차단(ADR-0036-08)·판정 결과 노출(배지, ADR-0036-09/10·PB-0008 PASS)까지 진행 · feature-0037 TASK-0008 만 `배포 → 라이브 합성 실측` 단계에서 미체크 · feature-0031/0033/0034 의 배포 후 라이브 대조도 여전히 잔여.
- feature-0038 프론트엔드 모듈화(ssot-consolidation **ITEM-P5b 완결**, 08-03 착수 ~ 08-04 본편 완결 ~ 08-05 후속 Phase A~B3 완결·PLAN-APPROVED by mckim·**Critical**): Cycle 1(styles.css → `css/` 7파일) 배포·POST-DEPLOY PB-0008 완료 + 롤백 리허설 Run-006(revert 왕복 diff 0). **Cycle 2~10 + Final 전건 완료(08-04)** — JS 는 미머지 docker cp 사전검증이 불가해(ES module 스탬프 미주입 시 모듈 캐시 이중 인스턴스) 배포·POST-DEPLOY PB-0008 이 완료 게이트였고 10 cycle 전건 통과했다(PR #1124/#1125/#1128/#1129/#1130/#1132/#1133/#1141/#1142/#1143). 최종 실측: styles.css(9,328) 소멸 + `css/` 7파일 · admin.js **14,007 → 4,804줄(-66%)** + `admin/` 9모듈(계 9,354줄) · app.js **11,119줄** + ES module 전환 + `app/` 4모듈(계 2,161줄). 재발 방지 = CONVENTIONS **§14 code-modularity** · ROADMAP ITEM-P5b done · Final 은 정적 자산 무접촉으로 배포 생략. **AC-3 부분 달성 thread 는 08-05 후속 Phase A~B3 로 해소** — 결합 매트릭스 실측(모듈-스코프 `let` 21개 × top-level 함수 297개)에서 도메인 경계를 넘는 양방향 재할당이 `_dqaDrag`·`_sidebarCatchupTimer` **2건뿐**임이 드러나, Phase A(그 2건만 `state` 편입·비-중립 mini-change·PLAN-APPROVED 08-05·계약 가드 `verify_state_intake.mjs`) 후 B1 사이드바(-720)·B2 composer(-1,891)·B3 progress 를 byte-동치 이동 → app.js **11,119 → 8,082줄(-27%)** · `app/` 6모듈, PR #1147~#1150 전건 배포·POST-DEPLOY PASS. 동반 pre-existing red 3종(`verify_admin_tab_gating.mjs` 2건 · `verify_perm_self_scope.mjs` · `win-browser-settings-notif.scenario.json` 의 `typeof openProfile` 전역 의존)도 feature-0003 harness-repair cycle(08-05)이 **전건 해소**(mjs 40/40 green + 시나리오 실 Windows Chrome 20스텝 OK). **잔여 thread**: ① admin.js **4,804줄**(공유 코어 — 계정/역할 권한 grid 인프라·pending/batch-apply 엔진·대시보드·탭 디스패치) 추가 감축 여부는 B 완료 후 재실측 판단(REPORT §8) ② app.js 8,082줄 안의 run-감지 동반 이동 ③ **`verify_*.mjs` 41개(Phase A 가 `verify_state_intake.mjs` 를 더해 40→41)가 여전히 make test 밖(CI 사각)** — red 자체는 해소됐으므로 남은 것은 배선 여부 결정이다 ④ 시나리오 17개의 `base_url` `:18080` stale + mention-hl-notify·notify-nobracket 시나리오의 죽은 전역 잔존(feature-0003 REVIEW 에 후속 후보로 이관 기록).
- 분석 오케스트레이션 트랙(feature-0031/0032/0033/0034, 07-30 머지): 4건 전부 `feature_status: in-progress`·**배포 후 라이브 대조는 feature-0032 만 완료**(PB-0008 PASS 2026-07-30)·**0031/0033/0034 는 잔여**(0031 TASK-0010 · 0032 TASK-0008 · 0033 TASK-0008 · 0034 TASK-0007 — 전부 `배포 → 라이브 확인` 단계에서 미체크). 후속 ROADMAP 잔여: ITEM-08(L3 도메인 합성 lazy) · ITEM-10(분석 검증층) · ITEM-11(결정적 플래너) 전부 pending.
- feature-0020: cycle-finalize 후 라이브 실배포(POST-DEPLOY §16.3 deploy-backed) — 첫 워커 핀 배포는 agent last-good 부재라 attended. gateway 상시 2-replica 는 메모리 예산 사람 결정 보류.
- feature-0011 P5a **종결(08-05)**: Step 6(feature 단위 Dockerfile 분리)은 **불채택**(ADR-20260805T153000-p5a-closeout — 분리의 전제였던 import 얽힘·경계 불명이 shared/ 추출 + CODEBASE_MAP §7 Known Gaps 로 해소되고, 이후 배포 스파인 feature-0014/0020/0039 가 단일 이미지를 전제로 안정화됐다). 재개 조건 = 이미지 크기·보안 요구가 실측으로 등장할 때 별도 initiative. 동반으로 P3-제외 로드맵 유보 3건을 전수 실측해 100% 검증(wiki 백필 기완료 실증·skeleton 기해소·PB-0008 통합 불채택) + wiki/README §5.1 대상 범위 명문화. app.py router 분할은 P5b 별도(07-01 전체추출 완료).
- feature-0012 완결(07-12 라이브 4c203f9c — 148 route 전량 byte-동치 추출+ITEM-10/11): 잔여 initiative ITEM-09(그래프 CSS/JS 세분화·외부 브랜치 blocked)만.
- feature-0010 활성화 cycle 이월: 라이브 토큰교환·refresh 회전·Google revoke·설정 UI·mcp_client seam 배선.
- insight-worker(TASK-0305) GRANT 적용 · NL2SQL few-shot A/B.
- baseline 추적 **해소(07-29)**: `make test` 가 운영 `.env` 상속 + compose 네트워크 잔존으로 **라이브 PG·라이브 런타임 설정에 직결**됐던 근본 원인 제거(Makefile `TEST_ISOLATION_ENV` + 루트 `conftest.py` 2중 방어, 97be4cda/770c436b) → 전량 **exit 0**(2976 passed / 2 skipped / 0 failed, 이전 13건). main 대조 실행 비용 소멸 — 재발 시 `AGENT_TEST_ALLOW_LIVE_BACKENDS` escape 여부를 먼저 확인.
