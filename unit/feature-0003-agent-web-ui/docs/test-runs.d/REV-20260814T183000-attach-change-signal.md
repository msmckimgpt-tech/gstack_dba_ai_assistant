---
run_at: 2026-08-14T19:55:00+09:00
session: ai/claude/feature-0002-agent-core
scope: feature-0002 attach-change-signal-server-authority — 프론트 축(composer.js 재수화 보존 + 전송 강등)
verdict: PASS
---

# Run — 첨부 ★신규 표식의 재수화 보존 / 전송 강등 (PB-0008, 머지 전)

- **Environment**: Windows-browser · **Runner**: AI · **Bridge**: relay `http://172.26.144.1:9223`
  (`win-browser.py doctor` → `Chrome/150.0.7871.128`)
- **대상**: 브랜치 코드 bind-mount 격리 컨테이너(`mysql-ai-web:ee4eb08d` + worktree `static/` 마운트,
  `https://localhost:18099`). 라이브 서비스 **무접촉** — 라이브는 같은 시각 구코드를 서빙했다
  (`keepNewIds` 라이브 0회 / 검증본 2회).
- **판정 기준**: DOM pill 이 아니라 **서버가 받은 `ask_jobs.payload.new_attachment_ids`**.
  pill 은 대화 전환 시 재렌더되지 않아 stale 을 읽는다(관측). 그래서 사용자에게 실제로 의미 있는
  축(= 프롬프트에 실리는 신호)을 ground truth 로 삼았다.

## 시나리오 · 결과 (전부 실 브라우저 조작, 최종 코드 기준)

새 대화 생성 → 파일 업로드(`attachFileInput`) → 전송, 그리고 대화 전환 왕복.

| # | 조작 | job | `new_attachment_ids` | 판정 |
|---|---|---|---|---|
| 1 | 새 대화 + 업로드 → 1턴 전송 | 700 | `[1203]` | ★신규 신호가 실린다 |
| 2 | 1턴 **완료 후** 2턴 전송 | 701 | `[]` | 강등 정상 — 영구 ★신규 없음 |
| 3 | 업로드 → 다른 대화로 전환 → 복귀 → 전송 | 702 | `[1204]` | **재수화 보존**(구코드는 `[]`) |

- 업로드 직후 pill: `session-source` 클래스 없음(★신규) + 배지 `1`.
  1턴 완료 후 pill: `session: true` 로 강등. 둘 다 화면에서 확인.
- 새로고침(F5)은 버킷 자체가 사라지므로 프론트 축이 아니라 **서버 파생**이 덮는 경로다 —
  실측에서도 새로고침 후 기존 첨부는 `session`(배지에서 제외)으로 정상 복귀했다.

## 실측 중 얻은 함정 2개 (기록)

1. **강등은 `/api/ask` 응답 시점에 일어난다.** 직전 턴이 끝나기 전에 다음 턴을 보내면 같은 id 가
   다시 실리는 것이 **정상**이다(그 시점엔 아직 전송 성공이 아니다). 이 타이밍을 결함으로 한 번
   오독했고, 완료 대기 후 재측정해 정정했다.
2. **단일 파일 bind-mount 는 inode 를 고정한다.** 편집이 파일을 교체하면 컨테이너는 옛 내용을
   계속 서빙한다 — "서빙본에 신규 심볼이 있다" 는 확인이 무관한 줄에 우연히 매칭돼 거짓 통과할
   수 있다(실제로 발생). 디렉터리를 마운트하고, 확인은 **해당 코드 블록 자체**를 출력해서 한다.

## 정리

검증용 대화·첨부는 전부 삭제했다(`/api/delete_conversation` 200). 격리 컨테이너 제거.
