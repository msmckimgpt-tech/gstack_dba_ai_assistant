---
run_at: 2026-09-10T13:30:00+09:00
session: claude:claude-corp:1199f9ef-2ad9-438d-95a3-fe8c0d7cf065 (implementation subagent)
scope: TASK-20260910-item03-product-atomic-create
verdict: PARTIAL
---

# ITEM-03 검증 — 비공개 Product 원자 생성 · 초기 접근 권한 · Description 길이

**현재 판정: 서버 계약과 프론트 행위는 실측 PASS. 실 DQA 클라이언트 Run 은 `NOT-RUN`.**
이 변경은 아직 배포되지 않았고(배포는 오케스트레이터 담당) 실 DQA 클라이언트는 사용자 Windows
머신에 있다. 아래 CLI·Node Run 을 실 클라이언트 PASS 로 **합산하지 않는다**(§15.4.1).

<!-- product-atomic-create: 행위 하네스 CI 미배선 — 아래 Node Run 이 실행 경로를 기록한다.
     `make test` 는 agent 이미지에 node·node-jsdom 을 설치하므로 컨테이너에서는
     `test_product_create_atomic.py::test_a12_behaviour_harness_runs_or_gap_is_documented`
     가 하네스를 실제로 돌린다. node/jsdom 이 없는 호스트에서는 그 테스트가 이 마커를 찾아
     «조용한 skip» 대신 «gap 이 기록됐는가» 로 강등 판정한다. -->

Environment: CLI
Result: PASS
Scenario: 서버 계약 — 비공개 생성의 생성자 grant · 단일 commit(감사행 포함) · 결손 주입 rollback ·
  grant 헬퍼가 INSERT 1행만(AST) · self-scope 가드 2곳 무변경 · 무권한 403 + 쓰기 0 ·
  길이 경계 양측(255/256 한글 · 128/129 이름 · 이모지 255/256) · 특수문자 6종 파라미터 저장 ·
  예외 분류 3분기(1062→409 / 1406→400 / 기타→500) 및 원문 미노출 · PATCH·접근DB 동일 상한 ·
  상수↔DDL↔프론트 3자 동치 · 복구 스크립트 9축
Evidence: `python3 -m pytest unit/feature-0003-agent-web-ui/tests/test_product_create_atomic.py -q`
  → **76 passed / 0 failed**. 관련 회귀
  (`test_product_list_rbac` · `test_model_access_rbac` · `test_product_multi_datasource_api` ·
  `test_product_delete_block_conv` · `test_web_perf_p1`) → **66 passed / 0 failed** (신규분 76 포함 합산 **142**).
  환경: 호스트 python3 + `PYTHONPATH=unit/feature-0002-agent-core/src:unit/feature-0003-agent-web-ui/src:.`
  (worktree `.worktrees/feature-0003-product-atomic-create`).
Boundary: DB 는 stub conn 이다 — 실제 MySQL 트랜잭션 의미론(격리 수준·락)이 아니라
  **핸들러가 발행하는 문장 순서·commit/rollback 횟수·파라미터**를 잠근다. 실 DB 왕복은
  배포 후 라이브 확인 항목(아래 「오케스트레이터 인계」).

Environment: CLI
Result: PASS
Scenario: 결손 주입 — 「방어를 넣었다」와 「방어가 성립한다」를 가른다
Evidence: 정본 `routers/admin_products.py` 에 실제로 뮤테이션을 적용한 뒤 실행하고 원복했다.
  ① `if affected < 1: raise` → `if False:` (영향행 0 검사 제거) → `test_a3b` **FAIL**.
  ② `if not default_role_access:` → `if False:` (생성자 grant 제거 = E-03b 결함 복원) →
  `test_a1` · `test_a3` · `test_a3b` **FAIL**(3건).
  ③ `conn.autocommit = False` → `pass` (**트랜잭션 자체 제거**) → **18건 FAIL**.
  세 뮤턴트 모두 KILL, 생존 0.
Boundary: 뮤턴트 표본은 내가 고른 3종이므로 «사각지대 없음» 의 증거는 아니다.

## ⭐ 적대 검증이 잡은 검증 맹점 — ③ 뮤턴트는 처음에 **생존했다**

qa 패널이 결손 주입으로 실증했다: 초기 stub 의 `commit()`/`rollback()` 은 `conn.autocommit`
과 무관하게 문자열만 적재했으므로, 정본에서 `conn.autocommit = False` 를 지워도 **70/70 전건
통과**했다. 즉 stub 은 「`.commit()` 을 몇 번 불렀나」만 셌고 「트랜잭션이 존재했나」는 보지
못했다 — 실 MySQL 이라면 각 INSERT 가 즉시 확정되고 이후 rollback 이 무효라, ITEM-03 이
없애려던 고립(E-03b)이 그대로 재현되는데도. **이 ITEM 의 중심 주장(원자성)이 검증되지 않은
상태였다.**

수정: `_Cursor` 가 쓰기 문장을 만나면 `conn.autocommit` 을 확인해 `True` 면 raise 한다
(+`dictionary=True` 커서 지원 — 미지원 상태로 두면 `is_default` 경로를 덮으려는 다음 사람이
test-double 인공물에 걸린다). 같은 뮤턴트를 재실행해 **18건 FAIL** 로 KILL 됨을 확인했다.

부수 효과로 그 감시가 **선재 결함을 하나 드러냈다**: `admin_update_product_databases` 의
`DELETE`→`INSERT` 루프는 `autocommit=False` 를 걸지 않는다(backend 패널 P2 와 동일 지점).
ITEM-03 은 그 루프에 **도달하는 과길이 입력**만 상한 정합으로 막았고 루프의 원자화는 승인
범위 밖이라, 그 사실을 `test_a9b2` 가 `pytest.raises` 로 **보이게** 못박았다 — 원자화되면
그 테스트가 먼저 깨져 갱신을 요구한다.

Environment: CLI
Result: PASS
Scenario: 복구 스크립트 **라이브 dry-run** (읽기 전용 — 배포된 web replica 의 실 DB)
Evidence: `bash bin/product-access-repair.sh` → 래퍼가 `repo-web-a-1` 로 stdin 전달 후 실행
  (세션 중 병행 세션이 web 을 재배포해 replica 세대가 `mysql-ai-web:1c620a97` 로 바뀌었으므로
  **재배포 후 같은 진단을 다시 돌려** 같은 결과임을 재확인했다 — 래퍼가 스크립트를 stdin 으로
  넘기므로 이미지 세대와 무관하게 동작한다).
  요구서·EVIDENCE E-03a 가 지목한 고립 제품을 **실제로 찾았다**:
  `#990002 DK_KR_S3_20260903 'DK/KR 시즌3 하드코어 - 20260903 복원본' active=N
  default_role_access=N code=product.access.dk_kr_s3_20260903 reason=no_grantee ·
  생성자 후보 account=1(bootstrap_admin) @2026-09-09 13:49:08.779000` (grantees=0,
  permission_id=204682). `--active-only` 는 0건, `--json` 은 같은 내용을 기계 판독으로 출력.
  `--apply --product 990002`(계정 미지정) → 거부 + **rc=1**.
  **쓰기 0 확인**: 시도 후 재진단에서 `grantees: 0` 그대로이고, 라이브 DB 직접 조회로
  `WebAuditEvents WHERE ActionCode='admin.product.access.repair'` = **0행**,
  `WebAccountPermissionOverrides WHERE PermissionId=204682` = **0행**.
Boundary: **`--apply` 는 실행하지 않았다.** 복구 대상 계정 지정은 운영자 결정이다(§12.3).
  이 Run 은 진단·거부 경로만 실측했고, 복구 경로의 실 DB 왕복은 미검증이다.

Environment: Node
Result: PASS
Scenario: 프론트 **행위** — 카운터 갱신·경계 양측 `.is-limit` · 상세 pane 의 실제 maxLength ·
  생성 흐름의 POST body(`description` · `default_role_access`) · 설명 초과 시 **입력 보존 재질문** ·
  접근 범위 오답 시 재질문(임의 해석 0) · 모든 단계의 취소가 POST 0 · 서버 400 사유의 토스트 노출
Evidence: `node unit/feature-0003-agent-web-ui/tests/verify_product_create_flow.mjs`
  → **50 passed / 0 failed** (jsdom@22 `/tmp/node_modules`, Node v18.19.1).
  같은 하네스가 realm 스크립트 오류 0건도 단언한다.
Boundary: jsdom 은 픽셀을 보지 않는다 — 카운터의 배치·색(`.admin-char-counter.is-limit` 의
  `--danger`)과 `prompt` 3연속의 실사용 체감은 실 클라이언트에서만 판정된다.

## 이 검증이 실제로 잡은 것 (실측이 소스 대조보다 앞선 지점)

0. ⭐ **진단 도구의 기본 모수가 자기 목적을 배제하고 있었다.** 초기 구현의 기본값은
   「활성 제품만」이고 비활성은 `--include-inactive` 로만 보였다. 라이브 dry-run 을 돌리자
   기본 실행이 **「고립 Product 없음」**을 출력했다 — 요구서가 지목한 990002 가 비활성이기
   때문이다. 즉 이 도구가 만들어진 이유인 데이터를 기본 실행이 숨겼다. 단위 테스트는 전부
   통과하고 있었다(그 테스트가 「비활성은 기본에서 제외된다」를 *의도된 동작*으로 고정하고
   있었으므로). 기본을 **전부**로 뒤집고 좁히는 쪽(`--active-only`)만 flag 로 남겼으며,
   테스트도 「비활성 고립이 기본 진단에 나온다」를 단언하도록 반대로 고쳤다
   (`test_a11c` · `test_a11h2`). AGENTS §16.7 G12(게이트 모수 = 노출면 전체)의 사례.

1. `renderProductDetail()` 은 **인자를 받지 않는다**(`adminState` + `#productDetail` 앵커를 읽는다).
   처음 작성한 하네스가 인자를 넘겨 호출해 입력이 0개였고, 그래서 「maxLength 가 붙었는가」가
   조용히 vacuous 하게 통과할 수 있었다 — 앵커·상태를 실제 화면대로 갖춰야 판정이 성립한다.
2. `tests/esm-classic-inject.mjs` 의 ESM 제거기가 **재export-from**
   (`export { a } from "./x.js";`)을 지우지 않아 그 줄이 남은 classic 스크립트 전체가
   SyntaxError 로 죽었다. jsdom 은 그 오류를 throw 하지 않고 virtualConsole 로만 흘리므로
   증상이 «`gateLlmControl` 이 undefined» 로 위장됐다. 제거 규칙을 보강하고, 하네스가
   **realm 스크립트 오류 0건**을 단언하게 해 같은 위장이 다시 조용히 지나가지 못하게 했다.
   (같은 helper 를 쓰는 다른 하네스 **10개** 재실행 — 회귀 0.)

## NOT-RUN — 실 DQA 클라이언트 (AC-03-6)

Environment: DQA-client
Result: NOT-RUN
Scenario: 관리 콘솔 > 제품 > 「+ 새 제품」 에서 비공개 제품 생성 → 생성 직후 작업 화면 제품
  선택기에 그 제품이 보이는지 · 제품 상세 설명에 256자 입력 시도(카운터·maxLength) ·
  「모두 적용」 실패 토스트의 사유 노출과 입력 보존
Reason: 이 변경이 **아직 배포되지 않았다**(서버 자산은 배포된 이미지에서 서빙된다 —
  §16.5.1 대로 PR·머지·배포는 오케스트레이터가 수행한다). 또한 실 DQA 클라이언트는 사용자
  Windows 머신의 설치 앱이며 본 위임 세션은 격리 worktree 에서만 작업한다.
대체 검증: 위 Node Run(jsdom 행위 50축) + CLI Run(서버 계약 76축). **이것을 실 클라이언트
  PASS 로 승격하지 않는다.**
후속 조건: 배포 직후 PB-0009 절차로 위 Scenario 3축을 실측하고 그 결과를 이 fragment 에
  append 한다(Environment: DQA-client · Result 명시).

Environment: CLI
Result: PASS
Scenario: feature-0003 전체 회귀 + 컨테이너(정본 실행 환경) 귀책 대조
Evidence: 호스트 전체 — `python3 -m pytest unit/feature-0003-agent-web-ui/tests`
  → **2219 passed / 7 failed**(전부 아래 선재 1건). 종료 코드를 파이프 끝단에 삼키지 않고
  직접 포획했다(§16.7 G14-f).
  컨테이너(`repo-unittest-agent:latest`, `make test` 와 같은 이미지·PYTHONPATH) —
  `test_share_redaction_invariant.py` + `test_product_create_atomic.py` + `test_web_perf_p1.py`
  → **100 passed, PYTEST_RC=0**. 즉 호스트에서 실패한 7건은 컨테이너에서 전건 통과한다.
Boundary: `make test` 자체는 이 worktree 에서 돌지 않는다 — compose 가 읽는 `.env` 는
  공유 `repo/` 에만 있어 `dc-build` 가 `no port specified` 로 죽는다(선재 제약, 프로젝트
  메모리에도 기록됨). 그래서 같은 이미지·같은 PYTHONPATH 로 `docker run` 해 등가 실행했다.

## 알려진 선재 실패 (이 변경과 무관 — 귀책 대조 결과)

- `tests/test_share_redaction_invariant.py` 7건 — 호스트 직접 실행에서 `ModuleNotFoundError:
  No module named 'web'`. 이 파일은 컨테이너 레이아웃(`/app`, `uvicorn web.app:app`)을 전제한다
  (`_imports()` 가 `sys.path.insert(0, "/app")` 로 폴백). **컨테이너에서 7건 전건 통과**를
  실측했으므로(위 Run) 내 변경의 회귀가 아니다.
- `unit/feature-0043-external-llm-bridge/tests/test_unknown_is_not_ready.py::test_the_fail_open_initial_value_is_gone_from_source`
  — **컨테이너 전체 회귀에서만** 1건 실패. 이 diff 의 산물이 아니라는 근거 4가지(모두 실측):
  ① 내 커밋은 feature-0043 파일을 **0개** 바꿨고 `shared/**` 도 무변경
     (`git diff --name-only bca600df^ bca600df -- unit/feature-0043-external-llm-bridge/` → 0).
  ② 그 테스트를 **단독** 실행하면 base `b7aa2ac0` 에서 통과(`BASE_PYTEST_RC=0`).
  ③ 그 테스트를 **단독** 실행하면 내 트리에서도 통과(`RC=0`).
  ④ `unit/feature-0043-external-llm-bridge/tests` **전체**를 내 트리에서 격리 실행하면 통과
     (`RC=0`) — feature-0003 테스트가 전혀 개입하지 않은 상태다. 또 내 신규 테스트 파일을
     **바로 앞에** 붙여 실행해도 재현되지 않았다(`RC=0`).
  즉 전체 회귀에서만 나타나는 **교차-feature 실행 순서/오염 상호작용**이며 이 변경과 독립이다.
  ⚠️ **다만 base 의 «전체 회귀» 재현은 완료하지 못했다** — 그 실행을 내가 중단시켰다(rc=137).
  따라서 「base 에서도 같은 전체 회귀 실패가 난다」는 **주장하지 않는다**. 최종 판정은
  오케스트레이터의 PR CI(전체 회귀)에 맡긴다.
- ruff `F821 Undefined name _CSS_DIR` in `tests/test_conn_chip_css_scope.py` — base `b7aa2ac0`
  의 같은 파일에서도 동일하게 나온다(`git show b7aa2ac0:… | ruff` 로 확인). 내가 추가·수정한
  파일은 ruff clean.
- `tests/verify_folder_dnd_shared_group.mjs` — `buildCompactItem` 에서 TypeError. **원본
  `esm-classic-inject.mjs`(git HEAD)로 되돌려 재실행해도 동일하게 실패**하므로 이번 변경의
  회귀가 아니다(선재).

## 라이브 DB 미수행 (명시)

`bin/product-access-repair.sh --apply` 를 **라이브 DB 에 실행하지 않았다.** 고립 Product
990002 의 복구 대상 계정 지정은 운영자 결정이며(§12.3), 스크립트도 자동 선택을 거부한다.
진단(dry-run)은 라이브에서 실행했고 — 래퍼가 스크립트를 stdin 으로 넘기므로 배포 이미지에
이 변경이 없어도 현재 배포본에서 그대로 돌았다 — **쓰기는 0행**임을 재진단과 DB 직접 조회로
확인했다(위 CLI Run). 절차는 `docs/RUNBOOK-product-access-repair.md`.

**오케스트레이터 인계**: 990002 복구는 「그 제품을 실제로 써야 하는 계정」을 사용자가 정한 뒤
`bin/product-access-repair.sh --apply --product 990002 --grant-account <계정Id>` 로 수행한다.
감사 로그가 지목한 생성자 후보는 account=1(`bootstrap_admin`)이지만 그것은 참고값이다.
이 제품은 `active=N` 이므로 접근 부여만으로는 사용할 수 없다 — 필요하면 제품 활성화가 동반된다.
