---
run_at: 2026-09-07T03:00:00+09:00
session: session-cookie-lifetime (ai/claude/feature-0003-session-cookie-lifetime)
scope: 세션 쿠키 수명 — 앱 창에서 로그인이 유지되는가
verdict: PASS (원인 실측 확정 · 단위·뮤테이션) / 앱 창 완주는 배포 후
---

### Run (2026-09-07) — 원인 확정 — **Environment: Windows (사용자 프로필 직접 열기)**

사용자 창을 닫은 뒤(승인받음) **그 WebView2 프로필로** 창을 열어 측정했다.

| 항목 | 값 |
|---|---|
| 영구 쿠키 저장소(`Cookies` SQLite) | **0건** |
| `profileName` | `—` (미로그인 플레이스홀더) |
| `/api/ai/connect/status` | `200 {"logged_in": false}` |
| 상태 칩 | `ai-conn hidden` |

⚠ 이 측정이 결정적이었다. 그 전까지는 전부 **브라우저**에서만 재현했고, 브라우저에서는
세션 복원이 결함을 가려 준다.

### Run (2026-09-07) — 계약 — **Environment: agent-container pytest**

- 신규 6건 PASS(실제 `Response` 에 쿠키를 심어 `Set-Cookie` 헤더를 읽는다 — 소스 검사 아님).
- 뮤테이션 **5/5 KILL**: 수명 제거 · 수명=슬라이딩 창 · `HttpOnly` 제거 · `Secure` 끔 ·
  `SameSite=none`.
- 실제 헤더: `mysql_ai_session=…; HttpOnly; Max-Age=7776000; Path=/; SameSite=lax; Secure`.

### Run (2026-09-07) — 앱 창 완주 — **Environment: Windows-browser (배포 후로 이연)**

- 계획: 배포 → 앱 창에서 **다시 로그인** → 트레이 [종료] → 재실행 → 로그인 유지 · 칩 표시.
- ⚠ 이미 발급된 쿠키에는 수명이 없으므로 **한 번은 다시 로그인해야** 한다.
