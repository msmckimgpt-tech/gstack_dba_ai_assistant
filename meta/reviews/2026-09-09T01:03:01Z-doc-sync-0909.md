# doc_sync 09-09 — META(wiki+정책문서) 정합 리뷰 아티팩트

- Timestamp: 2026-09-09T01:03:01+09:00
- Branch: `ai/claude/doc-sync-20260909-010301` · base HEAD `2249addf` (== `origin/main`)
- Meta-Cycle: `doc-sync-20260909` · META-0075

## 1. 창 정의
직전 doc_sync META `84bda1ce`(09-08, META-0073) 이후 `84bda1ce..2249addf` — **53 non-merge / 47 merge · 변경 파일 397**, 착륙일 전량 2026-09-08.
무게중심: `feature-0046-native-client` 155파일 · `feature-0003-agent-web-ui` 96 · `feature-0043-external-llm-bridge` 51 · `feature-0002-agent-core` 22 · `feature-0041-external-ai-tool-surface` 6.
신규 `unit/feature-*` **0개** → 기능 수 46 불변.

## 2. reconcile-first (자가수리 ①)
doc delta 계산 **전에** 서빙 static 파리티를 실측했다 — 라이브 `https://localhost/static/release-notes-data.js` 가 `origin/main` blob 과 **바이트 동일**(sha256 `d8b4285e60b2629c…`, 465,521 bytes) · 라이브 캐시토큰 `?v=94c87eec12e1`(빌드 주입, 소스는 `?v=dev` 고정) · `artifacts/deploy/deploy-web.state` `current=2249addf` = HEAD. **파리티 갭 0** → early-exit 예외 불발동, doc delta 정합만 수행.

## 3. 방법
4축 병렬 sweep(wiki 미러 / 정책문서 / 릴리즈노트 / 크로스-표면 폐기값·이월 재실측) + 2조 적대 교차검증(각 조가 상대 2축을 재측정, MISS 탐지 명시 지시). 6에이전트 · 1,255,303 토큰 · 도구호출 346 · 38분.

## 4. 판정
- 축 산출 80건(wiki 33 · cross 31 · docs 14 · rn 2) + 검증조 MISS 11건.
- CONFIRMED 68 · CORRECTED 10 · REFUTED 2(`X-12`, `W-12`) · report-only 14.
- **span 충돌 7건**: `D-02`≡`X-01` / `D-10`≡`X-23` / `D-11`≡`X-05` 는 바이트 동일 중복 → 각 1건만. `D-09` → `X-20` 채택(D-09 는 같은 줄에 폐기 괄호를 남긴다). `X-17`·`X-18`·`X-22` → `D-08` 병합안(CORRECTED)에 흡수.
- 최종 META 적용 **58건** / 릴리즈노트 별도 커밋 2건(블록 + `generated`).

## 5. 적대 검증이 잡은 것 (대표)
- `RN-R-01` CORRECTED — 아이콘 항목 제목의 `(1.1.1 · 1.1.2)` 가 사실오류다. 1.1.1 은 러너 복구로 먼저 게시된 별개 릴리스이고 아이콘은 **1.1.2 로만** 나갔다(정본 `feature-0043 REPORT.md` · `MODIFY.md` · 머지 `6bf4ace3`). 또 직전 3블록은 제목에 버전 라벨을 쓴 적이 없다 → `(1.1.2)` 로 축약.
- `W-04` — hot.md thread 가 「1.1.2 게시 후 실측」이라 적었으나 딥링크 `show.path` 수용 커밋(`800bd16f` 20:05)이 마지막 버전 bump(`cdd7f003` 15:17)보다 **뒤에** 착륙했다 → 게시된 1.2.4 에도 그 코드가 없고 조건은 「1.2.4 다음 릴리스」다.
- `D-08` CORRECTED — `docs/CODEBASE_MAP.md` 11줄 span 의 수치 6건을 검증조가 전건 재측정(routers 40 · route-module 30 · `_` 접두 9종 · admin.js 4,943 · app.js 9,085 · admin/ 12)하고 X-17/18/22 를 흡수한 병합안을 냈다.

## 6. 검증 결과
| 게이트 | 결과 |
|---|---|
| `bin/wiki-lint.sh` | total **32** (WARN-only, baseline 동일) |
| `bin/ssot-lint.sh` | **4 WARN** (tracked secret 백업 4, ADR-0031 §4 rotation 대기 — baseline 동일) |
| `bin/gen-status.sh --check` | **rc=2 — 선재 정본 결함**(§7 참조), 이 run 이 만든 것 아님 |
| 카운트 정합 | `ls -d unit/feature-*` 46 = wiki 카드 46 = Features MOC 46 = STATUS 46행 |
| ADR 색인 | 정본 실측 `## ADR-` heading 47 · timestamp+slug 15 = 3 미러 표면 정합 |
| `Log.md` | append-only 준수(신규 H2 1건 append, 과거 entry 무변경) |
| Log/hot wikilink | 경로형 리터럴 **0** · glued 파이프 신규 도입 **0** |
| `verify-completion` META mode | check #9 = 본 REVIEW entry |

## 7. report-only (정본·소유자 소관 — 미러만 고치면 재발한다)
- **ⓐ `gen-status` 차단 (신규·high)**: `unit/feature-0046-native-client/docs/TASK.md:6` `feature_status: implemented` 무효값. `docs/STATUS.md:122` 생성물 구간 행이 수기 오염(`review`, 날짜·요지 공란)돼 어느 frontmatter 값과도 무관하다. **owning feature cycle 이 frontmatter 를 유효값으로 고치고 `bash bin/gen-status.sh` 를 재생성해야** 표가 파생값으로 복귀한다. 직전 창(META-0073)이 같은 회귀를 기록 → **2창 연속**.
- **ⓑ `docs/SECURITY.md` 의 feature-0046 위협모델 절**: 창의 `800bd16f` 가 `/api/public/share/{token}` 행에 공유-앱 진입(딥링크 무토큰 · 미서명 설치기 안내 · 표시이지 집행 아님)을 적재해 **엔드포인트 표 축은 갱신됐다**. 그러나 전용 절은 여전히 없고 마지막 절은 §49 다. 소유권은 doc_sync 가 아니다 — 절 신설은 미러 정합이 아니라 정본 저술이라 §18.8.1 적대 패널을 요구한다(같은 창의 뒤 판정 `meta/REVIEW.md` REV-20260904T190701 · ROADMAP ITEM-09 guard 6 이 착수 cycle 로 이관).
- **ⓒ feature-0046 `FUNCTION.md` 내부모순** · **ⓓ 연결 체크리스트 단계 수(`TASK.md` 5단계 vs 코드 key 4개)** — 정본 내부모순이라 미러에 개수를 쓰지 않고 단계 이름만 쓴다.
- **ⓔ 오케스트레이터 지시문의 캐시버스터 수기 bump 요구는 stale** — `docs/CONVENTIONS.md` §14.1 이 「`?v=dev` 고정 · 빌드 `inject_asset_stamp` 가 content-hash 주입 · **수기 bump 금지**」다. 실측 정합(소스 `?v=dev` / 라이브 `?v=94c87eec12e1`)이고 직전 RN 커밋도 두 HTML 무접촉. 수기 실값은 배포 ABORT 가드를 무력화하므로 **bump 하지 않았다**.

## 8. 소유권 경계
landing(push/merge)·배포 소유권은 **wrapper** 다(실행환경 명시 위임). 본 run 은 로컬 commit + verify pre-commit 까지 수행하고 push/merge/deploy 를 하지 않았다 — 이중 landing·배포 racing 방지(자가수리 ④).
