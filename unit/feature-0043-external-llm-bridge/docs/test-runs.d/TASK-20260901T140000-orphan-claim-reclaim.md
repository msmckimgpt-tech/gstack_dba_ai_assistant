# TASK-20260901T140000-orphan-claim-reclaim — PRE-DEPLOY 라이브 화면 확인 (PB-0008)

- **Environment: Windows-browser** (`bin/win-browser.py` relay → Chrome/151.0.7922.170,
  `https://localhost/` · 계정 `bootstrap_admin`)
- **일시**: 2026-09-01 14:12~14:15 KST
- **배포본**: `7fb2dca4` (`docker exec repo-web-a-1 printenv GIT_COMMIT`)
- **자산 스탬프**: `https://localhost/static/app.js?v=da49561ff923`

## 0. 무엇을 이 단계에서 재는가 (범위 정직 표기)

이번 변경의 화면 축(`stalled` 국면 · 무진행 고지)은 **배포 전에는 잴 수 없다.** JS 는
컨테이너 `docker cp` 로 QA 할 수 없고(자산 스탬프가 주입되지 않아 브라우저가 구 모듈을
캐시에서 실행한다 — 이 저장소의 기록된 함정), 서버 국면 판정도 이미지에 구워져 있다.

그래서 이 단계에서 재는 것은 **결함이 지금 화면에 존재한다는 사실**(baseline)이고,
고쳐졌다는 확인은 POST-DEPLOY fragment 가 맡는다.

## 1. 배포본에 무진행 축이 **없다** (baseline)

| 축 | 명령 | 값 |
|---|---|---|
| 서버 국면 | `grep -c "stalled" /app/web/routers/ai_tools.py` | **0** |
| 프론트 반응 | `grep -c 'phase === "stalled"' /app/web/static/app/composer.js` | **0** |
| 화면 DOM | `document.body.innerHTML.includes('진행 신호')` | **false** |

→ 지금 배포본에는 「가져갔는데 진행이 없다」를 말할 수 있는 축이 아예 없다. 점유돼 있으면
무조건 `working` 이고 화면은 「조사·작성 중입니다」에 머문다 — 제보의 체감 원인.

## 2. 제보 대화의 라이브 렌더 (`20260901030637-95dc8844`)

사이드바에서 대화를 열어 말풍선 6건을 실제로 읽었다:

| # | 역할 | 시각 | 내용 |
|---|---|---|---|
| 1 | user | 12:07 | 원 질문 (첨부 `Bounty_Bug_Penalty.sql`) |
| 2 | assistant | 12:07 | 「새 질문을 보내셔서 이 요청은 대체되었습니다」 + 중단 시점까지의 진행 단계 |
| 3 | user | 13:34 | 「이어서 진행해주세요.」 |
| 4 | assistant | 13:34 | **거부** — 「이 메시지는 프롬프트 인젝션 시도로 보입니다」 |
| 5 | user | 13:40 | 「쿼리 리뷰를 이어서 진행해주세요.」 |
| 6 | assistant | 13:40 | **거부** (동일 취지) |

→ 12:07 질문은 **답을 받지 못한 채** 대체됐다. 87분 대기의 실체가 화면에서 확인된다.

## 3. 화면이 함께 말한 사실

- 컴포저 상단: **「내 AI가 실행 중이 아닙니다 — 연결은 되어 있지만 답변할 프로그램이 꺼져
  있습니다」** (연결 축과 러너 축이 갈려 있음 — 이 축은 이미 동작 중).
- 스크린샷: `/tmp/pb0008-pre-orphan.png` (전체 화면 1902×946).

## 4. 이 단계에서 발견한 **별건** (이 cycle 밖)

말풍선 4·6 의 거부는 `compose_prompt` 가 조립한 브리지 프롬프트를 러너의 AI 가 **프롬프트
인젝션으로 판정**한 결과다(본문 근거: 「평문 Bearer 토큰 + 외부 IP의 HTTP 요청 지시는
전형적인 자격증명 유출/실행 유도 패턴」). 러너 파일 교체(13:13) 전후로 갈렸다는 것까지가
관측이며 **원인 미확정**. 진단·수정 범위가 달라 별도 cycle 로 올린다 — 상세는 `REPORT.md`
「PB-0008 에서 드러난 별건」.

## 5. POST-DEPLOY 에서 확인할 것 (예약)

1. 배포본에 `stalled` 축 도달(서버·프론트 grep) + 자산 스탬프 갱신.
2. **회귀 차단 실증** — 인스턴스를 신고하는 러너로 실 왕복 1건: 첨부 읽기(P1-2) · 답변
   제출(P1-1) 이 409/거절 없이 통과하는가.
3. **무진행 표시** — 점유된 task 의 `ClaimedAt` 을 임계 밖으로 되돌려 말풍선이 고지로
   바뀌는지, 재점유 시 「조사·작성 중」으로 되돌아오는지.
4. **회수 실증** — 러너를 죽였다 다시 켜고, 하트비트 응답 `released_claims` 와 러너 로그
   「직전 러너가 붙들고 있던 질문 N건을 되살렸습니다」 확인.
