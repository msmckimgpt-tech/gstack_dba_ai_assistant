---
run_at: 2026-07-29T12:00:00+09:00
session: ai/claude/feature-0021-review-continuation
scope: 배포(13d047ba) 후 회귀를 실제로 유도해 측정 + 과교정 가드 추가 (CHG-20260729-0002)
verdict: PASS
---

### Run 1 — 라이브 유도 (Environment: **라이브 서비스**, 로그인 세션으로 `POST /api/ask` 2턴)

사용자 요청("라이브 측정 또한, 유사한 이슈를 유도하면서 검토")에 따라 **실제 대화를 태웠다**.
새 대화 `20260729024453-3a608d3b`, 추론 강도 **매우높음**(원 사고와 동일):

| 턴 | 입력 | 답변 | red-team 판정 |
|---|---|---|---|
| 1 | "아래 쿼리 리뷰를 진행해주세요 …" + SQL 본문 | **6,068자** 실질 리뷰 | id=137 `pass` · rounds=0 · WARN 2 |
| 2 | **"네 맞습니다."** | **797자** — 사전 검증 쿼리·FK 제약·배치 DELETE·다음 단계 제시 | id=138 `pass` · **rounds=0** · BLOCK **0** |

**수정 전 대조**(같은 구조, 배포 이전 라이브 기록):

| | 수정 전 (run #132) | 수정 후 (run id=138) |
|---|---|---|
| 수정 라운드 | **14** | **0** |
| scope BLOCK("'네 맞습니다'만 입력했는데…") | 발생 | **없음** |
| 최종 답변 | **152자** 비-답변("필요하시면 말씀해주세요") | **797자** 실행 가능한 continuation |
| 사용자 후속 | 같은 요청을 **다시 입력해야 했다** | 불필요 |

턴2 판정의 findings 는 WARN 2건뿐이며 내용도 정당하다("DB 현재 상태를 검증하지 않았음을 명시
하지 않음", "이전 턴의 개선 쿼리를 참조하나 evidence 범위 밖"). **결함을 못 잡은 것이 아니라
파괴적 오판만 사라졌다** — 이 구분이 중요해 WARN 원문을 함께 남긴다.

### Run 2 — 인과 확정 A/B (Environment: 배포 컨테이너 내 `run_review` 직접 호출)

라이브 turn-2 초안·모델·evidence 를 **고정**하고 `CONVERSATION REQUEST` 유무만 토글(N=3):

| 지표 | CONTROL(요청 맥락 없음) | TREATMENT(요청 맥락 있음) |
|---|---|---|
| `grounding` 오판 ("SQL 근거 없음" — SQL 은 턴1 사용자 메시지에 있었다) | **2** | **0** |
| `completeness` 과교정 ("요청한 리뷰를 안 줬다") | 0 | **2** |

→ D1/D3 교정 효과는 확인(grounding 오판 소멸). 동시에 **반대 방향 과교정**이 드러났다:
리뷰어는 이전 턴을 보지 못하므로 "이미 전달한 리뷰"를 알 수 없어, continuation 답변을
"요청 미이행"으로 판정한다.

### Run 3 — 과교정 가드 재측정 (동일 조건, 프롬프트 규칙만 추가, N=4)

continuation 규칙 추가 후: `pass` 3 · `revise` 1 → **과교정 2/3 → 1/4**.

- 잔여 1건은 완전 제거하지 않았다. 더 밀면 **진짜 completeness 결함**을 놓치기 시작한다
  (이번 회귀의 대칭 반대편). 두 오류의 비용이 비대칭이라는 점이 근거다 — 과답변 오판은 답변을
  **파괴**했지만, 과교정 오판은 답변을 **늘릴** 뿐이고 붕괴 가드가 파괴를 막는다.
- 회귀 스위트: 전체 **2883 passed / 0 failed** · ruff clean.

### 미커버 (정직 표기)

- **N=3/4 는 작다.** 리뷰어 판정은 확률적이라 위 수치는 *방향성 근거*이지 정밀 추정이 아니다.
- **단일 대화·단일 초안** 측정이다. 다른 도메인에서 같은 비율이 나온다는 보장은 없다.
- **첨부(D3) 는 end-to-end 로 태우지 못했다** — 유도 대화는 SQL 을 본문에 붙여넣었다(첨부 업로드
  자동화 미구현). 첨부 경로는 A/B 에서 "초안↔첨부 불일치를 리뷰어가 검출" 하는 것으로 간접
  확인했다(가짜 첨부를 넣자 grounding BLOCK 이 정확히 발생 = 첨부가 리뷰어에 도달함).
- 장기 지표(`revision_rounds` 분포의 14 같은 꼬리 소멸, `revise_collapsed` 빈도)는 트래픽 누적
  후 `redteam_reviews` 로 재평가한다.
