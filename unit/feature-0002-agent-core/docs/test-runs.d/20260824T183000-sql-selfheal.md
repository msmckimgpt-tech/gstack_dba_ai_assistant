---
run_at: 2026-08-24T18:30:00+09:00
session: ai/claude/feature-0002-sql-error-selfheal
scope: SQL 실행 오류 자가수정(표적 지목·반복 감지) + 상한 소진 시 오귀인 차단
verdict: PASS
---

### Run 2026-08-24 — 20260824T1830-sql-selfheal (Major §12.3) — **Environment: unit + 라이브 DB 실측(진단)**

백엔드 전용 — 웹/UI 자산 변경 0 → §15.4.1 / CHECK#13 Windows-browser 미해당.

**진단 (라이브 실측)**: PG `agent_runtime` 에서 대화 `20260824085807-a761f842` 의
`core_messages`(8839~8846) · `steps`(4·6·8) 를 직접 조회해 실패 SQL 전문·오류 원문·최종 답변을 확보.
MySQL `agent_memory.WebSystemPrompts` census 로 `Scope='global'` row 1건 실재 확인(적용면 판정 근거).

**단위 테스트**
- `tests/test_sql_error_selfheal.py` 신규 **25 PASS** — 실측 오류 원문(`LIVE_ERR_1/2`) 기반.
- `tests/test_self_reflection.py` 기존 **6 PASS** (선행 계약 회귀 없음).

**결함 주입 실증 (§16.7 G11-b) — 6종 전부 KILL**

| 주입 | 결과 |
|---|---|
| M1 focus 추출 무력화 | KILL ×2 |
| M2 예약어 처방 제거 | KILL ×2 |
| M3 시그니처 상수화 | KILL ×2 |
| M4 코드-주입 directive 제거 | KILL ×2 |
| M5 반복 격상 억제 | KILL |
| M6 정직성 문단 삭제 | KILL |

초판 단언 2건이 M1·M2 를 **MISS** 했다 — `current_time` 은 원 SQL 에도, "예약어" 는 일반 힌트
문구에도 있어 통과(§16.7 G11-a 와 동형의 약한 단언). 고유 표식으로 강화 후 재실증하여 전부 KILL.

**전체 스위트 회귀 대조**
- 컨테이너 경로(`make test`)는 **DNS 단절**로 이미지 빌드 불가 → 호스트에서 `TEST_ISOLATION_ENV`
  동등 env(라이브 DB 차단)로 오프라인 실행.
- main baseline **13 FAIL** = 본 브랜치 **13 FAIL**, 동일 집합(`test_scratch.py` 6 ·
  `test_share_redaction_invariant.py` 7) → **회귀 0**. 해당 13건은 호스트 실행의 환경성 실패로
  본 변경 이전부터 존재하며 별건이다.

### Run 2026-08-24 (2차) — 자체 적대 리뷰 후속 (P1 2건 수정) — PASS

- 신규 **28 PASS**(25 + 회귀 3: focus 정제 · 정상 식별자 보존 · dialect resolver 생존) + 기존 6 PASS.
- **결함 주입 9종 전부 KILL**(M1~M9). 추가분:
  M7 `_sanitize_focus` 무력화 → 신뢰경계 우회 테스트 KILL /
  M8 dialect resolver 를 dead code 로 복원 → KILL /
  M9 정제 과잉(정상 식별자 훼손) → KILL.
- 검증 채널: `codex review` 는 호스트 외부 DNS 단절로 도달 실패, subagent panel 은 세션의
  상위 우선순위 도구 제약으로 미호출(§18.8.2 carve-out). 자체 적대 읽기로 발견한 2건은 모두
  **실행으로 재현 확증**한 뒤 수정했다. 독립 제3자 판정은 미수행 — 미검증 범위로 명시한다.
