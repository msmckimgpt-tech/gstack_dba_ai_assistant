---
run_at: 2026-07-28T16:04:00+09:00
session: ai/claude/feature-0016-graph-label-lod
scope: unit/feature-0003-agent-web-ui/src/static/graph (graph-state·graph-core·graph-renderer-pixi)
verdict: PASS
---

### Run (2026-07-28) — graph-label-lod: 과도한 줌아웃 시 라벨(글자) 붕괴 최소화 — **Environment: Windows-browser**

> 정본 TASK/DECISION 은 `unit/feature-0016-metadata-graph/docs/TASK.md` `## 20260728T1604-graph-label-lod`.
> 본 파일은 **파일 소유 feature**(feature-0003, static/graph) 쪽 Run 상세다.

#### 1. 사용자 질의

> 그래프 뷰에서 카메라 줌 아웃을 과도하게 설정할 경우 글자가 깨지는 이슈 — 최소화할 방법이 있을까요?

추가 요구: **"성능적인 비용을 차후에 관측할 수 있는 구조"**. 사용자 선택 = **A(라벨 LOD) + B(아틀라스 mipmap) 병행**.

#### 2. 근본 원인 — 라벨 텍스처의 극단 다운샘플 aliasing (AA 문제가 아니다)

§80 이 라벨을 `PIXI.Text` → `BitmapText`(dynamic font)로 전환했는데, PixiJS v8(vendor 8.19.0)의 dynamic font 는
글리프를 **항상 100px** 로 구워(`baseRenderedFontSize=100`·`overrideSize=true`·`BitmapFontManager.defaultOptions.resolution=1`)
아틀라스에 넣고, 표시할 때 `fontSize/100` 으로 축소한다.

- 테이블 라벨 `fontSize:12` → 평시에도 1/8 축소. 여기에 zoom 0.1 · DPR 2 를 곱하면 화면 물리 2.4px = **텍스처 대비 ~1/42**.
- 그 아틀라스에는 **mipmap 이 없다**(`TextureSource` 기본 `autoGenerateMipmaps=false`·`mipLevelCount=1`) + `scaleMode:"linear"`
  → GPU 가 2×2 텍셀만 평균 = 사실상 임의 점 샘플링 → 글자가 노이즈로 붕괴하고 팬 중 반짝인다(모아레).
- Text 폴백 경로도 `fontSize×_labelRes`(≤4)=48px 라 ~1/20 — 덜 심할 뿐 같은 기전.
- 악화 요인: §67 이후 `aggActive=false` + PixiJS 모드는 뷰포트 컬링 비활성 → **극단 줌아웃에서도 전 노드 라벨이
  전량 방출**되어 깨진 글자가 화면을 덮는다. `zoomRange` 하한 0.05 에서는 ~1/80.

#### 3. 채택 — A: 판독 하한 아래 라벨 미방출 (LOD)

`_metaApplyLabelLod(zoom, nodes, combos, edges)` 가 방출 말미에 화면 실효 크기(`labelFontSize × zoom`, CSS px)가
하한 미만인 라벨의 style 키(`labelText` + 배경 3키)를 제거한다. 하한 2단:

| 축 | 상수 | 대상 |
|---|---|---|
| 본문 | `_META_LABEL_MIN_PX=5` | 테이블·컬럼·루틴·파라미터·용어·접기 컨트롤·엣지 count·스키마 카드 개수 badge |
| 헤더 | `_META_LABEL_HEADER_MIN_PX=3.2` | 카테고리 밴드 헤더·스키마 클러스터 combo·접힌 스키마 카드·컨텐츠 그룹 헤더·제품 개요 |

헤더를 더 오래 유지하는 이유: 개요에서 "여기가 어디인가"를 주는 **소수의 큰 라벨**이라 판독 가치가 본문보다 오래 남는다.
배지는 라벨보다 작아 먼저 붕괴하므로 본문 하한으로 동반 소거한다.

**핵심 계약 — band-invariant**: 제거는 style 의 라벨 키만 건드린다. 좌표·size 불변(reflow 0, §61 col-lod 와 동일 계약),
노드/combo/엣지 개수·`renderedIds` 불변(hit-test·선택·관계선 무손실), 미니맵 기하 서명(`_metaMinimapGeomSig`) 불변
(§74/§77 재복제 유발 0). 확대 시 밴드 전이 rebuild 로 그대로 복귀 — **정보 손실 0**.

밴드 양자화(`_metaLabelBandOf`)는 억제 경계 폰트 크기(`MIN/z`)를 **반포인트(0.5) 격자**로 올린 뒤 실사용 폰트
범위 **[9, 24] 로 클램프**한다.
- **클램프**가 없으면 억제 대상이 없는 구간(zoom 1.0↔0.9)에서도 밴드 문자열이 바뀌어 무의미한 rebuild 가 걸린다 —
  이것은 추정이 아니라 신규 테스트 **F1 이 실제로 적발해 수정한 결함**이다.
- **반포인트 격자**여야 하는 이유(§8 codex P2 ②): 실사용 폰트에는 `10.5`(컨텐츠 그룹 헤더)·`11.5`(제품 개요) 같은
  소수가 있어 정수 `ceil` 은 그 임계 교차를 놓친다 → rebuild 가 걸리지 않아 **그 라벨이 판독 하한 밑에서 계속
  렌더된다**. 0.5 격자는 0.5 배수인 모든 실사용 폰트의 억제 집합 변화와 1:1 대응하고, 런타임 계산 폰트
  (스키마 카드 `_cardLF`/`_cardBF` = `Math.round` 정수)도 정수 격자를 포함하므로 함께 커버된다.

#### 4. 철회 — B: 아틀라스 mipmap (구현·라이브 실측 후 벤더 한계 확정)

dynamic font 아틀라스 페이지 텍스처에 `autoGenerateMipmaps=true` + `mipLevelCount=floor(log2(max(w,h)))+1` +
`style.mipmapFilter="linear"` 를 세우고 `updateMipmaps()` 를 호출하는 경로를 구현해 **라이브에서 A/B 실측**했다.

- 결과: **렌더 픽셀 차이 0** — 동일 줌 0.263 캔버스 크롭 45,050px 전수 비교(`ImageChops.difference` bbox=None, 변경 0).
- 진단: 속성은 의도대로 반영된다(`mipLevelCount=10`·`autoGenerateMipmaps=true`·`mipmapFilter=linear` 실측 확인).
  그런데 **GL 텍스처 스토리지와 샘플러가 아틀라스 최초 업로드 시점에 mip 없이 굳어** 사후 변경이 렌더에 도달하지 못한다.
  `TextureStyle` 을 새 인스턴스로 교체해도 값 기반 캐시라 `_resourceId` 가 동일(72→72)해 샘플러가 재생성되지 않고,
  `resize(w,h)` 강제 재할당도 화면을 바꾸지 못했다.
- PixiJS v8.19 는 dynamic BitmapFont 생성 옵션에 `textureStyle` 만 노출하고 **source 의 mipmap 옵션을 넣을 seam 이 없다**.
  아틀라스를 파괴 후 전 글리프 재래스터화하는 길만 남는데, 비용·회귀 위험이 얻는 것(판독 하한 *위* 구간의 미세한 품질)에
  비례하지 않는다.
- → **코드 철회**(죽은 경로를 남기지 않음). 시도·실측·근거는 정본 TASK.md TL.5 / 결정 기록에 보존해 재시도 시 같은 벽을
  다시 치지 않게 한다. 사용자 증상은 **A 만으로 해소**됨을 before/after 실촬로 확인했다.

#### 5. 관측 구조 (사용자 추가 요구)

`window.__META_GRAPH_PERF` 단일 지점(계측 전용·동작 분기 0·fail-soft):

```
label  { zoom, band, total, dropped, headerTotal, headerDropped, badgesDropped }
render { drawMs, objects, reused, made, labelsCreated, labelsBitmap, labelsText, labelMs }
```

억제가 라벨 생성·draw 비용을 실제로 얼마나 줄였는지 한 객체에서 대조된다. 브라우저 콘솔·PB-0008 relay·헤드리스에서
동일하게 접근한다. **B 의 무효 판정도 이 지점 하나로 수행했다 — 관측 구조가 없었다면 mipmap 무효를 발견하지 못하고
출하했을 것이다.**

#### 6. 자동 검증 (PASS)

- 신규 `tests/headless/test_g6build_labellod.js` **36 PASS / 0 FAIL** —
  A 임계 5 · B 헤더우대 5 · C col-lod 겹침 3 · D band-invariant 5 · E 미니맵서명 1 · **F 밴드양자화 7** · G 관측 5 ·
  **H 오독-가드 게이트 5**. F5~F7·H1~H5 8건은 §8 의 codex 적대 검증이 적발한 P2 2건을 고정한 것이다.
- 회귀 **16 스위트 502 PASS / 0 FAIL**(재검증 실측 2026-07-28): agglod 8 · category 26 · collod 20 · cullrefkeep 15 ·
  edge_visibility 73 · layoutmemo 19 · minimap_reuse 65 · simgroups_p2 16 · viewportcull 6 · vpack 19 · colnav 22 ·
  reveal 17 · detail_colsel 9 · detail_dbgroups 78 · catcluster_panel_scroll 36 · edge_flow 73.
- PixiJS 어댑터 `test_pixi_adapter.js` **190 PASS / 0 FAIL**.
- 합계 **728 PASS / 0 FAIL**. `node --check --input-type=module` PASS(graph-state·graph-core·graph-renderer-pixi).
  > **병합 후 재확인(2026-07-28)**: `origin/main` 18 커밋(병렬 그래프 세션 다수)을 흡수한 뒤 헤드리스 전 스위트
  > **21개 875 PASS / 0 FAIL** 재실행 — 회귀 0. (수치가 위 728 보다 큰 것은 병렬 세션이 추가한 테스트가 합류한
  > 것이며 본 cycle 의 신규 36 PASS 는 불변이다: catcluster_panel_scroll 36→65 · pixi_adapter 190→205 · 신규 파일
  > ancestor_focus 32 · hover_flow 41 · routine_colref 30.) `node --check` PASS(3 파일).
- 기존 `test_g6build_collod.js` T3 검증 줌 **0.3 → 0.45 이동**: §61 의 '▤N' 컬럼수 배지는 "컬럼 억제 시 정보 손실 방지"
  계약인데, col-lod 밴드(<0.5)와 라벨 LOD 밴드(테이블 12px 기준 <0.4167)가 겹친다. 겹침 아래에서는 배지 자체가 화면
  4px 미만이라 판독 불가 — 라벨과 함께 제거되는 것이 정합이다. 따라서 배지 계약은 **두 밴드가 동시에 성립하는
  구간**(0.45: 컬럼 억제 ON · 라벨 5.4px 유지)에서 검증하고, 겹침 구간의 동반 소거는 신규 테스트 Section C 가 별도로
  고정한다. **테스트를 통과시키려 계약을 약화한 것이 아니라, 두 LOD 계약의 유효 구간을 명시한 것이다.**

#### 7. PRE-LANDING PB-0008 실 Windows 브라우저 검증 (PASS)

**Environment: Windows-browser** — 실 Chrome 150 via `bin/win-browser.py` 무권한 userspace relay. 라이브 이미지
`mysql-ai-web:b6882c7d` 격리 컨테이너에 변경 3파일을 스탬프 정합 주입, `mssql-qa-idc`(스키마 135개·객체 231) 로드.
(정적 자산은 web 이미지 baked 라 pre-commit 라이브 검증에 격리 컨테이너 주입이 필요하다.)

| # | 항목 | 실측 |
|---|---|---|
| ① | before/after 실촬 | 원본(라이브 서빙)은 줌아웃 시 카드마다 깨진 글자 노이즈가 덮였고, 적용 후 동일 줌에서 **완전 소멸**(`compare_before_after_3x.png`) |
| ② | zoom 0.126 계측 | 라벨 215/215 · 헤더 141/141 · badge 135 억제, `labelsCreated 0`(라벨 생성 비용 0), drawMs 36.4 |
| ③ | zoom 0.325 | 본문 74 억제 · 헤더 0 억제 — **헤더 우대 실동작** |
| ④ | 상태줄 오독-가드 | `· 줌아웃 — 이름표 표시 축약(확대 시 전체 표시)` 노출 |
| ⑤ | **확대 복귀** | zoom 0.7713 에서 `total 215 · dropped 0 · headerDropped 0 · badgesDropped 0 · labelsCreated 215`, 상태줄 마커 소멸 → **정보 손실 0 실증** |
| ⑥ | pageerror | **0** |

⑤ 원시 relay 응답:
`{"label":{"zoom":0.7713405876367242,"band":"9/9","total":215,"dropped":0,"headerTotal":141,"headerDropped":0,"badgesDropped":0},"labelsCreated":215,"errs":0,"status":"…를 클릭하면 그 스키마의 테이블을 펼칩니다. 검색으로 바로 탐색도 가능."}`
— `status` 에 줌아웃 마커가 없다는 것이 ④ 마커 소멸의 근거다.

증적: `artifacts/feature-0016-metadata-graph/20260728-graph-label-lod/`(repo 루트 기준. `.gitignore` 대상이라 커밋되지
않는 로컬 증적 — before/after 3× 크롭 대조 · mipmap A/B 대조 2종 · 최종본 포함 15 파일).

- **Pass/Fail: PASS** (PRE-LANDING). CHECK#13 충족 — 실 Windows 브라우저 렌더링으로 사용자 증상 소멸과 정보 손실 0 을
  모두 실촬·계측했다. **POST-DEPLOY 는 배포 후 1-probe**(TL.10) — 격리 컨테이너 주입본이 아닌 실제 배포 자산에서
  ②~⑥ 재확인 **+ §8 수정이 새로 만든 두 표면**: ⑦ 배지-단독 억제 구간(접힌 스키마 카드 씬)에 '이름표 표시 축약'
  마커가 노출되는지 ⑧ 반포인트 밴드 경계(컨텐츠 그룹 헤더 10.5px 의 z*≈0.305)를 지날 때 rebuild 가 걸려 그 헤더
  라벨이 실제로 사라지는지. Runner: AI.
  > **정직 표기**: 위 ①~⑥ 실촬은 §8 수정 **이전** 코드로 수행했다. 수정은 억제 판정(`fs * z < minPx`)을 건드리지
  > 않고 밴드(=rebuild 시점)와 마커 게이트(=추가 발화만, 제거 없음)만 바꾸므로 ②③⑤⑥ 은 계약상 그대로 성립하지만,
  > 수정이 만든 두 표면은 이 probe 가 통과한 줌(0.126/0.325/0.771)에서 발화하지 않으므로 POST-DEPLOY 로 넘긴다.

#### 8. §18.8 적대 검증 — codex-review (PASS-WITH-FIXES · P1/GATE 0 · P2 2건 in-cycle 흡수)

`codex review --uncommitted`(codex-cli 0.145.0). `[CODEX:*]` 는 §18.4/§18.9 의 check #9 accepted verdict —
본 세션은 `Agent` 툴 사용이 제한된 환경이고, 이 변경은 **라벨 폰트 상수의 실제 사용처**와 기존 LOD 계약의 겹침을
봐야 판정되므로 bundle-only reviewer 보다 repo 접근 리뷰어가 적합하다.

**P1(GATE) 0건** — band-invariant · `_makeText` 래핑(반환값·BitmapText/Text 판별·예외 경로) · `_labelStat` 초기화
시점 · col-lod 겹침 · `collod` T3 줌 이동의 정당성에는 반박이 성립하지 않았다.

| # | 등급 | 결함 | 수정 |
|---|---|---|---|
| ① | P2 | **오독-가드 배지-단독 구멍** — 카드 제목(`_cardLF`≈13) > 배지(`_cardBF`≈10) 이라 *제목 유지 + 배지만 억제* 구간이 실재(zoom 0.45 부근: 12×0.45=5.4 유지 / 10×0.45=4.5 억제). 마커 게이트가 `dropped` 만 봐 **개수 정보가 마커 없이 사라짐** — 이 cycle 이 '이름표' 항을 추가한 목적 자체가 뚫린다 | 게이트를 `dropped > 0 \|\| badgesDropped > 0` 로 확장. 마커 항은 '이름표' 하나 유지(항을 늘리면 상태줄 두 줄). 테스트 Section H(H1~H5)가 게이트 진리표 + '배지 폰트 < 제목 폰트' 불변식 고정 |
| ② | P2 | **소수 폰트 임계 교차 누락** — 실사용 폰트에 `10.5`(컨텐츠 그룹 헤더, graph-core.js:626)·`11.5`(제품 개요, :1033)가 있는데 `ceil(MIN/z)` 는 정수 경계에서만 바뀐다. 헤더 하한 3.2 와 10.5px 의 교차 z*≈0.30476 양옆이 동일 밴드(`17/11`) → **rebuild 미발화, 그 헤더가 판독 하한 밑에서 계속 렌더** | 밴드를 **반포인트(0.5) 격자**로 양자화(`ceil((MIN/z)/0.5)*0.5`). 0.5 배수 폰트 전량과 런타임 `Math.round` 정수 폰트를 함께 커버. 수정 후 같은 두 지점이 `16.5/10.5` vs `16.5/11` 로 갈라짐. F5·F6 이 교차를, F7 이 '격자 세분화가 F1/F3 무의미-rebuild 차단을 깨지 않음' 을 고정 |

- **수정 전 재현 확인**: 두 지점의 밴드를 구/신 양자화로 직접 대조해 구 구현 **MISS**(동일)·신 구현 **detect**(상이)
  를 실측했다 — 테스트가 결함 자체를 잡는지 검증한 것이며, 통과만 보고 넘기지 않았다.
- PB-0008 이 기록한 실측 band `9/9`(zoom 0.7713)는 격자 변경 후에도 **불변**(재계산 확인) → §7 증적의 유효성 유지.

#### 9. 한계 (정직 표기)

- 실제 렌더 품질(GPU 샘플링·mipmap 실효)은 GPU 의존이라 헤드리스가 검증할 수 없다 — 헤드리스는 **방출 계약만** 고정하고
  품질 판정은 전부 PB-0008 라이브가 담당했다.
- 하한 값(5px / 3.2px)은 이 데이터셋(스키마 135개)에서 육안·계측으로 정한 값이다. 폰트·DPR·언어(CJK 자형 밀도)가 크게
  다른 환경의 최적값은 재측정 대상이며, 두 상수는 `graph-state.js` 단일 소스라 조정 비용이 낮다.
