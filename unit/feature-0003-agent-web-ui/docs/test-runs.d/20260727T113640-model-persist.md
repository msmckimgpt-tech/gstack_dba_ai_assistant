---
run_at: 2026-07-27T12:05:00+09:00
session: ai/claude/feature-0003-model-persist
scope: [model-persist, loadHistory-hydration, _resetComposerModelSelection, _modelHydrationShouldSkip, ask-model-kv, history-model-payload]
verdict: PRE-COMMIT PASS (pytest 14 + node 32 + make test 회귀 0) · POST-DEPLOY PB-0008 배포 후 실측 예정
---

### Run (2026-07-27) — model-persist: 대화별 "마지막 요청 모델" 보존 / '+ 새 대화'=haiku — **Environment: Windows-browser (PB-0008 배포 후 실측 예정 — 새로고침·대화 전환·'+ 새 대화' 3-경로 라이브 확인은 배포 자산 + 로그인 세션 + 실 KV 왕복이 필요해 headless 로 정본 대체 불가; de-risk = 서버 계약 pytest 14 + 프론트 순수함수·구조계약 node 32 + 전체 make test 회귀 0, visual_verification_scope: always)**

- 대상 변경: `src/routers/conversations.py`(`_model_kv_key`·`ask()` KV 저장·`history()` payload `model`) · `src/static/app.js`(hydration·`_modelHydrationShouldSkip`·`_resetComposerModelSelection` 4 호출지점·pick 마커·pending entry 모델 캡처) · `src/static/index.html`(모델 라벨 초기 placeholder). 2R 반영 추가 — `_shouldSendModelField`(전송 clobber 가드)·`_modelHydratedForConvId`·`moveConversationToFolder` 히스토리 재로드·랜딩 리셋 가드·모델 메뉴 재렌더 가시성 한정·`_model_kv_key` fail-closed.

- **PRE-COMMIT 검증 ① 서버 계약 — `tests/test_model_persist.py` 14 PASS** (agent 이미지, DB/LLM 없이 monkeypatch/fake):
  - H1 저장값 복원 · **H1b 계정별 격리**(타 계정 키 `model:99` 는 내 payload 로 새지 않음) · H2 allowlist 밖(`auto`) → `""` · H2b 형식 위반 → `""` · **H2c 열람 불가(`conv_id=""`) → `""`** · **H2d 가시 window `DENY` → messages 빈 + `model=""`** · **H2e 계정 식별 불가 → 빈 키 fail-closed(공유 슬롯 금지)** · H3 미저장 → `""` · H4 KV 조회 실패 fail-soft.
  - A1 명시 model → `(conv-1, model:7, claude-sonnet-4)` 저장 · **A1b 세션 기본값과 동일 → 빈 값 저장(해제)** · **A1c allowlist 밖 alias → 400 + KV 미도달**(저장이 검증 게이트 위로 올라가는 회귀 차단 — 이 하네스는 `_is_allowed_api_model`/`_is_safe_model_name` 을 patch 하지 않는다) · A2 model 미지정 재dispatch → 미저장 · A2b 공백 model → 미저장.
- **PRE-COMMIT 검증 ② 프론트 — `tests/verify_model_persist.mjs` 32 PASS** (순수 node, app.js 실소스에서 함수 추출):
  - G1~G5 hydration 가드: 초기 상태 hydration 수행 / 같은 대화 미전송 선택 보존 / 다른 대화는 hydration / A→B→A 복귀 시 저장값 복원 / pending 선택이 실 대화 hydration 미차단.
  - M1~M2 현재 모델 결정: 선택 없음 → `claude-haiku-4`, 복원값이 기본값보다 우선.
  - R1~R3 리셋: 리셋 후 기본값 + 픽 마커 해제 / `_modelHydratedAt` 단조 전진(리셋 대상 아님) / **4 호출지점 전수**(`beginPendingConversation`·`selectConversation`·`loadHistory` 활성대화없음 분기·`handleLogout`).
  - **R3b/R4/R5 미전송 선택 보존**: 랜딩 분기 리셋이 가드된 호출인지 · pending 컨텍스트 미전송 픽 보존 · 직전 대화 hydration/타 대화 픽은 리셋 대상.
  - **D1~D4 전송 clobber 가드**: 신규 대화 동봉 / 이 대화 명시 선택 동봉 / 이 대화 hydration 동봉 / **미hydration 대화 미동봉**(서버가 저장값 보존).
  - S1~S8 구조 계약: `payload.model` hydration + hydration 시각 전진 / 리셋 헬퍼가 선택·마커 3종 해제 / **모델 localStorage 미러 부재**(+ S3b 대조군 — 추론 강도 미러는 실제 검출되어 검출기가 죽지 않았음을 고정) / pending entry 모델 캡처.
- **PRE-COMMIT 검증 ③ 회귀** — `make test` 전체 실행: 신규 14건 포함 PASS. 선존 FAIL 4건(`test_routine_dbanalysis::test_schema_analysis_fail_loud_on_status_aggregation_failure`·`test_runtime_settings::test_missing_snapshot_is_fail_open`·`test_item11_batch8_update_conv_product::test_auto_happy_200`·`test_runtime_settings_api::test_get_returns_registry`)은 **clean main(84f2e5ab)에서 동일 4건 재현 확인** → 본 변경과 무관(DB 미기동 host resolve · `.env` `AGENT_TIMEOUT_SEC=300` 유입). `node --check`(app.js) · `python3 -m py_compile`(conversations.py) · ruff `All checks passed`.
- **§18.8 적대 패널 2라운드(둘 다 BLOCK, 지적 10건 전건 수정)**: 1R B1/C1/C2/C3/C4 · 2R **B-B(1R 수정이 만든 자체 회귀 — 랜딩 재로드가 미전송 선택 삭제)**/C-A(폴더 이동 hydration 공백 → clobber)/C-B(최종 fallback sonnet)/C-C(열린 메뉴 재렌더)/C-D(`model:unknown` 공유 슬롯). B-A(스테이징) = 검토 시점 타이밍 아티팩트, 커밋 직전 재확인으로 해소. 상세 = REVIEW REV-20260727T113640-model-persist.

- **POST-DEPLOY PB-0008 라이브 계획(정본)**: 배포 후 실 Windows Chrome 로 —
  - AC-MP-1: 대화 A 에서 sonnet 선택 → 전송 → F5 새로고침 → composer 모델 라벨이 sonnet 으로 복원.
  - AC-MP-2: 대화 B(기본값)로 전환 → 라벨이 haiku → A 로 복귀 → 다시 sonnet(각 대화 독립).
  - AC-MP-3: '+ 새 대화' → 라벨 haiku(직전 sonnet 미상속). 추가로 대화 삭제 후 랜딩·로그아웃→재로그인 경로도 haiku 확인(적대 리뷰 B1 회귀 가드).
  - AC-MP-9: '+ 새 대화'에서 sonnet 선택 → 전송하지 않고 사이드바 대화 일괄삭제 → 선택이 sonnet 으로 유지되는지(2R B-B 회귀 가드).
  - 콘솔 에러 0 · 서빙 자산 curl 확증(`app.js` `_resetComposerModelSelection`/`_modelHydrationShouldSkip`, `/livez` git_commit).
