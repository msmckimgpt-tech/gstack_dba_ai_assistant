### Run (2026-07-22) — share-menu-perm-wiring: 말풍선 ☰ '여기까지/여기부터 공유' 권한 연결(action 매핑) 회귀 수정 (Minor §12.3 — feature-0003 web/UI app.js 2줄) — **Environment: Windows-browser**

- **신고(사용자)**: 서비스 내 대화에서 '여기부터/여기까지 분기'(말풍선 ☰ 메뉴의 여기서 분기 · 여기부터/여기까지 공유) 기능에 대한 **권한 연결이 제대로 작동하지 않음**.
- **근본원인(action↔권한 매핑 mismatch)**: 말풍선 ☰ 메뉴 항목 '여기까지 공유'/'여기부터 공유'(`openMessageBubbleMenu`, app.js ~L7811/L7816)가 메뉴 팩토리 `make`(=`makeMenuItem`)에 `action` 으로 **권한 코드** `"conversation.share.create"` 를 넘겼다. 그러나 `requiredPermissionsFor(action)`(app.js ~L589)의 switch 는 **추상 action 이름**("conversation.share" 등)만 처리하고 미매칭은 `default:{ codes: [] }` 를 반환한다. codes 가 비면 `markAccessBlocked`(app.js ~L637)가 `blocked = !hasAnyPermission([]) = true` 로 계산 → 두 메뉴 항목이 **로그인한 모든 사용자에게 항상 비활성**(`is-access-blocked`·aria-disabled·필요권한 `undefined` 툴팁)으로 렌더되고, 클릭 시 `onSelect` 대신 `showPermissionDeniedToast` 오류 토스트만 떠 **☰ 경로의 공유(여기부터/여기까지) 기능이 완전 동작 불능**이었다. 형제 항목 '여기서 분기'(action `"conversation.create"`·정상 case)·'AI 로 고치기'(`"conversation.ask"`)와 conv-item 목록 메뉴 '공유'(action `"conversation.share"`·정상 case)는 영향 없음.
- **도입 provenance**: `36ca1d4d`(2026-07-04, 말풍선 ☰ 통합 + 여기부터 공유 범위 UI). 신규 ☰ 공유 항목 작성 시 conv-item '공유'가 이미 쓰던 추상 action 이름 대신 권한 코드를 그대로 사용. (perm-atomic-split 계열의 `ds-test-gate-fix`(REV-20260716T051931)와 동형 — 프론트 권한 표시 게이트 배선 오류.)
- **수정(app.js 2줄)**: '여기까지 공유'·'여기부터 공유'의 `action: "conversation.share.create"` → `action: "conversation.share"`. `requiredPermissionsFor("conversation.share")` → `codes:["conversation.share.create"]` → `hasAnyPermission` 이 `can("conversation.share.create")`(display-permissive, 로그인=true) 평가 → `blocked=false` → 항목 정상 활성·클릭 시 onSelect 실행. **보안 무영향**: 백엔드 enforcement 불변(`create_conversation_share` 가 `conversation.share.create` 403 계속 요구·소유 IDOR 게이트 유지) — 순수 표시 게이트 복구.
- **회귀 잠금 테스트(신규)**: `tests/test_menu_action_permission_wiring.py` — app.js 소스를 파싱해 모든 메뉴 `action:` 문자열이 `requiredPermissionsFor` switch 의 처리 case 인지(=비어있지 않은 codes 반환) 검증. pre-fix 소스에서 정확히 FAIL(`unresolved: conversation.share.create`), fixed 에서 PASS 실증.
- **pre-commit 검증(PASS)**: `make test` 전체 스위트 GREEN(feature-0002+0003) — 신규 테스트 3 PASS 포함. (첫 run 의 4건 실패는 `--no-deps` 컨테이너의 `postgres-replica` 호출 레이스 = run-to-run flake: base main·재실행 모두 RC=0로 확증, 실패 4건 중 2건이 feature-0002라 feature-0003 테스트파일 추가와 인과 없음.) ruff PASS.
- **Environment: Windows-browser — pre-commit 라이브 미수행 사유**: 정적 자산 web 이미지 baked → merge + `make deploy-web` 후 서빙(cache-buster content-hash 빌드 자동주입). **POST-DEPLOY PB-0008 라이브 append 예정** — win-browser relay.
- **Pass/Fail: pre-commit 정적·단위·문법 PASS · 라이브 = POST-DEPLOY**. CHECK#13 충족.
- **POST-DEPLOY 검증 항목(append 예정)**: ① 로그인(비-admin 포함, `conversation.share.create` 보유) → 자신의 대화 말풍선 ☰ 열기 → '여기까지 공유'·'여기부터 공유' 항목이 **비활성 아님**(is-access-blocked 없음·클릭 가능) ② '여기부터 공유' 클릭 → floor arm 배너·마커 표시 ③ '여기까지 공유' 클릭 → 공유 링크 생성 다이얼로그/토스트 ④ pageerror 0.
