---
run_at: 2026-09-05T00:30:00+09:00
session: live-record (ai/claude/feature-0046-live-record)
scope: 최종 설치본 라이브 완주 — 업그레이드 정리 · 창 · 브리지
verdict: PASS
---

### Run (2026-09-04) — 최종 설치본 — **Environment: Windows-browser (실제 설치·실행)**

배포 `bd5f0821` · 설치본 sha256 `A6E1123C…` · 25,688,507 bytes.

| 항목 | 결과 |
|---|---|
| 덮어 설치 후 옛 그룹 폴더 `DQA Connect\` | **없음** ✅ |
| 새 그룹 폴더 `DQA\` | `DQA.lnk` · `DQA 제거.lnk` **둘뿐** ✅ |
| 설치 폴더·AppId | 그대로(업그레이드 경로 보존) |
| 아이콘 실행 → 창 | 제목 `DQA` — **DQAConnect.exe 자신의 창** |
| 브리지 | 같은 프로세스가 포트 보유 |

앞선 주기들에서 확인된 것(이 설치본에도 동일 코드):
- 내장 창 안에서 브리지 왕복 — preflight → POST `HTTP 200 {"ok": true, …}`(origin·nonce·
  `Sec-Fetch-Site` 전부 통과) · GET 은 설계대로 `405`(응답이 읽힘 = CORS 정상)
- 창을 닫으면 트레이 상주(프로세스 생존) · 아이콘 재실행 시 창 복귀 · 인스턴스 1개
- 딥링크 없이 **아이콘만으로** 서비스가 열린다(동봉된 `service.json`)

### 남은 것 (정직하게)

- **앱 안에서 한 번 로그인이 필요하다.** 브라우저 세션과 분리된다 — 내장의 대가이고,
  전달 문서에 적었다.
- 코드 서명 없음(설치 1회 SmartScreen) · 서버 다운로드 채널 없음.
- Gemini 로그인 대행은 이 머신에 설치돼 있지 않아 **측정할 수 없다** — 커버리지를 과장하지
  않으므로 「직접 로그인」으로 강등된 현재 동작이 옳다.
