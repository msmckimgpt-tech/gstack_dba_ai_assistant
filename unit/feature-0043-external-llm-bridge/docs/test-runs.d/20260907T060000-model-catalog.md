---
run_at: 2026-09-07T06:00:00+09:00
session: ai/claude/feature-0003-model-list-visible
scope: 능력 협상 — CLI 자체 카탈로그 · 배경 재시도 · 원장 폴백
verdict: PASS
---

### Run (2026-09-07) — 목록이 비는 3중 원인 — **Environment: CLI + 실 codex 종단**

#### 1. 진단 실측 (라이브 러너 · 사용자 머신 Windows → WSL codex)

| 경로 | 시간 | 결과 |
|---|---|---|
| `_CAPS_PROBE_PROMPT`(가드+`effort=low`+`--ephemeral`) | **22.7초** | `{"models": [], "model_flag": ["--model","{model}"]}` |
| 같은 질의, 가드 제거 | **200초** | `TimeoutExpired` |
| 모델 축만 좁게 (가드 有/無) | **240 / 200초** | `TimeoutExpired` |
| 서버 원장 확인 질의 (좁은판) | **600초** | `TimeoutExpired` |
| 서버 원장 확인 질의 (현행) | **603초** | `TimeoutExpired` |
| **`codex debug models`** | **0.23초** | **노출 모델 7종 + 등급** |

러너 로그 근거 (`C:\Users\mckim\.dqa-connect\bridge.log`, run `f2a94dbda501`):

```
14:02:17  run.start ai=codex build=ee0453d7c445
14:02:17  쓸 수 있는 모델·추론 수준을 물어보는 중… (codex — 최초 1회, 수십 초)
14:03:36  codex: 사유 — 응답에 모델 목록이 없습니다.
14:03:36  고를 수 있는 AI 를 찾지 못했습니다 — 웹 선택기는 표시되지 않습니다.
```

DB 실측: `WebOAuthTokens(AccountId=10)` 의 `RunnerCapabilities = '[]'`,
`CapabilitiesAt` 이 9분간 불변(= 재시도 0회). 배포본 지문과 신고 지문 일치
(`ee0453d7c445`)라 「구 빌드」도 아니었다.

#### 2. 수정 후 종단 실행 (worktree 빌드 · 실 codex)

```
catalog_runtime_caps("codex")  →  0.229초, source=catalog
  models  = gpt-6-astra · gpt-5.6-sol · gpt-5.6-terra · gpt-5.6-luna
            gpt-5.5 · gpt-5.4-mini · gpt-5.3-codex-spark   (7종, priority 순)
  efforts = low · medium · high · xhigh                      (노출 모델 교집합)
```

- 노출 필터 확인: `gpt-reserve`·`codex-auto-review`(`visibility: "hide"`)는 **제외**됨.
- 원장(2026-09-02 probe)과의 차이: `gpt-5-codex`·`gpt-5.2`·`gpt-5.4` 는 **더 이상 없고**
  `gpt-6-astra`·`gpt-5.4-mini`·`gpt-5.3-codex-spark` 가 새로 있다 — 원장이 실제로 낡았다는
  실증이자, 원장 폴백을 **맨 뒤**에 둔 근거다.

#### 3. 회귀 (첫 구현이 «존재하지만 실행되지 않는» 상태였다)

`_CAPS_PROBE_MAX_BYTES`(256KB)로 읽어 실측 출력 **413,777바이트**가 잘렸고 JSON 파싱이 항상
실패했다 — 종단 실행에서 `카탈로그 출력을 해석하지 못했습니다` 0.198초로 드러났다. 단위
테스트는 작은 표본을 써서 통과했다. `_CAPS_CATALOG_MAX_BYTES = 8MB` 로 분리하고,
`test_a_large_catalog_is_not_truncated` 가 **종전 상한을 넘는 표본**으로 잠근다.

#### 4. 단위 테스트

- 신규 `tests/test_cli_model_catalog.py` 8건 · `tests/test_caps_negotiation_retries.py` 6건
- 계약 갱신 `test_caps_live_sync.py` 4건(허용집합·원장 폴백·수신 게이트·출처맵)
- `make test` 전건 GREEN (아래 REPORT 의 Git 동기화 결과 참조)

- Pass/Fail: **PASS** (라이브 계정 반영은 배포 + 러너 재기동 후 별도 확인 — TASK 미체크 항목)
