---
doc_type: REVIEW_ARTIFACT
feature_id: feature-0003-agent-web-ui
agent: backend-security-qa-ux
timestamp: 2026-09-08T20:45:00+09:00
trigger: 라운드 3 확인 라운드 (verification_debt 상환) — 라운드 2 조치 11건 폐쇄 + 신규 결함 탐색
verdict: CONCERN
---

한도 리셋 후 부채로 남겼던 subagent 3차 교차검증을 수행했다. 컨테이너 시험 직접 실행 +
라이브 원장 10,114행 read-only 재생 + 제품 코드 **16종 뮤턴트 주입**.

## Half A — 라운드 2 조치 11건

CLOSED 8 (L7 가드 모수 · census 조회 1차 · 사유 파생 되돌림 · 빈 census 지뢰 · `_code_only`
3형 주석 · scratch 인자·연산중립 동작 · 라벨 30종 정합 · 서버↔클라 규칙(a)와 라이브 재생
`ROWS=10114 DROPPED_WORK=21 DROPPED_REASON=0 LEAKED_AFTER=0`).
PARTIALLY 3 (`intent` · 폴백 제목 중복 · 활동 툴팁).

## 1. Blocking issues

- **F1 (medium) 사유 축 배선이 무봉인** — 이음매의 `allow_tool_names` 를 뒤집는 뮤턴트가
  55건 전건을 통과했다. 같은 뒤집기를 라이브에 적용하면 사유 3,321건 중 **21건이 사라진다**.
  기존 테스트는 판정기를 직접 호출해 축의 의미만 보고, 이음매가 인자를 넘기는지는 안 본다.
  - Location: `src/routers/_conv_store.py:6495`
  - Action: 이음매 산출로 사유 보존을 단언하고 뒤집기 뮤턴트를 대조군으로 둔다.
- **F2 (medium) 라운드 2·3 조치 5건이 무봉인** — scratch_sql 문구 회귀 · scratch_import 인자
  무시 · scratch_reset 라벨 · list_schemas 제목 · **서버 규칙(a) 앵커 제거**가 전부 생존.
  클라이언트 앵커만 하네스가 잠그고 있어 같은 계약의 두 층 중 한 층만 봉인됐다.
  - Location: `src/routers/_conv_store.py:6313` 외
  - Action: 문구·라벨의 **값 단언**과 서버 앵커의 산문-JSON 표본을 추가한다.
- **F3 (medium) 「신규 적재의 도구명 접두 제거」가 브리지 한정이고 그 조치도 무봉인** —
  `agent_core.py:9709` 이 여전히 접두를 적재하고(라이브 3,382행), `/api/ask` 의 `steps` 는
  표시 이음매를 우회해 그 `intent` 를 클라이언트로 내보낸다.
  - Location: `unit/feature-0002-agent-core/src/agent_core.py:9709`
  - Action: `/api/ask` 를 이음매로 통과시키고 문서를 「브리지 적재 한정」으로 정정한다.

## 2. Cross-domain concerns

- **C1** 단계 사이드 패널의 「배지 · 폴백 제목」 중복(라이브 실발생 0, 구조적 잔존).
- **C2** census **부분** 실패가 폴백·로그 없이 서명을 좁히고 프로세스 수명 동안 캐시된다
  (브리지 2종 소실 실측).
- **C3** 「지어내지 않는다」가 표시층에만 적용되고 **적재층**은 여전히 사유를 파생한다
  (라이브 22행). 프런트는 `reason_source` 를 읽는 코드가 0줄이라 구분되지 않는다.
- **C4** 문서 수치 4곳이 코드와 어긋난다(하네스 17항목 · 주입 8 FAIL · 55건 · 라벨 30 · census 25).

## 3. Challenge to current spec

규칙 (b) 는 (a) 와 달리 «호출 형태» 가 아니라 «이름 등장» 이라, census 에 흔한 명사가 늘면
손익비가 조용히 뒤집힌다. 이음매 비용은 입력 SQL 길이에 무제한이다(1.2MB SQL 에서 41.9ms/행,
산출은 캡했는데 입력은 안 캡했다). `_BRIDGE_ONLY_NARRATION[0]` 은 여전히 죽은 값이면서 정본과
다른 문장을 들고 있다. 옛 라벨 5종은 PG 원장에 실사용 0건이고 제목이 무정보로 떨어진다.

## 4. Verdict

CONCERN
